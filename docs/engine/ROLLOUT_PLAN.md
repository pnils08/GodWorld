# GodWorld — Rollout Plan

**Reframed S173 (2026-04-24)** — building a sim, not running one. The S172 halt framing has been reversed. Each cycle is a new approach to test; editions are journalised audits, not finished products. The C92 contamination event surfaced a real gap (infrastructure in place without an agent layer driving canon) — that gap is closed S175 via the canon-fidelity rollout (25/25 agents). [[POST_MORTEM_C92_CONTAMINATION]] stands as historical record but is superseded by the reframe. **Active C93 priorities live in `SESSION_CONTEXT.md`.**

**Status:** ACTIVE (building) | **Last Updated:** Session 185 (2026-04-29)
**North star:** `docs/ARCHITECTURE_VISION.md` — Jarvis + persistent sessions. Everything we build points there.
**Completed phase details:** [[engine/ROLLOUT_ARCHIVE]] — read on demand, not at boot.
**Research context:** `docs/RESEARCH.md` — findings log, evaluations, sources.
**Wiki layer:** [[SCHEMA]] (conventions) + [[index]] (catalog) — read at boot. (Phase 41.1 + 41.2, S146.)
**Plan-file contract:** [[plans/TEMPLATE]] — every new plan copies this shape (S152).
**Phase backlog:** [[plans/BACKLOG]] — designs catalogued but not yet scheduled. Promote to its own plan file when a session picks one up.
**Terminal handoffs:** Items tagged `(engine terminal)` or `(media terminal)` are handed off to that persistent chat. Research/build terminal designs; engine/sheet terminal executes code.

---

## The Spine (S145 — 10-step ordered rollout)

The planned rollout, in order. Read top to bottom: this is what the project has been building, and where it's going next. Each step is a distinct deliverable; steps ahead depend on steps behind.

| # | Phase | Status | Session | Plan |
|---|-------|--------|---------|------|
| 1 | 41.1 + 41.2 wiki layer — SCHEMA.md + index.md | DONE | S146 | [[SCHEMA]], [[index]] |
| 2 | 38.1 engine auditor detector — 8 modules + JSON output | DONE | S146 | [[engine/PHASE_38_PLAN]] |
| 3 | 38.7 + 38.8 anomaly gate + baseline briefs | DONE | S146 | [[engine/PHASE_38_PLAN]] |
| 4 | 39.1 capability reviewer — 9 assertions, E91 replay passes | DONE | S146 | [[engine/PHASE_39_PLAN]] |
| 5 | 38.2 + 38.3 + 38.4 mitigator + remedy + Tribune framing | DONE | S146 | [[engine/PHASE_38_PLAN]] |
| 6 | 39.2–39.7 + 39.3 three-lane review + Final Arbiter + two-pass hallucination | DONE | S147 | [[engine/PHASE_39_PLAN]] |
| 7 | 39.9 + 39.8 + 39.10 tiered review + reward-hacking scans + adversarial review | DONE | S148 | [[engine/PHASE_39_PLAN]] |
| 8 | 38.5 measurement loop (engine) + 38.6 skill shrink | DONE | S156 | [[plans/2026-04-16-phase-38-5-measurement-loop]], [[plans/2026-04-16-phase-38-6-skill-shrink]] |
| 9 | 40.1 session-log interface + 40.6 layered injection defense | DONE | S156 | [[plans/2026-04-16-phase-40-1-session-log-interface]], [[plans/2026-04-16-phase-40-6-injection-defense]] |
| 10 | Sandcastle + Daytona evaluation | DONE | S156 | [[engine/PHASE_33_PLAN]] §33.13 |

**Spine walked — all 10 steps closed S156.** Step 9 shipped all six Phase 40.6 Layer tasks (memoryFence, memory-write gate hook, contextScan + skill/script wiring, tool-gate permissions, Rhea injection scan + agent extension); Entry 123 pressure test all three vectors blocked. Step 10 closed via code inspection of `/tmp/sandcastle` 0.4.5 templates + live PoC round-trip — `scripts/sandcastlePoC.js` created a Daytona Tier 1 sandbox, ran `echo` and `uname` inside it, destroyed cleanly. Findings + Phase 39 fit analysis in §33.13. Off-spine work lives in the sections below.

**Full analysis:** mags Supermemory doc `n5cBYS3vVN5DKrddnNp7K8` (S144 keystone framing, still load-bearing).

---

## Other Ready Work (off-spine, plans exist)

Work with plan files that is NOT on the spine. Use when the next spine step is blocked or when a session has cycles for a side-quest.

| Item | Why off-spine | Detail |
|------|---------------|--------|
| Phase 33 — Riley integration (12 subitems remaining) | Pre-S145 work, 7 of 19 done | [[engine/PHASE_33_PLAN]] |
| Phase 40.2 cattle refactor | Phase 40 now at 5/6: 40.1 + 40.3 + 40.4 + 40.5 + 40.6 DONE; only 40.2 remains and still needs its own plan file | [[engine/PHASE_40_PLAN]] |
| Phase 41 remainder — 41.3, 41.4, 41.6 | Phase 41.1/41.2/41.5 were step 1; the rest is smaller cleanup | [[plans/BACKLOG]] §Phase 41 |
| Skill eval framework | (promoted: C93, severity: HIGH) HIGH priority, separate from spine — grades skills against criteria files. **First skill `/skill-check` shipped S156** covering `/write-edition` + `/sift`. Sift patched to dump full proposal set so next cycle grades cleanly. Expand to city-hall, dispatch when their assertion files land. | [[plans/skill-eval-framework]] |
| Memento CBR case-bank | Research-landscape → 4-phase rollout. Framing + reward-tuple capture have standalone value even if Memento never ships (structured reward-signal capture is missing today). Shape B build contingent on ≥500 tuples + droplet headroom (4.6 GB free at S170). | [[plans/2026-04-21-memento-cbr-case-bank]] |
| `/md-audit` skill — existence-staleness audit | **Phase 1+2 DONE S189.** `scripts/mdStalenessDetector.js` + `.claude/skills/md-audit/SKILL.md` shipped. First run: 0 orphans / 0 stale-but-linked / 48 stable-by-reference / 109 fresh at 60d/30d baseline (after directory-walk detector patch — voice files load by `docs/media/voices/` glob, not per-file ref). Phase 3 archival script not built — no orphans to archive. Phase 4/5 deferred. Move to ROLLOUT_ARCHIVE next session-end sweep. | [[plans/2026-04-21-md-audit-skill]] |
| `/disk-audit` skill — full `/root` inventory + dead-file detection | (promoted: C93, severity: MEDIUM) NEW S202. Triggered by droplet hygiene pass (88%→81% required ad-hoc audit; no standing tool re-runs the same triage). Two phases: read-only Node walker producing JSON manifest (path/size/ext/mtime/sha1/git-tracked) → basename ref-scan + `claude-batch` classification (mechanical handles ≥80%, batch API handles ambiguous tail). Complements `mdStalenessDetector.js` (docs/ only) + `scanDeadCode.js` (function-level only). 5 Phase 1 open questions to resolve before tooling. Read-only end-to-end; archival human-gated like `/md-audit`. | [[plans/2026-05-05-disk-inventory-and-dead-file-detection]] |

---

## Open Work Items

### Edition Post-Publish (carry-over from E91/S139)

- **DONE S190: Discord bot edition currency — Task 5 (research-build, cross-terminal scope).** Plan: [[plans/2026-04-26-discord-bot-edition-currency]]. Tasks 1-4+6 DONE S184. Task 5 closed S190: `.claude/agents/letters-desk/RULES.md:50` + `.claude/agent-memory/letters-desk/MEMORY.md:12` now reference `output/production_log_edition_c{XX}.md` Story Lineup section instead of deleted `latest_edition_brief.md` (S184 deletion). All 6 tasks of the Discord bot edition currency plan now closed. Move to ROLLOUT_ARCHIVE next session-end sweep.
- **WIRE: `ingestPlayerTrueSource.js` into a skill — DONE S201 (engine-sheet).** Path (a) chosen — `/post-publish` substep `2d. Refresh A's player truesource — incremental sweep` added between Step 2c world-summary and Step 3 civic wiki. Invocation: `node scripts/ingestPlayerTrueSource.js --apply --skip-subfolder --include-flat --include-prospects` (flag set per ROLLOUT acceptance — `--skip-subfolder` because Pass A bootstrap shipped S180 commit `c15050f`, `--include-flat --include-prospects` for full MLB + Top_Prospects coverage). Per-type matrix updated: ✓ edition, — interview, ✓ supplemental, — dispatch (interview + dispatch rarely surface MLB roster changes; manual re-run available if needed). Verification gate: per-player ingest count + POPID resolution table to stdout, `output/intake_player_truesource.json` written, failures surface in production log Step 12. Path (b) /sync-truesource skill not built — script's existing manual-run flags (`--apply --include-flat --include-prospects` without `--skip-subfolder`) cover the deliberate-bootstrap case if needed.
- **FIX: validateEdition.js false positives — DONE S197 (BUNDLE-C, verified S199 d9763ca neighborhood).** 96% false-positive rate in E91 dropped to ~0% via: (a) last-name tier with full-name presence check (`scripts/validateEdition.js:92,188-199` — CRITICAL only when council member's full name absent from text + first-name token doesn't match; otherwise downgraded to NOTE — handles Maria Reyes vs Marcus Carter vs Vanessa Tran-Muñoz vs Bobby Chen-Ramirez vs Priya Patel last-name collisions); (b) cycle-language patterns removed (S146 reversal, line 46-48); (c) extended title lookbehind list including Deputy/Vice/Acting/Former (line 183-186) so "Deputy Mayor X" doesn't false-positive against Mayor Santana. Verified live in code. Closes the MEDIUM and the related S195 HIGH G-W22 (last-name-collision plague: ~95% false positives).
- **ADD: Gender to citizen briefs** — Gender exists at Simulation_Ledger column AU (confirmed S146). All angle briefs must specify gender for every citizen. Update brief generators to pull from column AU. MEDIUM.
_(items archived S186 — see [[engine/ROLLOUT_ARCHIVE]] §Edition Post-Publish closures: POP-00004 Lucia Polito row drift, Empty-cell rows in lifecycle fields, Ingest gender column AU to citizen cards, WorldEvents_V3_Ledger keyword classifier polish)_
- **NON-EDITION PUBLISHING PIPELINE: plan file written S179 — [[plans/2026-04-26-non-edition-publishing-pipeline]].** 10 tasks across three phases (format contract / skill consolidation / script flags). Format-contract keystone: every artifact lands as `editions/cycle_pulse_<type>_<cycle>_<slug>.txt` with Bay Tribune masthead + standardized sections, so existing scripts work without parser changes — only `--type`/`--cycle` flag retrofits. Validation fixture: C92 Mayor Santana interview replay through the unified pipeline. Absorbs DONE-S179 Step 8a/8b skill bug + the C93 observation task on Step 8d coverage. **Three-way terminal split:** research-build owns skill `.md` edits (T1 DONE-S179, T2–T5 next); engine-sheet owns script `.js` edits (T6–T9) + new standing-intake item below; media runs T10 validation. **Architecture + grill reasoning:** Supermemory `mags` doc `bm8sccZCRzdCsX6VWAZ2iS` (retrieve via `curl -s "https://api.supermemory.ai/v3/documents/bm8sccZCRzdCsX6VWAZ2iS" -H "Authorization: Bearer $SUPERMEMORY_CC_API_KEY"`) — fetch when next session needs the WHY behind format-contract decisions. MEDIUM. Promote to active when next interview/supplemental session has cycles for the build.
- **DISPATCH C92 GAP FOLLOW-UPS — DONE S189.** Plan: [[plans/2026-04-30-dispatch-gap-followups]]. All 11 items closed: R1–R4 research-build skill/spec edits + E1-E8 engine-sheet code. 6 commits to origin/main (`e83a5a3` E1 ingestEditionWiki NAMES INDEX parser + `15e7f3e` E2 ingestPublishedEntities CUL-skip + `e9b0d37` E3 --cycle filename-fallback + `6c2f45a` E4 postRunFiling --type flag + `c20bb3d` E5a/b/c PDF parser one-fix-three-bugs + `a805e76` E8 verifyNamesIndexParse helper). E6 cultural-card refresh shipped via existing `scripts/buildCulturalCards.js` (S182) + `/post-publish` SKILL.md wiring (S189). E7 closed-no-action (Marin Tao BirthYear=2009 already on ledger; Mike accepted as canon). Detail archived to [[engine/ROLLOUT_ARCHIVE]] §S201 Archive Pass. Stale-pointer surfaced + cleared S201 (this entry was claiming "7 engine-sheet items remain active" 12 sessions after closure).
- **C93 OBSERVATION TASK: `/interview` Step 8d coverage-ratings — DEFERRED PENDING EVIDENCE S179.** Interim decision: interviews don't go through `rateEditionCoverage.js` (compiled-edition `.txt`-only). When city-hall runs C93, check civic voice statements for references to the C92 Mayor interview's commitments (eight-vote architecture, sunset-vote contingency, DA gate triage). If voice agents pick up the canon → bay-tribune container retrieval is sufficient, 8d stays DEFERRED indefinitely. If voice agents don't reference it → promote `rateSupplementalCoverage.js` build into the bundled non-edition publishing pipeline plan (above). Live in `/interview` SKILL.md Step 8d as an inline DEFERRED note (S179 fix). LOW until evidence justifies the build.
- **BAY-TRIBUNE UNIFIED INGEST REBUILD: plan file written S189 — [[plans/2026-04-30-bay-tribune-unified-ingest-rebuild]].** Sibling to S183 wd-rebuild. 14-tag taxonomy (`bt-edition`, `bt-supplemental`, `bt-dispatch`, `bt-interview-article`, `bt-interview-transcript`, `bt-wiki-citizen`, `bt-wiki-storyline`, `bt-wiki-continuity`, `bt-wiki-cultural`, `bt-wiki-business`, `bt-roster`, `bt-game-result`, `bt-canon-correction` + parent `bay-tribune`) + customId-as-slug discipline + DELETE-by-customId wipe primitive (`scripts/wipeBayTribuneByCustomId.js`). Endpoint migration for `ingestEditionWiki.js` (legacy `/v4/memories` → `/v3/documents`) closes divergence with `ingestEdition.js` + `/save-to-bay-tribune`. After rebuild, Perkins-class scrub = `wipeBayTribuneByCustomId <slug-prefix>` + re-ingest. **Phase 1 + Phase 1.5 inventory + disposition map DONE S189** (engine-sheet — `scripts/auditBayTribune.js` + `scripts/auditBayTribuneUnknowns.js` + 22-doc disposition map appended to plan §Phase 1 findings; 175 docs total / 6 buckets resolved). **Phase 2-7 ON HOLD** pending SMFS pilot outcome (S189 Mike call — Supermemory Filesystem v0.0.1 released 2026-04-29; if mags-first pilot succeeds, Phases 2-7 collapse to `cp` + directory structure since 16-tag taxonomy maps 1:1 to paths). Validation fixture: C92 KONO Second Song dispatch. MEDIUM.
- **C93 GAP TRIAGE EXECUTION PLAN — S197+S198 (research-build), 5 waves spanning 115 gaps.** Cross-cycle triage frame across all 6 C93 gap-log sidecars below. Plan: [[plans/2026-05-03-c93-gap-triage-execution]]. **Wave 1** DOC-drift sweep (~15 gaps, DONE S197 commit `279b290`); **Wave 2** canon RULES hardening (~8 gaps, DONE S197 commit `cd89cc5`); **Wave 3** engine-sheet handoff bundles A-H (~22 gaps closed in code, DONE S197, archived S198 — see [[engine/ROLLOUT_ARCHIVE]] §S198 Archive Pass); **Wave 4** 3 architectural design plans — all closed S198: [[plans/2026-05-03-vote-trigger-mechanism]] REWRITTEN as engine-sheet wiring handoff (engine owns vote resolution; original /council-vote skill design retired), [[plans/2026-05-03-run-cycle-gap-log-surface]] REWRITTEN with all 4 Q's closed (engine-sheet MDs rule loosened wholesale; coder-persona hybrid; 8-class taxonomy), [[plans/2026-05-03-rollout-triage-cadence]] READY with all 5 Q's blanket-approved; **Wave 5** FLUX research note (G-PR10/11/14 cluster, DONE S197 commit `e62133e`). **Acceptance:** every gap entry's `Status:` line moves from `logged` to `DONE S<n>` / `WAVE 3 HANDOFF S<n>` / `WAVE 4 DESIGN S<n>` / `WAVE 5 RESEARCH S<n>`. ~30 LOW gaps stay unwaved (opportunistic close during related work). The 6 gap logs already pointed at by entries below.

#### Wave 3 — Engine-Sheet Handoff Bundles (S197 research-build → engine-sheet) — DONE S197

All 8 bundles (A-H) DONE S197 — archived to [[engine/ROLLOUT_ARCHIVE]] §S198 Archive Pass → §Wave 3 Engine-Sheet Handoff Bundles. Closed ~22 cross-cycle gaps in 16 commits same day (research-build wrote bundle specs in commit `374d7a9`; engine-sheet shipped all 8 same day). Plan: [[plans/2026-05-03-c93-gap-triage-execution]] §Wave 3. **Surfaced + parked separately as still-active engine-sheet work:** `resolveCitizens` Sr./Jr. tokenizer fix — see §Data & Pipeline below.


- **CITY-HALL-PREP C93 GAP LOG — S192 (civic terminal), 16 entries, MEDIUM.** First /city-hall-prep run with explicit gap-tracking instruction. Source of truth: `output/production_log_city_hall_c93_gaps.md` (sidecar to `output/production_log_city_hall_c93.md`). 16 gaps surfaced across 6 classes (pipeline-fragility / user-soft / process-gap / quiet-pipe / canon-risk). **HIGH-severity entries:** G-15 (skill doesn't pull between-cycle interview canon — would have shipped 6 stale voice directives without Mike's catch), G-14 (Brenda Okoro has no voice agent but Mara directive named her on record — routed through Mayor M7 fallback). **Cross-terminal pipeline-fragility:** G-1 (`mcp__godworld__lookup_initiative` returns semantic faith-card matches instead of Initiative_Tracker rows), G-2 (`mcp__godworld__get_council_member` returns empty for D1-D5/D7 — every district queried), G-3 (URL encoding bug in `lookup_initiative` related-articles branch). **Skill text fixes (research-build):** G-9 (Mara-is-additive framing), G-10 (voice-routing table misleads on faction-bloc topology), G-13 (sheet-read step skipped because world summary already snapshots ledger), G-15 + G-16 (between-cycle published-canon ingestion automation). **Auto-elimination paths logged per gap.** Promote individual gaps to standalone work items as bandwidth allows; for now the gap log is the canonical inventory.
- **SIFT C93 GAP LOG — S194 (media terminal), 14 entries, HIGH.** (promoted: C93, severity: HIGH) First /sift run with explicit gap-tracking instruction. Source of truth: `output/production_log_edition_c93_sift_gaps.md` (sidecar to `output/production_log_edition_c93.md`). 14 gaps surfaced — 6 HIGH / 4 MED / 4 LOW. **HIGH-severity bundles (cross-terminal):** **G-S7+S8+S10+S12 MCP citizen-verification outage — DONE S197 BUNDLE-B + S199 verified.** Cluster closed across 4 fixes: (a) `godworld-mcp.py` switched to hybrid+0.3 threshold (`scripts/godworld-mcp.py:55,69-70` — default memories-mode 0.6 missed citizen cards); (b) `lookup_citizen` rewritten to read 3 sources (WD-CITIZENS structured + WORLD-DATA broader + BAY-TRIBUNE history); (c) `get_roster` reads `truesource_reference.json` directly + handles `asRoster` shape; (d) `queryLedger.js` + `rateEditionCoverage.js` env-loader fix picks up `GODWORLD_SHEET_ID` from `~/.config/godworld/.env`. **S199 boot verified end-to-end:** `lookup_citizen("Patricia Nolan")` → 3-source structured response (similarity 0.80+); `get_council_member("D4")` → truesource direct (Ramon Vega / D4 / IND / active); `get_roster("as")` → 90 players in asRoster shape. Per S170 canon-hierarchy *sheets are primary canon* — paths all live. Bay-tribune fallback no longer required. **G-S1+S5 pipeline sequencing** — world summary + engine audit both generate BEFORE `/city-hall`, so derivative artifacts carry stale civic state into sift; 4 of 5 `suggestedFrontPage:true` patterns framed initiatives as stuck that civic just moved. Fix: auto-rebuild both after `/city-hall` completes. **G-S2+S3 auditor citizen-coverage** — `tribuneFraming.storyHandles[*].citizens` and `affectedEntities.citizens` always empty across all 38 patterns, breaking Phase 38.4 "auditor seeds, sift gates" promise; sift falls back to manual citizen discovery. Fix: auditor extension to populate from world-data + newsroom-memory linkage. **MED-severity:** G-S4 NEWSROOM_MEMORY exceeds Read limit (1,155 lines); G-S9 bay-tribune canon drift (Patricia Nolan 66→55, Dante Nelson Adams Point→West Oakland — multiple inconsistent ingest entries surface together); G-S14 brief template spec stale (300-500 word target vs working practice 800-1,100). **LOW:** G-S6 sift_proposals.json dump-ordering, G-S11 new-citizen acceptance pattern, G-S13 two `carmen_delaine` directory variants. Promote individual gaps to standalone work items as bandwidth allows; gap log is canonical inventory.
- **WRITE-EDITION C93 GAP LOG — S195 (media terminal), 25 entries, HIGH.** (promoted: C93, severity: HIGH) First /write-edition run with explicit gap-tracking instruction (sibling to S193 city-hall run + S194 sift gap logs — completes the C93 pipeline trio). Source of truth: `output/production_log_edition_c93_write_gaps.md` (sidecar to `output/production_log_edition_c93.md`). 25 gaps across 7 categories — 8 HIGH / 8 MED / 9 LOW. **HIGH-severity automation candidates (top 7 by leverage):** **G-W19** SKILL.md compile template uses `===` dividers + rich section labels (CIVIC AFFAIRS / CITY LIFE / ACCOUNTABILITY / QUICK TAKE / SPORTS / FEATURES) but `scripts/capability-reviewer/parseEdition.js` requires `^-{10,}$` divider + fixed allowlist (FRONT PAGE / EDITOR'S DESK / CIVIC / CULTURE / BUSINESS / OPINION / SPORTS / LETTERS) — first compile produced 0-section parse, forced full reformat (single biggest one-button-compile unblocker). **G-W12 + G-W14** civic-desk + freelance-firebrand RULES.md need canonical 9-member council roster injection at boot (Vega D4 President, Rivers D5 OPP spokesperson, Tran D2 IND, Crane D6 CRC recovering, Ashford D7 CRC, Chen D8 CRC, Carter D1 OPP, Mobley D9 OPP, Delgado D3 OPP) — Carmen S4 fabricated "Council President Janae Rivers / District 2 motion" (Vega is President, D2 is Tran, Rivers is D5); same failure class as S193 G-R6/R7/R10 (project agents fabricated 6 non-canon council members + 6-3 vote outcome). **G-W16 META** — S193's G-R6/R7/R10 was promoted to ROLLOUT, never actioned in 2 cycles, recurred this cycle in civic-desk; gap-log → ROLLOUT pattern lacks triage cadence (HIGHs sit on shelf and compound across cycles). **G-W15** MCP `get_council_member` returns empty for D1-D9 from media terminal (compounds with sift G-S7-S12 outage; bay-tribune fallback worked because all council members had prior canon, but new-faction cycles would silently land unverified). **G-W22** validateEdition.js produced 99 "critical" findings, ~95% false positives — last-name-collision plague (Maria Reyes Oversight Committee suggests Caleb Reyes Public Defender; Marcus Carter Temescal hair stylist suggests Denise Carter D1 council; Vanessa Tran-Muñoz OARI Director suggests Leonard Tran D2; Bobby Chen-Ramirez Health Center Director suggests Nina Chen D8; Priya Patel Oversight Committee suggests Jonas Patel Medical Examiner). **G-W2** sift produces per-reporter briefs, Task tool exposes per-desk agents only — Mags must hand-route 8 reporters to 4 desk agents per cycle (sift should write `dispatch_c{XX}.json` map). **G-W24** world_summary builder typo "Eric Tavares" propagated through brief into Anthony's article — canon roster has "Eric Taveras"; world-summary builder should read canon roster as name authority. **G-W7** sift Step 4 should pre-resolve POP-IDs when brief calls for "find me a 65+ East Oakland fan" — Hal S5b invented Cordell Mays + Mezran S1 invented Vivienne Torres because briefs permitted invention without pre-resolved candidates. **MED:** G-W3 (no reporter→desk routing index), G-W4 (skill rule "brief + IDENTITY only" conflicts with desk-agent boot sequence which reads 8+ files), G-W5 (output path mismatch), G-W6 (desk workspace files referenced but not built), G-W13 (editor verification ordering — must check world_summary first then roster), G-W20 (parseEdition collapses multi-article sections to 1), G-W21 (`front-page-leads-with-highest-severity-ailment` rule rigid against editor judgment — Mike S195 override on Health Center cyclesInState=90 vs Transit Hub structural-gap), G-W26 (Mara delivers free-form prose, maraJsonReport.js expects structured markdown), G-W28 (Final Arbiter doesn't carry editor overrides forward from Step 3.5 — capability gate enforced as hard block regardless, forcing second override at Step 5.5). **LOW:** G-W1 launch order spec stale, G-W8 reporter agent persistence, G-W9 Jax routing ambiguity nightlife/culture vs accountability, G-W10 anonymous-source convention not graded, G-W11 firebrand boot heavier than desk agents, G-W17 CRC RULES.md missing Delgado from canonical-roster list, G-W23 initiative budget warning conflates total vs line-item, G-W25 Rhea verification 14:32 / 24 tool uses, G-W27 Mara delivered out of pipeline order. Promote individual gaps to standalone work items as bandwidth allows; gap log is canonical inventory.
- **EDITION-PRINT C93 GAP LOG — S196 (media terminal), 20 entries, HIGH.** (promoted: C93, severity: HIGH) First /edition-print run with explicit gap-tracking instruction (and first full-edition print run since E91/S188 — 2-cycle silence). Source of truth: `output/production_log_edition_c93_print_gaps.md` (sidecar to consolidated `output/production_log_c93.md` §Print Pipeline C93). 20 gaps across 7 classes — 6 HIGH / 5 MED / 9 LOW. **Tier-1 autonomy blockers (sharpest single-run dies):** **G-PR8** slug convention mismatch — SKILL.md + DJ subagent prompt examples use kebab-case, `generate-edition-photos.js:122` validator regex `^[a-z0-9_]+$` requires snake_case; fresh run rejected ALL 6 specs at validator, required surgical JSON edit before re-run. **G-PR1** stale per-skill log filename — SKILL.md Step 0 references `production_log_edition_c<XX>.md`; current convention is consolidated `production_log_c<XX>.md` (S195 fix); Step 0 fails or creates orphan log. **Tier-2 FLUX canon-fidelity ceiling cluster (affects every cycle):** **G-PR10** FLUX cannot reliably suppress text in negative-framed regions — 4 of 6 photos hit text-legibility issues; mesa fail (real QuikCAM/ERB stadium logos), baylight fail (legible "BAYLIGHT AUTHORITY" on signage), transit_hub flag (gibberish "Ovaratora"/"Commcittee"/"Curstica" on placards); negative-frame paragraphs are soft suggestions to FLUX, not hard constraints. **G-PR11** regen-on-fail re-rolls same prompt — canon failures are prompt-shape failures, not seed failures; 1 retry with same prompt rolls same dice; mesa initial QuikCAM logos → retry jersey-number-visible (different failure, same class). **G-PR14** prompt-engineering trade-off — composition-first text-suppression rewrite (manual third pass on mesa) successfully avoided text but FLUX rendered wrong subject (fielder/baseman, not pitcher mid-windup); 3 attempts, 3 different failure modes (logos → jersey → wrong subject). Together: text suppression in image generation is the new canon-fidelity frontier; tooling needs OCR post-check + prompt-rewrite on retry escalation, not seed re-roll. **Tier-3 silent-defect risk:** **G-PR12** PDF generator does not respect `editorialFlag: true` — `generate-edition-pdf.js` reads `manifest.json` only, never `qa_report.json`; mesa editorial-flag photo would silently ship to print unless operator manually edits manifest. C93 worked around by manual manifest drop with `dropped[]` annotation; defeats QA gate ceremonially in autonomous mode. **Tier-4 process design:** **G-PR15** no documented abort threshold — skill provides 1 auto-retry; operator picked 3-strike convention ad-hoc for mesa; without code-enforced abort autonomous mode either loops on hard-canon-fail spec or ships flagged photos. **G-PR18** SKILL.md Step 4 says "photos also upload if Drive destination configured" but `scripts/saveToDrive.js` has zero photo / directory / uploadDir references — script uploads PDF only; E91+E92+E93 photos all local-only (E92 + E93 pipeline-silence-masked). **MED:** G-PR2 (2-cycle pipeline silence — last full edition print was E91), G-PR3 (stale world_summary fed to DJ bundler — same root pattern as sift G-S1+S5), G-PR9 (SKILL.md prompt word band 120-180 vs validator 100-220 — same DOC-drift class). **LOW (9):** G-PR4 (bundler unmatched-proposal silent counter), G-PR5 ("indexed: 19 total" misleading vs 14 actual articles+letters), G-PR6 (DJ word counts above SKILL.md band on 4/6 specs), G-PR7 (DJ low-end of budget with no skip-rationale block), G-PR13 (atlas_bay PASS reveals composition-suppresses-text pattern; promote to dj-hartley/RULES.md), G-PR16 (PDF generator silent on missing-section-photo), G-PR17 (no Date in masthead — generator metadata empty), G-PR19 (saveToDrive doesn't parse cycle from filename), G-PR20 (saveToDrive "no manifest found" warning every run). **Cross-cycle pattern observations:** (1) DOC drift dominates 7/20 gaps — same skill-text-vs-validator-constants class as sift G-S* and write-edition G-W*; ~30% of accumulated gaps across all four media skills would close with one reconciliation pass. (2) FLUX ceiling is real and repeatable — G-PR10/11/14 cluster forms a structural canon-fidelity boundary, not a bug. (3) Auto-pipeline silent on partial-success — generator says "Generated: 6/6" with no QA-issue signal; PDF generator says "Photos found: 5" with no missing-section signal; operator must read every output. (4) 2-cycle silence (G-PR2) compounds gap discovery — write-edition G-W16 meta-pattern (HIGHs sit on shelf and compound) applies to print pipeline too. C93 outcome: PDF on Drive (`1Mcb18Jk560ICLx5KHZK9GxTbAckoI1p3`); 5 photos placed (FRONT PAGE + CIVIC ×2 + BUSINESS + ATMOSPHERIC); SPORTS/LETTERS/CHICAGO text-only; mesa dropped from manifest with audit trail. Promote individual gaps to standalone work items as bandwidth allows; gap log is canonical inventory.
- **POST-PUBLISH C93 GAP LOG — S195 (media terminal), 25 entries, HIGH.** (promoted: C93, severity: HIGH) First /post-publish run with explicit gap-tracking instruction (closes the C93 pipeline quartet — city-hall-prep + city-hall-run + sift + write-edition + post-publish). Source of truth: `output/production_log_edition_c93_post_publish_gaps.md` (sidecar to consolidated `output/production_log_c93.md`). 25 gaps across 6 categories — 15 HIGH / 6 MED / 4 LOW. **Top automation bundles (ranked by leverage):** **BUNDLE-A Format-contract enforcement on .txt outputs (G-P8 + G-P9 + G-P6) [HIGHEST PRIORITY]** — three HIGHs share root: /write-edition compile emits CITIZEN USAGE LOG instead of NAMES INDEX, BUSINESSES NAMED is absent, downstream parsers (ingestPublishedEntities + rateEditionCoverage + verifyNamesIndexParse) silently no-op or false-pass; **3 new citizens (Vivienne Torres / Diane Foster / Thomas Webb) + Atlas Bay Architects firm + Greater Hope Pentecostal Church faith-org silently dropped from intake on E93**; same root as write-edition G-W19. Single fix bundle: add `emitFormatContractSections` to /write-edition compile + parser fallback to CITIZEN USAGE LOG + section-existence assertion in verification gate. **BUNDLE-B Skill spec audit — bash invocation arg mismatches (G-P12 + G-P13 + G-P19) [QUICK WIN]** — three HIGHs are skill spec saying `<XX>` where script wants path or missing `--apply` flag (buildCitizenCards.js, gradeEdition.js, extractExemplars.js). 30-min fix; eliminates "skill ran but wrote nothing" class. **BUNDLE-C Grading pipeline rebuild (G-P14 + G-P15 + G-P16 + G-P17 + G-P18) [SECOND-PRIORITY]** — five gaps (4 HIGH + 1 MED). gradeEdition.js produced celebratory A on Mara A- cycle, missed Mara grade entirely (`Mara grade: not found`), counted 7 of 10 articles, hallucinated B+ on zero-article desks, and gradeHistory split Mags/Margaret as separate reporters. Fix: canonical Mara audit JSON shape + gradeEdition reads from it + ARTICLE TABLE-indexed counts + reporter-name normalization. **BUNDLE-D Pre-flight env + auth check (G-P7 + G-P24) [QUICK WIN]** — two HIGHs on env var missing (`GODWORLD_SHEET_ID` not in media terminal env — same drift class as sift G-S10) + Supermemory HTTP 401 from buildCitizenCards.js. Add Step 0 pre-flight: read required env vars + ping each external service with noop call; fail-fast at boot. Saves the ~2.3-hour 401-retry waste pattern (script retries 401 four times at 8s sleep). **BUNDLE-E Sequencing fixes — early ingest + bot restart (G-P1 + G-P23) [LOW EFFORT]** — /save-to-bay-tribune at /write-edition close duplicates Step 1b text ingest; /post-publish bot restart conflicts with /session-end's restart. Move text-ingest into /post-publish 1b (single canonical home); move bot restart into /session-end (single canonical home). **BUNDLE-F Stale derivative artifact protection (G-P3 + G-P22) [STRUCTURAL]** — world summary and postRunFiling manifest both reflect pre-pipeline-v2 state; same root pattern as sift G-S1+G-S5 (stale derivative artifacts presented as fresh inputs). Step 21 G-P21 also notable — Step 10 /skill-check is the single most token-heavy substep; deferred this cycle (criteria-file changelog entries written directly from gap-log + Mara verdict + Mike's audience verdict). **C93 actual state from this run — DO NOT trust:** citizen cards partial/unknown (575/836 attempted, 401-fail count unknown); Edition_Coverage_Ratings sheet has no C93 row; engine Phase 2 next cycle has no C93 coverage signal; 3 new citizens + 1 firm + 1 faith-org never landed in their canonical sheets. Promote individual gaps to standalone work items as bandwidth allows; gap log is canonical inventory.
- **CITY-HALL RUN C93 GAP LOG — S193 (civic terminal), 15 entries, HIGH.** (promoted: C93, severity: HIGH) First /city-hall execution-phase run with explicit gap-tracking instruction (sibling to S192 prep gap log). Source of truth: `output/production_log_city_hall_c93_run_gaps.md`. 15 gaps surfaced across 5 classes — 8 HIGH / 3 MED / 4 LOW. **HIGH-severity automation candidates:** G-R14 `assembleDecisions.js` script bridging `output/civic-voice/*.json` → `output/city-civic-database/initiatives/*/decisions_c{XX}.json` (currently 15 min hand-assembly per cycle), G-R11 vote-that-didn't-trigger (Transit Hub vote scheduled C93 didn't fire because only 1 of 9 council voices had INIT-003 on desk — needs /council-vote skill OR /city-hall-prep route-to-all-9 pass), G-R6/R7/R10 project agent council-canon fabrication (Transit Hub invented 6 non-canon council members + 6-3 vote outcome — RULES.md hardening + canonical roster injection at agent level), G-R8 date/year leakage in 11/11 outputs (newsroom rule needs hard enforcement in agent RULES files, sed scrub at end-of-cycle is wrong layer), G-R9 project agent JSON schema drift (wrapped in `{statements: []}` vs flat array). **Reviewer-side gaps:** G-R12 City Clerk reads quarantine directory (canon-scope rule needs codification), G-R13 Clerk audit JSON malformed. **Pipeline gaps:** G-R15 applyTrackerUpdates writes civic_sentiment in dry-run (gate behind `--apply`). **Canon-fidelity rollout regression case:** S175 framework didn't catch Transit Hub agent's roster fabrication; project-agent four-file structure needs roster-injection at IDENTITY level. Promote individual gaps to standalone work items as bandwidth allows; gap log is canonical inventory.
- **DEDICATED SESSION: Perkins&Will C92 contamination scrub — SCRUB-SIDE DONE S186 (research-build), HIGH.** (promoted: C92, severity: HIGH) S186 closed the entire scrub-side workload in one session. **Canon-fidelity rule:** §Read-Time Contamination Check added to `docs/canon/CANON_RULES.md`; 23-agent RULES.md refresh (20 generators with Read-Time Contamination Scan block + 2 reviewers with Read-Time Contamination Audit block + final-arbiter with Read-Time Contamination Propagation block). **Layer 1 live signal:** 7 files + 3 desk packets scrubbed; `buildDeskPackets.js 92` re-ran clean post-sheet-edit (sheet cell handled by Mike directly). **Layer 2 canonical historical:** 7 files scrubbed (edition.txt + 4 reporter briefs/articles + 2 PDFs). **Layer 3 audit corrigendum:** 3 files (Mara review + Rhea .json + Rhea .txt) carry top-of-file `[CORRIGENDUM C92→post-scrub S186]` with body preservation. **Bay-tribune chunked re-ingest:** old `T1KLnnJSqNybHsEjxt3gVM` (Part 1, 2 hits) + `i9gbnZLtb7sZBjX3KuxzYY` (Part 2, 3 hits) DELETE'd; new clean `NnpkqYpTwnKAm1qyxN5Xag` (Part 1) + `SCZcxjcMkrK4CW41tufWJd` (Part 2) ingested via `node scripts/ingestEdition.js`. **Verification:** `grep -c Perkins` returns zero across all Layer 1+2 files; bay-tribune full-content audit returns 0 literal Perkins across 9 search-surfaced docs; Layer 3 audit bodies retain Perkins with corrigendum block at top. **Wiki ingest skipped intentionally** — per-entity records carry structured metadata only, no firm-name freeform text; no contamination cleanup required at wiki layer. **Architectural follow-up filed** above this entry: Bay-tribune unified ingest rebuild applying S183 `wd-` pattern; motivated by the scrub friction. Full corrigendum block in `output/production_log_edition_c92.md`. **Remaining (civic terminal, separate session):** `/city-hall` C93 dry-run smoke test, verify `grep -c Perkins output/civic-voice/*_c93.json` returns zero. Original full plan: mags `WL8kvoxQgmcvxSPW3Ph47n`. Connects to S175 Canon Fidelity Rollout (regression case).

### Data & Pipeline

- **TECH DEBT: Tech debt audit 2026-04-15.** Path 1 CLOSED S156 — archived to [[engine/ROLLOUT_ARCHIVE]] §Data & Pipeline closures (Math.random sweep across cycle-path engines). Path 2 active below.
- **PHASE 42 — Writer Consolidation: §5.6 phase05-ledger REDESIGN DONE S188 (engine-sheet); B2 unblocked; B2-B7 mechanical migrations next.** Plan: [[plans/2026-04-28-phase-42-writer-consolidation]]. Inventory: [[engine/PHASE_42_INVENTORY]]. Patterns: [[engine/PHASE_42_PATTERNS]] (§5.6 carries the closed redesign + changelog). **B0 canary surfaced systemic blocker (S184, engine-sheet):** 11 engines (cohort A: 8 direct-writers + cohort B: 3 already shipping queueRangeIntent_) do full-table read-mutate-write on Simulation_Ledger per cycle. Direct sites: runAsUniversePipeline:587, runEducationEngine:454, generateNamedCitizensEvents:715, runNeighborhoodEngine:529, runRelationshipEngine:596, runHouseholdEngine:507, runCivicRoleEngine:437, runCareerEngine:882. Already-queued: generateGameModeMicroEvents:556, generateCivicModeEvents:505, generateMediaModeEvents:466. Cohort A currently safe (direct writes materialize sequentially, downstream engines read post-previous-engine state); **cohort B is shipping a live bug today** — 2 of 3 engines' mutations get clobbered at Phase 10 last-write-wins each cycle, masked because cohort A's direct-writes prop the chain forward. **REDESIGN LOCKED + SPEC AMENDED S185: approach (a) shared in-memory `ctx.ledger`** (canonical doc: mags `hQE4rREEWBpS9aS1g3mQ3M` — covers original reasoning `fTzSivJgpXmaBcB5vrPEn1` + engine-sheet audit `2Lh8xsEHc6BMbBARM6mwHU` + verified amendments) — Phase 1 reads SL once into ctx.ledger at godWorldEngine2 pre-phase-04 entry; **18 SL touchers** (16 full-range writers + 2 per-row + 5 post-phase05 readers — see PHASE_42_PATTERNS §5.6 A1 for full enumeration tables) route through ctx.ledger; Phase 10 commits via single replace intent. Approach (b) per-row cell intents rejected (doesn't fix read-staleness — engine B still sees pre-A state regardless of how A's write is queued); (c) hybrid rejected (inherits b's flaw + 2 patterns to maintain). Side benefit: cohort B bug closes the moment redesign lands; ~10 fewer SL reads per cycle. Full spec in PHASE_42_PATTERNS §5.6. **B0 carved to P1 append-only** (LifeHistory_Log line 480) — line 507 ledger commit absorbed into the redesign commit. **finalizeWorldPopulation NOT a peer hazard** — verified at canary time: 27 sites are P3 cell writes to World_Population (single-row scope), B5 unblocked. Real cycle-path scope still 37 files / 175 sites. Tiered acceptance preserved: B6 stop-point (~58%) / B7 (full). **Phase 1 inventory + Phase 2 patterns DONE.** **Phase 3 (engine-sheet) S184:** API + harness shipped (`95c4144` + `3c9c856`); B0 pilot append-only carved. **B1 status:** unblocked. **B2 status:** unblocked once engine-sheet verifies the §5.6 redesign + ships the Phase 1/Phase 10 bookends + 11-engine read/commit removal — after that B2's run*Engine × 4 migrations become trivial (already converted away from direct writes by the redesign). Estimated 6-8 engine-sheet sessions for full close; B0-B6 is the recommended stop point. Closes ENGINE_REPAIR Row 8. **Engine-sheet handoff (research/build → engine/sheet):** verify §5.6 spec against actual code (Phase 1 init location, header-cache patterns, audit non-phase05 SL readers); ship redesign as one batch ahead of B2; flag back if audit surfaces unexpected SL writers outside the 11. **S185 §5.6.6 AUDIT COMPLETE (engine-sheet) — REDESIGN ON HOLD:** read-only audit surfaced 7 additional cycle-path SL writers (5 full-range cohort-A-equivalent: `generateGenericCitizenMicroEvent` Phase5-GenericMicroEvents, `generationalEventsEngine` Phase5-Generational, `generateCitizensEvents` Phase5-CitizenEvents, `runCivicElectionsv1` Phase5-Elections, `compressLifeHistory` Phase9-CompressLifeHistory + 2 per-row: `checkForPromotions` Phase5-Promotions, `processAdvancementIntake` Phase5-Advancement) + 4 post-phase05 readers (`bondEngine` fallback, `buildEveningFamous` Phase 7, `mediaRoomIntake` Phase 7, `compressLifeHistory` Phase 9) + 1 latent function-name collision (`generateCitizensEvents_` defined in BOTH `phase04-events/generateCitizenEvents.js` and `phase05-citizens/generateCitizensEvents.js`). Decision (a) still correct for amended scope. **Research-build amended §5.6 S185 (post-audit verification)** — all 7 findings verified against actual code; amendments locked: scope 11→18 with full enumeration tables, Phase 1 init at godWorldEngine2 pre-phase-04 entry, compressLifeHistory routes through ctx.ledger (no special case), `runCivicElectionsv1.js:451` row-1 quirk handled via header/body split at init, prerequisite-delete `phase04-events/generateCitizenEvents.js` (v2.4 dead — verified loses flat-namespace race to phase05 v2.8 via alphabetical clasp push order; strict feature subset; nothing to salvage), cost revised to ~10-13 commits in one engine-sheet redesign session. Full amended spec: [[engine/PHASE_42_PATTERNS]] §5.6 + canonical mags doc `hQE4rREEWBpS9aS1g3mQ3M`. **Status S188 (engine-sheet):** §5.6 redesign batch DONE — 9 commits `0e31e66..6609c4a` migrated all 16 full-range writers + 2 per-row + 5 spec'd readers + 3 audit-miss readers + Phase 1 init + Phase 10 commit handler to shared `ctx.ledger`. Prerequisite-delete shipped S185 (`1a77e54`, research-build). Spec entry A1 #8 `generateNamedCitizensEvents:715` confirmed orphan (zero cycle callers; documented inline at `phase01-config/godWorldEngine2.js:1079`). B2 (run*Engine × 4 mechanical migrations) unblocked — already converted away from direct writes by the redesign. **Smoke-test S191 (engine-sheet):** C93 ran end-to-end after pre-flight surfaced + closed a critical missing wiring — `runWorldCycle` had no `Phase10-CommitLedger` call (S188 batch wired only `runCyclePhases_`); production entry would have silently dropped every staged ledger write. Closed commit `188ce5b`. Cycle log surfaced two function-name collisions in non-§5.6 paths (`loadActiveStorylines_` + `getCurrentCycle_` — same A4 pattern as `generateCitizensEvents_`); closed commit `2926e76`. Two cohort-C-equivalent migration debts surfaced — `generationalWealthEngine` + `educationCareerEngine` bypass `ctx.ledger`, get clobbered by Phase 10 commit. Logged as ENGINE_REPAIR Row 18 (decision pending: migrate / sequence post-commit / accept clobber). Cohort-B clobber claim from §5.6 redesign not fully verifiable from sheet inspection alone (no pre-cycle baseline); next cycle should show storyline + arc errors gone, validating rename fix landed. See PHASE_42_PATTERNS Changelog 2026-04-29 (S188) for per-commit breakdown + ENGINE_REPAIR Rows 18, 19 for open work.
_(items archived S186 — see [[engine/ROLLOUT_ARCHIVE]] §Data & Pipeline closures: Dead-code detection scan, Dead-code DELETE batch, /pre-mortem C92 findings)_
- **POST-E92 ENGINE REPAIR: /engine-review C92 findings (engine-sheet) — S166.** Open `output/engine_review_c92.md` for full ailment framing. Enricher misattribution bug DONE S185 (commit `6a30094`) — archived to [[engine/ROLLOUT_ARCHIVE]] §Data & Pipeline closures. One repair-side item remains open:
  - **`advance-initiative` remedy threshold calibration — REGISTRY-SIDE TASK (engine-sheet) S185.** C92 data: 5 advance-initiative measurements, 0 firing-as-expected; verdict distribution 3 not-firing + 2 overshot. EventAttractiveness threshold 0.02 vs observed 29 (1450x) indicates unit-scale mismatch in `mitigatorRegistry.json` — field is in [0, 100] range but threshold calibrated as if [0, 1] normalized. RetailVitality 0.02 vs observed 0.11 (5.5x) suggests cycle-by-cycle expected band is too narrow. Needs per-metric recalibration grounded in multi-cycle observed range, not a code fix. Pull C91 + C92 + future C93 measurement records, derive p25/p75 of observed deltas per metric, set `magnitudeThreshold` to median observed under "active phase" conditions. MEDIUM. Pick up after C93 audit JSON lands.
  - **Persistence note:** `output/engine_review_c92.md` lives in gitignored `output/`. Bundled with `ADD: Briefing versioning` item — same root cause, same fix.
- **CLEANUP: Dead spreadsheet tabs — PARTIAL S139.** 6 of 8 tabs backed up and hidden. Faith_Ledger consumer DONE S180 (see changelog). **Youth_Events stays** — `youthActivities.js` actively writes, but ledger has only 1 citizen under age 20. Engine works, population doesn't. Needs kids — see Youth population gap below.
- **CLEANUP: Generator-side case discipline for `ev.domain` — DONE S199 (commit `23b3303`, recordWorldEventsv3 v3.6 → v3.7).** Picked option (a)+ — UPPERCASE end-to-end (matches dominant 209-row state + reader expectations). Audit found 3-layer inconsistency: writers UPPERCASE, internal lookups (domainMap/domainNeighborhoods/deriveEventType) Title Case, readers UPPERCASE. Internal Title Case silently mismatched UPPERCASE incoming domains, breaking per-domain neighborhood pool selection AND eventType classification (root cause of Row 6's 91% misc-event Domain side — S184 closure addressed citizen attribution via LifeHistory_Log but left this side broken). 5 changes in single file: domainMap values / domainNeighborhoods keys / deriveEventType comparisons all UPPERCASED, defensive `String(...).toUpperCase()` at domain pickup site, header bump. 4 historical Title Case rows left as-is (orphaned but harmless).
- **FIX: Household structure + youth population gap (engine-sheet terminal) — PLAN DRAFTED S201, parked for dedicated session.** Plan: [[engine/LEDGER_REPAIR_HOUSEHOLDS]]. Three-stage build pair-married-couples + reconcile-NumChildren + ingest-120-youth. ENGINE_REPAIR Row 20 points here. S201 audit surfaced bigger picture than originally framed: 426 married/partnered citizens have no actual partners (S184 backfill set MaritalStatus via CDF without spouse linkage), 526 of 529 households are singletons, 1,161 NumChildren claims reference children that don't exist, 0 actual teenagers (13-17 cohort). Cannot add youth without households; cannot form households without pairing partners. Mike-decided rules: age-range pairing within ±5 years, same-neighborhood preference, couples share last names, Tier-1 bypass for manual review. Estimated 2-3 engine-sheet sessions + 1 Mike-review session for Tier-1. Pre-build prerequisite: C94 cycle (cohort-C processing may shift ledger state).
- **FIX: `resolveCitizens` Sr./Jr. suffix tokenizer (engine-sheet) — DONE S199 (commit `d9763ca`).** Trailing `Sr.`/`Jr.`/`II`/`III`/`IV` (with or without dot) is now stripped from `nameTokens` before first/last/middle picking; suffix appended back to `last` for new-citizen candidate rows so display + ledger stay aligned; lookup key uses the stripped form so an existing "Calvin Reeves" row matches a "Calvin Reeves Sr." input (correct default for editorial ingest where editor almost always means same person; generational duplicates still land via POP-ID path which skips name lookup). Verified against 9 representative inputs incl. Bishop Calvin Reeves Sr., Maria Elena Reyes Jr., John Smith III. Unblocks /post-publish C93 retry --apply backfill.
- **AUDIT: `Math.random` engine sweep — DONE S199 (commit `2c7397f`).** All 13 hits classified as false-positives — every one was inside a `/** ... */` JSDoc changelog block describing the S156 fix (e.g., "Replaced Math.random() with ctx.rng", "Falls back to Math.random()"), not an actual invocation. Filter tightened with `if (/^\s*\*/.test(line)) return;` to skip JSDoc star continuation lines. After fix: 0 real hits across phase01-phase10; engine.md S156 Phase 40.3 closure stands. C93 audit baseline drops 23 → 22 entries (HIGH 7 → 6).
- **WIRE: /sift consume engine pre-routes from Story_Seed_Deck (research-build) — NEW S201, MEDIUM.** Header drift fix `a05e5f6` made `SuggestedJournalist`/`SuggestedAngle`/`VoiceGuidance`/`MatchConfidence` consumer-readable. Engine matches per-seed via `rosterLookup.js v2.2 suggestStoryAngle_` (theme-overlap scoring + confidence levels). Live verified — 1108/1109 active C89-C93 seeds carry valid match (95% high-confidence). Two paths: (a) `/sift` Step 2 pre-fills reporter assignments from sheet, Mags overrides editorially (closes S195 G-W2 hand-routing gap); (b) leave engine matches as a parallel signal for `buildDeskPackets:2472` + `buildMediaPacket:209` to surface in desk briefings only. Recommend (a) — eliminates per-cycle re-derivation effort. Effort: ~30 min skill edit. Connects to: `phase07-evening-media/applyStorySeeds.js` v3.9, `phase10-persistence/saveV3Seeds.js` v3.5, sift Step 2/3.
- **INVESTIGATE: hookLifecycleEngine.js wiring gap (engine-sheet) — NEW S201, LOW.** File exists at `phase06-analysis/hookLifecycleEngine.js` with entry function + 7 cols pre-defined on Story_Hook_Deck (Severity / CreatedCycle / HookAge / ExpiresAfter / IsExpired / PickedUp / PickupCycle, preserved at cols 14-20 by S201 header drift fix). All 7 cols 0% fill across 443 rows — function never fires. No caller in `godWorldEngine2.js` cycle path. Three options: (a) wire into Phase 6, (b) confirm dead and delete the 7 cols + the file, (c) leave as-is (cosmetic only). MEDIUM impact if (a) — gives expired-hook detection. LOW priority until evidence justifies (a). Trace via `grep "runHookLifecycleEngine\|hookLifecycle.*Engine_" phase01-config/godWorldEngine2.js`.
- **DESIGN: Writer-vs-header schema alignment detector for engineCycleAudit (engine-sheet) — DRAFT S202.** Plan: [[plans/2026-05-05-writer-header-alignment-detector]]. New V2 detector class catches the structural drift S201 surfaced (Story_Seed_Deck/Story_Hook_Deck writer column expectations diverged from live header layout for ~10 cycles, silently dropping engine routing data into stale calendar header cols). Compares engine-writer field-name + col-position expectations against `schemas/SCHEMA_HEADERS.md` regen across the ~38 documented direct-write exceptions. 4 type tags (out-of-range / field-name mismatch / position drift / schema-shrunk-writer); HIGH for Types 1+3+4, MED for Type 2. 5 open Phase 1 questions for grill (taxonomy class slot, writer-set scope, self-test path, field-name extraction depth, cadence). LOW priority — flag for V2 expansion design alongside next /tech-debt-audit cycle. Trigger: S202 LEDGER_HEAT_MAP §Dead Column Inventory + §Column Cleanup Roadmap re-audit found 2 additional schema-shrunk sheets (Storyline_Intake / Citizen_Usage_Intake) where writers may still reference removed cols — first sample of the Type 4 class beyond the S201 incident.
- **AUDIT: engineAuditor 1.0.0 cyclesInState measurement-window bug (engine-sheet) — DONE S199 (commit `30d0e0f`).** Diagnosis correction: not a "wrong column" bug. The actual issue was a missing phase-match check in `detectStuckInitiatives.detect()` — the per-prior-audit loop trusted `prev.cyclesInState + 1` without verifying phase still matches, so a phase transition (INIT-003 vote-scheduled C92 → vote-ready C93) inherited the stale stuck count from the prior phase. Fix: guard carry-forward with `prevPhase === phase`. Synthetic verification (C92 audit as prior + queryLedger-confirmed C93 row state): INIT-003 89 → 7 (correctly resets after phase transition); INIT-001/002/005/006 unchanged (phases stable, counts honest measurement of audit-window stuck-time). Downstream `engineCycleAudit.js` cross-cycle-debt severity scaling (≥30 HIGH / ≥5 MED) now sees INIT-003 at MED instead of false HIGH. Node-only fix; no clasp push.
- **PROJECT: World Memory remaining (engine-sheet terminal)** — (3) ingest key archive articles to bay-tribune, (5) historical context in desk workspaces. See `docs/WORLD_MEMORY.md`. MEDIUM.
- **Supplemental strategy (media terminal)** — One supplemental per cycle minimum. Ongoing.
- **EVALUATE: Document processing pipeline (research-build terminal)** — Qianfan-OCR (Baidu, 4B params) does end-to-end document parsing: PDF/image → structured markdown in one pass. Layout analysis, table extraction, chart understanding, document QA. Could feed real civic documents (council minutes, zoning permits, budget reports) into the civic terminal as structured data. MEDIUM — evaluate when civic pipeline needs real-world document input. Added S137.
- **IDEAS: Arc engine grafts + typed municipal-vehicle character system + micro-event overlap detection (research-build → engine-sheet) — S190, LOW / idea-park.** Two grilling passes on DeepSeek drafts: v1 `GodWorld_Escalation_Engine_v1.0` (914 lines, `output/drive-files/GodWorld_Escalation_Engine_v1.0.txt`), v2 `Micro_Event_Generator_v2.0` (612 lines, truncated, `output/drive-files/Micro_Event_Generator_v2.0.txt`). Both drafts rejected as-is — v1 duplicates Phase 6 arc/hook/storyline infrastructure (`arcLifecycleEngine.js` + `processArcLifeCyclev1.js` + `hookLifecycleEngine.js` + `storylineWeavingEngine.js`); both use non-existent `ctx.getLedger()` accessor (real engine reads via `ctx.tracker.*` / `ctx.snapshot.*`); both assume ledger fields that don't exist (`secretDesire`, `is_athlete`, `c.street_address`, `n.crime_index`, `n.infrastructure_state`); v2 uses wrong tab name (`Neighborhood_Ledger` — actual is `Neighborhood_Map`); v1 had 24 stub functions where the judgment lives; v2 truncated mid-function. **Three usable patterns kept across both:**

  **PATTERN A — Four grafts onto existing arc engine (Phase 6).** Lands in `arcLifecycleEngine.js` + Event_Arc_Ledger.
  1. **COLLAPSED stage + abandonReason field.** Sibling to current `resolution` for when world state invalidates an arc (citizen dies, initiative cancelled, athlete traded). Stops forced-CRISIS-at-resolutionCycle from detonating on dead context.
  2. **Role-typed citizen attachments with stake fields.** PRIMARY_ACTOR / AFFECTED_RESIDENT / POLITICAL_OBSERVER / WITNESS / RIVAL_A/B / MEDIATOR / SECRET_KEEPER / TRUTH_SEEKER / ATHLETE / FAN_ANCHOR / ORGANIZATION_FIGURE — typed handles with per-role `stakeField`. Replaces current flat-name attachments. Gives Mara and reporters structured input at brief time.
  3. **ProjectedResolutionCycle countdown.** New column on Event_Arc_Ledger set at planting (`plantedCycle + rng.nextInt(2,5)`). Soft target — tension can pull forward or push back — but Media-Room-readable countdown lets reporters foreshadow without reading tension scores.
  4. **narrativeHooks block at resolution.** Per-outcome `{ angle, hook, citizensToInterview[] }` structure emitted on RESOLVED/COLLAPSED. Read by `mediaRoomBriefingGenerator.js`. The structured editorial-handoff that Mara's been doing manually.

  **PATTERN B — Generalize `generateGenericCitizenMicroEvent.js` into scoped variant (Phase 4).** New file `utilities/generateMicroEvent.js` accepting `scope` parameter: citizen | business | neighborhood | vehicle. Original citizen function becomes thin wrapper calling with `scope: 'citizen'`. Texture-rich flavor sets per scope (DeepSeek v2's prose quality is genuinely good — keep). Mood impact + visibility + tags integrate with Phase 6 pattern detection. Need new MicroEvents_Ledger tab schema spec OR append to existing event logs (decide at promotion time). Cap below 30/cycle — needs Phase 6 noise filter or this drowns signal.

  **PATTERN C — Typed municipal-vehicle character system (Phase 4, vehicle scope).** Each vehicle is its own character with its own personality cluster + voice + flavor set written consistently. Vehicles are *ephemeral presences* not persistent entities — generated per-cycle based on neighborhood dynamics, crime rates, infrastructure needs. **Final list: 10 vehicles, each a distinct tone (no redundancy):**

  | # | Vehicle | Character | Tone trigger | Distinct because |
  |---|---------|-----------|--------------|------------------|
  | 1 | **Cop car** | Surveillance + tension | Power, ambiguous threat, neighborhood-relations-dependent | Only one that injects fear-or-relief depending on who you are |
  | 2 | **Fire engine** | Response + competence | Crisis-handled, professional trust | Brief, urgent, then gone — narrative is in the response not lingering |
  | 3 | **Ambulance** | Crisis + intimate | Private-into-public, dignity, sometimes tragic | Only vehicle whose presence implies someone's worst day |
  | 4 | **OARI van** | Civic experiment + alternative crisis response | Hope + scrutiny + provisional legitimacy | **Canon-anchored — see below.** Only vehicle whose presence is itself contested politics |
  | 5 | **Building inspector** | Bureaucracy + gatekeeping | Slow friction, paperwork, permit politics | Only vehicle that *negotiates* — leaves with notes, holds power over property |
  | 6 | **Garbage truck** | Routine + class infrastructure | Pre-dawn invisible labor, taken-for-granted | The 4:47am alarm-clock vehicle. Class commentary baked in |
  | 7 | **Mail truck** | Daily ritual + declining institution | Continuity, federal-presence-on-block, package-theft anchor | Only vehicle that stops at every house. Erosion-of-institution narratives |
  | 8 | **Ice cream truck** | Childhood + community signal | Summer, nostalgia, *its absence says something too* | Seasonal. When it stops coming, that's a story |
  | 9 | **Street sweeper** | Parking war + performed compliance | Commuter friction, bureaucratic predator, weekly-Tuesday dance | Only vehicle citizens actively scheme to avoid |
  | 10 | **PG&E truck** | Infrastructure + corporate-vs-resident | Outage response, branded power, contested utility | Only vehicle that arrives *because something already failed* |

  **OARI van canon anchor:** OARI deployed by C91 (18 responders, three pilot districts D1/D3/D5, 6.2-min response time). C95 D2 expansion vote depends on renewal-threshold data — every OARI call is evidence. **Implication for build:** OARI flavor events need a `coverageContribution` field that flows up to the C95 evaluation surface (Initiative_Tracker OARI row). Other 9 vehicles produce texture; OARI produces texture *and* evidence. OARI van + cop car overlap = contested handoff (canon-active). OARI van + ambulance overlap = medical escalation. OARI van + repeat-address = Tran-Muñoz renewal-threshold data.

  **Considered and dropped to avoid redundancy:** recycling truck (overlaps garbage), AC Transit / BART police (overlaps cop), school bus (overlaps ice cream on community/childhood), food truck (commercial-private, belongs in business scope), water utility / city works pothole crew (fold flavor into PG&E), health inspector (keep as business-scoped event per DeepSeek v2 BUS_LABOR_002, not vehicle-scoped).

  **PATTERN D — Micro-event overlap detection (Phase 4 / Phase 6 boundary).** Replaces v1's "chaos intersector at arc-arc level" idea with overlap detection at micro-event level (cleaner primitive, more frequent, more usable than waiting for two arcs to share a citizen). Per-pair overlap templates: `vehicle_business`, `vehicle_neighborhood`, `business_neighborhood`, `citizen_vehicle`, `vehicle_vehicle`. Spatial check (same neighborhood / same street) + temporal check (same time window). When overlap detected, generate combined event with merged moodImpact + tagged with both source events. Example: patrol car idle outside business that just had break-in → "officers took a report on the burglary, owner came out with coffee, said it's the third time this month." Example: OARI van + cop car at same address → contested handoff narrative. Example: garbage truck + march = "the truck waited at the corner. The driver watched the march pass. Didn't honk."

  **Status:** all four patterns parked, not scheduled. When promoted: research-build drafts one plan file covering all four (Pattern A grafts + Pattern B scoped generator + Pattern C typed vehicles + Pattern D overlap), engine-sheet implements against `arcLifecycleEngine.js` + new `utilities/generateMicroEvent.js` + Phase 4 generators. Pre-build prerequisites: (1) verify actual `ctx.tracker.*` / `ctx.snapshot.*` accessor signatures, (2) audit Simulation_Ledger A-AU + Business_Ledger + Neighborhood_Map for assumed fields, (3) decide MicroEvents_Ledger tab vs append-to-existing-logs, (4) noise-filter spec for Phase 6 if 30/cycle is the target. Not Phase 4 escalation-engine reinvention. LOW.

### Infrastructure

- **DONE S187: /doc-audit never-audited cleanup — all 3 groups closed (research-build) — MEDIUM.** Infra (14 docs, 5 fixes), Data (6 docs, 2 fixes + 3 engine-sheet handoffs), Persona (18 docs, 2 fixes + 6 Mara/Mike handoffs flagged). Total: 38 docs audited under v2.0 first pass; 9 fixes applied; 10 cross-terminal handoffs flagged in tracker for Mara/Mike/engine-sheet. **Active follow-ups filed below** as new ROLLOUT entries: (a) Mara-vance handoffs from persona audit, (b) engine-sheet ledger doc handoffs from data audit. Tracker entries in `.claude/skills/doc-audit/SKILL.md`. Move to ROLLOUT_ARCHIVE next session-end sweep.
- **HANDOFF: Mara-vance documentation refresh (Mike → claude.ai sync) — S187, LOW.** Persona-group audit (S187) flagged 6 mara-vance docs for Mara/Mike attention. Not blocking; pick up when civic-engine framework iterates past v1.7. (1) `AUDIT_HISTORY.md` — missing E85-E92 audit results (~8 cycles); only Mara writes this. (2) `CLAUDE_AI_SYSTEM_PROMPT.md` — verify S171 rehire is reflected; Mike's lane (paste-into-claude.ai project instructions when changed). (3) `README.md` — "What's Next" Week 2/3/4 plans never landed; remove section or mark deferred. (4-6) `OPERATING_MANUAL.md` / `CIVIC_VETO_IMPLEMENTATION.md` / `CIVIC_ELECTION_ENGINE.md` / `INITIATIVE_TRACKER_VOTER_LOGIC.md` — all v1.7-anchored; framework-stable but version-locked since Feb. Full handoff list: `.claude/skills/doc-audit/SKILL.md` persona row.
- **HANDOFF: Engine-sheet ledger-doc refresh — DONE S199+S201 (closure pass).** (1) **LEDGER_AUDIT.md DONE** (commit `250d06e`) — full S199 refresh via `auditSimulationLedger.js`; 836 rows / POPID → POP-00951; Row 17 sentinel verified closed. S201 Status enum drift closed (151 lowercase → 826 Active + 9 Retired + 1 Recovering). (2) **SCHEMA_HEADERS.md DONE** (commit `250d06e` regen, S201 regen post-header-drift-fix `a05e5f6`) — 1416 lines / 57 tabs reflecting Story_Seed_Deck 12-col + Story_Hook_Deck 21-col post-fix. (3) **LEDGER_HEAT_MAP.md MOSTLY DONE** — S199 Heat Rankings refresh; S201 `37cec06` Top Bloat Risks §1 (LifeHistory_Log: archive script SHIPPED S31 / compression SHIPPED S116) + §2 (Sim_Ledger LifeHistory col reclassified RED→YELLOW post-compression) + §Archival Strategy §Priority 0 added (Story_Seed_Deck archive SHIPPED+RAN S201 — 2,667→1,109 active / 1,558 archived). Deferred: §Dead Column Inventory + §Column Cleanup Roadmap structural sections (S30/S31 vintage but still broadly accurate). (4) **LEDGER_REPAIR.md DONE S199** (commit `215ba1c` — A-AT→A-AU, flipped from "still open" S201 triage). (5) **NEW S201 — engine routing header drift FIXED** (commit `a05e5f6`). Story_Seed_Deck + archive + Story_Hook_Deck had v3.4/v3.5 schema drift causing engine-computed routing data (suggestedJournalist/suggestedAngle/voiceGuidance/matchConfidence) to land under stale calendar header labels. Renamed cols + deleted orphans + scrubbed 469 stale pre-v3.4 rows (335 archive + 134 hooks). 1108/1109 active seeds now carry valid journalist match (95% high-confidence, top: Simon Leary 838 / Maria Keen 160 / Luis Navarro 38). Unblocks `buildDeskPackets:2472` + `buildMediaPacket:209` consumers + opens path for /sift to consume engine pre-routes as default signal.
- **MIGRATE: Desk agents off Claude → OpenRouter+DeepSeek (research-build terminal) — RESEARCH/WATCH S156.** First desk test (business c87) done. DeepSeek V3.1 first-shot beat 3-pass Claude on prose quality at ~1/25th cost. Plan + next steps + what-survives in [[MIGRATION_OFF_CLAUDE]]. **Reclassified from HIGH → research/watch S156** — spine is walked, API pressure is off, and migrating would unravel the harness that produces cycles. Not cycle-bearing work; revisit if cost/limits bite again or if the migration becomes goal-aligned rather than escape-aligned.
- **UPGRADE: Instant compaction (research-build terminal)** — Proactive compaction before context full. Pattern from `claude-cookbooks/misc/session_memory_compaction.ipynb`. **Concrete reference S187:** `affaan-m/everything-claude-code` (MIT) ships a PreToolUse `Strategic compact` hook on `Edit|Write` that suggests manual compaction at logical breakpoints rather than waiting for context-limit-driven compaction. Worth porting the hook shape directly when this item picks up. Source: `https://github.com/affaan-m/everything-claude-code/tree/main/hooks`. MEDIUM.
- **EVALUATE: Context clearing strategy (research-build terminal)** — Beta flag `context-management-2025-06-27`. LOW.
- **MONITOR: KAIROS background daemon (research-build terminal)** — Unreleased Claude Code feature: persistent background daemon. If Anthropic ships this, could replace PM2 + cron + scheduled agents setup with native Claude Code infrastructure. Also watch ULTRAPLAN (30-minute remote reasoning sessions). MEDIUM. Added S137.
- **MONITOR: Hermes Agent (Nous Research) — reference architecture** — Source: `https://github.com/NousResearch/hermes-agent`. NOT for install. Worth mining before next skill-audit pass. Overlaps: self-improving skills, Daytona/Modal serverless persistence (KAIROS alternative), `agentskills.io` open standard, agent-curated memory. Patterns already captured in [[plans/BACKLOG]] §Architectural patterns. LOW-MEDIUM. Added S145.
- **ADD: Inter-agent conversation harness — test (research-build) — S172.** Persistent-process two-way messaging between two agent runtimes. Test shape: one Tier-1 citizen + one desk reporter, same question/answer scaffolding as current briefing packets plus follow-up + off-script room, output NOT published — transcript analyzed only, zero canon risk. Learn whether agents hold character across multi-turn interaction, how badly drift accumulates, actual token bill, transcript usability. Preconditions: Phase 39 reviewer chain closed + Phase 40 injection defense closed. Direction: path (a) build our own minimal MCP harness (S172 decision — Solo not adoptable, agent-MCP surface not in public soloterm repo). Interim Task-tool PoC available without harness build. Reference: Solo MCP (Aaron Francis, @aarondfrancis, 2026-04-22) — reference pattern, not adoption artifact. Wiki: [[RESEARCH#s172--inter-agent-conversation-capability-solo-mcp--aaron-francis]]. LOW.
- **DONE S190: `/diagnose` skill (research-build).** Shipped at `.claude/skills/diagnose/SKILL.md`. Six-phase loop (feedback loop / reproduce / hypothesise / instrument / fix+regression / cleanup+post-mortem) adapted from MIT-licensed `mattpocock/skills/engineering/diagnose/SKILL.md` (Matt Pocock 2026) with attribution preserved. Localized to GodWorld surfaces (engine scripts, MCP tools, dashboard API, dispatch fixtures, Supermemory tags). Disambiguated from `/self-debug` (agent is failing thing → that skill) at the top. GodWorld-specific examples for dispatch parser bug (S188 #4/#5) and Discord bot edition currency (S180). Inbound link: TERMINAL.md Engine Health Skills table. Move to ROLLOUT_ARCHIVE next session-end sweep.
- **BACKFILL: ADR-0002 — Phase 42 §5.6 phase05-ledger redesign (research-build) — S187, LOW.** Substance already exists in Supermemory `mags/hQE4rREEWBpS9aS1g3mQ3M` (covers original reasoning `fTzSivJgpXmaBcB5vrPEn1` + engine-sheet audit `2Lh8xsEHc6BMbBARM6mwHU` + verified amendments). Job: reshape that reasoning into ADR-0002 form (Context / Decision / Consequences / Alternatives considered / References) per `docs/adr/0001-adopt-context-and-adrs.md` template. Captures the (a)/(b)/(c) trade-off enumeration durably so future sessions don't have to grep the Supermemory chain. Test case for "is the ADR pattern actually working" — if backfilling this is awkward, the template needs adjustment. Effort: ~20 min. Pick up after engine-sheet ships the redesign batch (so Consequences section can include observed outcomes, not just predicted).
- **ADOPT: Module/Interface/Depth vocabulary into Phase 42 narrative (research-build) — S187, LOW.** Pocock's `improve-codebase-architecture` skill (MIT) defines a precise glossary for refactor work: Module, Interface, Implementation, Depth, Seam, Adapter, Leverage, Locality, Deletion test ("imagine deleting the module — if complexity vanishes, it was a pass-through; if complexity reappears across N callers, it was earning its keep"). Phase 42 §5.6 redesign is the textbook deepening case — `ctx.ledger` is the new deep module behind a small interface, replacing 18 shallow direct-write sites. Job: (a) add these terms to `CONTEXT.md` §Architecture as canonical vocabulary, (b) reframe `docs/engine/PHASE_42_PATTERNS.md` opening paragraph from "consolidate writers" to "deepen `ctx` as a module behind the simulation seam." Reads sharper, sells better to future sessions. Effort: ~15 min. Source: `https://github.com/mattpocock/skills/blob/main/skills/engineering/improve-codebase-architecture/SKILL.md`.
- **DONE S190: `disable-model-invocation` audit across 45 skills (research-build).** 40 deliberate-invocation-only skills carry `disable-model-invocation: true` (was 18 pre-audit; +22 added S190 + session-end fixed false→true). 5 stay flagless for auto-fire on inferred relevance: `context-budget` (heavy-context trigger), `diagnose` (broken/throwing/failing trigger), `doc-audit` (staleness questions), `health` (engine-feels-off trigger), `self-debug` (agent-stuck trigger). Decision frame: heavy/write/ceremony skills → flagged; scan/read/structured-trigger skills → flagless. Each skill's flag state is now a deliberate decision recorded in its own frontmatter. Move to ROLLOUT_ARCHIVE next session-end sweep.
- **PROPOSE: ROLLOUT_PLAN state-machine labels (research-build) — S187, LOW.** Pocock's `triage` skill structures issues through canonical states: `needs-triage` / `needs-info` / `ready-for-agent` / `ready-for-human` / `wontfix`, plus category (`bug` / `enhancement`). Our ROLLOUT items already carry priority + terminal tag, but state is ad-hoc (DONE, HOLD, BLOCKED, DEFERRED, WATCH, IN PROGRESS scattered through prose). Adapted state machine for ROLLOUT_PLAN: `ready` (clear acceptance, picker can grab) / `needs-info` (waiting on Mike or another terminal) / `in-progress` (active work claimed) / `blocked` (dep not landed) / `done-pending-archive` (completed, awaiting move to ROLLOUT_ARCHIVE) / `wontfix` (decided not to do). Job: research-build session writes a §Convention section at top of ROLLOUT_PLAN documenting the labels, then walks the existing ~30 items and tags each. **Open question:** adopt Pocock's vocabulary directly or use the adapted set above? Decide before writing. Effort: ~45 min including item-by-item tagging pass.
- **DONE S187: `/self-debug` skill (research-build) — HIGH.** Shipped at `.claude/skills/self-debug/SKILL.md` (~150 lines). Four-phase loop (Capture / Diagnose / Contained Recovery / Introspection Report) for when the agent is the failing thing. Adapted from MIT-licensed `affaan-m/everything-claude-code/agent-introspection-debugging` (Affaan Mustafa 2026) with attribution preserved. ECC cross-references swapped for our skills (`/diagnose`, `/grill-me`, `/boot`); GodWorld-specific recurring patterns section added (S122 / S128 / S135 / S168 / S187). Inbound link: `.claude/rules/identity.md` Anti-Loop section now points at `/self-debug` as the structured response when patterns match. Move to ROLLOUT_ARCHIVE next session-end sweep.
- **DONE S187: `/context-budget` skill (research-build) — MEDIUM.** Shipped at `.claude/skills/context-budget/SKILL.md` (~210 lines). Token-overhead audit across `.claude/agents/`, `.claude/skills/`, `.mcp.json`, `.claude/rules/`, CLAUDE.md, MEMORY.md, CONTEXT.md. Adapted from MIT-licensed `affaan-m/everything-claude-code/context-budget` (Affaan Mustafa 2026) with attribution preserved. Localized to GodWorld component shape: 25-agent four-file canon-fidelity stack, path-scoped rules with `identity.md` always-loaded, MEMORY.md size-threshold flag (we hit 25.9KB on close S187), CONTEXT.md per ADR-0001, terminal-scoped persona levels factored into baseline math. **First-audit-run not executed this session** — skill body describes the process; first run is a separate engine maintenance task (recommend pairing with the next `/doc-audit` infra group pass). Move to ROLLOUT_ARCHIVE next session-end sweep.
- **RESEARCH: Compare claude-mem to ECC continuous-learning v2 instinct architecture (research-build) — S187, LOW.** ECC's v2 design (extracted from v1 SKILL.md's comparison table): PreToolUse/PostToolUse hooks 100% deterministic (vs Stop-hook 50-80%), background Haiku agent processes session in parallel, atomic "instincts" with confidence scoring 0.3–0.9, instincts decay when contradicted, categorical labels (code-style / testing / git / debugging), aggregate related instincts → skills/commands. Our claude-mem (autodream + observation extraction, switched to Gemini 2.5 Pro free tier S141) covers part of v1 but lacks the decay-on-contradiction and confidence-weighting parts. Job: one focused research-build session walking ECC v2 spec (`docs/continuous-learning-v2-spec.md` in their repo), compare to claude-mem's actual storage shape, decide if there's a meaningful upgrade path or if claude-mem's simpler model is enough. Could feed dormant [[plans/skill-eval-framework]] work — confidence-scoring is the missing primitive there. Source: `https://github.com/affaan-m/everything-claude-code/tree/main/skills/continuous-learning-v2` (MIT). Effort: ~1 research-build session for comparison; build is downstream and gated on the comparison's verdict.
- **ADD: Bounded test surface (research-build → engine-sheet) — S170.** Dict-parity assertions, pure-helper unit tests, parser output snapshots. Targets drift-class bugs caught at edit time rather than post-cycle audit: S170 PRE-C93 `PHASE_INTENSITY` / `PHASE_SENTIMENT` parity gap (OARI decision silently scored 0 for one cycle), SCHEMA_HEADERS alignment drift (S146 retroactive fix), `utilities/utilityFunctions.js` `Math.random` reference-passes (ENGINE_REPAIR row 12, missed by the S156 sweep). **NOT a full engine test suite** — scoped to places where logic is computational (parsers, dict-parity, pure helpers) rather than state-transformational (cycle phases, sheet writes). Test framework install (~50-100MB for jest/vitest) deferred until droplet headroom improves (4.6GB free at S170). Promote to plan file when scheduled. Origin: S170 outside AI code review (`docs/research/godworld_review_2026-04-20.pdf`) — one of four review-surfaced items; three others either already-planned (Phase 42), already-mitigated (credential/env centralization S156), or too generic to act on (dep management refactor). LOW.

### Edition Production (orphan items)

- **FIX: Metadata leak at desk emission (media terminal) — S172. HIGH.** (promoted: C92, severity: HIGH) Jax (`output/reporters/jax-caldera/articles/c92_youth_apprenticeship_where_are_they.md`) and Jordan Stab Fund (`output/reporters/jordan-velez/articles/c92_stab_fund_bottleneck.md`) desks append `## EVIDENCE` / `ARTICLE TABLE ENTRIES` / `CITIZEN USAGE LOG` / `CONTINUITY NOTES` / `FACTUAL ASSERTIONS` blocks to article body. New in C92 — C91 source articles clean. Architectural fix: `/write-edition` skill changes so desks emit two files — `article.md` (body, print/ingest-safe) + `article.meta.json` (audit data for Rhea / cycle-review / Mara reviewer chain). `CITIZEN USAGE LOG` is redundant with `buildCitizenCards.js` which already searches bay-tribune for appearance history per POP ID and writes compiled cards to world-data — strip cleanly, do not replicate elsewhere. Unblocks HOLD above. HIGH.
- **FALLBACK: Extend PDF metadata filter (media terminal) — S172. MEDIUM.** `scripts/generate-edition-pdf.js:53` `textToHtml` has `/^Names Index:/` pattern but misses `## Names Index`, `**NAMES INDEX**`, `## EVIDENCE`, and the audit-block markers (hence the C92 PDF leak). Extend to skip-to-EOD on any of those patterns. Band-aid only — leaves `editions/cycle_pulse_edition_*.txt` dirty so raw ingest remains blocked unless ingestEdition strip is also in place. Skip this entry if FIX at desk emission lands first. MEDIUM.
- **AUDIT: Agent briefing context bloat (from O'Reilly Mezzalira essay) — MEASUREMENT DONE S156, recommendations pending.** Report: [[research/briefing_bloat_audit_2026-04-17]]. c91 snapshot measured across 6 desks: `interviewCandidates` 520 names shipped to every desk, 1.2% hit rate in edition; `base_context.json` byte-identical across all 6 desks (6× duplication); `packet.json` 60-80% of bundle weight, duplicated by `summary.json` (same keys, smaller lists); bundle sizes 693 KB–1.2 MB per desk (~5.3 MB total). Archive-context stale for 4 of 6 desks (c89=c90=c91). Craft-layer angle briefs (`{desk}-desk/current/angle_briefs/`) are 40-100× smaller and the right shape, but only c90 briefs were produced — c91 bypassed. **Cross-edition correlation (briefing size vs. Mara grade / Rhea score) NOT POSSIBLE** — `output/` gitignored and briefings overwritten per cycle, so only c91 exists on disk. Separate rollout item for versioning below. Pairs with Phase 26.2.3 (briefing evolution). Reference: `output/drive-files/Googlepaper.pdf`. MEDIUM.
- **ADD: Briefing versioning (media terminal) — NEW S156, blocks future briefing audits.** `output/desks/{desk}/current/` is overwritten every cycle. Add post-cycle copy step (either `cp -r` to `{desk}/archive/c{XX}_briefing_bundle/` or Supermemory tag-by-cycle ingest) so retrospective "did this input produce that output" questions become answerable. Without this, every cycle's briefing is lost as soon as the next cycle runs — already cost us c89/c90 in the S156 briefing bloat audit. Bundled with briefing-trim in [[plans/2026-04-17-briefing-bundle-trim]] Task 4. MEDIUM.
- **PLAN READY: Briefing bundle trim (media terminal) — S156.** [[plans/2026-04-17-briefing-bundle-trim]]. Five tasks covering audit recs 1/2/3/7: `base_context.json` shared load, always-empty field deletion, `interviewCandidates` 520→20 relevance ranking, post-publish versioning hook, c92 smoke-test + audit re-run. Expected bundle-size savings: 50%+ per desk. Blocked-by: none. Research-build drafted; media terminal picks up. MEDIUM.
- **REBUILD: Photo pipeline — agent-driven art direction (media terminal) — S170 (C92). HIGH.** (promoted: C92, severity: HIGH) Mike (2026-04-22): *"the photos should literally capture my world, so far not one has come even close"* / *"zero creativity and drags down the publication, it's not visualizing all I'm doing"* / *"I can't even look at this edition with that front page photo, has nothing to do with anything in the edition."* Current state: `scripts/generate-edition-photos.js` scans the edition for section headers, picks 2 sections (front page + culture), writes its own one-liner prompt from the first sentence, hands it to FLUX (Together AI). DJ Hartley's IDENTITY.md never enters the loop — his name is a credit-line label on a generator. Every C92 photo defaulted to training-data Oakland (FLUX associates "Oakland" + "street" with blight / poverty street-doc). Front page for the Mayor's Day umbrella piece came back as a ghetto-aesthetic corner shot that has no relationship to the Temescal Health Center storyline, the Mayor's five-decision day, or any of the actual edition content. Session S170 patch: added "Canon: This Is Prosperity-Era Oakland" section to `.claude/agents/dj-hartley/IDENTITY.md` — not a fix, a guard rail on a broken pipeline. **The real rebuild:** (1) DJ reads the full compiled edition text + engine audit + world summary; (2) DJ picks 5–8 images (not 2) tied to specific storylines — Baylight cranes, Temescal Health Center site fence with planning sign, Coliseum tunnel pre-weeknight-game, Jack London Square at shift change, Mayor on her own block not a podium, ferry terminal at dawn, Temescal & 47th at morning traffic; (3) DJ writes each prompt at real specificity — address, time of day, camera, light, what's in frame AND what's explicitly not ("no tents, no boarded storefronts, no barred windows"), 120–180 words per prompt, not one-liners; (4) generator executes DJ's prompt, doesn't synthesize its own; (5) `photoQA.js` actually returns real Haiku verdicts (currently returns template strings `[assessment]` / `[1 sentence]` — prompt-template bug); (6) bad photos get regenerated on QA fail, not flagged and shipped; (7) fix `generate-edition-pdf.js` hard-coded "October 2041" date line; (8) photo pipeline needs to be explicitly NOT inactive — S156 Phase 40.3 credential audit removed `TOGETHER_API_KEY` under the framing "photo pipeline inactive" per `docs/CANCELLATION.md`; that framing is wrong, either re-enable intentionally or select a replacement provider. Evidence: `output/photos/e92/`, `output/photos/e91/` (single file), Mike's direct feedback logged in `docs/mags-corliss/NEWSROOM_MEMORY.md` E92 entry, print-pipeline backlog in `output/production_log_edition_c92.md`. Blocks: nothing technical. Priority: HIGH — the photo drags the publication down. **E92 (S172) confirms the pattern unchanged:** front page photo shows two men on a night street with taxi cabs; the Temescal Avenue & 47th corner that the lead story references 6+ times does not appear. DJ IDENTITY.md prosperity-canon patch from S170 did not fix the root pipeline — as predicted, a guard rail on a broken generator, not a fix. **Plan:** [[plans/2026-04-25-photo-pipeline-rebuild]] (drafted S176) — 13 tasks, DJ four-file canon-fidelity structure LOCKED, Together AI default + OpenAI gpt-image-1 A/B task.

---

## Phase Status

### DONE / SUPERSEDED (pointer-only)

- **Phase 31 — Canon-Grounded Briefings — DONE S134.** See [[engine/ROLLOUT_ARCHIVE]].
- **Phase 2.2 — Desk Packet Query Interface — SUPERSEDED by MCP (S137b).** See [[engine/ROLLOUT_ARCHIVE]].
- **Phase 39 — Editorial Review Layer Redesign — DONE S147–S148.** Full implementation detail: [[engine/PHASE_39_PLAN]]. Post-build notes: [[engine/ROLLOUT_ARCHIVE]] §S147 + §S148. All sub-phases 39.1–39.10 complete.
- **Canon Fidelity Rollout — DONE S175.** Three-tier framework + per-agent four-file structure across 24 generators/reviewers. Plan: [[plans/2026-04-25-canon-fidelity-rollout]]. Post-build detail: [[engine/ROLLOUT_ARCHIVE]] §Phase-level closure. S186 read-time extension (regression-class follow-up) lives in active rollout under the Perkins scrub entry.

### Active / Ready (detail in plan files)

- **Phase 33 — Riley Integration & Hardening** — IN PROGRESS (7 of 19 subitems DONE). [[engine/PHASE_33_PLAN]].
- **Phase 38 — Engine Auditor** — MOSTLY DONE S146. 38.5 measurement loop → [[plans/2026-04-16-phase-38-5-measurement-loop]], 38.6 skill shrink → [[plans/2026-04-16-phase-38-6-skill-shrink]]. Design intent + S142 framing in [[engine/PHASE_38_PLAN]] and mags Supermemory doc `FzoBwCif9ZA3PGBqv5bBAW` (retrieve with `curl -s "https://api.supermemory.ai/v3/documents/FzoBwCif9ZA3PGBqv5bBAW" -H "Authorization: Bearer $SUPERMEMORY_CC_API_KEY"`).
- **Phase 40 — Agent Architecture Hardening** — 5 of 6 DONE (40.1 + 40.3 + 40.4 + 40.5 + 40.6 complete; only 40.2 cattle refactor remains). [[engine/PHASE_40_PLAN]].
- **Phase 41 — GodWorld as LLM-Wiki** — IN PROGRESS (3 of 5 DONE S146). Remaining 41.3/41.4/41.6 in [[plans/BACKLOG]] §Phase 41.
- **Phase 43 — Engine Expansion (city-functions)** — DESIGNED, plan file pending. Wiki: `docs/research/godworld_city_functions_analysis_2026-04-20.pdf` (outside-AI gap analysis; 17 active domains + ~35 engines enumerated, ~15 missing domains tiered by story-generation potential). Priority order after S170 canon correction: (1) Public Health (extends Temescal Health Center + OARI), (2) Environmental / Sustainability (Oakland port pollution + weather-engine synergy), (3) Legal / Justice (completes crime → arrest → prosecution pipeline, grounds DA + Police Chief voice agents), (4) Tech / Innovation (already canon-live via `supplemental_tech_landscape_c84` + Varek + DigitalOcean-Baylight ties + family tie through Sarah; engine layer grounds existing narrative infrastructure), (5) Parks & Rec + Food Systems (texture). Port excluded — already on civic rollout track per S170. SimCity/Sims framing applies (see MEMORY.md `project_simulation-is-oakland-framing`) — canon beats real-world plausibility when evaluating new-engine fit. Preconditions: E92 ships + Phase 42 Writer Consolidation ideally first (reduces direct-write sprawl before adding ~5 new write paths) + sheet-bloat impact on per-desk packet weight assessed. Extract to `[[plans/TEMPLATE]]`-shaped plan file when scheduled.

### Backlog (designed but no active session)

All embedded phase designs for Phase 4.1, 5.4, 7, 8.6, 9, 11.1, 12, 20, 21, 23, 24, 25, 26.2, 26.3, 27, 28, 29, 30, 32, 35, 36, 37 moved to [[plans/BACKLOG]] S152. Plus research reference material (Hermes architectural patterns, scheduled-agent ergonomics, S115 skill-audit remainder). When any backlog item becomes active work, extract to its own [[plans/TEMPLATE]]-shaped plan file.

---

## Completed Phases (one-line summaries)

All detail in [[engine/ROLLOUT_ARCHIVE]].

| Phase | Name | Completed |
|-------|------|-----------|
| 1 | Edition Pipeline Speed + Safety | S55 |
| 3 | Engine Health (pre-mortem, tech-debt-audit, stub-engine) | S55 |
| 6.5 | Discovery Skill `/grill-me` | S99 |
| 10 | Civic Voice Agents (Mayor + 6 extended voices) | S63-64 |
| 13 | Simulation_Ledger Data Integrity Audit | S68-72 |
| 14 | Economic Parameter Integration | S69-72 |
| 15 | A's Player Integration | S70 |
| 16 | Citizen Ledger Consolidation | S71 |
| 17 | Data Integrity Cleanup | S72 |
| 18 | Civic Project Agents (5 initiative agents) | S78 |
| 19 | Canon Archive System (378 files organized) | S79 |
| 22 | Agent Infrastructure Fixes (CIVIC mode, arc fix, write access) | S83-85 |
| 26 | Agent Grading System (Karpathy Loop) | S99 |

### Recently Completed (historical)

S105–S141 detail lives in [[engine/ROLLOUT_ARCHIVE]] and git log. Key milestones: S110 Supermemory overhaul + workflow split, S113 harness improvements (hooks, status line, dashboard), S114 craft layer + long-running research, S122 container redesign decided, S131 canon breakthrough, S133 Riley Phase 33.1–33.5, S134 pipeline v2 (4 terminals, 9 reporters, bounded traits), S135 terminal architecture + Remote Control, S137b MCP + wiki ingest + citizen cards + feedback loop operational, S139 external review round + faction canon + Maurice Franklin, S140 dispatch + audio tools + doc audit, S141 skills architecture prep + eval framework + Gemini autodream switch.

---

## Session Harness / Discord / Dashboard — DONE S110–S113

Full detail in [[engine/ROLLOUT_ARCHIVE]]. Still open from this bucket:

- **Cloud session + Channel** (`claude --remote` + Discord) — infrastructure for Phase 12.3 autonomous cycles. HIGH. (promoted: C93, severity: HIGH) Not started.
- `/btw` and `/branch` — available now, no build needed; awareness only.

---

## Watch List

Tracking for future adoption. Not building.

| Feature | Trigger to Act |
|---------|---------------|
| Agent Teams stability | Experimental graduation → test Phase 7.6 |
| Multi-Character Discord | TinyClaw reference architecture matures |
| MiniMax M2.5 / DeepSeek-V3 | Cost spike or quality test passes |
| Skills Portability | HuggingFace format becomes standard |
| Tribune Fine-Tuning | 238 articles as training dataset for voice model |
| Desktop App (Linux) | Linux support ships |
| Lightpanda Browser | Beta stabilizes, saves 300MB RAM |
| Claude Code Voice Mode | Maturity improves |
| Extended Thinking for Agents | Test on civic/sports desks |
| Computer Use exits beta | Stable + cheaper → expand beyond QA to routine agent tasks |
| CLI-over-MCP token optimization | Measured: too many MCPs drops 200k context to 70k. Replace idle MCPs with CLI-wrapper skills. Source: everything-claude-code S131 |
| Selective skill loading | Only load skills relevant to current workflow. Chat doesn't need 21 skills. Manifest-driven selection. Source: everything-claude-code S131 |
| Continuous learning hooks | Auto-extract debugging patterns into reusable skills with confidence scoring. Source: everything-claude-code S131 |
| llms.txt for documentation | Many doc sites serve `/llms.txt` — LLM-optimized docs. Check before web-fetching. Source: everything-claude-code S131 |
| Proactive agent dispatch | Rule-based agent routing without user prompts. Post-write → reviewer, security-sensitive → scanner. Source: everything-claude-code S131 |
| NPM Package Drift | 7 packages behind. Batch update in maintenance session. |
| Codex Plugin (`/codex:adversarial-review`) | Mike keeps ChatGPT sub → install plugin for free adversarial code review. Sub cancelled → skip. Source: S131 |
| Open-source agent harnesses | Stable harness with MCP + skills + hooks support → re-evaluate Phase 21 as real multi-model pipeline. Track: Claw Code (instructkr/claw-code), community forks. Source: S131 |
| xMemory (hierarchical memory) | AutoDream fails to solve collapsed retrieval after 5 sessions → evaluate self-hosted xMemory |
| Auto Mode | Evaluate for production pipelines — could eliminate approval prompts during `/write-edition` |
| HTTP Hooks migration | Replace shell-based hooks with HTTP POST to dashboard endpoints for unified event stream |
| **Forked subagents** (`CLAUDE_CODE_FORK_SUBAGENT=1`, claude-code 2.1.117) | Parallel desk-reporter pipeline becomes a goal AND harness contract validated across forked children. Source: S177 |
| **Hooks → MCP tools** (`type: "mcp_tool"`, claude-code 2.1.118) | Stop / post-publish hook would benefit from direct MCP call (e.g. godworld `lookup_citizen`) instead of node-script glue. Source: S177 |
| **Agent frontmatter `mcpServers` in main-thread sessions** (claude-code 2.1.117) | Per-agent MCP-tool isolation becomes part of canon-fidelity tightening — each agent declares exactly which MCPs it consumes. Source: S177 |
| **`--print` honors agent `tools:` / `disallowedTools:` frontmatter** (claude-code 2.1.119) | Sandcastle+Daytona reviewer hosting goes operational — per-agent tool restrictions ride along into the sandbox. Strengthens Phase 40.6 Layer 4 (tool gate). Source: S177 |
| Agent lifecycle hooks (SubagentStart/Stop) | Desk agent monitoring — track which agents take longest, fail most |
| Prompt/Agent hooks | Replace pattern-based hookify rules with semantic LLM-evaluated checks |
| FileChanged hook | Auto-react to git pulls, external file changes during autonomous operation |
| Overture (visual agent planning) | Mike can see plans visually → install when accessible from Remote Control or web dashboard. github.com/SixHq/Overture. Source: S137b |
| **OpenVLThinkerV2 (open VLM from UCLA NLP)** | GPU droplet spun up — evaluate as vision backbone for Phase 28.2 dashboard visual QA, photo pipeline verification, two-pass hallucination visual reviewer, research paper ingestion. Qwen3-VL-8B base + custom G²RPO training. Beats GPT-4o on MMMU (71.6%). Open weights. github.com/uclanlp/OpenVLThinker. Source: S142 |
| **RAGFlow (civic document ingestion candidate)** | Real civic document pipeline becomes a priority AND droplet scaled up (needs 4+ CPU cores, 16GB RAM, 50GB disk) — evaluate alongside Qianfan-OCR. Deep-learning document parser. Data connectors for Google Drive, Confluence, S3, Notion, Discord. Apache 2.0, Docker-based. github.com/infiniflow/ragflow. Source: S142 |

---

## Changelog

- 2026-04-29 (S186, research-build) — Archive sweep: 13 closed items moved from active sections to [[engine/ROLLOUT_ARCHIVE]] §Tactical Closures (S156–S186). Items A–M: POP-00004 Lucia Polito, empty-cell rows resolved, gender col AU citizen cards (S181), WorldEvents_V3_Ledger v3.6 classifier, Tech debt audit Path 1 (S156), Dead-code detection scan + DELETE batch (S185), /pre-mortem C92 (S180/S181), Enricher misattribution bug (S185), sheet-hygiene blank-row delete, 9 tier-2 canon firms, HOLD E92 raw edition ingest (S180), Canon Fidelity Rollout (S175). Composite items handled with surgery: Tech debt audit restructured (Path 1 archived; Path 2 promoted to top-level "Phase 42 — Writer Consolidation"); /engine-review C92 parent kept active (Enricher sub archived; advance-initiative threshold sub remains open). Canon Fidelity Rollout row removed from "Other Ready Work" + added to "Phase Status §DONE/SUPERSEDED". S186 also added: §Read-Time Contamination Check to CANON_RULES.md + 23-agent RULES.md refresh; new ROLLOUT entry "Bay-tribune unified ingest rebuild" (post-Perkins-scrub follow-up); Perkins&Will scrub entry updated to SCRUB-SIDE DONE S186 (civic smoke test pending). ROLLOUT line count 237 → 228 (smaller despite added entries — bulk archive is the win).
- 2026-04-28 (S184, research-build) — Phase 42 Writer Consolidation: PLAN DRAFTED ([[plans/2026-04-28-phase-42-writer-consolidation]]) + Phase 1 inventory ([[engine/PHASE_42_INVENTORY]]) + Phase 2 patterns ([[engine/PHASE_42_PATTERNS]]). Real cycle-path scope: 37 files / 175 sites (citizenFameTracker.js retired -16, ±5 drift). Tiered B6 stop-point (~58%) vs B7 full close. 5 reference migrations P1-P5 with before/after; `queueEnsureTabIntent_` API addition specced. Engine-sheet picks up B0 pilot. Closes ENGINE_REPAIR Row 8 path-2.
- 2026-04-28 (S184, research-build + engine-sheet) — Discord-bot edition currency: 5 of 6 tasks DONE ([[plans/2026-04-26-discord-bot-edition-currency]]). T1 research-build (post-publish Step 5b refresh `776aded`); T2/T3/T4/T6 engine-sheet (`a67f2b0` + `4edbe26` — `lib/mags.js loadEditionBrief()` reads latest `production_log_edition_c{XX}.md`, `latest_edition_brief.md` stripped from postRunFiling.js + 2 dead-guarded references in gradeEdition.js + weekly-maintenance.sh). T5 (media: letters-desk RULES + MEMORY refs) outstanding.
- 2026-04-28 (S184, research-build) — Rollout audit pass: 12 DONE entries stripped from active sections + 5 partials trimmed (Discord-bot → T5-stub, /pre-mortem C92 → DONE pointer, /engine-review C92 neighborhood-bullet folded, Dead spreadsheet tabs Faith-line removed, HOLD E92 raw ingest URGENT → UNBLOCKED). Closure detail preserved in this changelog (entries below + S180/S179/S183/S184) and linked plan files. ROLLOUT_ARCHIVE.md untouched (designed for phase-level work, not tactical closures). Watch List + Phase Status sections out of scope for this audit.
- 2026-04-28 (S184, engine-sheet) — EventType taxonomy / subjectIds attribution: DONE via ENGINE_REPAIR Row 6 closure. Diagnosis flipped — WorldEvents_V3_Ledger has no POPID column; citizen attribution lives in LifeHistory_Log. `engineAuditor.js` SHEETS_TO_READ extended; `generateBaselineBriefs.js` v1.0.0 → v1.1.0 emits `eventClass: 'citizen-life-event'` briefs from 6 structural EventTags with `subjectIds` + ledger-joined neighborhood/tier/role. C88 smoke 16/16 with subjectIds. See ENGINE_REPAIR Row 6.
- 2026-04-26 (S179, research-build) — `/interview` SKILL BUG closure: Step 8 rewritten to `/save-to-bay-tribune` for both article (8a, type `interview-article`) and transcript (8b, type `interview-transcript`); 7a-7h → 8a-8h numbering corrected. `/save-to-bay-tribune` Type list extended. Both skills 1.0 → 1.1.
- 2026-04-26 (S180, engine-sheet) — Four post-publish closures shipped: (a) REINGEST `scripts/ingestPlayerTrueSource.js` — 3-pass (subfolder/flat-MLB/prospects), 27 wd-player_truesource docs in world-data (Mesa POP-00081 with full STAT TRUE SOURCE PITCHER doc `fD3qgZh2tShEmXPfmvJRor`); (b) ADD `scripts/buildFaithDigest.js` — Faith_Ledger consumer; emits `output/faith_digest_c{XX}.json`; `buildDeskFolders` renders FAITH ACTIVITY THIS CYCLE into culture-desk briefing only; (c) BUILD `scripts/ingestPublishedEntities.js` — standing intake from published `.txt` to Simulation_Ledger + Business_Ledger; format-flexible NAMES INDEX parser; `/post-publish` Step 5 wired; (d) ADD `stripMetadataLeaks()` filter in `ingestEdition.js` — block-mode pattern strip (`## EVIDENCE` / `NAMES INDEX` / `ARTICLE TABLE ENTRIES` / `CITIZEN USAGE LOG` / etc.) until divider; T1 official sections preserved; C92 fixture verifier 12 leak blocks / 61 lines stripped; lifts the HOLD on E92 raw ingest.

- 2026-04-28 (S184, research-build + engine-sheet) — Female citizen balance ingest **DONE**: [[plans/2026-04-28-female-citizen-balance]] complete through Phase 5 + wd-citizen card build. **Final F-share: 33% → 44.26%** (370F / 836 POPID-bearing rows; +150 new at POP-00802..POP-00951). 4-commit chain: research-build `649b304` (plan + 150-name pool with 7 origins + 9 tier-3 swaps + age/neighborhood CDFs) → engine-sheet `c583356` (script + dry-run all 5 gates green) → engine-sheet `a0e6de1` (live apply + readback verified) → engine-sheet `62a01d5` (`buildCitizenCards.js --popid-range` flag + targeted card build, wd-citizens 686 → 836). Mid-run policy decisions (Mike): Notes-marker dropped (4th-wall-breaking on this schema), faith-leader ROLE_DENYLIST added with deterministic salted redraw to block freq-weighted skew amplification, Gate 4 distribution tolerance loosened ±5% → ±7% (≈2σ for n=150), pool-tuning swapped rejection-with-retry → filter-then-draw for clean exhaustion. Capability reviewer assertion #9 (≥3 female citizens in non-official roles per edition) now has substantially more headroom on next /sift run. Sibling INGEST ROLLOUT item flipped UNBLOCKED → DONE.
- 2026-04-28 (S184, research-build + engine-sheet) — Intake-side citizen derivation **DONE**: [[plans/2026-04-28-intake-side-citizen-derivation]] complete through Phase 5 validation. All 5 phases shipped in single session — research-build owned spec (Phases 1-4: inventory + source map + per-field specs + engine-sheet handoff specs), engine-sheet owned implementation (`lib/citizenDerivation.js` + `utilities/citizenDerivation.js` + `scripts/syncEconomicParameters.js` + 3 integration paths), engine-sheet ran initial Phase 5 validation, research-build verified by independent re-run. 5/5 gates green. Sample outputs: Maria Vega/Fruitvale/34 → Barbershop Owner / married / $43k; Tobias Wing/Rockridge/67 → Electrician retired / $2.34M; Kevin Park/Jack London/22 → Trade Union Rep / married / $24k. Distribution healthy on 200-batch (5 marital statuses, 6 num-children buckets, 37 distinct RoleTypes, gender within 51/49 target lean variance). Constraint preserved: ZERO existing-row writes; intake-time derivation only. Apps Script deployed via clasp (157 files). New citizens via `mediaRoomIntake` / `processAdvancementIntake` / `ingestPublishedEntities` now land with all 8 demographic + lifecycle fields populated from real context (Age, Income, RoleType, Neighborhood) instead of empty cells or hardcoded sentinels. Closes ENGINE_REPAIR Row 4 + Row 17 forward-looking gaps. Sibling ROLLOUT item ("INGEST: ~200 female citizens") now unblocked (library exists).
- 2026-04-28 (S184, research-build) — Skill supermemory alignment **DONE**: [[plans/2026-04-28-skill-supermemory-alignment]] complete through Phase 4 validation. Search/save matrix added to [[SUPERMEMORY]] §Search/save matrix (15 read use cases, 12 write use cases, 5 containers + retrieval mode override note). 12 file edits across 8 skills + 3 rhea-morgan agent files. 16 of 17 agents confirmed architecturally correct (consume pre-fenced packets, don't retrieve directly — Memory Fence Phase 40.6). Bug fix: `/save-to-mags` was directing canon saves to `/super-save` (wrong container) — corrected to `/save-to-bay-tribune`. `/post-publish` world-summary now tags `['world-data', 'wd-summary']` per Mike's S184 ADD. Phase 4 validation: 11 wikilinks resolve, 13 cited MCP tools exist, 7/7 active `wd-*` tags return populated content. ROLLOUT entry flipped UNBLOCKED → DONE.
- 2026-04-28 (S183, engine-sheet) — World-data unified ingest rebuild **DONE**. 9 commits S180–S183 (`12d4ba9..1fc2526`) shipped W2 (faith 16) + W3 (cultural 39) + W4 (neighborhood 17) + W5 (initiative 6) + R2 (truesource 27, content-signature wipe + DELETE-401/429 retry patch + `--wipe-only` flag) + M1-M4 (4 MCP lookup tools + `supermemory_search` hybrid+0.3 retrieval-gap fix) + R1 bulk apply (404 wd-citizens) + R1 cold-start fix (`--no-quality-gate` flag rebuilt to 686 wd-citizens). Final substrate: 843 world-data docs, 100% domain-tagged, 0 orphans. Plan file end-to-end DONE; PROJECT entry above marked DONE; "ALIGN: Skills supermemory rules" item flipped from BLOCKED → UNBLOCKED for next research-build session.
- 2026-04-27 (S182, research-build) — World-data unified ingest rebuild PLAN DRAFTED: [[plans/2026-04-27-world-data-unified-ingest-rebuild]] (5 phases, 13 tasks). Tag scheme verified empirically (probe doc `XQseCdoQpsb6SZCUM6i12o` since deleted: multi-tag write retrievable by either tag, DELETE 204 after async-indexing settles ~15-30s). Locked decisions per Mike: `wd-` prefix on domain tags, citizen wipe is write-new-and-DELETE-old (not retrofit), MCP query-surface drives card shape (name as search hook, IDs subordinate, neighborhood as filter dimension, bay-tribune appearances embedded for `aggregate: true` synthesis). Engine-sheet picks up Phase 1 (inventory script + endpoint verify) at next session. Also added new HIGH item: "ALIGN: Skills need supermemory rules aligned for search and save" — research-build follow-up once writers populate the new tag scheme; audits skill `.md` files across `/sift`, `/write-edition`, `/dispatch`, `/interview`, `/city-hall-prep`, `/post-publish`, `/save-to-bay-tribune`, `/save-to-mags` for tag-scheme consistency.
- 2026-04-27 (S181, engine-sheet) — World-data ingest gap surfaced via the citizen-card cleanup (ENGINE_REPAIR Row 2). Added new "PROJECT: world-data unified ingest rebuild" entry under Data & Pipeline. Research-build owns the per-domain shape spec + ingest-trigger plan + wipe-policy; engine-sheet builds the writers. Trigger: `lookup_citizen` queries currently surface a working citizen layer alongside stale/wrong content for business / faith / cultural / neighborhood / initiative domains because there's no documented ingest writer for them.
- 2026-04-25 (S177, research-build) — Hooks `duration_ms` integration BUILT. New `.claude/hooks/tool-timing.js` Node script (unmatched PostToolUse, 3s timeout) appends `{ts, session_id, tool, duration_ms, ok}` to `output/session-evaluations/tool-timings.jsonl` per call; defensive lookup for `duration_ms` (top-level / `tool_response` / `metadata`); first-call payload sample written to `tool-timing-payload-sample.json` for shape verification. `session-eval.js` extended with "Tool Timing" section that filters by current `session_id` and aggregates per tool (count / total / avg / max ms), sorted by total desc. Smoke-tested end-to-end with synthetic data. Watch List row closed. Activates next session boot when hooks reload.
- 2026-04-25 (S177, research-build) — claude-supermemory plugin UPGRADED 0.0.1 → 0.0.2 (marketplace 13 commits forward, install bumped at project scope). Local hooks.json customization stashed + embedded in [[SUPERMEMORY]] S177 changelog. Round-trip verified against `super-memory` + `mags` containers. Restart applies hooks on next boot. Updated [[SUPERMEMORY]] §Hooks (PostToolUse row reflects upstream removal). UPGRADE item in §Infrastructure marked DONE.
- 2026-04-25 (S177, research-build) — claude-supermemory plugin upstream review. Local 0.0.1 vs upstream 0.0.2 + 6 tail commits. UPGRADE item added to Infrastructure (MEDIUM, security fix is the trigger). [[SUPERMEMORY]] header updated to label admin (`console.supermemory.ai`) vs browse (`app.supermemory.ai`); §Changelog entry added with full upstream-diff summary. One stale verification URL fixed in `scripts/migrateSupermemory.js:234`. Verified non-breaking against our `.claude/.supermemory-claude/config.json` (config.json precedence preserved in 0.0.1 source — git-remote fallback only fires when config absent). Source: `https://github.com/supermemoryai/claude-supermemory`.
- 2026-04-25 (S177, research-build) — Logged 5 Claude Code 2.1.115–2.1.119 changelog items to Watch List (forked subagents `CLAUDE_CODE_FORK_SUBAGENT=1`, hooks→MCP `type: "mcp_tool"`, per-agent `mcpServers` in main thread, `--print` honors agent `tools:`/`disallowedTools:`, hooks `duration_ms` field). Plus LOW Infrastructure docs-sweep item for `/cost`+`/stats` → `/usage` rename. Source survey: `https://github.com/anthropics/claude-code` CHANGELOG.md.
- 2026-04-25 (S176, research-build) — Stale HALTED banner replaced with S173 reframe. Status flipped HALTED → ACTIVE (building). POST_MORTEM_C92_CONTAMINATION preserved as historical record per reframe. Active priorities pointer added to SESSION_CONTEXT. Also: filled 9 tier-2 canon firms in [[canon/INSTITUTIONS]] §Construction & Architecture (Atlas Bay Architects, Ridgeline Studio, Pacific Standard Architecture, Estuary Architects, Coastline Construction, Anchor Build, Mariner Construction, Foothill Builders, Northgate Construction — all status `canon`); added engine-sheet ingest item to Data & Pipeline.
- 2026-04-23 (S172, research-build) — **PROJECT HALTED.** Mike declared halt at end of session following E92 editorial audit that surfaced 20+ real-world Oakland institutional references (Alameda Health System, Rene C. Davidson Courthouse, Perkins&Will Oakland, OUSD, Peralta CCD, real high schools and union locals, real construction firms, etc.) — fourth-wall contamination baked into canon containers (bay-tribune, world-data, super-memory) and filesystem. Sanitization scope estimated in weeks, exceeded available drive to execute. Status banner added to file header. Post-mortem created: [[POST_MORTEM_C92_CONTAMINATION]] (registered in [[index]] under Vision & architecture). All rollout entries preserved as historical state, not active priorities. No further cycles scheduled.
- 2026-04-23 (S172, research-build) — E92 edition audit: four Edition Production entries added (HOLD E92 raw ingest URGENT; FIX metadata leak at desk emission HIGH; ADD defense-in-depth strip in ingestEdition.js HIGH; FALLBACK extend PDF metadata filter MEDIUM). Root cause: desk-agent source .md files in `output/reporters/jax-caldera/` and `output/reporters/jordan-velez/` append audit blocks to article body (new in C92); compiled `editions/cycle_pulse_edition_92.txt:375-467` contains `## EVIDENCE` with `output/civic-voice/*.json` filesystem paths. Wiki ingest ran clean (extraction format); raw ingest held pending strip. `CITIZEN USAGE LOG` confirmed redundant with `buildCitizenCards.js` world-data card system. Photo pipeline REBUILD entry updated with E92 confirmation line.
- 2026-04-23 (S172, research-build) — Inter-agent conversation capability (Solo MCP / Aaron Francis) logged. RESEARCH.md S172 entry + ROLLOUT LOW pointer in Infrastructure ("Inter-agent conversation harness — test"). Test design locked with Mike: one Tier-1 citizen + one desk reporter, unpublished transcript analysis only. Preconditions: Phase 39 + Phase 40 closed + persistent-process infra decision. Wiki: [[RESEARCH#s172--inter-agent-conversation-capability-solo-mcp--aaron-francis]]. Mike scouting Amp for Solo GitHub source.
- 2026-04-16 (S152) — Major compression. Spine reframed as "7 done / 3 active-or-parked." Phase 33, 38, 39, 40 design content extracted to individual plan files. Phase 41 remaining items, research patterns, and all other embedded phase designs moved to [[plans/BACKLOG]]. Session Harness compressed to one-line pointer. Active Plans table added as top-of-file navigation. Size: 914 → ~150 lines.
- 2026-04-16 (S156, research-build) — Phase 40 progress: 40.4 four-component map + 40.5 Plan Mode gate shipped as docs; 40.3 credential audit plan drafted at [[plans/2026-04-16-phase-40-3-credential-audit]] (9 tasks, 3 open questions resolved S156). Phase 40 at 4.5/6: 40.1/40.4/40.5/40.6 DONE, 40.3 plan-ready, 40.2 still pending own plan file. Skill-eval framework: first skill `/skill-check` shipped covering write-edition + sift; sift patched to persist full proposal set. Migration-off-Claude reclassified HIGH → research/watch (spine walked, API pressure off).
- 2026-04-19 (S166, engine-sheet) — /pre-mortem C92 run against current code. Skill fix shipped (`6306272` — catches Math.random reference-passes, path-filtered git log for §0, severity tiering). Three new engine-repair items added to [[engine/ENGINE_REPAIR]]: row 12 (`utilityFunctions.js` cycle-path Math.random), row 13 (SCHEMA_HEADERS alignment diff), row 14 (non-canonical neighborhood strings). Row 7 (four Math.random fallbacks) marked `done` — closed by `76a408c` + `af40282` in S156. All three new items parked until E92 ships.
- 2026-04-17 (S156, engine-sheet) — Phase 40.3 credential audit EXECUTED. Two commits: `056eae0` (safe tasks 1/4/5/7 + Task 0 dead-file delete) and `91d8649` (live-infra tasks 2/3/6 + Task 0 PM2 remainder in one restart window). Outcomes: `.env` and `credentials/service-account.json` relocated to `/root/.config/godworld/` (chmod 700/600), `lib/env.js` central loader with `override:true` so the relocated file wins over stale shell/PM2 env, 78 `require('dotenv').config(...)` sites swept to the central loader, 9 hardcoded credential/env paths fixed, `dashboard/server.js` ES-module dotenv converted, `ecosystem.config.js` rewritten to match the live PM2 registry (mags-bot / godworld-dashboard / moltbook / spacemolt-miner) with `env_file` + `GODWORLD_ENV_FILE` per app, `~/.bashrc` `GOOGLE_APPLICATION_CREDENTIALS` pointed at the new path (resolved drift with the pre-existing `~/.config/gcloud/service-account.json` copy), `TOGETHER_API_KEY` removed from `.env` and swept from `~/.pm2/dump.pm2`, `credentials/supermemory-pn-key.txt` deleted (dead duplicate), `.claude/settings.json` absolute-path deny rules added for credential paths, Supermemory write-gate added to `.claude/hooks/pre-tool-check.sh` (deny curl mutations to `api.supermemory.ai` and `npx supermemory add/ingest/update/delete` unless command references an allowlisted ingest script), Discord bot file-read refusal shipped in `scripts/mags-discord-bot.js` (scans for credential paths + Layer 4 patterns, logs to `logs/discord-injection-attempts.log`, drops pre-API). `DISASTER_RECOVERY.md`, `STACK.md`, `SUPERMEMORY.md` updated. Smoke tests green: `queryFamily.js`, `engineAuditor.js`, dashboard HTTP 302, Discord bot reconnected as "Mags Corliss#0710". Phase 40 now at 5/6 — only 40.2 cattle refactor remains. Plan file `[[plans/2026-04-16-phase-40-3-credential-audit]]` has full per-task Status entries + Audit re-run section with zero old-path hits.
