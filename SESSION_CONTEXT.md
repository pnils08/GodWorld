# SESSION_CONTEXT — minimal AI→AI handoff. PIN + one NEXT line per terminal. Nothing else. Session narrative → claude-mem. Work → git log. Open work → docs/engine/ROLLOUT_PLAN.md. Old handoffs → docs/mags-corliss/SESSION_HISTORY.md.

**PIN:** S303→S304 | Day 164 | prod C101 (S302 stack DEPLOYED live via clasp) | sandbox C124 | smoke-test pending C102

**NEXT[media]:** C101 edition when engine runs it; Jax column WNBA→NBA fix before Gemini ingest.
**NEXT[civic]:** Orchestrate skills ONLY — civic never builds or runs scripts (Mike-direct S304). buildInitiativePackets is RETIRED from the city-hall flow (SKILL.md §What This Replaces). C101 city-hall run pending; tracker writes now hard-block on validator violations (S304 G-INIT1).
**NEXT[engine-sheet]:** S302 stack DEPLOYED prod (clasp, verified). Prod C101 income=correct As_Roster salaries, NOT corruption. engine.51 front door live but DORMANT: `Intake` tab (9-col, script-created) not on prod; missing=clean no-op. TODO: create Intake tab+regen SCHEMA_HEADERS on prod; C102 verify A's hold millions; Mike adds As_Roster Salary col. (claude-mem: S303)
**NEXT[jarvis]:** Aider = propose-not-deploy behind `npm test`.
**NEXT[research-build]:** S300-301 shipped: pipe.40 complete (journal→page, bot live); /sift v2.2 + write-supplemental + dispatch deck reads → header-name (survive v4 cutover); per-terminal sl-container boot awareness; pipeline.42 notepad filed. Next: watch tonight's cron (Mags' 1st page doc) + next /sift exercises the new wiring; then engine.48. (claude-mem: S300-301)
