# SESSION_CONTEXT — NEXT WORK ONLY. One PIN + one NEXT line per lane. What already happened lives in git, claude-mem, ROLLOUT_PLAN and the gap/production logs — never here. If a line describes a session instead of an assignment, it is written wrong.

**PIN:** S484 | Day 197 | canonical C108 | **PROD @116 = clasp v103 = `78eb6935`** (civic.38 Task 4 through step 3: stage columns + `StageHold`, stage handler, upkeep, frozen-cohort Delivering comparator + regress, `completed` +3 once, the losing clock in every domain + eligible revival, a stalled Delivering row falls, staged rows never `silence`, T7 yields on staged — all inert on blank `Stage`; web app read back @103) | Bench SANDBOX 0908 @87 = `78eb6935`, sheet C122 bench-only (fixtures `INIT-901`–`INIT-906`), re-sync from live before the next cut | claude-mem observer on `nvidia/nemotron-3-super-120b-a12b:free` (infrastructure.11 open); a new worker env var needs `npx claude-mem stop` then `start` — `restart` re-spawns with the OLD env | `diag187` rides the web-app fire response only, an editor fire has none | engine-sheet LEADS the sim repairs | never `--amend`; wiring card before every cut; deploys from an isolated `git archive` stage | DOCTRINE §13/§15/§16/§17 | Sunday 21:00 city-hall slot runs the C108 chain | civic.38 is the house project: plan `docs/plans/2026-09-19-civic-wake-game-loop.md` §Lane assignments, one owner per file, dry-run only | Ledger 952 rows | agy runs auto (`--dangerously-skip-permissions`, relaunched 2026-09-21; read-only by instruction) | OpenRouter Claude `:batch` was rejected 2026-09-21, Gemini/DeepSeek batch work

**NEXT[engine-sheet]:** Code deployed live PROD @116 (the @109–@116 civic.38 stage cuts, all inert on blank `Stage`), smoke-test rides the weekly live fire, C109 ≈ Sunday 2026-09-27 — expectations in DEPLOY_HISTORY §PROD @109–@116 (tracker gains col 38 `StageHold`; World_Config gains `civicDeliverMargin_health` 0.1, `civicDeliverRegressShare` 0.5, `civicStageUntendedStallCycles` 12), then `node scripts/regenSchemaHeaders.js`; NOTHING below waits on it, the bench is the gate. Live `Engine_Errors` holds one C108 row (WeekRecord, sports feed row 226 lacks a game-result) — the builder's feed. Codex delivering review closed (F3/F4/F5 fixed @113; F1/F2 carried as the run-boundary limit — an engine row if the builder wants exactly-once payouts). Bench re-sync note: `syncSandboxFromLive.js --apply` needs auto mode OFF on this seat (classifier refuses it in auto). Step 3 (losing clock) LIVE @116 with both reviews folded in (agy `output/antigravity/2026-09-21-review-civic38-stall-clock.md`, codex `docs/research/2026-09-21-codex-civic38-stall-clock-review.md`; codex is out of messages until 2026-09-27). Then (4) six-row conversion — OARI = INIT-002, INIT-003 per the plan's amended ruling, Baylight unstaged, tell research-build first, and NOT before directors' work-wake shifts stamp `LastWorkCycle` (scripts lane). engine.252: a provable measurable per not-playable domain → builder. engine.253 (builder, own session): transit made true as its own system. Bench trap: an HTTP 503 on a fire is not proof the run did not start — read the sheet. Then engine.249, engine.251; own session engine.248; rest `output/production_log_run_cycle_c108_gaps.md` G-EC53–69.

**NEXT[research-build]:** civic.38/.39 orchestration. Codex is low on budget — don't load it up (recheck/review duty only, nothing new). Agy is staged for ingest work, not civic. Kimi + engine-sheet carry the rollout. Kimi's queue is now CLEAR through `51fdee28` (packs + call-vote move, 35/35 + 54/54 green, pushed) — dispatched 2026-09-22 to start civic.39 Task 3 (batch client/submit-collect, checking orBatch.js per-model pricing first). Pinged engine-sheet on two unblock checks: (1) six-row initiative conversion — was waiting on directors' LastWorkCycle stamps, which may now exist since the work-wake packs landed; (2) housing lever exec (design filed, engine-sheet owns the build, ruled ahead of safety). Codex's health-numerator amendment landed (`4f77b1ec`). engine.254 filed (`docs/plans/2026-09-21-care-and-justice-system.md`): chaos-cars reviewed, zero OARI-van events in OARI's own target hoods C100-108 measured; builder ruled build the service layer (scoped, no code); judicial-ledger shape + mental-health states still open. Builder adds Anthropic credit 2026-09-22 → run `docs/plans/2026-09-22-agent-model-fit-test.md`. Run `node scripts/civicPetitions.js --dry-run --json` into `output/` each cycle once a band exists. Check whether the Monday datawake needs this week's apply or tolerates last week's board. Keep `docs/reference/CIVIC_GAME_LOOP.md` labels current.

**NEXT[kimi]:** civic.38 chain A COMPLETE, 6 commits local, none pushed (mixed stack; builder: hold the push until fixes land): 8b07fc29 T1+3, ef57efe0 T2 (ledger + Sunday fold + Status edge + candidates through validator/clerk/gate), 48ebf9df T8 (directive = 10 elected seats + per-seat evidence), 1ddcf92f T9 (rota = 11 seats; 4 directors → work-wake packs), 9d22388a T6.3 (petitionGateSweep; PETITION_SUPPORT_BANDS empty — nothing gates live until the builder names a band), a1cd97e2 handoff/debt notes, 41c6892c Reflection_Intake live-header fix (ReflectionExcerpt; names verified against codex's counter — agy's T3.2 fixtures use Event/Snippet guesses, reader accepts both). Catalog (89b5c36d) + codex counter both live; propose path closed-loop. agy's T3.2/T3.3 failures resolved at HEAD (game suite 25/25). Flags: touched workWakePackages.js (off my editor list); debt captures in output/kimi/2026-09-20-civic38-code-debt-captures.md. Next: no petition band yet (builder ruled — set from dry-run counts); add a Baylight (INIT-006) `initiative-project` work-wake entry in `cron-work-wake.js` (codex holds until you return); codex review of 8b07fc29; engine-sheet T4-5 bench. Standing: adversarial review duty on repair waves (output/kimi/). Signal from engine-sheet: after the first live fire on PROD @109, `applyTrackerUpdates` stops warn-skipping `LastWorkCycle`/`LastWorkSeat` — check your Task 2 side against that fire. Codex adopted your files while you were out (mint, board helper, repairs, four rulings): re-read `git log` for your owned files before editing. NEW when you return: civic.39 (`docs/plans/2026-09-21-civic-sunday-stage-machine.md`) Tasks 1-2 first — the `tick` stage machine and the deterministic apply gate (missing voices pending, model audits become deferred verdicts). Read its §Failure handling before Task 3. Builder ruling: keep the cheap models per seat, no upgrade. ALSO (builder-ruled 2026-09-21): work-wake JSON entries in `scripts/work-wake-packages.json` for Baylight (INIT-006, sports domain non-playable so no Delivering gate yet) and DA Dane (justice beat, `civic-office` + `crime-metrics` nodes); the `call-vote` move.

**NEXT[codex]:** civicPetitions.js: health numerator Sick, denominator population; in-care separate. Then call-vote: mayor/district seat, domain-rules-deferred, once/row/week. Spec: docs/plans/2026-09-19-civic-wake-game-loop.md §Open questions. Later board comparator/config/history exports. Recheck Kimi ownership; Claude lands mixed stack.

**NEXT[antigravity]:** civic.38 Task 9 tests — you own `scripts/cron-civic-game.test.js`. Scaffold exists and runs against the real Task 1+3 code (`8b07fc29`); T3.2 (petition pool) and T3.3 (confrontation parse) currently fail — decide fixture error vs kimi defect, and report defects to kimi in tmux. Add fixtures for Tasks 2, 6, 8 as each lands; adversarial review of each landed task goes in `output/antigravity/`. Re-read the file before every claim.

**NEXT[grok]:** No pane; nothing queued.
