# SESSION CONTEXT - GodWorld

**The thin state pin + one next-opener per terminal. Boot injects only the PIN and your terminal's NEXT line — nothing else is carried.** What shipped → `git log`. What's open → `docs/engine/ROLLOUT_PLAN.md`. Why a thing was done → claude-mem search.

---

**PIN:** Session: 267 | Day: 161 | Cycle: 99 RAN | Edition: C99/E99 CANONIZED end-to-end S265 (graded A; Arbiter 0.866; bay-tribune COMPLETE 53 wiki+906 cards; PDF Drive 1sPDVe928GBGBQuYGCYGIiH-Uf5rleZIJ) | engine v3.4 — **engine.38 Phase A SMOKE-TESTED CLEAN 5/5 at C99 S265** (coverage 65.9% single-gen / 83.6% all-gen 758/907; archiver dormant 5199<12k; B3 regime-shift guard suppressed spurious spike; patternFlag NOT volume-pinned — "High event volume (8)" normalized identical to C98 despite 149→859 jump; named citizens Vinnie/Benji/Deacon covered). **engine.11 chaos-cars CLASP-DEPLOYED LIVE S265 (Mike go-call):** 5 new + 5 modified Apps Script files, 323/323 tests, 0 collisions, .test.js excluded (C98 crash lesson held). **SMOKE PENDING C100** (first chaos cycle — Chaos_Cars tab lazy-creates; 3-15 events; 0-1 scale; asymmetric decay; NM fold consumes; Tier-1→createChaosArcs_; Watch Q3 arcLifecycle). **engine.38 C100 WATCH: sentiment-uniformity** (G-EC33 — 20/21 hoods +0.4-0.5 into tight 0.6-0.86 band C99; confirm divergence not re-compression). 2 manual editor deletes STILL owed (generateNamedCitizensEvents.gs + applyCycleWeightForLatestCycle.gs — clasp doesn't auto-delete; dead code).

**NEXT[media]:** C99 CANONIZED end-to-end (sift→write-edition→edition-print→post-publish all DONE S265; edition A / Arbiter 0.866; bay-tribune COMPLETE 53 wiki + 906 cards + 27 civic records; coverage ratings written). **Next media run = C100** once engine runs it (watch: first chaos-cars cycle may surface vehicular/sideshow events in editions — engine.11 deployed but did NOT execute C99). **NEW LANE (Mike, S265): "direct pieces" run through Gemini — Gemini writes + corrects + ingests them; my role = REVIEW + canon-check, and the fix must land BEFORE Gemini's ingest because ingest = canon (a wrong fact propagates into next sift).** Live example: Jax "The Heat Shield" column (jax_varek_column.md, Drive 1PKyP93Qbc11L8b35jiNduWvIaLPvJj80) — WNBA→NBA correction owed before ingest (Oaks are NBA, canon-solid E91/93/94/97); byline mislabeled civic-not-opinion + Paulson-as-subject are Mike's judgment calls, not vetoes. Open handoffs to research-build (gap logs, do NOT write ROLLOUT from media): G-P-C99-1 (Step 5b base_context cycle field at .baseContext.cycle, gate text ambiguous) + G-P-C99-2 (2a wall-time ~36min understated; stale citizen_card_failures dump survives clean run); carried Davis POP-00021 drift in newsroom.md Five Goods table + checkLetterEligibility.js exclusion-line false-positive HALT (it barred REAL reporter Elliot Graye POP-00012 this cycle, G-W1). 5 canon-drift citizens owed engine-sheet backfill (canon_drift_c99.json: Elliot Abraham, Herbert Jones, Rosario Medina, Delia Vargas-Ruiz, Jerome Pittman). FLUX photoQA over-filters stadium frames (G-PR-C99-1, research-build→engine-sheet rubric retune).
**NEXT[civic]:** run `node scripts/buildInitiativePackets.js` each cycle so the tracker JSON doesn't re-stale (last auto-refresh ~C89, civic.14).
**NEXT[engine-sheet]:** **C99 RAN S265 — engine.38 Phase A SMOKE CLEAN 5/5 + engine.11 chaos-cars CLASP-DEPLOYED LIVE (Mike go-call).** /run-cycle 99 complete (pre-flight READY · pre-mortem SAFE · `output/{engine_review,world_summary}_c99.md` + `output/production_log_run_cycle_c99_gaps.md` on disk, 0 HIGH). engine.38 smoke: coverage 65.9% single-gen / 83.6% all-gen (758/907), archiver dormant 5199<12k, B3 guard suppressed spurious spike (self-clears C100), patternFlag NOT volume-pinned (normalized (8) = C98 despite 149→859 jump), Vinnie/Benji/Deacon covered. Plan: `docs/plans/2026-06-19-living-city-full-population-coverage.md`. **C100 SMOKE OWED — two surfaces:** (a) **engine.11 chaos first-run** — Chaos_Cars tab lazy-creates; 3-15 events; Sentiment/CrimeIndex 0-1 scale; asymmetric decay; NM fold consumes-not-accumulates (verify-fix); Tier-1 hit → createChaosArcs_ cascade; Watch Q3 chaos-arc↔arcLifecycle (StaleAfterCycles=6); pre-existing `weaveStorylines_` wired production-block only. Runbook `docs/engine/CHAOS_CARS_DEPLOY.md`. (b) **engine.38 sentiment-uniformity (G-EC33)** — 20/21 hoods +0.4-0.5 into tight 0.6-0.86 band C99 (Thanksgiving recovery off depressed C98, plausibly organic); confirm C100 *diverges*, not re-compresses → else event-flood is flattening sentiment signal. **2 MANUAL EDITOR DELETES STILL OWED (clasp doesn't auto-delete):** `generateNamedCitizensEvents.gs` (engine.38 A3 orphan) + `applyCycleWeightForLatestCycle.gs` (S238) — dead code, clean when convenient. **REMAINING engine.38:** Phase B (valence/divergence — GATED on research.14 affect re-audit; do NOT scale composure-writing classes before it); Phase C (wake eligibility → all 906, bot). **S5 (resolved):** named sim citizens shaped partly by ambient life — no edition-canon conflict (sift/famous-spotlight don't read ambient LifeHistory). **S2 follow-up:** confirm pulse dampen/cap against C99 live ranges (`PULSE_REF_EVENTS=8`). ROLLOUT engine.38 + engine.11. — **(0) S263 CLOSED (all done-pending-archive):** gov.40 substrate-scan confirm (all `/health`+determinism+ledger scans deterministic-script-backed; `/health` Scan 3 repointed to `ctxMap.js`, skill v1.1) · gov.39 (engine-sheet's 5 rows tightened to bare canonical state tokens — `docLoopStatus.js --lint` now CLEAN) · **engine.37 photo bump SHIPPED** (`lib/photoGenerator.js` → `black-forest-labs/FLUX.2-pro`, steps 4→28 both sites — measure-twice caught the pro-honors-steps mismatch that would've poisoned the test; Node-side, no clasp). **WATCH: engine.37 verdict falls out of the next edition's photo-QA** — Isley-to-canon + in-frame text suppression; still misses an axis → outside-vendor bake-off (Watch List). — **(1) CITIZEN-LOOP (research.14) wake-side BUILT + LIVE S262** — `lib/reflectionClassifier.js`+`scripts/classifierGate.js`, affect tags in `citizenDialMap` (negative-pole), SM-tag col **AW** (ledger 48→49), **`citizen-pages`** Supermemory container + `lib/citizenPage.js`, `lib/citizenDials.js`, **`Reflection_Intake`** tab, **`scripts/citizen-wake.js`**. **Cron LIVE 3×/day (07:30/12:30/19:30 UTC)** rotating citizens around Mags' anchor; first auto-wake tomorrow 07:30. Live wake validated (POP-00304). **ONLY open = gated dial write-back** (cycle reads `Reflection_Intake` → `applyTaggedEvent_` @ ~0.45) — DO NOT wire until the **Phase-1 daily audit** (let pages accrue a few days, read them, then audit). **`citizenDialMap` affect tags committed but NOT clasp-pushed** — inert until the write-back deploys. Read-side cosmetic: page v4 read returned empty `content` field (doc present); eyeball when wiring the consumer. — **(2) engine.11 CHAOS-CARS — ENGINE-SHEET BUILD COMPLETE S265 (ultracode):** all engine-sheet tasks built + on origin/main (8 commits `486d070a`→`8822e6b5`, rode research-build's `f769f9af` push; 323 test assertions / 6 files, 0 collisions). T1.5 clobber fix · T2 config + T4.1 decay (utilities/ clasped dual-use — lib→utilities relocation since lib/ is claspignored) · T3.1-13 generator+ledger+wiring (both entry blocks) · T4.2/3 business decay · T5.1/4/5 Tier-1 cascade (orchestrator flag + createChaosArcs_ + chaos-cascade seed) · T6.4 engine-side friction hook. Verify = 5-agent adversarial (`wf_4e2b7b99-1b4`): one real blocker (neighborhood fold accumulated-not-consumed) FIXED via PropertiesService residual (T8 pattern: load→decay→add fresh→writer fold; also fixes multi-column loss). **CLASP-DEPLOYED LIVE S265 (Mike go-call):** pre-deploy gates re-verified green (323/323 tests, 0 collisions, single config, .test.js excluded), `clasp push` exit 0, 5 new + 5 modified files verified in Tracked scope, test files in Untracked. Runbook executed: `docs/engine/CHAOS_CARS_DEPLOY.md`. **SMOKE PENDING C100** (first chaos cycle). `Chaos_Cars` tab lazy-creates on first run (ensure-tab intent). Scale fix: Sentiment/CrimeIndex magnitudes → live 0-1 scale (Mike-approved). **Open: Q3** chaos-arc↔arcLifecycle (mitigated StaleAfterCycles=6, observe C100); **pre-existing flag** `weaveStorylines_` wired in production block only, not cycle-phases (`applyStorySeeds_` in both). **Cross-terminal NEXT (research-build):** T5.2/T5.3 cascade SKILLs + T6.1/2/3 dry-run/magnitude/frequency — **now live-verifiable at C100** (producer deployed). ROLLOUT engine.11. — C98 has run (E98 canonized). **engine.36 filed** (isolated staging env). — **research.18 dial-essence Seam A — COMPLETE engine-sheet side (S266).** All **43/43** authored citizens (4 faces + 10 A's + Tier-3 family/Mara/Paulson/Lucia + 8 civic + 5 project directors + 9 media reporters) seeded LIVE via new `scripts/seedTier1EssenceLive.js` — NARROW path: essence→`LifeHistory_Archive` author-once (guard POPID+EventText), archive-only re-derive of DialState+TraitProfile, **NO population O-clear** (886 others untouched), idempotent + `batchUpdate` quota-safe. **38 in the wake loop (dev≥60); 5 below-gate** (thin canon → RB retune: Rivas 54 / Sarah 57 / Ellis 43 / Taveras 39 / Ramos 21). Paulson 00527 seeded (Mike go). Commits `c86030d1`/`51799b0c`/`df9dfbda`. **Bug found+fixed (S266):** per-row sequential writes hit the Sheets write-quota and silently aborted the high-row tail (Navarro/Caldera/01021 left neutral) — caught by an **independent read-back, NOT the script's own verify** (which only checks rows it reaches); fixed via single `sheets.batchUpdate` call, which also healed the prior partial-writes. **OPEN:** RB retune of the 5 thin-canon + **engine.39** pure-integrity map tag (ready, lets Dane et al. reach integrity-vhigh) — when either lands, re-run the idempotent seeder (newcomers seed, rest no-op). Plan `docs/plans/2026-06-21-tier1-dial-essence-backfill.md`.
**NEXT[research-build]:** **research.18 dial-essence — MEDIA REPORTERS is the open tranche (fresh session, S266).** ALL 34 civic+core Tier-1 authored+validated+pushed (harness 272/272 — `node scripts/seedTier1Essence.js`); engine-sheet already seeded **3 faces + 20 core LIVE** (`c86030d1`/`51799b0c`), civic 13 await the next live-write pass. **Author the media reporters** — Hal Richmond, P Slayer, Elliot Graye POP-00012, Maria Keen, Jordan Velez, Carmen Delaine, Luis Navarro, the desk personas, DJ Hartley, Jax Caldera. Method in plan **§▶ RESUME HERE**: MCP `lookup_citizen` for POPID (the `verify` script mis-hits hyphenated names), read `docs/media/voices/<name>.md` / coverage → target band per dial, **honor every disposition dial (the Deacon rule)**, `node scripts/seedTier1Essence.js --pop=<POPID>` to ESSENCE MATCH; then engine-sheet live-writes the full set. Plan `[[plans/2026-06-21-tier1-dial-essence-backfill]]` (research.18). — **Also open RB:** **governance.42** C99-triage RB rows (7 — sift canon-fence / Davis POPID rule-fix / operator-guardrail; `[[plans/2026-06-21-c99-gap-log-triage]]`); **engine.39** pure-integrity map tag (filed → engine-sheet); chaos-cars consumers **T5.2/T5.3** (now live-verifiable post-deploy, `scripts/buildWorldSummary.js` emitter + city-hall-prep rule); **research.17** layered-memory (Mike-gated); **research.14** citizen-loop classifier→affect (blocked, obs 31554). OpenRouter live (~$10).

---

## Maintenance Rule

**This file carries exactly two things: the PIN (current sim-state) and one NEXT line per terminal (what that terminal's next instance opens with).** Boot injects only the PIN + your terminal's NEXT line. That is also the whole write side — at session-end the closing terminal updates the PIN and its own NEXT line, nothing else.

- **What shipped** → `git log` (authoritative — no Shipped block here).
- **What's open** → `docs/engine/ROLLOUT_PLAN.md` (canonical for open/closed work).
- **Why a thing was done** → claude-mem search / commit bodies.

No STATUS narrative, no Shipped block, no Recent Sessions log — those duplicated git/ROLLOUT/claude-mem and went stale, so they're retired (ADR-0009 §loop-tightening). **The contract: boot-read set ≡ session-end-write set = {PIN, NEXT[terminal]}.** Prior narrative lives in git history of this file + `docs/mags-corliss/SESSION_HISTORY.md`.

Engine versions, cascade dependencies, and the documentation registry live in `docs/engine/DOCUMENTATION_LEDGER.md`. Project description lives in `README.md`. Critical rules live in `.claude/rules/identity.md`. Architecture concepts live in `docs/reference/V3_ARCHITECTURE.md`. Mobile/mosh instructions live in `docs/OPERATIONS.md`.

---

## Key Tools & Infrastructure

| Tool | Purpose | Usage |
|------|---------|-------|
| **Batch API** | 50% cost for non-urgent work (~1hr turnaround) | `/batch [task]`, `/batch check` |
| **Claude-Mem** | Automatic observation capture (SQLite + Chroma vector, port 37777) | `search()`, `timeline()`, `get_observations()` |
| **Supermemory** | 6 containers: mags (brain), bay-tribune (canon), world-data (city state), super-memory (bridge), mara (audit) | `/save-to-mags`, `/super-search` |
| **Discord Bot** | 24/7 presence (PM2: mags-bot) | Always-on, conversation logging |
| **Nightly Reflection** | Discord conversation journal at 11 PM CDT | `scripts/discord-reflection.js` (cron) |
| **Drive Write** | Save files to Google Drive | `node scripts/saveToDrive.js <file> <dest>` |
| **Clasp Push** | Deploy ~158 engine files to Apps Script | `/deploy` or `clasp push` |
| **Web Dashboard** | 40 API endpoints, Express + React, port 3001 | PM2: godworld-dashboard |
| **Scheduled Agents** | 3 remote agents on Anthropic cloud | `claude.ai/code/scheduled` |
| **AutoDream** | **DISABLED S228** — redundant with MD persistence + Supermemory + claude-mem observations | `autoDreamEnabled: false` in `~/.claude/settings.json` |
| **Engine Health** | `/health`, `/ctx-map`, `/deploy`, `/pre-mortem`, `/tech-debt-audit`, `/doc-audit` | See CLAUDE.md |
| **Hookify** | 5 active rules | `/hookify:list` |
| **GodWorld MCP** | 10 tools for structured city data | See CLAUDE.md |

**Desk agent architecture:** Agents read from `output/desks/{desk}/` workspaces built by `scripts/buildDeskFolders.js` (zero LLM tokens). Each agent's SKILL.md points to IDENTITY.md and RULES.md at `.claude/agents/{desk}-desk/`. 80/20 model tiering — complex desks (civic, sports, chicago) run Sonnet, routine desks run Haiku.

**Batch API guidelines:** Use for codebase audits, documentation generation, architecture analysis, character continuity reviews, post-edition analysis. NOT for interactive editing, desk agent writing, or real-time debugging. Results at `~/.claude/batches/results/`.

---

## Current Work

See `docs/engine/ROLLOUT_PLAN.md` — the single source for all project work status (active, pending, completed, deferred).
