# SESSION_CONTEXT — NEXT WORK ONLY. One PIN + one NEXT line per lane. What already happened lives in git, claude-mem, ROLLOUT_PLAN and the gap/production logs — never here. If a line describes a session instead of an assignment, it is written wrong.

**PIN:** S484 | Day 197 | canonical C108 | **PROD @117 = clasp v104 = `a7a9c79b`** (engine.251 housing lever Tasks 1–8 inert: `civicHousingReliefEnabled` 0, housing `playable:false`; G-EC70 owned units anchored; on top of civic.38 Task 4 steps 0–3; web app read back @104) | Bench SANDBOX 0908 @92 = BENCH-ONLY experiment build (main `655b3001` + staged housing `playable:true`), sheet C126 bench-only with `INIT-951` and hand-set housing dials — re-push main HEAD and re-sync from live before any unrelated proof | claude-mem observer on `nvidia/nemotron-3-super-120b-a12b:free` (infrastructure.11 open); a new worker env var needs `npx claude-mem stop` then `start` — `restart` re-spawns with the OLD env | `diag187` rides the web-app fire response only, an editor fire has none | engine-sheet LEADS the sim repairs | never `--amend`; wiring card before every cut; deploys from an isolated `git archive` stage | DOCTRINE §13/§15/§16/§17 | Sunday 21:00 city-hall slot runs the C108 chain | civic.38 is the house project: plan `docs/plans/2026-09-19-civic-wake-game-loop.md` §Lane assignments, one owner per file, dry-run only | Ledger 952 rows | agy runs auto (`--dangerously-skip-permissions`, relaunched 2026-09-21; read-only by instruction) | OpenRouter Claude `:batch` was rejected 2026-09-21, Gemini/DeepSeek batch work

**NEXT[engine-sheet]:** engine.251 housing lever Tasks 1–8 LANDED on main (7bb4fc3a…c257ebf1, all pushed) and bench-proven 2026-09-22: relief to the cent, vote baseline → Standing → Delivering (C117–C126) on a BENCH-ONLY playable build; main stays `playable:false`, `civicHousingReliefEnabled` 0. (1) Rate 0.20 relayed by research-build as the builder's ruling 2026-09-22 — confirm with the builder directly before the LIVE `civicHousingReliefRate` moves; MEASURED at 0.20 on the bench (edges 0.243/0.249/0.251 vs baseline, Delivering at C135) → margin seed 0.15 (`engine94SheetContract.js`, bench cell set). When the builder confirms: hand-set live `civicHousingReliefRate` 0.20 + `civicDeliverMargin_housing` 0.15 (the live sheet arms 0.10/0.20 at C109 from the @117 seeds), research-build sequences Task 9 (flip + pins), `civicHousingReliefEnabled` 1 is the builder's switch. Background: measured 10% edge ≈ +0.02 vs the city reference's ±0.04–0.06 wobble → cannot be judged; recommend rate 0.20 + `civicDeliverMargin_housing` 0.10 (plan changelog 'Vote-time flow'). On the ruling: engine-sheet sets the dials (World_Config live self-arms the seeds at the C109 fire), research-build sequences Task 9 (lib+engine `playable:true`, label 'Tenant rent discount', test pins listed in the plan changelog) with agy/codex. (2) Read back tonight's Tue 2026-09-22 20:18 work-wake: director `work` lines in `output/cron-civic/moves/moves_c108.jsonl` (`217fffba`, every due pack wakes `8c41f7da`). (3) Bench 0908 is on experiment build @92 with hand-set dials at C126: re-push main HEAD and re-sync from live before any unrelated proof. G-EC70 fixed (`772ae416`, owned units anchored) — rides the next live push with the lever. (4) LIVE PROD @117 = `a7a9c79b` (v104): smoke-test rides the C109 fire Sun 2026-09-27 — expect World_Config +4 housing dials, Household_Ledger 13 → 17 cols with gross copied, MonthlyRent unchanged, 0 Engine_Errors; then `node scripts/regenSchemaHeaders.js`. Then engine.252 (measurable per non-playable domain → builder), engine.253 transit (own session), engine.249/.248, gap log G-EC53–70. Smoke-test for PROD @109–@116 rides the C109 fire Sun 2026-09-27; INIT-005's conversion baseline stamps then. Live `Engine_Errors` holds one C108 row (sports feed row 226) — the builder's feed. Codex out until 2026-09-27; kimi civic.39 Task 4 awaits builder word. Bench trap: an HTTP 500 on a fire is not proof the run did not start — read the sheet.

**NEXT[research-build]:** civic.38/.39 orchestration overnight (Mike asleep, tracking + dispatching per his 01:15 note; codex low-budget/review-only, agy freed from ingest-only for review+test work, kimi/engine-sheet carry the build). **For Mike, waiting on him specifically (blocked by design, not by us):** (1) LIVE six-row initiative conversion — the auto-mode classifier refuses a live "Production Deploy" tracker write; runner is proven on bench (C123–C124) and sitting at `output/engine-sheet/2026-09-22-convertInitiativeStages.js`, command in NEXT[engine-sheet]; also needs his ratify on two mechanism calls (INIT-001/005 signed-at-vote-Cycle, INIT-003 → Stage `Proposed`). (2) Finding 2 from agy's `51fdee28` review — `cron-work-wake.js` `LIMIT` default (1) + the Tue/Thu-only crontab mean only 2 director/staff wakes per cycle citywide; directors will starve 4-5 cycles between shifts, which will manufacture stall pressure once the six-row conversion is live. That's a crontab/cadence change — his call, not mine to fire alone. Fixed tonight from agy's `51fdee28` review: Finding 1 CRITICAL wokenCycle tue/thu dedup bug + Finding 3 HIGH callVoteEligibility crash guard (`1c1f563b`), Finding 5 MEDIUM board text told agents "signatures move it" on domains where call-vote is the only path (`cf6632b7`) — this one mattered before Mike's other blockers because it's exactly the path his six-row conversion unblocks. Findings 4 (domain-bypass ordering, informational for whenever a new domain gets a real band) and 6 (dangling pending move on same-cycle double-file, rare edge case) left open, non-urgent. Kimi still on its own CLI 5h usage wall (civic.39 Task 3, no code lost). Codex's health-numerator amendment landed (`4f77b1ec`). engine.254 filed (`docs/plans/2026-09-21-care-and-justice-system.md`): chaos-cars reviewed, zero OARI-van events in OARI's own target hoods C100-108 measured; builder ruled build the service layer (scoped, no code); judicial-ledger shape + mental-health states still open. Builder adds Anthropic credit 2026-09-22 → run `docs/plans/2026-09-22-agent-model-fit-test.md`. Run `node scripts/civicPetitions.js --dry-run --json` into `output/` each cycle once a band exists. Keep `docs/reference/CIVIC_GAME_LOOP.md` labels current.

**NEXT[kimi]:** civic.39 COMPLETE — all 4 tasks landed + pushed (Task 3 `9e076842`, Task 4 `90b83a68`). Directive is now post-close: names only seats close left passed-over/unanswered (builder's verify covered by test), runs last in the chain; prep consumes the prior cycle's AUTO. Tests 50/50 + 54/54, lint clean. Open for the builder: install the tick/batch-submit/collect schedule; first live exercise is the C108 Sunday chain. Standing: adversarial review duty on repair waves (output/kimi/).

**NEXT[codex]:** civicPetitions.js: health numerator Sick, denominator population; in-care separate. Then call-vote: mayor/district seat, domain-rules-deferred, once/row/week. Spec: docs/plans/2026-09-19-civic-wake-game-loop.md §Open questions. Later board comparator/config/history exports. Recheck Kimi ownership; Claude lands mixed stack.

**NEXT[antigravity]:** Adversarial review filed on 9e076842 (civic.39 Task 3 batch transport, output/antigravity/2026-09-22-review-civic39-task3-batch-transport.md). Findings 2 (HIGH), 3 (MEDIUM), 4 and 5 (LOW) verified real and fixed in `0f897824` (commit message got shell-mangled by backticks in the -m string — clean summary in the SESSION_CONTEXT commit right after it). **Finding 1 (the CRITICAL "usage is not defined" claim) was a false positive** — checked `git show 9e076842:scripts/orBatch.js` directly, line already read `ex.usage`, not bare `usage`; told the reviewer with the exact content as proof, for calibration on future reviews. Pricing/model audit (Finding 6) confirmed correct: D6-D8 gemini-3.7-flash only. Suites 46/46 + 54/54 + 37/37 + 15/15 + 12/12 green. Standing by for next wave.

**NEXT[grok]:** No pane; nothing queued.
