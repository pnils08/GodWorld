---
title: Engine Repair Tracker
created: 2026-04-16
updated: 2026-05-30
type: plan
tags: [engine, citizens, active]
sources:
  - SESSION_CONTEXT.md (S148 audit findings — Next Session Priority)
  - docs/engine/tech_debt_audits/2026-04-15.md
  - docs/engine/tech_debt_audits/2026-04-27.md
  - docs/engine/LEDGER_REPAIR.md
  - S166 /pre-mortem for C92 (items 12, 13, 14)
  - S181 /tech-debt-audit §6 closure (items 15, 16)
pointers:
  - "[[engine/ROLLOUT_PLAN]] — strategic phases; this tracker is tactical repairs"
  - "[[engine/LEDGER_REPAIR]] — read before touching ledger"
---

# Engine Repair Tracker

Working list of known engine/simulation issues. One row per issue. Pointers only — no
exposition. Update status as work moves. Source findings: S148 audit
(SESSION_CONTEXT.md), `docs/engine/tech_debt_audits/2026-04-15.md`.

**Status legend:** `todo` · `wip` · `done` · `blocked` · `deferred`

---

## P0 — Foundation (blocks everything downstream)

| # | Issue | Status | Session | Evidence | Fix pointer |
|---|-------|--------|---------|----------|-------------|
| 1 | Promotion pipeline typo — generator writes `CreatedCycle`, sheet has `EmergedCycle`. 11 promotions in 91 cycles. | done | S181 | S148 audit; S181 verification | Closed S181 — `phase05-citizens/generateGenericCitizens.js:58,495` renamed `iCreatedCycle` → `iEmergedCycle` (matched live Generic_Citizens schema col H) and value format aligned with `checkForPromotions.js:511` (`"Cycle " + cycle`). `utilities/cycleRollback.js:55` Generic_Citizens cycleCol fixed to `EmergedCycle`. Chicago_Citizens kept on `CreatedCycle` (verified col H of live schema). Note: the "11 promotions in 91 cycles" stat is about promotion frequency — separate from this column-name typo. |
| 2 | Supermemory `world-data` citizen cards cross-contaminated; MCP `lookup_citizen` reads poisoned data. | done | S183 | S148 audit; S181 partial close (filter); S183 full DELETE pass | Closed S183 — full container rebuild closes the contamination. R1 cold-start fix (`1fc2526`) wiped all 408 prior wd-citizens + wrote 686 fresh via `--no-quality-gate` + idempotency-filter drop, so old contaminated cards no longer co-exist with new ones. Substrate post-S183: wd-citizens=686, 100% domain-tagged, 0 orphans. S181 full-name post-filter (`097b792`) eliminated cross-contamination at write time; S183 wipe eliminated the historical co-residence problem. MCP `lookup_citizen` now reads only the fresh layer (verified S183 via tag-count snapshot). Plan-file: [[archive/plans/2026-04-27-world-data-unified-ingest-rebuild]]. |
| 3 | Simulation_Ledger corruption flagged S68 — `LEDGER_REPAIR.md` claims S94 recovery complete. Confirm or reopen. | done | S181 | `docs/engine/LEDGER_AUDIT.md` §"Current State — S181 refresh" | Closed S181 — verified live state against S94 closure claims. 0 missing names, 0 historical "Citizen" roles, all tiers numeric, age sanity holds (0 OOB on 2041 anchor). 4 new "Citizen" roles are post-S94 drift (Row 17). Audit tool: `scripts/auditSimulationLedger.js`. Boot blocker text in `.claude/hooks/session-startup-hook.sh:132` updated to reflect post-recovery reality. |

## P1 — Simulation depth (the sim doesn't simulate)

| # | Issue | Status | Session | Evidence | Fix pointer |
|---|-------|--------|---------|----------|-------------|
| 4 | Lifecycle engines stamp identical defaults on 600+ citizens (YearsInCareer=12.5, DebtLevel=2, NetWorth=0, MaritalStatus=single, NumChildren=0, etc.) | done | S184 | S148 audit; live ledger 95-99% top-value share confirmed pre-backfill | Closed S184 — root cause was the migration scripts (`addEducationCareerColumns.js`, `addHouseholdFamilyColumns.js`, `addGenerationalWealthColumns.js`) seeding uniform constants across every existing row when columns were added. Lifecycle engines (`educationCareerEngine.js` increments YearsInCareer +0.1/cycle, hence 10 → 12.5 over ~25 cycles; `generationalWealthEngine.js` handles inheritance; `householdFormationEngine.js` handles marriage/divorce/birth) update on triggers, but most citizens never had a trigger so 95-99% sat at the migration default. **Fix:** `scripts/backfillLifecycleDefaults.js` (Node + service-account direct write per engine.md) gives each row a demographically-plausible starting value derived from existing context (Age via BirthYear, Income, RoleType, CareerStage). Determinism: djb2 hash on POPID — same POPID always gets same values; safety: only overwrites rows currently at the migration constant; preserves engine-applied updates and real edits. Field derivation: YearsInCareer (age-bracket bell, retiree-aware), DebtLevel 0-10 (income-inverse + age curve), NetWorth (age-conditioned + income-multiplied + retiree-bonus), MaritalStatus (age-bracket CDF), NumChildren (age + marriage-conditioned fertility CDF). **Live state post-backfill** (top-value share dropped from 95-99% to 2.6-49.6%): YearsInCareer 308 distinct values (was 5); DebtLevel 9 distinct 0-7 bell-centered at 2-3 (was 1); NetWorth 557 distinct (was 3); MaritalStatus married 49.6% / single 21.7% / divorced 14.3% / partnered 7.2% / widowed 7.1% (was 98% single); NumChildren 0-5 distribution peak 0 (was 99.3% zero). Sample verification: Vinnie Keane (POP-00001, age 38) net worth preserved at $240M canonical; Marcus Osei (POP-00036, age 68, council) → 44.9 yrs career / widowed / 2 children / $940k; Benji Dillon (POP-00018, age 36, MLB) → 5.6 yrs / married / $623k. Node-only changes; no Apps Script, no clasp deploy. **Out of scope (separate ROLLOUT items):** (a) POP-00004 Lucia Polito column-misalignment ('Aura ' in RoleType + 'Wellness Practitioner' in DebtLevel — looks like split write); (b) 137-165 empty-cell rows (no value in column at all, lacks BirthYear or row shorter than schema); (c) engine-side derivation so new emergences get realistic values at intake time (currently lifecycle engines only fire on triggers; intake leaves these fields empty). |
| 5 | Citizen generator name clusters — 62 first / 53 last names for 686 citizens. Dupe check misses first-name clustering. | done | S184 | S148 audit; live GC tab inventory shows pre-v2.6 clusters Ronald=11, Johnson=12 | Closed S184 — `phase05-citizens/generateGenericCitizens.js` v2.6 → v2.7. (a) Pool expanded 62 → 186 first names + 53 → 144 last names = 26,784 combos (was 3,286). New entries diverse for 2041 Oakland: Latino, Black, East/Southeast Asian, South Asian, Middle Eastern, African (West/East), Anglo. No Tier 3 real-public-figure names introduced. (b) `getUniqueName` now tracks per-first and per-last counts via `firstCounts`/`lastCounts` maps populated from existing GC tab at runtime; rejects new draws that would push a first name past `FIRST_NAME_CAP=3` or last name past `LAST_NAME_CAP=4`. Pre-v2.6 clusters (Ronald=11, Johnson=12, etc.) grandfathered in via the snapshot — cap blocks NEW additions only. (c) Smoke-test (500-draw simulation with grandfathered clusters seeded): 0 null returns, max-per-first 3 / max-per-last 4 as expected, 0 new Ronalds / 0 new Johnsons. Determinism preserved — `randItem` still uses ctx.rng pattern unchanged. Apps Script change in phase05; awaits clasp deploy (next batch). |
| 6 | EventType taxonomy collapsed to `misc-event` — Phase 38.8 baseline briefs can't attribute events to citizens. | done | S184 | S146 open items; S148 audit; S184 verification (C88 fixture, 16 briefs all with `subjectIds`) | Closed S184 — diagnosis was wrong target. The `WorldEvents_V3_Ledger.EventType` field IS 91% `misc-event` (212 of 233 rows), but that ledger has no `POPID` column at all — it never could carry citizen attribution regardless of taxonomy. Citizen life events live in `LifeHistory_Log` (3,589 rows, keyed by POPID, 34 distinct EventTags). The auditor wasn't reading that tab. **Fix:** added `'LifeHistory_Log'` to `engineAuditor.js` SHEETS_TO_READ; new section in `generateBaselineBriefs.js` v1.0.0 → v1.1.0 reads the log, filters by current cycle + 6 structural EventTag values (`Life Event`, `Promotion`, `Advancement`, `CivicRole`, `Retirement`, `Stabilized`), emits `eventClass: 'citizen-life-event'` briefs with `subjectIds: [popId]` + neighborhood resolved from ledger when LifeHistory carries placeholder ('Engine'/'Generational'). Promotion hints tier-aware (Tier-1/2 = feature candidate), tag-aware (Promotion / Retirement / CivicRole / Birth into population each get specific hints), neighborhood-aware (active-ailment overlap). Smoke test: C88 fixture (9 Promotions + 2 Retirements live) → 16 briefs, 16/16 with subjectIds populated, real names/neighborhoods/roles joined from ledger, tier-2 feature-candidate hint fires on Marcus Osei (advancing to council). C92 fixture → 0 briefs (correct — C92 had 0 structural events live, all 73 LifeHistory rows were texture). High-frequency texture tags (Micro-Event, Daily, Relationship, etc. — 27 of 34 tags, ~95% of rows) excluded by design — those are color, not structural events. **Out of scope:** improving `recordWorldEventsv3.deriveDomain` keyword coverage to reduce the 91% misc-event share — that's polish that doesn't affect subject attribution; logged as separate ROLLOUT followup. |
| 15 | `citizenFameTracker.js` silently no-ops because expected columns absent from live tabs — fame tracking has been dead for many cycles. | done | S184 | S181 audit `docs/engine/tech_debt_audits/2026-04-27.md` §6 Class 2; S184 Mike decision (Path B retire) | Closed S184 — Path B (retire). Decision rationale: tier system already encodes fame (T1 most-known → T4 least), `UsageCount` on Simulation_Ledger drives tier promotion (the appearance counter wd-citizen cards read), `EmergenceCount` on Generic_Citizens drives generic → SL promotion. The fame-tracker was a fourth signal trying to do the same job, blocked by missing columns. Adding 14 columns across three tabs to revive a redundant signal is sheet-bloat against the current architecture. **Retire steps:** (a) `phase07-evening-media/citizenFameTracker.js` deleted (`git rm`); (b) `phase07-evening-media/mediaRoomIntake.js` v2.6 → v2.7 — `typeof updateCitizenFameFromMedia_ === 'function'` block removed at line 149-151, header docstring updated; (c) `docs/engine/ENGINE_STUB_MAP.md` citizenFameTracker section replaced with retired-note + retired-architecture explanation; (d) migration helpers `scripts/addCitizenFameColumns.js` + `scripts/rollbackCitizenFameColumns.js` retained for archaeology with RETIRED header notes (do-not-run). **Cultural_Ledger.FameScore intact** — that's an independent system (live, read by `recordMediaLedger.js`, `compileHandoff.js:751`, `citizenContextBuilder.js:639`, written by `culturalLedger.js:280`). Touching it would break canon — out of scope for this retire. Apps Script change in phase07; needs clasp deploy. |

| 23 | **Citizen lives are frozen — the life-event engine fires near-zero milestones.** `generationalEventsEngine.runGenerationalEngine_` is wired into every cycle (Phase5-Generational) and is supposed to advance lives by random chance (graduation/wedding/birth/promotion/retirement/death). Empirically, across all 95 cycles, the SL `LifeHistory` column carries **0 `[Graduation]`, 0 `[Wedding]`, 0 `[Birth]`, 0 `[Promotion]`, 0 `[Retirement]`, 1 `[Death]`**. The 101 "Promotion" + 242 "Advancement" rows in `LifeHistory_Log` come from a DIFFERENT pipeline (`checkForPromotions.js` + `processAdvancementIntake.js`, direct log append) — not from this engine's `applyMilestone_` (which writes both column + log). Births are additionally chained behind an in-sim `[Wedding]` that never gets written, so reproduction is dead at the first link. This is the mechanism of citizen life and it has never run. | done | S243→S244 → closed S268 | **CLOSED S268 — live-verified across 4 cycles (engine-sheet).** Direct `LifeHistory_Log` read (5199 rows) shows milestones fire EVERY cycle C96–C99: Promotion 2/cyc, Wedding 1/cyc (C96/98/99), Retirement 1/cyc (C96/98/99), Death 1/cyc (C96/98/99). The "0 across all 95 cycles" freeze is decisively broken — 5 of 6 milestone types fire. The C96 smoke (S252: Wedding×1 + Promotion×1) was not a one-off; it held C97–C99. **Residual: `[Birth]` = 0 across all four cycles** — that is the reproduction chain (in-sim wedding→birth + household processing), governed by the dormant `householdFormationEngine` (Row 22, case-fold held — admits 0 citizens) and the lifecycle design (Row 25 / engine.29), NOT the generationalEventsEngine age-gate freeze this row tracks. Births stay owned by Row 22/25; re-check birth rate once those reactivate or at a higher cycle count. <br>— S244 ROOT CAUSE (engine-sheet): the bug was NOT birthYear (clean numeric, col 12, indexOf resolves). It was `simYear`. `advanceSimulationCalendar.js:54` computes `godWorldYear = Math.ceil(absoluteCycle/52)` = a year ORDINAL (=2 at cycle 95), and L208 aliased `S.simYear = godWorldYear`. Every age-math engine reads `age = simYear - birthYear` → `2 - 2003 = -2001` → every age-range check (`if age<min||age>max return null`) rejects → 0 milestones. Death + health fire because they have NO age-range gate (matches observed 1 death + health tags exactly). **Reproduced in harness (zero deploy):** flipping `/tmp/trace_generational.js` SIM_YEAR 2041→2 collapses 22 grad/67 wed/13 birth/113 promo/98 retire → 0/0/0/0/0; age-window eligibility 313→0. **Fix:** L208 `S.simYear = 2040 + (godWorldYear - 1)` (=2041 at cycle 95, matches all consumers' `2040+floor(cycle/52)` fallback). `godWorldYear` untouched (ordinal still used by cycleRef/elections/creationDay/display). **Blast radius beyond generational:** runEducationEngine/runNeighborhoodEngine/migrationTrackingEngine/educationCareerEngine/healthCauseIntake/ensureNeighborhoodDemographics all read `simYear-birthYear` — all unfrozen by this one line (checkForPromotions NOT affected — derives birthYear from stored Age). clasp pushed live S244. **Smoke-test pending C96 fire:** SL LifeHistory column should gain `[Wedding]`/`[Birth]`/etc. Follow-up (out of scope): Simulation_Calendar col is labeled "SimYear" but stores the ordinal — label/concept collision to clean up separately. <br>— ORIGINAL S243 DIAGNOSIS BELOW —<br>S243 diagnosis (engine-sheet): SL LifeHistory column tag scan (0 across types) + LifeHistory_Log source trace + Engine_Errors (0 generational throws) + read-side aliases ctx.ledger.rows (not Row-21) + retirement firing 2× proves age/simYear correct. RULED OUT: crash, Row-21 local-array, age/simYear, commit-path (other engines' `[Media]` tags persist). **S243 trace-harness results (`/tmp/trace_generational.js` + `/tmp/rng_proof.js`): LOGIC HEALTHY + RNG HEALTHY — bug is in LIVE EXECUTION.** (1) Replaying the exact check logic against the real 770 ENGINE/CIVIC citizens × 95 cycles with a uniform RNG produces 81 weddings / 12 births / 119 promotions / 95 retirements / 23 graduations — the logic works. (2) The live `seededRng_` (utilities/cycleModes.js — an LCG with a JS float-overflow smell: `state*1103515245` exceeds 2^53) profiles as statistically UNIFORM anyway: 0.45% of draws < 0.004, 5.2% < 0.05 (uniform expects 0.4% / 5%). So NOT the RNG. (3) All six milestone checks are gated `&& birthYear` (lines 281/292/306/318/330/342); the non-gated `checkHealthEvent_` still fires (Health/Recovery tags exist). LEADING HYPOTHESIS: at runtime the gated checks skip or their `row[iLife]` writes don't persist — most likely `birthYear` (or a shared column index) reads falsy from `ctx.ledger.rows` at runtime (reads fine from raw sheet → why the harness works, the live engine doesn't). | Sheet-side forensics EXHAUSTED. Next: instrument `runGenerationalEngine_` (log iBirthYear, count citizens passing the birthYear gate, count chance_ clears) → clasp deploy → Mike fires one cycle → read instrumentation → pinpoint → fix → redeploy + smoke-test. This is the real engine.5 Phase 2 (NOT building the dead `householdFormationEngine` stubs — Row 22). |
| 24 | **Career advancement is enum-frozen + the promotion signal is split across two decoupled engines (sibling to Row 23).** Found S248 tracing the promotion structural-sync. Two findings: **(a) `educationCareerEngine` (phase05, the CareerStage owner) is partially frozen by enum drift.** Its `CAREER_STAGES.ENTRY = 'entry-level'` matches **zero** live rows (live CareerStage vocab: `entry`=3 / `early`=6 / `early-career`=1 — never the string `entry-level`), so the ENTRY→MID advancement gate has NEVER fired; `MID = 'mid-career'` matches 278 rows but MISSES the 64 `mid` rows, so MID→SENIOR is partial. Verified the years/cooldown gates are NOT the bottleneck (`YearsInCareer` 858 positive, many ≥10; `LastPromotionCycle` mostly satisfies cooldown) — the freeze is purely the `careerStage === CAREER_STAGES.X` string comparison vs the drifted live vocab. **(b) Dual decoupled promotion systems:** `generationalEventsEngine` (phase04) fires a `[Promotion]` LifeHistory TAG on independent dice with NO structural effect (the Track-1 "vacuum"), while `educationCareerEngine` advances CareerStage structurally but emits NO LifeHistory tag — the two never meet (phase04 runs before phase05; phase04 can't write CareerStage without phase05 clobbering it). | todo | S248 | S248 engine-sheet measure-twice (caller-graph CareerStage/Income owners + live enum quant: `CAREER_STAGES` consts vs Simulation_Ledger CareerStage distribution + YearsInCareer/LastPromotionCycle population) | **Fix design (NOT shipped S248 — deliberate C96-class reactivation, surfaced to Mike, held):** (a) drift-tolerance in `educationCareerEngine` advancement comparisons (map `entry`/`early`/`early-career`→ENTRY-class, `mid`/`mid-career`→MID-class — non-destructive, the runYouthEngine case-fold pattern) to unfreeze advancement across 858 citizens; (b) stamp `[Promotion]` LifeHistory tag in the OWNING engine on real advancement (structural→narrative link, via `queueAppendIntent_`); (c) RETIRE `generationalEventsEngine`'s hollow phase04 promotion dice (vacuum event). **Why held:** unfreezing advancement is a rate-changing reactivation across 858 citizens that cascades into Income (runCareerEngine) + coverage — the same Row 22 / cityDynamics class; and the phase04 retirement is an editorial-cadence change (how often promotions surface in coverage) = Mike's seat. Must NOT ride C96 as a 4th unsurfaced reactivation alongside simYear + cityDynamics + Track 1 (attribution trap, per S247 deploy-record warning). Sequence as its own deliberate fire after C96 verifies Track 1. Full design in `docs/engine/LEDGER_REPAIR_HOUSEHOLDS.md` §Phase 2b promotion. **🚫 BLOCKED until C100 chaos-cars smoke clears (S268, Mike).** Rate-changing reactivation (unfreezes advancement across 858 citizens → cascades into Income + coverage) + an editorial-cadence change (phase04 promotion-dice retirement) — do NOT stack ahead of the first chaos cycle; fire as its own deliberate window after C100 chaos + G-EC33 sentiment-uniformity clear. |
| 25 | **Citizen lifecycle & fame system is half-wired — no death-by-age, no tier decay/demotion, no SL↔Cultural_Ledger bridge.** Design session S248 (Mike-direct). Four gaps: (a) `checkDeath_` has NO age gate (can hit any age — the only milestone without one); (b) SL Tier ratchets up via UsageCount (3/6/9) with NO decay and NO demotion path anywhere — tiers can only climb; (c) Cultural_Ledger (42 figures, fame layer with working TrendTrajectory decay) is DISJOINT from the SL (no POPID, zero overlap) so fame ascension is unbuilt + engine cultural output about SL citizens (e.g. Isley Kelley's documentary) never feeds their count; (d) Cultural_Ledger figure generation mints names without a POPID so they never become tracked citizens. | todo | S248 | S248 engine-sheet measure-twice (death no-age-gate; ClockMode dist ENGINE 721/GAME 90/CIVIC 49/MEDIA 43; Cultural_Ledger schema has no POPID, 0 SL overlap; UsageCount sole SL fame signal 97 positive/max 42; Isley Kelley on SL not CL) | S325 UPDATE: fame system now FULLY wired — SL<->Cultural_Ledger link live (engine.68: culturalStatusByPop_, fame bar 25, cultural decay), UsageCount decay + earned-tier demotion live (engine.69: decayMediaAttention_ — quiet>10 cycles fades attention, ladder-earned tiers give way stepwise to seeded floor; T1 + non-ENGINE exempt). REMAINING for this row: death-by-age exists (generational); row can close after live C102+ observation confirms demotion fires in canon. |
| 28 | **Crisis arcs feed fabricated specificity to desk packets — C97 pull DONE, rebuild as a connected per-hood story signal (do NOT restore).** `generateCrisisBuckets` (phase03) reads ONE city-wide `illnessRate` from World_Population, then DICE-picks the neighborhood (`pickNeighborhood` weighted) + POOL-picks the subtype (~30 canned strings / 6 categories) → "HEALTH crisis: Flu Season Strain in Fruitvale" is invented specificity, not a measured signal; zero citizen attribution (`CitizenCount=0`). Fed to desk packets → journalists write fiction off real neighborhood names. Compounded by 3 wiring breaks that froze the ledger: (a) post-load spawner `generateNewArcs_` DELETED S185 (`eventArcEngine.js:426`; v3Integration `typeof` guard silently no-ops); (b) phase03 spawn CLOBBERED by phase08 `loadActiveArcsFromLedger_` reassign (`v3preLoader.js:226`); (c) `age` dropped on load → `processArcLifecycle_` re-ages to 1 every cycle (`processArcLifeCyclev1.js:109`) → 36 arcs frozen at 'peak' since C81 (CycleResolved / PhaseStartCycle all blank). | wip | S256 | S256 engine-sheet trace — live tab audit (`/tmp/audit_c96_*.js`: WorldEvents H–AC deprecated-blank; Event_Arc 36 frozen, CycleCreated≤81) + code reads of generateCrisisBuckets + `determinePhase_` (peak→falling on `age≥7 OR tension<5`; age-restore alone clears the 36) + the generateNewArcs_ deletion marker; advisor reconcile on spawn-location | **C97 PULL DONE** (`38e22c0`, Node-side `buildDeskPackets.js arcs=[]`, no clasp, reversible — delete one line). **REBUILD (post-C97, deliberate):** connected per-hood signal driven by real metrics — `Neighborhood_Demographics` Sick/Unemployed + `Neighborhood_Map` Sentiment/CrimeIndex/RetailVitality (per-hood, 100% populated) + engine.33 `S.neighborhoodState` loader + citizen-ID linkage; surface the hood actually under stress with its real numbers. **Design fork (advisor-flagged):** spawn POST-load (dedups against ledger) NOT loader-merge (creates dupes); keep the phase03 `worldEvents` push, move only the arc decision. Plan doc + ROLLOUT engine.* entry to follow when design starts; ultracode design-panel candidate. **S270 SEQUENCING (Mike-direct): rebuild is POST-C100, gated on chaos-cars first-run.** engine.11 chaos-cars (deployed S265, first fires C100) is itself a per-hood stress/event source that will be a primary INPUT to the rebuilt crisis signal — so design the rebuild to READ live chaos-cars output (`Chaos_Cars` tab + per-hood Sentiment/CrimeIndex deltas) rather than blind. Do NOT start the ultracode design pass until C100 chaos smoke clears + the chaos-cars signal shape is observed live; building now would design against an unobserved input. Stays safe-but-dark (C97 pull holds) meanwhile. Pattern: feedback_measure-twice-cascading-effects |

| 32 | **ARC milestone EventText is engine bookkeeping, not person-readable — starves citizen-perception continuity (research.19 T1a' supply side).** The `[Advancement]` (242) + `[Promotion]` (111) LifeHistory_Log rows — 94% of all ARC-tagged rows — carry EventText like `"Updated to Tier 3. 29 West Oakland Server. Career confidence storyline."` / `"Added to Simulation_Ledger as Tier 3..."`: tier-tracking bookkeeping, not a milestone a person would think. Only ~12 rows read as genuine milestones (Wedding 4 / Retirement 6 / Graduation 1 / Stabilized 1; CivicRole 8 is templated, Death 3 incoherent for a living reflector). Effect: research.19 T1a' (canon-anchored self-state read-back into the citizen-wake perception) can surface a clean milestone for only **4 of 212 pooled citizens (1.9%)** — the wake-side omit-design renders the latest clean milestone or omits, but the amnesia hole stays open for ~98% because the SUPPLY is bookkeeping, not because the read is wrong. | todo | S272 | S272 engine-sheet — live LifeHistory_Log ARC-tag distribution (advancement 242 / promotion 111 bookkeeping-prefixed vs wedding 4 / retirement 6 / graduation 1 / stabilized 1 person-readable) + per-wake-pool reach 4/212 (1.9%) | Two sub-lanes (both clasp/engine, NOT wake-side): **(a)** the Advancement/Promotion write sites (`checkForPromotions.js` + `processAdvancementIntake.js`, the source per Row 23) should emit a person-readable EventText — what changed in the life — not the `"Updated to Tier N..."` tier-tracking string; **(b)** confirm engine.38 B3 (ordinary good/bad stakes) actually fires person-readable ARC-tagged Log rows = the milestone supply T1a' reads. Wake-side T1a' shipped S272 (`0110597f`, omit-design, no count fallback) — this row is the UPSTREAM continuity fix it surfaced. **S272 MEASURE-TWICE REVERSAL of sub-lane (a) — DROPPED (no clasp):** read both write sites — `checkForPromotions.js` L500-508 (`'Emerged into Tier-3 in '+neigh`) + `processAdvancementIntake.js` L476 (`'Updated to Tier '+tier+'. '+notes`) / L508 col-O (`'Promoted to Tier '+tier+' in Cycle '+cycle`). These events are **fame/visibility tier bumps driven by media UsageCount, NOT life events** — there is no underlying milestone to translate, the event IS the tier change. Rewriting their EventText serves **no live consumer**: (1) T1a' deliberately excludes Advancement/Promotion (reads only Wedding/Birth/Retirement/Graduation/Stabilized); (2) **0/215** pooled citizens carry engine-vocab tier-text in their last-5 col-O wake tail (compression folds it out before any wake reads it). So (a) is a no-benefit cosmetic rewrite + a fabrication risk (best case it invents a felt "growing recognition"; worst case a concrete milestone the engine never fired). **Sub-lane (b) is the whole row:** the continuity-supply lever is engine.38 B3 firing genuine person-readable life-event Log rows (the wedding/retirement-class supply T1a' actually reads). Row redirected to the engine.38 B3 lane. Couples to Row 24 (promotion-signal decoupling) + engine.38 B3; sibling to Row 23. Plan: [[../plans/2026-06-23-citizen-perception-immersion-layer]] §T1a refinement. Pattern: feedback_measure-twice-cascading-effects |

| 33 | **bondEngine new-bond detection has NEVER landed a bond — its citizen pool is structurally empty (engine.44 Class 3 bond diagnosis).** Live: all 6,202 bond rows (Relationship_Bonds 318 + Bond_Ledger 5,884) trace to the one-time seeder `seedRelationBondsv1.js` (origins: neighbor/random/household only; `domainTag:''` L335; literal `'cross-neighborhood'` L414 — Mike's DomainTag + cross-neighborhood observations are downstream symptoms). `runBondEngine_` UPDATE path IS live (all 318 bonds stamped LastUpdate=C100, engine-modified intensities), but `detectNewBonds_` exits at its `activeCitizens.length < 2` guard every cycle: the pool `S.cycleActiveCitizens` builds from 4 sources and every one is empty at Phase5-Bonds (godWorldEngine2 L293): (1) `S.citizenEvents` — NO producer exists anywhere in the codebase; (2) `S.storySeeds` — populated Phase 6/7, after; (3) `S.eventArcs` — generationalEventsEngine pushes arcs with `involvedCitizens: []`; (4) `S.worldEvents` — carry no citizens field (same no-POPID fact as Row 6). So festival/sports/first-friday/career/mentorship bonds — the entire calendar-bond layer with its DomainTags — has been dead since landing. citizenLookup itself is healthy (Citizen_Directory absent live → ctx.ledger fallback; UNI 112 / MED 44 / CIV 56 y-flags of 913). | done | S301 | S301 engine-sheet — live Origin distribution (neighbor 4737 / random 864 / household 283, zero bondEngine origins in 5,884 rows) + DomainTag 0-filled both tabs + LastUpdate=100 on all 318 (update path alive) + 4-source pool trace + phase-order read (Phase5-Bonds L293 before Phase5-CitizenEvents L297) | Fix is a design call (feeds engine.48 conversation-engine work): make a real producer for the pool. Cleanest: `generateCitizensEvents_` publishes its cycle's event citizens to `S.citizenEvents` (it's the natural producer and currently writes none of ctx.summary) + move Phase5-Bonds after Phase5-CitizenEvents (+Promotions) at both entry points. Alternative pool sources: LifeHistory_Log rows this cycle, or ctx.ledger sampling. DomainTag/cross-neighborhood column fixes are MEANINGLESS until the producer works — seeder rows are the only rows. Sibling: Row 23 (same "engine wired but starved" class). Plan: [[../plans/2026-07-03-sheet-walk-audit-triage]] §Class 3 findings. Pattern: feedback_measure-twice-cascading-effects |

## P2 — Architecture integrity

| # | Issue | Status | Session | Evidence | Fix pointer |
|---|-------|--------|---------|----------|-------------|
| 7 | 4 live `Math.random()` fallbacks — determinism violation. Flagged 2026-03-28. | done | S156 | `docs/engine/tech_debt_audits/2026-04-15.md` | Closed by `76a408c` (documented 4) + `af40282` (55-site sweep → `safeRand_(ctx)`) |
| 8 | 38 undocumented direct sheet writers / 197 call sites. | wip | S184 (plan); S188 (§5.6 done) | `docs/engine/tech_debt_audits/2026-04-15.md` | Plan: [[plans/2026-04-28-phase-42-writer-consolidation]] + Inventory: [[engine/PHASE_42_INVENTORY]]. Real scope post-S184: 37 files / 175 sites (citizenFameTracker.js retired -16, ±5 drift). Tiered acceptance B6 stop-point (29 files / 110 sites = 58%) vs B7 full close. Phase 1 inventory DONE S184; Phase 2 (research-build per-category decisions) next; Phase 3-5 (engine-sheet 6-8 sessions). Frame: efficiency not correctness per Path 1 closeout S156. **§5.6 phase05-ledger redesign DONE S188 (engine-sheet)** — 9 commits `0e31e66..6609c4a`, 23 cycle-path SL touchers (16 full-range writers + 2 per-row + 5+3 readers) + Phase 1 init + Phase 10 commit handler migrated to shared `ctx.ledger`. B2 (run*Engine × 4) unblocked — already converted away from direct writes by the redesign. Closure detail in PHASE_42_PATTERNS §5.6 Status + Changelog 2026-04-29 (S188). **Status post-S205:** B2 batch CLOSED S204 (8/8 sites + Row 21 §5.6 half-migration HIGH-sev fix) + S205 deferred-list triage shipped + Path B Generic_Citizens phase disabled + S210 §3.5 stale-classification fix (L662 dead-strike). All S204+S205 changes deployed via clasp S205 ("Script is already up to date" idempotency). **Open: B5-B7 batches (per PHASE_42_PATTERNS §3 routing — engine core / culturalLedger / intake heavies).** **§3.5 cluster CLOSED S229 — UNGATED** — all 11 §3.5 sites resolved across batches 1-3 (`7b68695` + `5d25656` + `a1eb569`); 2 clasp deploys live; C94 cycle has run + post-deploy state verified stable across C93/C94. runCivicElections + bondEngine + generateChicagoCitizens all on intent-based writers (or disabled in Chicago's case). Chicago Path B disable reduces cycle-path SL toucher surface area; B5/B6/B7 are now the primary remaining frontier. **S268 B7 confirm-and-close analysis (engine-sheet, read-only — REVERSAL of "near-closeable" hypothesis):** caller-graph verified all B7 files. RESOLVED as operator-fired carve-outs (no migration): parseMediaRoomMarkdown (S257), healthCauseIntake (S236), **continuityNotesParser (reconciled S268 — was stale in PHASE_42_PATTERNS §3 table, now struck; engine.md had it carve-out since S237)**. **S269: B7 ANALYTICALLY RESOLVED** (both files; local commits, clasp-gated). (1) `processAdvancementIntake.js` MIGRATED — 8/10 sites carve-outs, 2 genuine cycle-path flag-writes routed through `markUsageProcessed_` ctx-branch (cycle→`queueCellIntent_` atomic-at-Phase-10; operator `ctx.manualRun:true`→direct). (2) `mediaRoomIntake.js` FULL CARVE-OUT — no migration possible: cycle entry `processMediaIntake_` runs at **Phase 11, after the sole intent executor `Phase10-ExecuteIntents`** (L474/L1794), so a queued intent would silently drop (general structural rule + entry now in `.claude/rules/engine.md`). Reverses the S268 "designed split-helper" framing (it never checked phase-vs-executor ordering). **Row stays `wip` — closes after processAdvancementIntake clasp-deploys + smoke-tests live** (cycle-path behavior change; stub proved routing only). See the two S269 changelog entries below. Pattern: feedback_measure-twice-cascading-effects. **S270 DEPLOY-READINESS (engine-sheet, both commits confirmed on origin/main):** measure-twice phase-position check PASSED — `Phase5-Advancement` (`godWorldEngine2.js` L295 production / L1638 cycle) runs BEFORE `Phase10-ExecuteIntents` (L474 / L1794), so the cycle-path `queueCellIntent_('Citizen_Media_Usage')` lands in the Phase 10 batch (NOT the mediaRoomIntake silent-drop trap). Smoke (run-after-deploy): a Citizen_Media_Usage row marked processed in the CYCLE path shows `Processed='Y'` + matching UsageCount bump in the same commit; operator path still direct. CAVEAT: smoke is a no-op if no unprocessed media-usage rows exist at cycle time — confirm activity present before declaring smoke valid. **Deploy gated on the deliberate C100 clasp window** (rides tier1EssenceEvents.js + 2 manual deletes). |
| 9 | 78 orphaned `ctx.summary` / `S.` writes. | done | S156 (reclassified) | `docs/engine/tech_debt_audits/2026-04-15.md` + `2026-04-27.md` §3 | Not a bug — `utilities/exportCycleArtifacts.js` serializes the entire `ctx.summary` every cycle (`JSON.parse(JSON.stringify(S))` → Drive `cycle-<N>-summary.json`), and `phase10-persistence/buildCyclePacket.js` consumes most of it for the cycle packet. Every orphan field IS read, just by bulk serialization not per-field reference. Audit methodology codified in `/tech-debt-audit` skill so future passes don't rediscover it. |
| 12 | `utilities/utilityFunctions.js:29,36,53` — `pickRandom_`, `pickRandomSet_`, `maybePick_` use `Math.random` on cycle path (reference-pass). Callers: `godWorldEngine2.js`, `buildCityEvents.js`, `buildNightLife.js`, `buildEveningMedia.js`, `buildEveningFood.js`, `buildEveningFamous.js`, `sportsStreaming.js`, `generateChicagoCitizensv1.js`. Shipped through every prior cycle. Missed by `af40282` sweep (invocation-only grep). | done | S180 | S166 /pre-mortem C92 | Closed S180 — three helpers take `rng` parameter and throw if missing (matches S156 phase05 generateChicagoCitizen_ pattern). 8 caller sites updated to pass `rng`. Side-fix: `getChicagoOccupation_` was un-seeded (silent bug); now threads `rng`. |
| 13 | SCHEMA_HEADERS full alignment diff never run — 2026-04-15 export happened two days *before* the Phase 38/39/40 write-intent changes in later S147/S148/S156 commits. Unknown whether any new write-intent columns drifted from sheet headers. | done | S181 | S166 /pre-mortem C92 §4 | Closed S181 — schema regenerated via new `scripts/regenSchemaHeaders.js` (Apps Script API-executable path unavailable, fell back to local Node + service account per engine.md rule). Live-sheet diff harmless (2 new tabs, 1 column-shrink, all without engine references). Code-side diff surfaced 3 classes of real drift now tracked as items 15, 16 + Class 3 doc cleanup. See `docs/engine/tech_debt_audits/2026-04-27.md` §6. |
| 18 | §5.6 cohort-C migration debt — `phase05-citizens/generationalWealthEngine.js` (5 direct `setValues` at lines 222, 320, 494, 533, 611) + `phase05-citizens/educationCareerEngine.js` (4 direct `getDataRange().getValues()` reads + writes) bypass `ctx.ledger`. Under §5.6 commit semantics, both engines' mid-cycle SL writes get clobbered by Phase 10's consolidated `ctx.ledger.rows` replace. Not in §5.6 spec's 18-toucher list (mags `hQE4rREEWBpS9aS1g3mQ3M`); spec-A1 enumeration missed them. | done | S200 | S191 /pre-mortem C93 + smoke-test verification + S199 quant + S200 advisor-prompted re-audit | **CLOSED S200** — measure-twice pass before commit #1 surfaced that the §5.6.6 categorical orphan-clear (PHASE_42_PATTERNS:489) was wrong about 4 of 6 engines, not 2: `householdFormationEngine` (line 229, before Wealth) + `migrationTrackingEngine` (line 233, after Education) ALSO live, ALSO clobbered every cycle since §5.6 went live S188. Mike's call was Path (b): fix all 4 in one batch rather than ship Wealth+Education narrow + open follow-up row. **6-commit batch:** `a829c7f` householdFormationEngine v1.2 (SL reads + HouseholdId setValue → ctx.ledger), `ed25ea8` generationalWealthEngine v2.1 (8 SL touchpoints, sigs unified to (ctx,…)), `7f95521` educationCareerEngine v2.1 (3 read+write pairs + side fix for gated-write Mobility persistence bug), `93cd3a4` migrationTrackingEngine v1.1 (2 read+write pairs + 2 read-only consumers), `c589b0a` godWorldEngine2.js Session-30 stale rationale ×2 occurrences replaced with §5.6-aware note + commit pointers, plus this ENGINE_REPAIR + PHASE_42_PATTERNS A7 amendment commit. Helper readers for Neighborhood_Map / Household_Ledger / Family_Relationships stay direct (own-tracking sheets, exempt per engine.md). Spec scope updated 18 → 22 SL writers + 5 readers all routing through ctx.ledger. **Audit lesson recorded in PHASE_42_PATTERNS A7:** future cycle-path orphan checks must grep BOTH `<file-name>` AND `process<thing>_` (Apps Script flat-namespace dispatches by function name, not file name). Smoke-test pending C94 (operator-gated). |
| 19 | Function-name collisions in flat Apps Script namespace — multiple top-level `function name_` definitions across files cause silent overrides via alphabetical-load-order. §5.6 spec A4 flagged `generateCitizensEvents_` (closed S185 prerequisite-delete `1a77e54`); S191 smoke-test surfaced `loadActiveStorylines_` (3 defs broke storylineWeaving + storylineHealth) and `getCurrentCycle_` (3 defs broke eventArcEngine). | done | S199 | C93 cycle log + S199 systematic audit | **CLOSED S199** — `scripts/auditFunctionCollisions.js` `eb3cbcb` built as the systematic detector (881 unique top-level fn names across 12 clasp dirs; full alphabetical-load-order writeup). C93 baseline: 16 collisions. Cleared 16 → 0 across Phase A + B.1-B.6 (11 commits): `52f0026` Phase A (delete dead phase04 worldEventsLedger.js v2.2), `27c7d4c` Phase B.1 (delete arcLifecycleEngine.js 523 LOC + 10 dead fns), `5f14ecf` B.2 (rename seedRelationBondsv1 v1.4 — createBond_/generateBondId_/findColumnIndex_), `cb53e0c` + `f8a11af` B.3 (rename generationalEventsEngine v2.5 createBond_ → createGenerationalBond_), `6d7afc4` B.4 (dedup parseJSON + hashSeed_ + getMonthFromCycleInternal_), `1540311` B.5 (dedup mulberry32_ ×5 + citizenInActiveArc_ + getCitizenBonds_ + getCurrentCycleFromConfig_), `2cbe045` B.6 (delete rollbackToCycle80.js + dedup getCitizenBondsFromStorage_ + getCombinedEventBoost_). Cascading-effects review per Mike's "measure twice cut once" caught one Phase A reversal (originally targeted recordWorldEventsv25.js as dead; empirical Engine_Errors trace + caller graph showed it was the live legacy ledger writer; correct target was the opposite file). Bond infrastructure remains non-functional separate item (no Relationship_Bonds tab → bond paths short-circuit) — collision sweep was hygiene that protects future restoration. Future drift detection: `/tech-debt-audit` §8 wired S201 (commit `f053587`); auditor included in periodic 3-5 session cadence. |
| 20 | Household + family structure / youth gap. **PREMISE REFRAMED S243 (Mike-direct):** the ~858 tracked citizens are a representative SAMPLE of ~375,985 simulated Oaklanders, not the population. So "426 married with no partner" + "526 singleton households" are NOT bugs — spouses/kids are off-sample (Tier 5); demographic flags are properties of a representative voice. Live audit confirmed: 543 of 547 adults with NumChildren>0 have zero tracked children (off-sample), only 4 citizens have tracked kids (Corliss + Dillon Tier-1 canonical families). The real gap is the youth cohort (0.3% under-18 vs ~21% Oakland) — needs a functional seed so the engine can fire. `householdFormationEngine` birth/marriage/divorce all bare stubs; both household engines wired into cycle but no-op. | wip | S201→S243 | S243 ledger+engine audit (engine-sheet) — family-column shapes + Household_Ledger 529 rows + runYouthEngine/householdFormationEngine read contract | Plan REWRITTEN: [[engine/LEDGER_REPAIR_HOUSEHOLDS]] — Representative Sample model. Backfill pairing superseded (`scripts/pairMarriedCitizens.js` built S243 then shelved — pre-wiring couples = make-believe). New build: Phase 1 functional youth seed (~30–60, dual-write SL + Household_Ledger.Members, age-weight 5–17, invariant: tracked child needs ≥1 tracked adult in household) → Phase 2 engine life-event simulation (births/marriage/aging by calibrated random chance at real rates; **gated on Row 22 Status-case fix**) → Phase 3 publication-driven family materialization. Steward authority granted S243. Phase 1 building S243. |
| 21 | §5.6 half-migration — `phase05-citizens/runNeighborhoodEngine.js` claimed §5.6 compliance via `ctx.ledger.dirty = true` (line 529 pre-fix) but the read-side at lines 39-48 still pulled SL via `ledger.getDataRange().getValues()` and built a local `rows = values.slice(1)` array independent of `ctx.ledger.rows`. Mutations on the local copy (neighborhood assignments at line 358 + LifeHistory + LastUpdated stamps) were silently dropped at Phase 10 commit, which wrote the unmutated `ctx.ledger.rows` from Phase 1 init. Adjacent to but distinct from Row 18 (cohort-C debt) — different file, same bug class. S188 commit `4b602ae` listed the file in its 4-engine batch but the diff shows only the commit-side change landed; read-side migration was missed. | done | S204 | S204 B2/2 measure-twice — Phase A reversal triggered when prepping Item 2 LifeHistory_Log mechanical migration; READ surfaced direct getDataRange + local-slice mutation pattern; cross-checked the other 3 S188-batch files (runEducationEngine, runCareerEngine, runCivicRoleEngine) — all clean (alias `rows = ctx.ledger.rows` so `rows[r] = row` writes through). Only runNeighborhoodEngine had the half-migration. | **CLOSED S204** — fd9758e v2.5 read-side fix (HIGH severity standalone commit; ~7 cycles silent data loss C87+ closed) + 6aa4a3e B2/2 mechanical LifeHistory_Log → queueAppendIntent_. **Audit lesson generalization (PHASE_42_PATTERNS A7 amend pending):** files claimed migrated by a batch commit can carry half-migrations even when version stamps + comment markers say otherwise. Future audits must verify BOTH read-side (`var rows = ctx.ledger.rows` aliasing vs direct `getDataRange`) AND commit-side (`ctx.ledger.dirty = true` + no orphan setValues), per-file. The S185 §5.6.6 audit caught file-name vs `process*_`-name dispatch errors; this catch is finer-grained: same-file mid-migration drift. |

| 22 | Status-case mismatch in `householdFormationEngine.loadCitizens_` — filters `citizen.status === 'active'` (lowercase, strict equality) but the Simulation_Ledger `Status` enum is `"Active"` (capitalized; 827 of 858 rows). The loader therefore returns ~zero real citizens. Currently **latent/masked** because `generateBirths_`/`processMarriages_`/`processDivorces_` are bare stubs that no-op regardless. Becomes a live correctness bug the moment Phase 2 (LEDGER_REPAIR_HOUSEHOLDS) implements them — births would operate on an empty set. `runYouthEngine.getNamedYouth_` case-folds correctly (`String(row[iStatus]).toLowerCase()`), so the inconsistency is the tell. | done | S325 | S243 engine audit — `phase05-citizens/householdFormationEngine.js:250,261` vs live Status enum | Closed S325 (verify-only) — the live comparison is already case-normalized: `householdFormationEngine.js` L447 `String(citizen.status).toLowerCase() === 'active'`; FORMS_STATUS gate at L325 also lowercases. Additionally `loadCitizens_`'s only consumers are the generateBirths_/processMarriages_/processDivorces_ TODO stubs (S325 sweep) — inert path. No code change needed. |

## P3 — Specific bugs

| # | Issue | Status | Session | Evidence | Fix pointer |
|---|-------|--------|---------|----------|-------------|
| 10 | Temescal initiative stuck 88 cycles — crude date-string parse in Phase 38.1. | done | S181 | S148 audit; S181 verification via live `Initiative_Tracker` inspect | Closed S181 — `scripts/engine-auditor/detectStuckInitiatives.js:49-51` cold-start fallback was passing `row.LastUpdated` (a date-string `"4/21/2026"`) to `parseCycleHint(/C?(\d+)/i)`, which matched the month digit `4` and computed `cyclesInState = 92 − 4 = 88` for every initiative. Temescal got named because it was narratively load-bearing; bug affected all 6 active initiatives uniformly. Replaced with `parseCycleHint(row.VoteCycle)` — VoteCycle is an actual cycle number column (78/80/82/etc.), not a timestamp. Smoke-tested with C92 fixture: INIT-001=14, INIT-005 Temescal=12, INIT-007 (empty VoteCycle, announced phase) correctly filtered out. NextActionCycle dropped from the fallback — it's forward-looking and the `<= cycle` guard rejected it anyway. |
| 11 | Pipeline never gated production — /sift, Rhea, desk verification, Phase 39 reviewer lanes never ran on real editions. E89/E90/E91 hand-assembled. | done | S196 | C93 cycle artifacts (S194 sift + S195 write-edition + S196 edition-print) | Closed S196 — pipeline gates exercised end-to-end on E93. /sift ran S194 with 14-entry gap log + 9 reporter briefs. 8 desk reporters wrote real articles S195. Rhea verified S195 (G-W25: 14:32 / 24 tool uses). Phase 39 reviewer chain ran S195 — Mara A-, Final Arbiter weighted 0.898 PROCEED-WITH-NOTES, capability gate fired and was editor-overridden on highest-severity-ailment heuristic. /post-publish + /edition-print both ran with gap logs. The 6 C93 sidecar gap logs are post-closure tuning artifacts of pipeline-as-real-thing, not pipeline-doesn't-exist. Row framing was correct as of S148 but obsolete by C93. |
| 14 | Non-canonical neighborhood strings used as first-class (not mapped): `Eastlake`, `San Antonio`, `Glenview`, `Ivy Hill`, `Coliseum`, `Elmhurst`. Hits in `phase03-population/updateNeighborhoodDemographics.js:234`, `phase05-citizens/generationalWealthEngine.js:90`, `phase05-citizens/bondEngine.js:112-113`, `phase05-citizens/updateCivicApprovalRatings.js:110-115`, `phase08-v3-chicago/v3NeighborhoodWriter.js:38,188-192,333`. Either the skill's 17-canonical list is stale, or these paths write to ghost neighborhoods. `phase05-citizens/checkForPromotions.js:191-199` has a mapping table that normalizes some → canonical; other sites skip it. | done | S180 | S166 /pre-mortem C92 §5 | Closed S180 — root cause was the `/pre-mortem` skill's hardcoded 17-list (Downtown / Piedmont / Montclair / etc.) that didn't match either Simulation_Ledger (canon-12 where citizens live) OR Neighborhood_Map (17 fine-grained where world-state lives). Engine code itself is correct under parent-child ontology (canon-12 parents ← fine-grained children, mapping at checkForPromotions.js:190-209). Fix: SKILL.md §5 rewritten to recognize both layers as canonical; child names like Eastlake/San Antonio no longer flag as non-canonical. Folds in the engine-review C92 baseline-brief finding (same root cause). |
| 16 | `Advancement_Intake` / `Advancement_Intake1` tabs both missing from live spreadsheet — `phase05-citizens/processAdvancementIntake.js:260-262` falls back from `Advancement_Intake1` → `Advancement_Intake` → silent return `if (!intakeSheet) return results;`. Also referenced by `phase07-evening-media/mediaRoomIntake.js:537-538` (same fallback chain). Pipeline non-functional until at least one tab exists. Different from `Continuity_Intake` / `Election_Log` which lazy-create themselves; advancement intake silently drops. | done | S181 | S181 audit `docs/engine/tech_debt_audits/2026-04-27.md` §6 Class 1 | Closed S181 — lazy-create added on the writer side at `phase07-evening-media/mediaRoomIntake.js:539-544` (matches `continuityNotesParser.js:73` pattern). Inserts `Advancement_Intake1` with 10-col header (`First, Middle, Last, RoleType, Tier, ClockMode, CIV, MED, UNI, Notes`) when neither tab exists. Reader path (`processAdvancementIntake.js:262`) keeps its silent-return — correct no-op when nothing has been routed yet. |
| 17 | Post-S94 RoleType="Citizen" drift — literal "Citizen" sentinel landed on the live ledger from the intake writer's fallback path. Same anti-pattern S94 fixed (399 cases). Root cause: `phase05-citizens/processAdvancementIntake.js:296` defaulted to `'Citizen'` when intake row's RoleType cell was empty. | done | S184 | S181 audit `docs/engine/LEDGER_AUDIT.md` §"Current State"; S184 backfill verified 0 sentinels live | Closed S184 — Path B (demographic-voice fallback) chosen. Writer patched: `phase05-citizens/processAdvancementIntake.js` now declares a 65-role `DEMOGRAPHIC_VOICE_ROLES` pool drawn from the S94 cohort + LEDGER_AUDIT §7 15-category distribution; new citizens with empty intake RoleType receive a deterministic per-(first,last,popId) draw via djb2 string hash (Apps Script-safe, no Math.random). Existing-citizen branch preserved — empty `rawRole` no longer overwrites a real ledger value (truthy guard at line ~393 unchanged). Smoke test: 1000-draw distribution across full 65-role pool (max 30, min 5 per role; no dead entries). Backfill: of the 4 originally flagged (POP-00794 / 00795 / 00798 / 00801), only POP-00798 (Grace Yamamoto, Adams Point) and POP-00801 (Maurice Franklin, Rockridge) still carried the sentinel by S184; both patched via `lib/sheets.js` direct write (Independent Bookstore Owner / Drone Fleet Coordinator respectively, picker-derived). Live verification: 0 rows with `RoleType='Citizen'` sentinel. **Followup (research-build):** ROLLOUT entry — `RoleType is preferred upon intake`. Upstream intake writers (mediaRoomIntake.js + future intake paths) should compute RoleType BEFORE routing; the demographic-voice fallback is a safety net, not a replacement for real role assignment. EducationLevel + Gender empty on the same 4 rows is a separate gap (intake path doesn't backfill demographics) — not addressed here. |
| 26 | **wd-cards-daemon infinite retry storm — 433 `CardRebuildFailure` rows over a week (94% of all Engine_Errors), wd-citizens MCP cards stale since ~May 28.** The engine.27 Phase A card-invalidation daemon (`scripts/wdCardsDaemon.js`) dispatched `buildCitizenCards.js --apply --popid <FULL changed-id list>` in one invocation per tick. A large batch trips the Supermemory rate-limit (returned as HTTP 401 — claude-mem obs 25868) → `errors++` → buildCitizenCards' T6 gate exits non-zero. `tick()` committed new row-hashes ONLY on full success, so any failure left the ENTIRE changed set uncommitted → next tick re-detected the same rows → re-fired the same oversized batch → forever. State never advanced past the first failed dispatch; backlog = the whole 904-citizen population. Severity WARN, so it stayed silent. | done | S252 | S252 engine-sheet post-deploy `Engine_Errors` read (433/459 rows = wd-cards-daemon, earliest 2026-05-28) + read-only repro (single + 80-id dry-runs clean → failure isolated to the `--apply` write path) + daemon source read (all-or-nothing commit in `tick`) | **CLOSED S252** (`3924765`, Node-only, no clasp). `dispatchProjection` now chunks the id list (`CHUNK_SIZE`=25, 4s inter-chunk sleep), each chunk its own `--apply` invocation, returns `{ok, succeeded:[ids]}`; `tick()` commits per-success (merges only rebuilt ids' hashes; failed ids keep old hash + retry next tick) → drains the backlog instead of all-or-nothing re-firing. Env overrides: `WD_CARDS_CHUNK_SIZE` / `WD_CARDS_CHUNK_SLEEP_MS`. Caller-graph: `dispatchProjection` has one caller (`tick`). **Verified live:** pm2 restart (Mike go) → backlog drained 904→0 within ~36 min (state file = 904 committed hashes, `lastBuildAt` 08:40Z); 16 small-chunk transient rate-limit failures during the drain (id-counts ≤25, retried clean); steady state ticks #53–60 = `rebuilds:0 \| failures:0`. Pattern: `feedback_measure-twice-cascading-effects`. |
| 29 | **Ingest duplicate-matcher produced a FALSE-POSITIVE merge across unrelated citizens (C97 triage ES-2 step 3 regression).** The honorific-strip + last-name-anchored dedup in `ingestPublishedEntities.js resolveCitizens` (`6033c26`) flagged appended POP-01021 "Dr. Tran-Muñoz" as a duplicate of POP-00781, claimed to be "Dr. Vanessa Tran-Muñoz / OARI Director" — and a destructive merge (POP-01021→POP-00781 + delete) was staged HELD-for-go. **Ledger-verified false read (Mike S259):** POP-01021 (role Community Organizer, Downtown, status `pending`) and POP-00781 are different citizens, AND POP-00781 is `queryLedger.js citizen POP-00781` → **"Vanessa Treary," a Tier-4 barista in Lake Merritt** (income $30,732, 2 kids) — not the OARI Director at all. The matcher anchored on a surname the target row does not even carry (Treary ≠ Tran-Muñoz). The merge would have collapsed two unrelated citizens into a barista's row. **MERGE KILLED — do not run.** Matcher needs an inverse guard: never propose a merge unless the target row's name actually shares the anchored surname AND ≥1 corroborating field agrees (neighborhood / role / tier); honorific-stripping must not let a title-only name auto-match. | done | S259 | S259 research-build + engine-sheet — `queryLedger.js citizen POP-01021` + `POP-00781` (live ledger rows; POP-00781=barista Treary) + live `byLast` probe (only `Tran-Muñoz` surname row = POP-01021's own malformed self-row) | **CLOSED S259 (engine-sheet).** `resolveCitizens` title-only-name branch rewritten — a bare honorific+surname (no first name) NEVER auto-matches and NEVER auto-appends, even on a UNIQUE surname; routes to `ambiguous` carrying the surname candidate POP-IDs for brief-stage resolution. Test `ingestPublishedEntities.honorific-match.test.js` updated to the corrected contract → 6/6. **Measure-twice correction to the row's premise:** the committed matcher (`6033c26`) never produced — and *cannot* produce — the POP-01021→POP-00781 merge. It anchors via `byLast`; POP-00781's surname is `Treary` (≠ `Tran-Muñoz`), and the only `Tran-Muñoz` surname row is POP-01021's own malformed self-row (First=`Dr.` / Last=`Tran-Muñoz`), so the live code self-anchors at worst, never cross-links to Treary. The cross-surname merge was a **manual** proposal written into the ES-2 step-3 plan (author mis-recorded the OARI Director's POP-ID as POP-00781). The code's real latent bug was the narrower unique-surname auto-resolve — now removed. Surfaced via the C97 triage [[../plans/2026-06-13-c97-gap-log-triage]] ES-2 reframe (G-P-C97-1). Pattern: feedback_measure-twice-cascading-effects. |
| 30 | **Citizen ingest/generation mints malformed entries — honorific captured as the NAME + title↔education incoherence (surfaced via Row 29 false-positive).** POP-01021's `name` is literally **"Dr. Tran-Muñoz"** — a title glued to a surname with no first name; a complete sim entry can never have a title-only name. Same row: `educationLevel` = **`hs-diploma`** on a "Dr."-titled entity — no coherence check reconciles an honorific/role against education level. The entry is also a half-formed stub (`status='pending'`, age/income/wealthLevel/careerStage/traitProfile all `null`). Two distinct gaps: (a) ingest must reject/repair a name that is a bare honorific+surname (no first name) at append time — don't let "Dr." become the name; (b) a coherence validator should flag title↔educationLevel mismatches (Dr. + hs-diploma) before an entity is written. | done | S259 | S259 research-build + engine-sheet — `queryLedger.js citizen POP-01021` (was: name "Dr. Tran-Muñoz", role Community Organizer, status pending, educationLevel hs-diploma, Gender male, all demographic fields null) + live `byLast` probe + 4-director pattern read (Webb/Soria-Dominguez/Chen-Ramirez/Ramos = Tier-3 CIVIC) | **(a) DONE S259 (engine-sheet, code).** `isMalformedCitizenName(first,last)` backstop in `ingestPublishedEntities.js` rejects honorific-as-first + missing-first/last candidates BEFORE append + report (so neither mints a POP-01021); surfaced in console + report `citizens.malformed[]`. With the Row 29 honorific-strip upstream this rarely fires — it is the last-line guard. Unit test → 7/7. **(b) RESOLVED-BY-CONSTRUCTION:** honorifics are now stripped and never persisted as a name, so the specific "Dr. + hs-diploma" title↔education incoherence cannot arise from this ingest path; a broad role↔education validator deliberately NOT added (would false-flag legit prosperity-Oakland cases — self-made "Director" w/ hs-diploma). **(c) POP-01021 disposition = REPAIRED S259 (Mike directive: "the sheet is the truth and she must reside on them").** POP-01021 rewritten in-place to the canonical OARI Director, matching the 4-director pattern (Tier-3 / ClockMode CIVIC / CIV=yes / Status Active): First=Vanessa, Last=Tran-Muñoz, RoleType="OARI Program Director", Neighborhood=Rockridge, BirthYear=2002 (age 39), Gender=female, EducationLevel=doctorate (DrPH), CareerStage=senior, OrginCity="San Jose", Income=138000, + CitizenBio; MaritalStatus=single / NumChildren=0 retained. All 14 fields readback-verified live via `scripts/`-style `updateRowFields` (lib/sheets.js, direct write per engine.md no-maintenance-script rule). Canon source: `.claude/agents/civic-project-oari/IDENTITY.md` (Dr. Vanessa Tran-Muñoz, age 39, Rockridge, DrPH/MSW, single, San Jose-born). The matcher now resolves "Dr. Vanessa Tran-Muñoz" → POP-01021 cleanly (full-name path) and a truncated "Dr. Tran-Muñoz" → ambiguous-with-candidate-POP-01021 (Row 29). Surfaced via [[../plans/2026-06-13-c97-gap-log-triage]] ES-2 reframe. |
| 27 | **POP-00331 (Isabelle Louis) died twice — DIAGNOSIS CORRECTED same-session: zombie row from lost Status persistence, NOT a missing gate.** LifeHistory carries two `[Death]` milestones (2026-01-30 "died after a sudden illness", literal-`\n` old-writer style + 2026-06-01 same text, C96 run). Initial read ("generator doesn't gate on deceased") was WRONG — `generationalEventsEngine.js:261` skips `status === "deceased"` and 12 other engine files filter it too. Real sequence: the Jan-30 death wrote the LifeHistory line but the `Status='deceased'` flip never persisted (Row 21 clobber era — runNeighborhoodEngine half-migration silently overwrote SL writes C87+, fixed S204), leaving her Active-with-a-death-entry; C96's checkDeath_ legitimately re-rolled her; second death flipped Status properly. She is now inert (deceased gate holds). One-time data scar from an already-fixed bug, not a live hole. | done | S255 | S255 engine.30 build — milestone extraction across 904 SL rows surfaced the 2× `[Death]`; code read of checkDeath_ + L261 gate + 13-file deceased-filter grep corrected the first diagnosis; no other duplicate-death rows exist | No engine fix needed (gate exists; clobber source closed S204). Residue = the false Jan-30 entry on her LifeHistory — Mike's call: clean it or let the .31 deploy clear col O anyway (engine.30 card has both dates captured). Engine.29 still owns the separate Row 25 age-gate redesign (85+ escalating, ClockMode authority matrix). Card layer renders data as-is by design. |
| 31 | **LifeHistory_Archive early-cycle rows are column-shifted by one (Name slot holds the EventTag).** Cycle 1–5 rows (timestamp 11/23/2025, the first build) were written under an older 6-column schema with no `Name` column, then merged into the current 7-col archive (`Timestamp, POPID, Name, EventTag, EventText, Neighborhood, Cycle`) without inserting a blank Name — so every field after POPID shifted one slot left. Example POP-00036 (Marcus Osei): `Name="Health - Major"` (an EventTag value), `EventTag="Serious health condition diagnosed."` (the EventText), `EventText="Engine"` (the source). Surfaced during governance.42 ES-2 — the ~163 Archive "name drift" hits are a MIX of (a) genuine pre-rename names (same class as the Log, now reconciled) and (b) these structurally-shifted rows. A name-reconcile is WRONG for (b): it would put the right name in Name but leave EventTag/EventText still shifted. Needs a structural detector (eventish value in Name → shift row right one, insert blank Name) keyed to the early-cycle batch. Low-traffic/old; not blocking. POPID column itself is correct (not a full-row shift). | done | S267→S268 | S267 governance.42 ES-2 — live read of LifeHistory_Archive: headers + full-row dump of eventish-Name rows (POP-00036/00131/00016/00132), all Cycle 1–5 / 11-23-2025 timestamps; **S268 full live characterization corrected the scope (61 rows, cycles 1–22, not "Cycle 1–5")** | Separate from the Log name-drift (which is clean + reconciled S267). Fix = structural realign of the early-cycle shifted rows, its own pass; pairs with engine.34 ledger-integrity family. Do NOT bundle into a name-reconcile. Pattern: feedback_measure-twice-cascading-effects (the sweep distinguished name-drift from column-shift before any write). **CLOSED S268 (engine-sheet, Mike-go) — by DELETION, NOT the realign this row originally prescribed (measure-twice REVERSAL).** Full live characterization reversed the documented "shift row right one, insert blank Name" fix for three reasons: (1) scope was 61 rows across cycles 1–22 (not "Cycle 1–5"); (2) the malformed shape is `Name=<EventTag>, EventTag=<EventText>, EventText="Engine", Neighborhood="", Cycle=N` — **"Engine" is a source sentinel with NO home column in the 7-col schema**, so a realign can't place it; (3) ~17 of the 61 already have a clean twin elsewhere in the archive → realign would create duplicates, and the birth rows carry cycle-number discrepancies (malformed "Cycle 1" vs clean twin "Cycle 5") → rewrite would near-dupe. All 14 distinct affected POPIDs are live (several canon: Osei council / Crane council / Dane DA / Montez chief / Graye reporter), so the corruption was polluting real citizens' card milestones. **Fix = deleted all 61 via `deleteDimension` (descending, single batchUpdate, TOCTOU-guarded re-derive asserting count===61).** Read-back verified: 5008→4947 rows (Δ61), 0 residual, clean twins survived (e.g. POP-00036 EngineEvent/Chinatown health row). Signature: `EventText-slot=="Engine" && Neighborhood==""` (corroborated: all carry a Cycle, all Name-slots are event-types, zero person-names). Snapshot of all 61 deleted rows: `output/lifehistory_archive_row31_deleted_S268.json` (reversible). Sole-record early micro-events (cycles 11–22) lost with deletion — accepted (low-value ambient history; canonical survivors + recent rich history intact). Detector caught its OWN false-exclusion bug mid-pass (a name-regex treated "Life Event" as a person name → would have deleted only 17, left 44) — caught precisely because identify was separated from delete (S249 verify-before-write). |

---

## Log

- 2026-07-11 S312 — Row 33 FOLLOW-ON defect found+fixed same session (`2d7aabc4`): the 6 landed bonds carried NAMES in CitizenA/B (bondEngine name-keyed internally; every POPID-keyed consumer blind to them). Data repaired prod+sandbox (0 name rows remain); `normalizeBondCitizenId_` POPID-canonicalizes at makeBond_ + the persist replace-write; dual-shape membership map keeps the v2.6 intensity fix alive; buildDeskPackets name-filter fixed (was matching ONLY the 6 broken rows). Sandbox byte-OK; prod rides C102-smoke batch.
(Brief one-line entries when a row changes status. Date + session + item # + what moved.)

- 2026-07-06 S301 — Row 33 OPENED `todo` (engine-sheet). bondEngine detectNewBonds_ starved by empty citizen pool since landing; diagnosis complete, fix is a design call (producer + phase reorder). Surfaced via engine.44 Class 3.
- 2026-07-07 S301 — Row 33 CLOSED: C122 verify PASS — bondPool=858 resolved, 6 engine-origin bonds persisted (neighborhood/first_friday/sports_season) w/ DomainTags; LifeHistory names 98% (collision fix); shuffle rider `3646b89f` fixes first-citizen bond-budget monopoly (C122: all 6 were Brianna Lee's). Full batch deployed prod + sandbox, byte-OK. Prod smoke: next prod cycle (C101).
- 2026-07-20 S327 — **Row 24 BUILT + bench-proven; production deploy HELD (own window).** (a) careerStageClass_ read-normalization unfreezes advancement across the drifted vocab (S327 live quant: 10 ENTRY-class + 64 MID-class rows were exact-string-frozen); writes stay canonical so vocabulary heals forward. (b) stampPromotion_ — the owning engine (educationCareerEngine) stamps [Promotion] LifeHistory + Log intent with person-readable text at real advancement. (c) phase04 hollow promotion dice RETIRED (commented, reversible; checkPromotion_/triggerPromotionCascade_ uncalled). Bench 0717 C117 (@31): 6 real promotions (LastPromotionCycle=117), 0 errors — the pent-up release is a 5-15%/cycle trickle, not a coverage flood. Commit 128676ea. PROD PUSH HELD until Mike's C102 clears engine.70/71 (S250 attribution — three rate-changing classes must not share one canonical fire). Row stays `wip` until prod deploy + canon smoke. Rows 22 (closed S325) + 25 (observation-gated on canon demotion fire) audited same session — the S268 'blocked trio' tag is now fully resolved.
- 2026-07-20 S327 — **Row 28 CLOSED `done` (engine.71 SHIPPED).** generateCrisisBuckets v3.0: detection replaced invention — ≥2 independent real per-hood channels converging (sentiment/retail/crime z+floor, hospital clusters, engine.70 weather/transit, defensive migration/housing) → crisis arc with citizens attached at detection; mean-reversion lifecycle on previousCycleState carry; dice/pools/cooldowns/MAX_NEW deleted; 9 S.eventArcs consumers untouched. Groundhog 0717 C114-C116: bar-scale fix caught live (crime/housing → relative-AND-absolute), full lifecycle onset→decline→resolved proven, 0 errors ×3 fires. Production pushed. The C97 buildDeskPackets arcs=[] pull stays (deskPackets superseded by contract seeds anyway). Plan: [[../plans/2026-07-20-crisis-detection-rebuild]]. Commits b21f01f0/a06dace9/6e0e4051.
- 2026-07-20 S327 — Row 28 status audit (engine-sheet, doc-only). G-RC5 (the router's decay-pole mis-attribution, tracked under folded engine.35) CLOSED three ways: S265 fix `85553ea8` (decay test 11/11 re-run S327) + `routePatternSeeds.js` retired as seed path (Mike S296 standing rule 1, zero skill/ops callers) + live producer `buildContractSeeds_` sign-aware by construction. engine.35 Phase 5 superseded-purpose-delivered (sift v2.3 deck-enrichment consumption, S305 pipeline.42; detail in plan changelog). **Row 28 rebuild is now engine.35's sole live remainder and its S270 gate has CLEARED** — chaos-cars observed live C100–C101 + bench C108–C113 (per-hood Sentiment/CrimeIndex deltas + Chaos_Cars tab shape known). No design doc yet; ultracode design-panel candidate; design start awaits Mike's go. Row stays `wip` (C97 pull holds).
- 2026-07-11 S311 — Row 33 PROD SMOKE PASS (C101): 6 engine-origin bonds live in Relationship_Bonds — 5 first_friday (DomainTag CULTURE, Lake Merritt/Rockridge) + 1 career_overlap (CIV), all CycleCreated=101, notes populated. Spread across 2 citizens (Babe Bono, Mara Vance) — shuffle rider confirmed working in prod, no first-citizen monopoly. Row fully verified end-to-end.
- 2026-07-07 S301 — Row 33 C121 verify: 0 bonds — diagnosis was one level shallow. Pool was POPULATED but POPID-shaped (generateCitizensEvents_ L235 publishes popIds; metadata is name-keyed → all gates dead). Fix `c18a49d9`: ensureBondEngineData_ v2.6 resolves into bond-local ctx._bondActivePool, shared array untouched; regression test replicates exact C121 state → 4 bonds land. Sandbox redeployed byte-OK; C122 is the verify.
- 2026-07-07 S301 — Row 33 BUILT (`b7b702c0`, sandbox-deployed): generateCitizensEvents_ publishes S.citizenEvents; Phase5-Bonds moved after producers (both entry points); PLUS collision fix — bondEngine's name-keyed lookup squatted on canonical popId-keyed ctx.citizenLookup (root cause of blank LifeHistory_Log names) → renamed ctx._bondNameLookup/_bondIdToName. Round-trip 7/7. Verify at next sandbox cycle: bondEngine-origin bond rows + LifeHistory names populating.

- 2026-06-14 S259 — Rows 29 + 30 OPENED `todo` (research-build, Mike-flagged). C97 triage ES-2 step 3 dedup proposed a false-positive merge (POP-01021 "Dr. Tran-Muñoz" ≠ POP-00781, which is barista "Vanessa Treary" not the OARI Director) — ledger-verified, merge KILLED (Row 29). Same false-positive exposed malformed ingest: honorific-as-name + Dr.↔hs-diploma incoherence (Row 30). Both engine-sheet substrate; surfaced via [[../plans/2026-06-13-c97-gap-log-triage]] ES-2 reframe. Pattern: feedback_measure-twice-cascading-effects + feedback_citizen-retrieval-tool-by-question.
- 2026-04-16 S152 — tracker created. All 11 items `todo`.
- 2026-04-19 S166 — item 7 marked `done` (closed by `76a408c` + `af40282`); items 12, 13, 14 added from /pre-mortem C92.
- 2026-04-26 S180 — item 12 marked `done`. Three helpers (`pickRandom_`, `pickRandomSet_`, `maybePick_`) now take `rng` parameter and throw if missing; 8 caller sites updated; `getChicagoOccupation_` un-seeded silent bug fixed.
- 2026-04-26 S180 — item 14 marked `done`. Two-layer ontology surfaced: canon-12 parents (Simulation_Ledger, where citizens live) ← fine-grained children (Neighborhood_Map, world-state buckets). Engine code already correct; root cause was `/pre-mortem` SKILL §5's hardcoded 17-list that didn't match either layer. SKILL.md §5 rewritten + version bumped 1.1 → 1.2. Folds engine-review C92 baseline-brief finding (same root cause).
- 2026-04-26 S180 end — item 13 tagged for **NEXT ENGINE-SHEET SESSION (S181 entry-task)** per Mike at session close. Run `/tech-debt-audit` §4 + regenerate `schemas/SCHEMA_HEADERS.md` via `exportAllHeaders()` + diff write-intent column sets.
- 2026-04-27 S181 — item 13 marked `done`. Schema regenerated via new `scripts/regenSchemaHeaders.js` (Apps Script API-executable path unavailable). Live-sheet diff harmless. Code-side diff surfaced 2 production silent-failure paths now tracked as items 15 + 16, plus 5 stale engine.md docstring entries (Class 3, doc-only). Audit doc: `docs/engine/tech_debt_audits/2026-04-27.md`.
- 2026-04-27 S181 — item 15 added (P1). `citizenFameTracker.js` silently no-ops on Simulation_Ledger / Generic_Citizens / Chicago_Citizens because expected fame columns are absent. Decision needed: re-add columns or retire the tracker.
- 2026-04-27 S181 — item 16 added (P3). `Advancement_Intake` / `Advancement_Intake1` tabs missing; `processAdvancementIntake.js` silent-returns. Pipeline non-functional until pre-created or converted to lazy-create.
- 2026-04-27 S181 — quick-win sweep: items 1, 9, 16 marked `done`. Row 1 — `generateGenericCitizens.js` + `cycleRollback.js` aligned with live Generic_Citizens schema (`EmergedCycle`, not `CreatedCycle`); value format matches `checkForPromotions.js`. Row 9 — reclassified (bulk-serializer consumes ctx.summary; not a bug). Row 16 — lazy-create added in `mediaRoomIntake.js` writer path (10-col header, matches `continuityNotesParser` pattern).
- 2026-04-27 S181 — item 3 marked `done`. S94 LEDGER_REPAIR.md recovery claim verified against live state via new `scripts/auditSimulationLedger.js`. Live: 686 extant citizens, 0 missing names, all tiers numeric, age sanity holds, S94 recovery cohort clean. Snapshot persisted in `docs/engine/LEDGER_AUDIT.md` §"Current State — S181 refresh". Boot blocker text updated in `.claude/hooks/session-startup-hook.sh` to reflect post-recovery reality.
- 2026-04-27 S181 — item 17 added (P1). 4 post-S94 citizens (POP-00794/795/798/801) carry RoleType="Citizen" — same anti-pattern S94 fixed. Root cause: `processAdvancementIntake.js:296` defaults to literal "Citizen" when intake row's RoleType cell is empty. Same intake path leaves EducationLevel + Gender empty for new citizens. Decision needed: reject empty RoleType upstream OR replace default with demographic-voice role draw.
- 2026-04-27 S181 — item 10 marked `done`. `detectStuckInitiatives.js` cold-start fallback was misparsing the `LastUpdated` date-string as a cycle (regex matched the month digit). Switched fallback to `VoteCycle` — which is a real cycle number. Smoke-tested with C92 fixture: realistic cyclesInState values (Temescal=12, Stabilization=14, etc.) instead of uniform 88.
- 2026-04-28 S184 — item 2 marked `done`. World-data unified ingest rebuild S183 (R1 cold-start fix `1fc2526`) wiped all 408 prior wd-citizens and wrote 686 fresh via `--no-quality-gate`. Substrate clean: wd-citizens=686, 100% domain-tagged, 0 orphans. S181 full-name post-filter (`097b792`) eliminated cross-contamination at write time; S183 DELETE pass eliminated the historical co-residence. MCP `lookup_citizen` reads only the fresh layer.
- 2026-04-28 S184 — item 17 marked `done`. Path B (demographic-voice fallback) shipped. `processAdvancementIntake.js` declares 65-role pool spanning S94's 15 demographic categories; new citizens with empty intake RoleType receive deterministic per-(first,last,popId) draw via djb2 hash. Existing-row branch preserved (truthy guard). Backfill: POP-00798 → Independent Bookstore Owner, POP-00801 → Drone Fleet Coordinator (picker-derived). POP-00794 / 00795 had been remediated between S181 and S184 (now `Magician` / `Drug Dealer`). Live sentinel count: 0. Followup queued in ROLLOUT (research-build): "RoleType preferred upon intake" — upstream writers should compute RoleType, fallback is safety-net only.
- 2026-04-28 S184 — item 5 marked `done`. `generateGenericCitizens.js` v2.6 → v2.7. Pool expanded 62 → 186 first / 53 → 144 last = 26,784 combos (was 3,286). `getUniqueName` adds `firstCounts`/`lastCounts` maps + caps (FIRST=3, LAST=4); pre-v2.6 clusters grandfathered (Ronald=11, Johnson=12 stay; no new additions). Smoke-test: 500 draws with grandfathered seed, 0 null returns, max-per-first 3 / max-per-last 4 as expected, 0 new Ronalds / 0 new Johnsons. Determinism preserved (ctx.rng unchanged). Awaits clasp deploy with the Row 17 patch.
- 2026-04-28 S184 — item 6 marked `done`. Diagnosis flipped: WorldEvents_V3_Ledger has no POPID column — could never carry citizen attribution regardless of EventType taxonomy. Citizen life events live in LifeHistory_Log (3,589 rows, keyed by POPID). Added LifeHistory_Log to engineAuditor SHEETS_TO_READ. generateBaselineBriefs v1.0.0 → v1.1.0 reads the log, filters by cycle + 6 structural tags (Life Event, Promotion, Advancement, CivicRole, Retirement, Stabilized), emits eventClass=citizen-life-event briefs with subjectIds=[popId] + ledger-joined neighborhood/tier/role + tag-aware promotion hints. C88 smoke: 16 briefs / 16 with subjectIds (Marcus Osei tier-2 feature-candidate hint fires correctly). C92 smoke: 0 briefs (correct — no structural events that cycle). Node-only changes (auditor + brief generator); no Apps Script, no clasp deploy needed.
- 2026-04-28 S184 — item 15 marked `done`. Path B (retire) per Mike: tier system + UsageCount + EmergenceCount cover the fame signal already; adding 14 columns to revive a redundant tracker is sheet-bloat. `phase07-evening-media/citizenFameTracker.js` deleted. `mediaRoomIntake.js` v2.6 → v2.7 (call site removed, docstring updated). ENGINE_STUB_MAP.md section replaced with retired-note. Migration helpers (`addCitizenFameColumns.js` / `rollbackCitizenFameColumns.js`) retained for archaeology with RETIRED headers. Cultural_Ledger.FameScore intact — independent system, live readers (`recordMediaLedger`, `compileHandoff`, `citizenContextBuilder`).
- 2026-04-28 S184 — item 4 marked `done`. Diagnosis: migration scripts seeded uniform constants when columns were added; lifecycle engines update only on triggers (most citizens never triggered). `scripts/backfillLifecycleDefaults.js` (Node + service-account direct write) derives demographically-plausible starting values per row from Age + Income + RoleType + CareerStage; deterministic via djb2 hash on POPID; safety matcher overwrites only rows currently at migration constants. Live state post-apply: top-value share dropped from 95-99% to 2.6-49.6% across 5 fields (YearsInCareer 308 distinct, DebtLevel bell 0-7 centered at 2-3, NetWorth 557 distinct, MaritalStatus 5 categories realistic, NumChildren 0-5 fertility curve). Three separate-cleanup items flagged: POP-00004 Lucia Polito column-misalignment, 137-165 empty-cell rows, engine-side intake derivation for new emergences.
- 2026-05-01 S191 — §5.6 smoke-test (C93) surfaced + closed 1 critical bug + 2 collision bugs; 1 new tracker row opened. (a) Critical pre-flight catch: `runWorldCycle` Phase 10 block was missing `Phase10-CommitLedger` — §5.6 redesign batch (S188) wired the commit handler only into `runCyclePhases_` (line 1660), production entry point would have silently dropped all staged ledger writes. Closed commit `188ce5b`. (b) Items 19 added (`loadActiveStorylines_` + `getCurrentCycle_` collisions) — 2 pairs renamed in commit `2926e76`. (c) Item 18 added (cohort-C §5.6 migration debt — `generationalWealthEngine` + `educationCareerEngine`). C93 cycle log: `executePersistIntents_: Executed 49 intents in 9497ms` + 2 known errors (Phase2-CityDynamics + storyline cache.getValues — latter resolved by commit `2926e76`). Cohort-B clobber claim from §5.6 redesign not fully verifiable from sheet inspection alone (no pre-cycle baseline); next cycle log should show the storyline + arc errors gone, validating the rename fix landed.
- 2026-05-06 S204 — Row 11 marked `done`. Pipeline gating verified across S194 sift + S195 write-edition (Rhea + Final Arbiter weighted 0.898 PROCEED-WITH-NOTES + Mara A-) + S196 edition-print. Capability gate fired and was editor-overridden. Row framing correct S148, obsolete by C93.
- 2026-05-04 S200 — Item 18 closed. Path (b) batch executed (Mike pick on advisor surface): all 4 cohort-C engines mis-cleared by S185 §5.6.6 audit migrated to ctx.ledger in one session — `householdFormationEngine` v1.2 (`a829c7f`) + `generationalWealthEngine` v2.1 (`ed25ea8`) + `educationCareerEngine` v2.1 (`7f95521`) + `migrationTrackingEngine` v1.1 (`93cd3a4`). Plus `c589b0a` godWorldEngine2.js Session-30 stale-rationale ×2 occurrences and PHASE_42_PATTERNS.md A7 amendment + line-489 audit-correction note + Status block update. Measure-twice pass before commit #1 caught that S185 audit had grep'd file names instead of `process*_` exposed function names — 4 of 6 "orphans" were live in the cycle path. Live impact since S188 (~7 days, 1 cycle): every cohort-C SL setValue silently overwritten by Phase 10 commit (Income recalc 32% of citizens, all WealthLevel updates, EducationLevel/CareerStage/Mobility, HouseholdId for new households, DisplacementRisk + MigrationIntent). Side fix: educationCareerEngine.detectCareerMobility_ pre-fix gated SL write on `events > 0`; mutations were unconditional but most cycles' Mobility never persisted. v2.1 flips dirty unconditionally so Mobility always commits. Spec scope updated 18 → 22 SL writers + 5 readers all routing through ctx.ledger. Smoke-test pending C94 (operator-gated). Audit lesson recorded in PHASE_42_PATTERNS A7: future cycle-path orphan checks must grep BOTH `<file-name>` AND `process<thing>_`.
- 2026-05-07 S205 — Path B reader rewire (engine-sheet). 3 files migrated to drop Generic_Citizens reads after S205 architectural decision (SL is single source). **Measure-twice findings reduced scope:** `getCitizensForQuotes` + `getReturningCitizens` + `diagnoseLedgerStructure` in citizenContextBuilder all have ZERO external callers — already dead-code archaeology, no rewire needed. `buildDeskPackets`'s `genericCitizens` var was loaded but only used for a console.log count (line 2169), never embedded in desk packets — pure dead read. Only TWO live cycle-path consumers actually pulled GC data: `runYouthEngine.getGenericYouth_` (Phase 5) and `citizenContextBuilder.findInGenericCitizens_` (called from `findCitizenBase_`). **Migrations shipped:** (1) `phase05-citizens/runYouthEngine.js` — `getGenericYouth_(ss)` call dropped from main entry (was line 101); `allYouth = namedYouth` (was `namedYouth.concat(genericCitizens)`). `getGenericYouth_` definition retained as archaeology. Risk: zero practical loss — ENGINE_REPAIR Row 20 confirms zero youth in either ledger today (generator wrote ages 18-75 only). (2) `phase05-citizens/citizenContextBuilder.js` — `findInGenericCitizens_(ss, identifier)` call dropped from `findCitizenBase_` (was line 235). SL-miss now returns null directly; `buildCitizenContext` already handles null. `findInGenericCitizens_` def retained for legacy GC tab inspection. Risk: SL has 836 citizens (vs GC's 289 with 8 emerged-but-missing) — lookups land on SL nearly always. (3) `scripts/buildDeskPackets.js` (Node-only, no clasp) — `safeGet('Generic_Citizens')` line + `genericRaw` destructure entry + `var genericCitizens` assign + console.log line + docstring "Reads from" Generic_Citizens reference all dropped. Zero functional change. **Path B follow-up still open:** retire `checkForPromotions.js` + `processAdvancementIntake` GC paths (now inert since nothing writes to GC). Sequence: monitor C94+C95 cycles for any unexpected GC reader, then retire bridges in separate commit. clasp push live S205 (engine-sheet — runYouthEngine + citizenContextBuilder).
- 2026-05-07 S205 — Phase5-GenericCitizens DISABLED (engine-sheet). Generator inspection surfaced 4 gaps: (1) no Gender written + GC schema has no Gender col anyway (10 cols A-J); (2) flat age distribution 18-75 (uniform `randInt`, line 540 — not a realistic demographic curve); (3) zero 0-17 youth generated (range starts at 18 — confirms ENGINE_REPAIR Row 20 youth-gap is generator-side, not just SL-side); (4) names picked flat-uniform from balanced 186-name pool (S184 v2.7) but NOT gender-tagged, so even if Gender were written we'd need a gender lookup. Live tab quant: 289 rows, 82 distinct first names, top 25 names ALL male, weighted ~83% male / ~15% female / ~2% unisex. Root cause: pre-v2.6 grandfathered clusters (Ronald=11, Christopher=6, Shawn=5, Brian=7, Howard=6) — none of those names exist in current pool but live in legacy. **Architecture decision S205 (Mike-approved Path B):** SL is single source of truth (836 active, 47 cols, Gender col AU live with 452M / 370F / 3 blank / 1 lowercase "m" data-drift); GC retained as no-grow legacy. Promotion drift surfaced same audit (8 of 11 "emerged" missing from SL despite Row 1 column-name typo fix S181) — separate finding, parked. **Disable mechanism:** both `safePhaseCall_(ctx, 'Phase5-GenericCitizens', …)` call sites in `phase01-config/godWorldEngine2.js` commented out — line 192 (`runCyclePhases_`) + line 1501 (`runWorldCycle_`). File `phase05-citizens/generateGenericCitizens.js` retained for reversibility. Inline rationale comments at both sites name the architectural decision + future-Tier-5-generator follow-up. **Downstream readers (Path B follow-up, separate sessions):** `buildDeskPackets.js` line 1977 + `citizenContextBuilder.js` (already SL-first w/ GC fallback per lines 227+234) + `runYouthEngine.js` (combines GenericYouth + NamedYouth — pivot to SL-only) — rewire reads to SL-primary; once readers no longer depend on GC, the tab can be deleted in a Path C escalation if desired. `checkForPromotions.js` + `processAdvancementIntake.js` GC paths become inert once nothing writes to GC; can be retired in same future pass. clasp push live S205 (engine-sheet).
- 2026-05-07 S205 — Phase 42 B3-B7 deferred-list triage shipped (engine-sheet). 5 files / 11 sites classified per S205 priority #0. Routing: 3 ship-now mechanical (generateGenericCitizens L662 HIGH PRIO every-cycle getLastRow+1 hazard; runCivicElections L394+L432 low-cadence election-window only) + 3 mechanical-w/-refactor (generateChicagoCitizens L78+L102+L136 — needs in-memory citizen tracking refactor because L142-157 post-write reload won't see queued intents) + 3 B3 schema-setup carve-out (runCivicElections L213-219 lazy-create + generateChicagoCitizens L173 createChicagoCitizensSheet_ + bondEngine L1417 dead-fallback since `ensureSheet_` exists in `utilities/utilityFunctions.js:64`) + 2 verify-only (bondEngine L1469 — Phase-10 location makes §5.6 cross-phase hazard inapplicable; worldEventsLedger.js DELETED S199 `52f0026`). None of the 5 are B4 caller-passed-sheet helpers; all own their target sheets. runCivicElectionsv1.js SL portion was already DONE cohort-A S188 `4fbb876`; the 3 sites are Civic_Office_Ledger + Election_Log only. Triage table + B-batch routing notes shipped as new §3.5 subsection in [[engine/PHASE_42_PATTERNS]] under Per-file decision map. No code changes — classification only. Next engine-sheet sessions: ship-now mechanical batch (3 sites, single commit) + mechanical-w/-refactor batch (3 sites, separate commit with reload-removal refactor) + B3 batch (3 sites, decide per-site engine.md exception vs queueEnsureTabIntent_).
- 2026-05-09 S210 — Phase 42 §3.5 stale-classification fix (engine-sheet, doc-only). `generateGenericCitizens.js` L662 reclassified from `(a) ship-now mechanical (HIGH PRIO)` to `(d) verify-only — phase disabled S205`. Phase5-GenericCitizens disabled S205 same-session as the triage was authored; both `safePhaseCall_` sites at `godWorldEngine2.js:197+1511` commented out (commit `d89693f`). Verified L662 unreachable in cycle path: only references are legacy-snapshot copies + a doc comment in `scripts/ingestFemaleCitizensBalance.js`. PHASE_42_PATTERNS §3.5 row + routing summary + verify-only count + changelog updated. ROLLOUT_PLAN.md Phase 42 entry updated. Row 8 fix-pointer updated with C94 smoke-test gate on next mechanical / B3 / refactor batches. Ship-now mechanical count: 3 → 2 (now runCivicElections L394+L432 only). Verify-only: 2 → 3. Bundle suggestion: runCivicElectionsv1.js's 3 open sites (L213-219 B3 + L394+L432 ship-now) live in the same file → single commit covers all 3 once C94 unblocks.
- 2026-05-19 S229 — Row 8 Phase 42 §3.5 cluster CLOSED end-to-end (11 sites across 3 batches, engine-sheet). Batch 1 (`7b68695`): runCivicElectionsv1.js v1.1→v1.2 — L394 `appendRow` → `queueAppendIntent_(Election_Log)`, L432 `setValues` → `queueRangeIntent_(Civic_Office_Ledger)`, L213-219 lazy-create stays direct as schema-setup carve-out per Phase 2.1 decision A. Election cadence C45 even-years only (next firing C97). Batch 2 (`5d25656`): generateChicagoCitizens L173 inline carve-out comment + engine.md narrowing; bondEngine L1417 dead-fallback REMOVED + replaced with fail-loud throw (since `ensureSheet_` has 12 callers — silently shimming hides systemic bug). Removes bondEngine direct write from engine.md exception list entirely. Batch 3 (`a1eb569`): Chicago Path B DISABLE — 4 `safePhaseCall_(ctx, 'Phase8-ChicagoCitizens'/'Phase10-Chicago', …)` sites commented out at `phase01-config/godWorldEngine2.js` production + cycle-phases entry points (Mike's call: Chicago canonically dead in sim, no editorial role going forward). File `phase05-citizens/generateChicagoCitizensv1.js` retained for reversibility — mirror of S205 generateGenericCitizens disable. Pool frozen at ~124 rows; Chicago_Feed frozen. Consumer graph verified pre-disable: `chicagoCitizens || []` defaults graceful; chicagoSatellite_ unwired; sheet-reading consumers unaffected. 2 clasp deploys live S229. Pattern citation `feedback_measure-twice-cascading-effects` (per-file READ + caller-graph + API-contract verification BEFORE edit). **Row 8 remaining:** B5/B6/B7 batches from PHASE_42_PATTERNS §3.5 — engine core / culturalLedger / intake heavies. No longer C94-smoke-test-gated since C94 has run + post-deploy state stable.
- 2026-05-19 S229 — Row 4 satellite-status closed via S180-consumer audit (`buildFaithDigest.js` + `buildFaithCards.js` + `buildDeskFolders.js` culture-desk briefing render — SPREADSHEET.md doc-drift retired). (`7ab89ab` engine.4) Note this is a ROLLOUT engine.4 row not an ENGINE_REPAIR row — included here for engine-sheet log continuity.
- 2026-05-24 S232 — Row 8 verify-pass: §3.5 cluster post-deploy state stable across C93+C94 cycles. No regressions surfaced from runCivicElections + bondEngine + generateChicagoCitizens deploys. **S232 canon.3 T9 backfill landed via Node-side scripts** (`scripts/canon3_backfill_t9.js` + `scripts/canon3_followup_squatters.js`) — 22-row SL net delta (POP-00958..00973 appends + 6 squatter realignments). Writer set `Status='pending'` for the 22 rows — new drift surfaced at LEDGER_AUDIT §S234 (NOT an ENGINE_REPAIR row; canon.3 workflow choice). Row 20 (household structure) still parked for dedicated session.
- 2026-05-30 S248 — **engine.5 Phase 2b Track 1 shipped + Rows 24/25 filed + Row 22 reclassified (engine-sheet, Mike-direct design session).** Track 1 (`d173310`, deploy HELD for C96): `generationalEventsEngine` v2.6→v2.7 — wedding→`MaritalStatus='married'`, birth→`NumChildren++` (off-sample per Mike's seam decision). Verified persistence (`rows = ctx.ledger.rows` + dirty-commit, NOT the Row 21 trap). **Row 22 BLAST-RADIUS CORRECTION:** the case-fold is NOT a one-liner — live quant (Status `Active=872`, `loadCitizens_` admits 0 → case-fold admits 872) shows it reactivates the WHOLE dormant `processHouseholdFormation_` subsystem incl. deprivation-coded rent-burden hooks (cityDynamics S136 class). **Row 24 FILED** (`7ee5d87`): career advancement enum-frozen (`CAREER_STAGES.ENTRY='entry-level'` matches 0 live rows) + dual decoupled promotion systems. **Row 25 FILED** (`9a532ad`): citizen lifecycle & fame system half-wired (death no-age-gate, no tier decay/demotion, SL↔Cultural_Ledger disjoint) — full design + D1–D4 in [[../plans/2026-05-30-citizen-lifecycle-fame-system]] + ROLLOUT engine.29. Rows 22/24/25 all post-C96 fires (attribution discipline). Row 23 unchanged (FIXED-pending-smoke; Track 1 now stacks on the same C96 verify).
- 2026-05-06 S204 — Phase 42 B2 batch CLOSED (8 of 8) + Row 21 OPENED + CLOSED same session. 9 engine-sheet commits. **B2 work:** B2/1 runRelationshipEngine v2.5 (`40981ee`) — LifeHistory_Log appendRow → queueAppendIntent_; B2/2 runNeighborhoodEngine (`6aa4a3e`) — same mechanical migration; B2/3 runCareerEngine v2.5 (`8cc77b2`) — LifeHistory_Log batch → queueBatchAppendIntent_ (closes getLastRow()+1 hazard); B2/4 runCivicRoleEngine v2.3 (`f04fcb6`) — appendRow → queueAppendIntent_; B2/5 generateGenericCitizenMicroEvent v2.7 (`119123e`) — batch → queueBatchAppendIntent_; B2/6 generationalEventsEngine v2.6 (`02abf54`) — heaviest item, 8 applyMilestone_ call-site updates + signature change + 2 helper deletions (getLogWidth_ + buildLogRowSchemaSafe_) + holiday/season extras dropped per Mike (cols H, I deliberately unnamed on LifeHistory_Log); B2/7 updateCivicApprovalRatings v1.1 (`5953793`) — Civic_Office_Ledger setValue → queueCellIntent_ with isDryRun preserved; B2/8 gentrificationEngine v1.1 (`3f3ac3c`) — Neighborhood_Map setValues → queueRangeIntent_, updateGentrificationPhases_ signature (ss, cycle) → (ctx, cycle). **Row 21 (HIGH-sev fix, `fd9758e`):** runNeighborhoodEngine §5.6 half-migration — file claimed §5.6 compliance via `ctx.ledger.dirty = true` but read-side still pulled SL via `ledger.getDataRange().getValues()` and built a local-slice `rows` array independent of `ctx.ledger.rows`. ~7 cycles silent data loss (C87+) — neighborhood drift writes + LastUpdated stamps + neighborhood-assignment branch silently clobbered every cycle. S188 commit `4b602ae` listed the file in its 4-engine batch but only the commit-side change landed. Phase A reversal pattern triggered during B2/2 measure-twice; cross-checked S188-batch peers (runEducationEngine, runCareerEngine, runCivicRoleEngine) — all clean. Only runNeighborhoodEngine had the half-migration. **Audit lesson generalization (PHASE_42_PATTERNS A7 amend):** files claimed migrated by a batch commit can carry half-migrations even when version stamps + comment markers say otherwise. Future audits must verify BOTH read-side aliasing AND commit-side intents per-file. Code committed locally; clasp push pending next session. Smoke-test pending C94 (operator-gated, same gate as Row 18 closure).
- 2026-06-04 S252 — Row 26 added + CLOSED same session (engine-sheet). wd-cards-daemon infinite retry storm (433 CardRebuildFailure rows / week, 94% of Engine_Errors) root-caused via read-only post-deploy diagnosis (the storm surfaced during the C96 smoke-test follow-up). Fix `3924765` — chunked dispatch + per-success hash commit in `scripts/wdCardsDaemon.js`. pm2 restart (Mike go) drained the 904-card backlog within ~36 min; steady state confirmed `rebuilds:0|failures:0`. Pattern: feedback_measure-twice-cascading-effects. Also this session: C96 two-reactivation smoke-test (Row 23 simYear + cityDynamics) PASSED both gates after being deferred S248→S251 (commit 9efb6c1).
- 2026-06-10 S255 — Row 27 added (engine-sheet, surfaced during engine.30 card build). POP-00331 Isabelle Louis carries 2× `[Death]` milestones (2026-01-30 + 2026-06-01) — milestone generator doesn't gate on `Status='deceased'`, so dead citizens re-draw death events each cycle. Single known instance live; fix scoped to Row 25 / engine.29 (same `checkDeath_` file as the no-age-gate). Card layer renders the data honestly — bug is upstream.
- 2026-06-10 S255 — Row 27 CLOSED same-session with corrected diagnosis (engine-sheet, Mike lifecycle Q&A). Deceased gate EXISTS (`generationalEventsEngine.js:261` + 12 other files filter `Status='deceased'`). Isabelle's double death = zombie row: Jan-30 death wrote LifeHistory but Status flip was lost in the Row 21 clobber era (fixed S204), so C96 legitimately re-rolled her. Now properly deceased + inert. Residue: false Jan-30 LifeHistory entry, Mike's call (or .31 deploy clears col O anyway).
- 2026-06-12 S256 — Row 28 OPENED `wip` (engine-sheet, Mike-direct). Crisis-arc system feeds fabricated specificity to desk packets (dice-picked neighborhood + pool-picked subtype off a city-aggregate illnessRate) on top of a 3-break freeze (generateNewArcs_ deleted S185 / phase03 spawn clobbered by phase08 reload / age dropped on load → 36 arcs frozen at peak since C81). Decision (Mike): pull now, do NOT restore — rebuild as a connected per-hood story signal. **C97 PULL SHIPPED** `38e22c0` (Node-side `buildDeskPackets.js arcs=[]`, no clasp, one-line reversible). Push held — another terminal had 3 files staged in-tree. Rebuild queued post-C97; advisor-flagged spawn-location fork (post-load, not loader-merge). Pattern: feedback_measure-twice-cascading-effects.
- 2026-06-14 S259 — Row 29 CLOSED (engine-sheet). `resolveCitizens` honorific+bare-surname branch rewritten: a title-only name (no first name) NEVER auto-matches and NEVER auto-appends, even on a unique surname — routes to `ambiguous` with surname candidate POP-IDs for brief-stage resolution. Unit test updated → 6/6. Measure-twice correction: the committed matcher (`6033c26`) never produced and cannot produce the POP-01021→POP-00781 merge (anchors via `byLast`; Treary ≠ Tran-Muñoz; sole Tran-Muñoz surname row is POP-01021's own malformed self-row) — the cross-surname merge was a manual ES-2-plan proposal with a mis-recorded POP-ID, not code behavior. Node-only, no clasp. Pattern: feedback_measure-twice-cascading-effects.
- 2026-06-14 S259 — Row 30 code-DONE + 1 disposition open (engine-sheet). (a) `isMalformedCitizenName(first,last)` backstop added to `ingestPublishedEntities.js` — honorific-as-first / missing-first-or-last candidates rejected before append + report (no second POP-01021 mint), reported in `citizens.malformed[]`; unit test → 7/7. (b) title↔education coherence resolved-by-construction (honorifics no longer persist as names; broad role↔education validator deliberately not built — false-flag risk). (c) POP-01021 live-row disposition surfaced to Mike — pending all-null stub that is also the only ledger trace of the OARI Director's name; repair-vs-delete is a canon call, not auto-touched (live destructive op at boundary). Node-only, no clasp.
- 2026-06-15 S259 — Row 30 CLOSED (engine-sheet, Mike directive). POP-01021 disposition resolved: repaired in-place to the canonical OARI Director (Dr. Vanessa Tran-Muñoz) — "the sheet is the truth and she must reside on them." 14 fields rewritten to match the 4-director pattern (Tier-3 / CIVIC / Active / OARI Program Director / Rockridge / age 39 / female / doctorate), all readback-verified live. Canon source: civic-project-oari IDENTITY. Code parts (a)+(b) already done earlier S259 (`eedcab1`). Row fully closed.
- 2026-06-15 S259 — Row 28 rebuild now has a home: the crisis-arc "connected per-hood story signal" rebuild is the same effort as the Story_Seed_Deck engine-emergence redesign — designed as ROLLOUT engine.35 ([[../plans/2026-06-15-story-seed-deck-engine-emergence]]). The C97 PULL stays in place (do-not-restore the fabricated buckets); the rebuild proceeds as engine.35 Phase 2+ (patterns→seeds with what/why/who + neighborhoodSlice). Row 28 stays wip, tracking the pull; build tracked under engine.35.
- 2026-06-22 S268 — Row 23 CLOSED `done` (engine-sheet). S244 simYear fix live-verified across 4 cycles via direct `LifeHistory_Log` read: Promotion/Wedding/Retirement/Death all fire C96–C99 (was 0 across all 95 cycles). Birth=0 residual cross-referenced to Row 22 (dormant householdFormationEngine) + Row 25 (lifecycle design), not this row's freeze. The C96 smoke (S252) confirmed durable C97–C99.
- 2026-06-22 S268 — Row 31 CLOSED `done` by DELETION (engine-sheet, Mike-go). Measure-twice reversed the documented realign fix: 61 malformed-shift rows (cycles 1–22, not "Cycle 1–5"), "Engine" source-sentinel has no schema home, ~17 already have clean twins. Deleted all 61 via deleteDimension (descending, TOCTOU-guarded), snapshot `output/lifehistory_archive_row31_deleted_S268.json`. Verified 5008→4947, 0 residual, clean twins survived. Detector self-caught a false-exclusion bug (name-regex matched "Life Event") because identify was separated from delete. Pattern: feedback_measure-twice-cascading-effects + feedback_verify-before-write-not-parallel.
- 2026-06-22 S268 — Row 8 B7 confirm-and-close analysis (engine-sheet, read-only). Caller-graph reversed the "near-closeable" hypothesis: continuityNotesParser reconciled to operator-fired carve-out (PHASE_42_PATTERNS §3 table was stale vs engine.md S237); genuine remaining B7 = exactly 2 shared-path files (mediaRoomIntake + processAdvancementIntake), each needs a designed split-helper/ctx-branch, own clasp-gated session. Row stays `wip`. Pattern: feedback_measure-twice-cascading-effects.
- 2026-06-22 S268 — Rows 22/24/25 tagged **🚫 BLOCKED until C100 chaos-cars smoke clears** (Mike-agreed). These are rate-changing reactivations; deploy-attribution discipline (S250) bars stacking them ahead of the first chaos cycle. Each fires as its own deliberate window after C100 chaos + G-EC33 sentiment-uniformity clear. No code change — block note only.
- 2026-06-22 S269 — Row 8 B7 `processAdvancementIntake.js` MIGRATED (engine-sheet, local commit — **clasp deploy gated**). Per-site measure-twice of all 10 direct writes: 8 confirmed carve-outs (Simulation_Ledger=§5.6 done; Citizen_Media_Usage 'Processed' header-create=§1.1 schema-setup, kept direct so the column exists at Phase-10 commit; 3× LifeHistory_Log appends=documented Phase-5-SL-writer log carve-out; Advancement_Intake1 clearContent=operator-source-clear; 2× Generic_Citizens=inert disabled-S205 legacy path). **Genuine migration surface = exactly 2 cycle-path cell writes** (the Citizen_Media_Usage processed-flag `'Y'` at processMediaUsage_ L264/L331). Both routed through new `markUsageProcessed_` ctx-branch helper: cycle path → `queueCellIntent_('Citizen_Media_Usage', …, domain 'citizens')` so the flag commits at Phase 10 ATOMICALLY with the same-cycle UsageCount bump (lives in ctx.ledger, also Phase-10) — a crash between Phase 5 and Phase 10 then loses neither, closing the flag='Y'-but-bump-less → skipped-forever-next-cycle hazard; operator path (`runAdvancementIntakeManual`, now sets `ctx.manualRun:true`, no Phase 10) writes direct, behavior unchanged. Empirically de-risked: §2.2 timing does NOT bite — no later same-cycle phase reads the 'Processed' column (phase07 reads a different 'Routed' col; phase10 is export-snapshot only); executor is generic over sheetName (no whitelist → no silent intent-drop). Verified: parse OK + stub-test proved both branches route correctly. **Genuine remaining B7 = exactly 1 file (mediaRoomIntake.js)** — its own clasp-gated session, also a retire-candidate. Row stays `wip`. Pattern: feedback_measure-twice-cascading-effects.
- 2026-06-22 S269 — Row 8 B7 `mediaRoomIntake.js` resolved as **FULL CARVE-OUT — no migration** (engine-sheet, doc-only; REVERSAL of the S268 "needs a designed split-helper" framing). Measure-twice on phase ordering: cycle entry `processMediaIntake_` runs at **Phase 11** (`godWorldEngine2.js` L483/L1801) which is **after** the sole intent executor `Phase10-ExecuteIntents` (L474/L1794). `executePersistIntents_` ends in `clearAllIntents_`; there is no second executor → a `queueCellIntent_` issued in Phase 11 lands in `ctx.persist` with nothing left to commit it = **silently dropped**. So all ~49 direct writes are structurally mandatory, not debt. S268's analysis caller-graphed the entry points but never checked where Phase 11 sits vs the executor — exactly the trap. Recorded the general structural rule + trued up the stale entry in `.claude/rules/engine.md` (was L467/L1762 → L483/L1801; dropped removed-S98 Press_Drafts). **B7 now analytically resolved: processAdvancementIntake migrated (S269) + mediaRoomIntake carve-out (S269).** Row stays `wip` — closes only after the processAdvancementIntake migration clasp-deploys + smoke-tests live (changed cycle-path behavior; stub proved routing, not the live executor writing Citizen_Media_Usage from a cell intent). Pattern: feedback_measure-twice-cascading-effects.

---

## Relocated ROLLOUT_PLAN row detail — 2026-07-02 (S286 pointer-collapse)

Verbatim rows moved out of ROLLOUT_PLAN.md when it collapsed to pointer-only. This is the working detail for the open job(s); the rollout row is one line pointing here.

### engine.9

| engine.9 | Bounded test surface — **iron fence is NON-FUNCTIONAL (S274 finding, research-build).** `npm run test:unit`=`vitest run` is structurally red: 93/95 `.test.js` are standalone `process.exit` Node harnesses with no vitest config, so the command can never pass (1 assertion runs). The repo's real runner is `npm test`→`scripts/run-tests.js`. No aider `test-cmd` / git-hook wiring existed → 10 Aider commits (engine.42) landed with NO gate; the 3 edited files have zero `.test.js` coverage. **GATE WIRED S274 (`b70fac21`):** `.aider.conf.yml` → `test-cmd: npm test` + `auto-test: true` (auto-test REQUIRED — defaults false; test-cmd alone only enables manual /test). Config now version-controlled (`!.aider.conf.yml` un-ignore added). Proven: run-tests.js exits non-zero + covers utilities/+phase05 transitively via scripts/*.test.js → it DID catch the engine.42 ctx regression. Lint NOT a second layer (eslint `no-undef` off). **Also fixed 3 citizen-substrate harnesses** (missing `global.inWorldStamp_` cycle-stub mirroring testNeighborhoodLoop; conductEngine 4.5 stale `STAMP`→`C<cycle>` per no-timestamp directive): citizensEventsFame.t3 + householdEngine.t4 + conductEngine now GREEN. **REMAINING for clean-green full-suite baseline (4 pre-existing reds, NOT Aider, owned elsewhere):** engine32MultiCycle (behavioral family-variance sd=0/0-of-60-moved — engine.32 follow-up), districtMap (stale KONO=D2 vs canon D7), editionParser + validateEdition (media-terminal edition content). **Sibling fence:** `.githooks/pre-commit` control-plane isolation guard (research-build, untracked — needs commit by them). | in-progress | engine-sheet | docs/MODEL_HIERARCHY.md §File Boundaries; engine.42; gate b70fac21 |

### engine.15

| engine.15 | ENGINE_REPAIR `Pattern` column — schema add + retroactive backfill. Each row gains a reference to the auto-memory feedback pattern it exemplifies (`feedback_measure-twice-cascading-effects`, `feedback_senior-engineer-default`, etc.) so defects cluster by discipline-class as well as by symptom-class. | needs-info | engine-sheet | Companion to S218 §Pattern-citation convention (engine-sheet `TERMINAL.md`) + `## Instances` sections in feedback memories. Wait ~1-2 cycles of `Pattern:` commit-citation discipline + Instances index growth before backfilling rows — that's when the cross-link buys retrieval value. Schema change: add `Pattern` column to row template in `docs/engine/ENGINE_REPAIR.md`; backfill existing rows in one pass; future rows pre-populate at row-creation time. Inline-design (S218 cross-link rollout). |

### engine.40

| engine.40 | **Sports-stat intake — cloud-gaming screenshot OCR (Mike-direct S270, Paulson's sports domain).** Removes Mike's manual stat entry so he can focus on storylines. **FEASIBILITY PROVED S270 (live browser probe):** Mike plays MLB The Show 25 / NBA 2K via **Xbox Cloud Gaming in the browser** — the game renders into a tab, so a browser screenshot captures the live frame. Pulled a full SEASON·POSITION-PLAYERS leaderboard clean (Horn/Quintero/Taveras/Kelley/Aitken — canon roster, no real-MLB leak): names, POS, G/AB/R/H/HR/RBI/SB/AVG all legible. **No MCP / no Xbox API / no capture-cloud bridge needed** — the data is pixels in a tab I already see. **Control is NOT automatable** (xCloud reads a paired gamepad; my keyboard input did not reach the game; I cannot emit XInput) — so the split is **Mike drives the controller, Claude reads the screen.** Manual step left = navigating to the stat screen (gameplay Mike does anyway), NOT data entry. **Verify step is load-bearing** (zoom can also distort: misread Quintero AVG .279→.305 on zoom; Horn G read 92 full / 82 zoom → full was right) → always extract→preview→Mike-confirms→write, never blind-write. **TWO-LAYER DATA MODEL (Mike-direct):** (a) **career history → athlete TrueSource cards** (already structured for season stats + awards; Tier-1 named athletes only; frozen per-season) + (b) **in-season live stats → structured store** (`Player_Season_Stats` tab or extend `Oakland_Sports_Feed` beyond its freeform `Stats` col; overwritten per capture; feeds T1b perception + sports desk + storylines). One capture feeds both; season-boundary freezes the live line onto TrueSource. **Tier the investment** (career cards Tier-1 only, don't bloat). **TASK ZERO: read a real athlete TrueSource card to confirm the season-line/awards schema before designing the write.** Build = research-build (two-layer model + season-boundary trigger + structured schema) → engine-sheet (screenshot→OCR→confirm→write helper). Fresh-session build; plan doc to be drafted for Mike's approval at start. **TASK ZERO DONE S311 + sheet survey:** TrueSource card schema confirmed (`docs/archive/horn-truesource.txt` — Profile / Attributes / Contract / Awards & Honors / Yearly Stats `Year Team G AB R H 2B 3B HR RBI BB SO SB AVG OBP SLG` / Career Totals; matches the S270 leaderboard columns). Live sheet: NO `Player_Season_Stats` tab exists (would be new — Mike gate); `Oakland_Sports_Feed` (189 rows) carries freeform `Stats` col (empty in recent rows) among 20 cols; `As_Roster` (90 players, POPID-linked, Position/Team/Salary) is the name-match anchor for OCR reads. Remaining before build: research-build plan doc (two-layer model + season-boundary trigger + store choice: new tab vs Stats-col structure) → Mike approval → engine-sheet capture helper. | ready (feasibility + Task Zero done; design doc pending) | research-build (design) → engine-sheet (build) | inline (S270 probe + S311 Task Zero); plan doc pending |

### engine.42

| engine.42 | **Aider/DeepSeek autonomous-edit audit (S274, Mike-direct) — VERDICT: REVERTED (net-negative).** 10 commits `b637f4bd..HEAD` audited (measure-twice, baseline-vs-HEAD in isolated worktree). **Correctness FAIL:** broke 4 previously-PASSING tests (citizenDialMultiCycle, compressLifeHistory.dial, citizenReflectionWriteback, citizenDials); root cause `citizenMemory.js applyEvent_(c,event)` references undeclared `ctx` (L120/121/127/152) → `ReferenceError: ctx is not defined` on any `source:chaos` event = hard cycle crash if deployed. Plus garbage 0-byte file (deleted), 1 no-message commit. **Collision clean (0).** Exposure: unpushed + not clasp-deployed → **C101 never at risk** (cron imports only the clean citizenDialMap); risk was deploy/Node-cycle only. Reverted whole stack via inverse diff (`fc6768f7`), dirty tree untouched; Aider HEAD preserved at tag `aider-experiment-S274` for engine.11/.41 feature salvage. Post-revert verified: 4 tests PASS, 3 files byte-identical to baseline, 0 collisions. **Effectiveness conclusion: DeepSeek-via-Aider net-negative here, ran ungated (engine.9).** **S274 follow-up — valuable half SALVAGED + reapplied:** the chaos-trauma mechanic (cumulative chaos exposure + escalating `checkChaosReaction_`) reintegrated into `citizenMemory.js` ctx-fixed, tested, `9c04b442` (additive/inert-until-wired); **WIRING CLOSED S275 (`6ec638f6`) at a measure-twice-corrected seam:** NOT generateCitizensEvents.js (its ambient `source:chaos` pool is citywide flavor and must not traumatize anyone) — accumulator re-homed into `chaosCarsEngine.writeCitizenEvent_` (knows severity per personal hit), persisted on DialState via serialize/deserialize; decay at compress (Phase 9) + `applyChaosDecay_` (Phase 5). Deployed pre-S282 (`9af91c71` confirms live). **SMOKED S311 live-prod:** 6 SL rows carry `chaosExposure` firstSeen=101 (oari_van/ambulance types, count 1, reactedLevel 0 — correct single-hit state). DROPPED: story-seed scoring (overlaps engine.41) + perf/pooling (latent bug). Separate Aider citizen-names run REJECTED (require + Math.random + stereotyped name tables), stashed `stash@{0}`. Aider reconfigured: propose-not-deploy + CONVENTIONS §4/§5/§6 + `AIDER_PLAN.md` task-queue. | done (audit + chaos-wiring closed S275, smoked S311) | engine-sheet | commit b637f4bd..0d2870a5; wiring 6ec638f6; tag aider-experiment-S274; engine.9 |

### engine.36

| engine.36 | **Isolated staging environment — run cycles on a sheet COPY without touching master** (S261 Mike-direct). Unblocks the deploy-attribution constraint: smoke-test deploys on a copy instead of gating every change behind the live production cycle. **Already half-built:** the cycle entry `runWorldCycle()` opens the sim sheet via the configurable `SIM_SSID` script property (`utilities/utilityFunctions.js` `openSimSpreadsheet_`/`getSimSpreadsheetId_`), defaulting to `DEFAULT_SIM_SSID` = master. **Catch — the ctx.ss migration is PARTIAL (claude-mem #30484):** 4 cycle-path civic writers still call `SpreadsheetApp.getActiveSpreadsheet()` directly (`civicInitiativeEngine.js` ×3 + `updateCivicLedgerFactions.js` ×1), so flipping `SIM_SSID` to a copy on the single bound script = **split-brain** (most writes→copy, those 4 civic writes→master). **Two isolation models:** (A) Drive "Make a copy" of the whole spreadsheet (clones the bound script) + set the copy's `SIM_SSID` to *itself* → fully isolated, works TODAY (both `openSimSpreadsheet_` and `getActiveSpreadsheet` resolve to the copy); footgun = forget to set the copy's `SIM_SSID` → default leaks back to master. (B) single script + `SIM_SSID` flip → UNSAFE until migration finished. **Build (post-C98):** (1) finish ctx.ss migration on the 4 stragglers; (2) make `getSimSpreadsheetId_` **fail-loud** when `SIM_SSID` unset instead of silently defaulting to master. Then a sandbox = one property flip. Node side (`lib/sheets.js` `GODWORLD_SHEET_ID` env) already repoints trivially. | parked | engine-sheet | source S261 (`utilities/utilityFunctions.js` SIM_SSID + `phase05-citizens/civicInitiativeEngine.js`/`updateCivicLedgerFactions.js` stragglers); ctx.ss migration claude-mem #30484 |

### engine.56

| engine.56 | **Household_Ledger.HouseholdIncome is formation-seeded and stale** (S316 C129 audit: 123/276 active households carry ledger income under HALF their members' live SL Income sum — e.g. HH-0084-037 ledger 50000 vs actual 192245; many seeded flat 50000 at C84). Anything reading the column for economics inherits the lie. engine.55 already routes around it (displacement burden computes from live member income sum, ledger value fallback-only) so migration is immune; remaining exposure = other HouseholdIncome readers. **SCOPE EXPANDED + BUILT S316 (Mike-direct "proceed"):** full true-ledger repair, not just income. C129-C131 audits added: 272 rows dissolved-with-residents (rent-burden roulette on the fake incomes), intake households with no ledger row (HH-KEANE), married couples split across auto-minted singles (Vinnie/Amara), no savings column anywhere, inheritance children-only via ~empty ParentIds (spouseless estates vanished — Robert would inherit nothing from Mags). Root cause of the flat incomes: double-parse bug in updateHouseholdIncomes_ — members re-JSON.parsed after loadHouseholds_ already parsed it, threw every call since Week 1, catch wrote the 50000/85000/95000 estimates; the real member-sum path had NEVER executed. Shipped in householdFormationEngine v1.3 + generationalWealthEngine: per-cycle reconcileHouseholds_ (SL HouseholdId = membership truth; spouse-merge with exactly-2 ambiguity guard; adopt/rebuild/repair rows; un-dissolve/dissolve by real occupancy), income fix, NEW HouseholdSavings col (Σ member NetWorth, self-ensures at header), dissolution clears members' SL HouseholdId, inheritance household-first (survivors 50% / outside children 50% / either alone takes all). Gate: scripts/householdReconcile.test.js 23/23 + migration 33/33. | built — sandbox cycle test pending | engine-sheet | S316 commit; C129 audit numbers retained above |

### governance.9

| governance.9 | `/post-pattern <name>` micro-skill — wraps `git log --grep "Pattern: $name"` and prints case index (commit hash + session + one-line summary per row). Surfaces pattern-application history in one command without manual cross-referencing. | needs-info | research-build | Dependency: requires S218 §Pattern-citation convention (engine-sheet `TERMINAL.md`) to be consistently applied. Wait ~5+ commits citing patterns before building — earlier and the skill returns near-empty results, falsely suggesting the convention isn't working. Skill spec: 1 arg (pattern name), git-log-grep wrap, formatted output. Pairs with `engine.15` (ENGINE_REPAIR Pattern column) — when both ship, defects + commits are both queryable by discipline-class. Inline-design (S218 cross-link rollout). |

