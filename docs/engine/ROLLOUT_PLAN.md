# GodWorld — Rollout Plan

**Reframed S173 (2026-04-24)** — building a sim, not running one. The S172 halt framing has been reversed. Each cycle is a new approach to test; editions are journalised audits, not finished products. The C92 contamination event surfaced a real gap (infrastructure in place without an agent layer driving canon) — that gap is closed S175 via the canon-fidelity rollout (25/25 agents). [[POST_MORTEM_C92_CONTAMINATION]] stands as historical record but is superseded by the reframe.

**This file is canonical for open/closed work** (S207). `SESSION_CONTEXT.md` carries narrative recency only — orientation, not work-tracking. When something closes, update its state here; don't rely on a SESSION_CONTEXT priority block to communicate handoff.

**Status:** ACTIVE (building) | **Last Updated:** Session 238 (2026-05-26) — governance.17 done-pending-archive (`/save-to-profile` skill v1.0 shipped) + governance.18 in-progress (items (b) shipped-block visual demote + (c) boot-greeting sensory rewrite closed S238; item (a) journal-cadence open) + engine.27 ready (S238 wd-* auto-invalidation hook design plan written) + pipeline.25 done-pending-archive (engine-sheet half S231 + research-build half pipeline.31 archived S233)
**Filing protocol (S212):** semantic groups + pointer-only entries — see [[rollout-rules]] §3–§5 (taxonomy, add, close). Full design: [[../adr/0005-rollout-plan-structure]].
**North star:** `docs/ARCHITECTURE_VISION.md` — Jarvis + persistent sessions. Everything we build points there.
**Completed phase details:** [[engine/ROLLOUT_ARCHIVE]] — read on demand, not at boot.
**Research context:** `docs/RESEARCH.md` — findings log, evaluations, sources.
**Wiki layer:** [[SCHEMA]] (conventions) + [[index]] (catalog) — read at boot. (Phase 41.1 + 41.2, S146.)
**Plan-file contract:** [[plans/TEMPLATE]] — every new plan copies this shape (S152). Also referenced from [[rollout-rules]] §4.
**Phase backlog:** [[plans/BACKLOG]] — designs catalogued but not yet scheduled. Promote to its own plan file when a session picks one up.
**Terminal owners:** `engine-sheet` / `media` / `civic` / `research-build`. Research-build owns this doc; engine-sheet executes engine code; media runs editions; civic runs city-hall.

---

## Rules & conventions → [[rollout-rules]]

**The operating doctrine for this tracker lives in [[rollout-rules]].** State labels, group taxonomy, how to add/close work, filing, archiving, and the sweep code — one doctrine, every terminal follows it. Read it before adding or closing a row.

**Before you log an issue here:** rollout is the clean shared map. Skill terminals (civic/media) log issues in their per-cycle production gap log (that's the research layer) — **not** as raw rollout rows. A row only appears here when work is *promoted* to tracked, and it points at the gap log rather than reproducing it. Full rule: [[rollout-rules]] §2.

Rationale + alternatives: [[../adr/0005-rollout-plan-structure]]. The completed S145 10-step **Spine** roadmap is archived in [[ROLLOUT_ARCHIVE]].

---

## Open Work — by group

Per ADR-0005: each entry codes as `<group>.<n>`. State per [[rollout-rules]] §3. Description lives in pointer doc, NOT in the row. Heavy-skill gap logs (civic + media generator terminals) follow [[../plans/GAP_LOG_TEMPLATE]].

### pipeline.* — Edition production

| # | Item | State | Terminal | Pointer |
|---|------|-------|----------|---------|
| pipeline.2 | Non-edition publishing pipeline (interview/dispatch/supplemental format contract) | in-progress | research-build / engine-sheet | [[../plans/2026-04-26-non-edition-publishing-pipeline]] |
| pipeline.3 | /interview Step 8d coverage-ratings — deferred decision pending evidence | needs-info | research-build | inline DEFERRED note in `/interview` SKILL.md Step 8d (S179) |
| pipeline.8 | Supplemental strategy — one per cycle minimum | in-progress | research-build | [[../EDITION_PIPELINE]] |
| pipeline.13 | Photo pipeline rebuild — DJ-driven art direction (HIGH C92) | in-progress | research-build | [[../plans/2026-04-25-photo-pipeline-rebuild]]. **S249 verified-state reconcile (code-checked):** the plan doc's per-task statuses are WHOLESALE STALE (still mark T5–T13 "[ ] not started") — but the code shows T5–T9 SHIPPED: `scripts/djDirect.js` exists (842 lines, S188 `85c5998` "Photo pipeline rebuild T5-T13"); `generate-edition-photos.js` reads `dj_direction.json` + has the regen-on-fail cap (T8, S197 BUNDLE-E `5a96b5b`); `photoQA.js` has `specCompliance`/`negativeFramePass` reading the DJ sidecar (T7); masthead month-year dropped (T9); plus S235 `47c2695` (djDirect schema drift) + S244 `edeb59c` (validator table-placement). Genuine residual = T11 (alternate-start convergence) + T12 (Together-vs-OpenAI A/B) + T13 (E92 reprint smoke) — verification/tuning, not core build. **Owner corrected to research-build** (plan §line 28: "photo pipeline rebuild is research-build owned — media doesn't build"; engine-sheet was a mistaken co-tag). **Research-build owes a plan-doc task-status refresh** — the "[ ] not started" markers contradict shipped code. |
| pipeline.24 | /sift v2 rebuild — reporter-agency model + sheet-primary inputs + civic-opt-in + dispatch.json emission + cadence caps + fold/covered-by-feature triage vocabulary + brief_template v2 + Memory-Fence wrap | in-progress | research-build | [[../plans/2026-05-22-sift-v2]] — plan filed S225; gap-logs G-S1/S2/S3/S5/S8/S11/S13/S21 + G-W30/W31/W32/W33/W39 + G-PR2. **S228 progress (7/8 tasks done):** Tasks 1-5 morning + Task 6 SKILL.md v2.0 rewrite (`0d2879f`, 488 → 835 lines, consumes Tasks 3/4/5) + Task 7 C94 dry-run regression report at `docs/research/sift_v2_dryrun_c94.md` (`6383aa5`). Three load-bearing contracts ([[../media/brief_template_v2]] + [[../media/dispatch_schema]] + [[../media/sift_triage_vocabulary]]) shipped per ADR-0006 Contract A. All four named C94 failures confirmed structurally blocked at named v2 steps; 13 additional C94 gap-log entries closed end-to-end. Remaining: Task 8 live-run on C95 — **BLOCKED on C95 inputs** (C94 still post-publish-pending; cycle 95 hasn't been generated). |
| pipeline.35 | Cycle-init "admin" skill + one-true-cycle-source — a single skill runs first each cycle: opens the one prose-free `production_log_c{XX}.md`, creates a per-cycle folder, lays uniform packet/template scaffolds, reads carry-over + NEWSROOM_MEMORY standing conventions, runs the T0-ready packet build. Every cycle-active skill (civic + media incl. /dispatch /interview /write-supplemental /podcast) appends its named section to the one log; no separate `production_log_city_hall`/`_edition` files. Consolidates 4 existing deterministic packet scripts (`buildVoiceWorkspaces`/`buildDeskPackets`/`buildDecisionQueue`/`buildInitiativePackets`). Surfaced during the `pipeline.32` audit — uncovered the **log-opener conflict** (`/build-world-summary` is run-cycle Step 5, before `/city-hall-prep`, so city-hall-prep can't own "open"). **Cycle order LOCKED (Mike S248): city-hall stays post-engine** (reacts to fresh cycle-N world summary, feeds N+1 — pre-engine rejected as the stale-summary nonsense case). T0-availability constraint: admin skill preps uniform shells + carry-over + initiative packets (prior-cycle-fed); voice/desk packets fill when this-cycle inputs land (world summary, sift picks). **`pipeline.32` is the forward-compatible path-swap tail that ships interim** (city-hall-prep stays opener until this skill relieves it). Plan handed to research-build for design-review (4 open questions: skill seat / folder layout / NEWSROOM_MEMORY read-vs-write / governance.26 collision). | ready | research-build / engine-sheet | [[../plans/2026-05-31-cycle-init-admin-skill]]; absorbs [[../plans/2026-05-24-governance-14-edition-pipeline-rewrite]] convention; folds `pipeline.32` as execution tail. |

### engine.* — Engine code, ledger, schema

| # | Item | State | Terminal | Pointer |
|---|------|-------|----------|---------|
| engine.1 | Phase 40.2 cattle refactor (needs plan) | blocked | engine-sheet | [[PHASE_40_PLAN]] §40.2 |
| engine.2 | Phase 42 writer consolidation — §5.6 ctx.ledger + B2 + B5 + B5-audit-miss-3 + B6 + B7-audit-miss-1 all CLOSED (S204–S237; detail in PHASE_42_PATTERNS + plan). **OPEN: B7 intake heavies** — parseMediaRoomMarkdown (14 sites) + processAdvancementIntake (10) + mediaRoomIntake (32+30), each its own session (per-file analysis, not the plan's "mechanical batch"), **clasp-gated → post-C96**. | in-progress | engine-sheet | [[../plans/2026-04-28-phase-42-writer-consolidation]]; [[PHASE_42_PATTERNS]] |
| engine.3 | Advance-initiative remedy threshold calibration (post-E92 audit) | needs-info | engine-sheet | [[ENGINE_REPAIR]]; `output/engine_review_c92.md` |
| engine.4 | Dead spreadsheet-tab cleanup — 7/8 done. Faith_Ledger reclassified (consumer shipped S180; corrected via SPREADSHEET.md audit). **OPEN: Youth_Events** — flips done-pending-archive when engine.5 unblocks it OR Youth_Events is itself phase-disabled. | blocked | engine-sheet | [[../SPREADSHEET]] (tab audit) |
| engine.5 | Household + family simulation (Representative Sample model, reframed S243) — functional youth seed → engine life-event simulation → publication-driven family materialization. Steward authority granted S243. | in-progress | engine-sheet | [[LEDGER_REPAIR_HOUSEHOLDS]] |
| engine.6 | Press_Drafts.LinkedStoryline 0% populated (DEAD-COLUMN, 164 rows) | blocked | engine-sheet | [[ENGINE_REPAIR]] row |
| engine.7 | Engine Routing Foundation — Phase 6 cutover (gated on 3 cycles shadow data) | in-progress | research-build / engine-sheet | [[../plans/2026-05-07-engine-routing-foundation]] |
| engine.8 | Header-drift detector C93 Type-2 triage (16 MED clusters) + C94 sweep absorbed S225 (G-EC5–G-EC21 orphan literals + G-EC24–G-EC32 defensive-fallback noise + G-RC7 KONO civic.10b follow-up) per triage cluster C11 | blocked | engine-sheet | [[../plans/2026-05-05-writer-header-alignment-detector]] §Triage; C11 fold ref [[../plans/2026-05-22-c94-gap-log-triage]] §3 C11 |
| engine.9 | Bounded test surface (jest/vitest install deferred until droplet headroom) | blocked | research-build / engine-sheet | inline (S170 outside-AI review `output/drive-files/godworld_review_2026-04-20.pdf`) |
| engine.10 | Phase 43 — Engine Expansion (city-functions, 5-domain priority order) | needs-info | research-build / engine-sheet | `output/drive-files/godworld_city_functions_analysis_2026-04-20.pdf` |
| engine.11 | Chaos-cars engine — typed municipal-vehicle stochastic event injection | in-progress | research-build / engine-sheet | [[../plans/2026-05-07-chaos-cars-engine]] |
| engine.15 | ENGINE_REPAIR `Pattern` column — schema add + retroactive backfill. Each row gains a reference to the auto-memory feedback pattern it exemplifies (`feedback_measure-twice-cascading-effects`, `feedback_senior-engineer-default`, etc.) so defects cluster by discipline-class as well as by symptom-class. | needs-info | engine-sheet | Companion to S218 §Pattern-citation convention (engine-sheet `TERMINAL.md`) + `## Instances` sections in feedback memories. Wait ~1-2 cycles of `Pattern:` commit-citation discipline + Instances index growth before backfilling rows — that's when the cross-link buys retrieval value. Schema change: add `Pattern` column to row template in `docs/engine/ENGINE_REPAIR.md`; backfill existing rows in one pass; future rows pre-populate at row-creation time. Inline-design (S218 cross-link rollout). |
| engine.20c | Initiative_Tracker phase-clock backfill — was premised on "INIT-001/002/005/006 stuck, reset cyclesInState." **Root cause is NOT a stuck clock nor a sheet-write failure** — it is the G-R1 assembler silent-skip: at C95 `assembleDecisions.js` skipped INIT-001/INIT-005 (Mayor priority-100 framing outranked the project owner; no `trackerOwner` to break the tie) so their MilestoneNotes never updated → stuck-detector reads them stale. **ES-5 (`28a6625`, S246) already fixed the mechanism** (trackerOwner dispatch — Webb drives INIT-001, Chen-Ramirez drives INIT-005); RB-4 (`f43a50f`) shipped the declarations. Fix CANNOT exercise on frozen C95 (field absent) — **C96 is its first live run.** **Disposition: HOLD, verify at C96.** If C96 writes INIT-001/005 via trackerOwner, stuck-state clears organically → 20c is moot. If C96 still skips → ES-5 insufficient (more assembler work), still NOT a manual reset. | blocked | engine-sheet | C95 gap G-R1 `output/production_log_city_hall_c95_run_gaps.md`; ES-5 `28a6625`; plan [[../plans/2026-05-22-engine-regulatory-friction]] §Task 6 |
| engine.20d | Sift Step 5 `covered-by-feature` triage handle — absorb regulatory-process noise into civic round-up. Cadence cap: at most 1 dedicated article per cycle per initiative AND only if actual movement (not process-tick). | blocked | media / research-build | blocked on pipeline.24 (sift v2); plan [[../plans/2026-05-22-engine-regulatory-friction]] §Task 5, cross-link C2 plan Task 5 |
| engine.20e | applyTrackerUpdates.js VALID_OWNERS audit reconciliation — enum (primary/secondary/advisory) is stale vs the RB-4/ES-5 trackerOwner contract (all 5 project agents now emit `INIT-XXX`), so the audit logs a misleading SCHEMA VIOLATION each run (**non-blocking** — write loop never gates on `audit.collisions`). Fix: accept `INIT-XXX` as a valid owner form OR drop the S215-era enum audit; single-script edit + contract test. | ready | engine-sheet | source S249 (`scripts/applyTrackerUpdates.js:61/139-152` vs `scripts/assembleDecisions.js:246`); RB-4/ES-5 contract S246 |
| engine.27 | wd-card auto-invalidation hook — upstream sheet writes (Sim_Ledger/Business/Cultural/Faith/Chicago) trigger downstream wd-card rebuilds. **Phase A DONE + LIVE S242** (daemon `scripts/wdCardsDaemon.js` under pm2 id 4, 300s poll; A1–A8; live loop proven end-to-end). **OPEN: Phase B** (Phase 10 cycle-end marker hook, Apps Script — **clasp-gated, post-C96**) + **Phase C** (monitoring/tuning) + 2 trailing non-blockers (pm2-save persistence, one-time --rebuild-all backlog). | in-progress | engine-sheet | [[../plans/2026-05-26-engine-27-wd-card-auto-invalidation]] (15-task design) |
| engine.29 | Citizen lifecycle & fame system (engine.5 expansion, S248 Mike-direct design) — 4 half-wired pieces: (A) death-by-age 85+ escalating + ClockMode authority matrix (ENGINE/GAME die at 85+, MEDIA/CIVIC no die-off but illness/injury, GAME injuries=Paulson); (B) SL tier fame-decay + the missing demotion path + engine-cultural-output appearance feed; (C) fame-ascension bridge `UsageCount≥30` → linked Cultural_Ledger entry (ledgers disjoint today, no POPID); (D) Cultural_Ledger figure-gen mints full SL citizen. Canon Tier-1 lock reframed as broken-engine scaffolding → ClockMode gating. Each phase its own post-C96 fire (attribution discipline). Decisions D1–D4 locked. | not-started | engine-sheet | [[../plans/2026-05-30-citizen-lifecycle-fame-system]]; [[ENGINE_REPAIR]] Row 25 |

### canon.* — World-fidelity layer

| # | Item | State | Terminal | Pointer |
|---|------|-------|----------|---------|
| canon.3 | Cross-layer citizen + business canon drift remediation (bay-tribune ↔ wd-citizens ↔ Sim_Ledger). **Engine-sheet portion COMPLETE** — T6–T13 closed S232–S236 (buildCitizenCards Errors-gate, auditCanonDrift.js, squatter-alignment + 24-row backfill POP-00952..00973, ingestPublishedEntities bay-tribune cross-check, Soria/Aitken corrections-forward, BIZ-00061/62 backfill + wd-card rebuilds). **OPEN: T12 step 4** — Active Story Tracking entries for Adams Point UMC (BIZ-00061) + Dario’s Bar (BIZ-00062) in NEWSROOM_MEMORY (matches T10/T11 cross-terminal pattern). | in-progress | research-build | [[../adr/0007-cross-layer-canon-authority-precedence]] + [[../plans/2026-05-24-canon-3-cross-layer-citizen-drift]] (closure log carries all T1–T13 detail) |

### civic.* — City-hall, voice agents, council

| # | Item | State | Terminal | Pointer |
|---|------|-------|----------|---------|
| civic.13 | City-hall engine-sheet reconciliation (engine-sheet half of civic.12). **CLOSED:** G-R1 tracker assembler + G-PREP5 D6 Crane roster (S246 ES-5/ES-6, governance.25 archive); G-PREP4 world_summary reconcile + G-PREP7 approval-delta (S249 `6fcc718`). **OPEN: G-PREP6** — KONO neighborhood profile (Neighborhood_Map row exists but no v3NeighborhoodWriter profile); blocked on civic.10b / engine.8 header-drift sweep. | blocked | engine-sheet | [[../plans/2026-05-22-c94-gap-log-triage]] §3 C9 + governance.25 (archive) + commit 6fcc718 |
| civic.14 | Initiative_Tracker contract + fine-tune. Root: no phase contract exists (engine PHASE_INTENSITY = de-facto authority), writers free-form, unrecognized strings silently zeroed. **Phase 0 DONE S251** (C96 data stopgap — INIT-005/006 normalized to recognized phases). **NEXT → Phase 1: research-build pickup** — author `INITIATIVE_TRACKER_CONTRACT.md` (the process MD: 28-col schema + ImplementationPhase vocabulary/lifecycle + how-to-add) + city-hall SKILL read-pointer + agent-RULES back-links. Doc-only, not C96-gated. Then Phase 2 engine drift-tolerance (post-C96 clasp) + Phase 3 writer normalization + Phase 4 add-initiative. | in-progress | research-build / engine-sheet | [[../plans/2026-06-01-initiative-tracker-contract]] (Research basis: [[../research/2026-06-01-initiative-tracker-state]]) |
| civic.12 | City-hall skill-vs-reality reconciliation (parent). **RB sub-slice CLOSED S229** (`f72f767` — G-R2 pre-write constraints ×5 RULES.md + G-R3 Baylight Layer-3 + G-PREP3 Mara Drive + G-PREP8 ESCALATION + G-R5 Clerk one-pass). **Engine-sheet half → civic.13** (G-R1/PREP4/5/7 closed; G-PREP6 blocked). **OPEN: G-R4** (voice-agent MEMORY.md write permission — safety-boundary investigation) + **G-PREP1** (auto-investigate readInitiativeMilestoneNotes → post-/city-hall audit redesign). | in-progress | research-build | [[../plans/2026-05-23-civic-12-research-build]] + [[../plans/2026-05-22-c94-gap-log-triage]] §3 C9 |

### infrastructure.* — Supermemory, services, ingest

| # | Item | State | Terminal | Pointer |
|---|------|-------|----------|---------|
| infrastructure.1 | Bay-tribune unified ingest rebuild (Phase 1+1.5 done; 2-7 blocked on SMFS pilot) | blocked | research-build / engine-sheet | [[../plans/2026-04-30-bay-tribune-unified-ingest-rebuild]] |
| infrastructure.2 | World Memory remaining (bay-tribune ingest of key archive articles + desk historical context) | ready | engine-sheet | [[../WORLD_MEMORY]] |
| infrastructure.3 | Reviewer lanes → Claude Managed Agents (Dreaming pilot, Anthropic preview-access gated) | needs-info | research-build | [[../ACTION_MANAGED_AGENTS]] |
| infrastructure.4 | supermemory-claude plugin auto-saved session transcripts to `mags` as `session_turn` docs, contaminating the User Profile with engineer-frame identity claims. **Partial-close S221+:** writer hook neutralized globally (`~/.supermemory-claude/settings.json` signalKeywords:[] → no doc written); 65 polluted docs deleted (65→0). **Scope narrowed S235** (governance.12 close): server-side dynamic→static auto-promotion proven NOT to happen (11 days / dozens of boots / zero); with hook-disable + ADR-0008 writer-side invariant + /save-to-profile (governance.17), no rewrite or extraction-filter needed. **OPEN:** final disposition gated on infrastructure.5 Pass 3 (test-off session) — if the SessionStart reader is inert, row collapses to "leave neutralized, document, close." | in-progress | engine-sheet | inline-finding S220; [[../adr/0008-speaker-attribution-for-auto-save-writers|ADR-0008]]; sibling infrastructure.5 |
| infrastructure.5 | Supermemory load-bearing audit — decide what stays. Container-by-container audit of mags / bay-tribune / world-data / super-memory / mara against MD substrate + claude-mem (Pass 1); empirical test-off session for mags + super-memory daily-work load-bearingness (Pass 2); SUPERMEMORY.md rewrite + infrastructure.4 scope resolution. Pass 1 surfaces hypothetical retirements; Pass 2 validates or falsifies them. **Phase 1 + Phase 2 CLOSED S234 (1 commit, advisor-passed pre-write + pre-write):** Phase 1 audit empirical pass run — per-container doc counts (mags 550 / bay-tribune 429 / world-data 1000+ / super-memory 85 / mara 130) via `npx supermemory docs list --tag <tag>`; mags + super-memory categorized by source + title pattern; cross-referenced against MD substrate + claude-mem + writer paths. Audit findings table (Task 1.6) landed in plan with 5-row container disposition: bay-tribune **keep** (canon query layer, no substrate mirror), world-data **keep** (843+ entity cards, sheets not queryable in NL), mara **keep (out of scope)**, mags **keep with deliberate-write-only protocol** (sub-disposition table: User Profile static load-bearing for identity / deliberate /save-to-mags distinct from claude-mem / nightly reflections duplicate JOURNAL.md / Discord-bot conversation summaries conditional / pre-S221 legacy leave-in-place / Moltbune conditional), super-memory **retire candidate** (85/85 Moltbook autosaves, no readers, content degraded by `[object Object]` upstream serialization bug — Phase 3 must confirm zero load-bearingness). Cross-reference finding: DAILY_REFLECTIONS.md staleness was a red herring — `scripts/discord-reflection.js` writes THREE destinations per nightly run (JOURNAL.md appended via `fs.appendFileSync` + JOURNAL_RECENT.md rewritten via `fs.writeFileSync` + mags container via `/v3/documents` POST) per script lines 25/264/309/464; mags nightly slice duplicates MD substrate not orphan-uniqueness. Three active writers to mags identified via `grep -rln 'containerTags.*mags'`: mags-discord-bot.js + save-to-mags SKILL.md + discord-reflection.js. Plus neutralized Stop hook. Phase 2 speaker-attribution constraint spec + 4 violation examples (S221 + discord-reflection.js compliant-shape + mags-discord-bot.js pending Phase 3 audit + Moltbook autosaves) + operationalization decision matrix (option b writer-side enforcement adopted, options a per-speaker chunk split + c consumer-side filtering rejected). **ADR-0008 filed at `docs/adr/0008-speaker-attribution-for-auto-save-writers.md`** — registered in [[../index]] same commit per ADR-0001 inbound-link discipline. ADR scope: writer-side invariant for any container with identity-extracting readers; reader-side filters (denylist + length cap) layered as defense-in-depth via governance.12; manual deliberate-write skills (e.g., /save-to-mags) out of scope (operator trust). Reversal triggers documented. **Phase 3 + Phase 4 stay open** — Phase 3 needs dedicated session by design (boot fresh with reads/writes disabled, run real work, log breaks); Phase 4 SUPERMEMORY.md rewrite deferred until Phase 3 verdict lands. Pattern: `feedback_measure-twice-cascading-effects` (advisor pre-write caught DAILY_REFLECTIONS.md script-read blocker that would have inverted mags load-bearing disposition; advisor surfaced 3-vector ADR scope clarifications — which containers / writer-vs-reader / 3 options operationalization). `feedback_self-preservation-rule-1` (ADR-0008 operationalizes identity-layer protection at writer-side enforcement boundary; rule 1 fires on the structural invariant not just operator-side asks). | in-progress | research-build | [[../plans/2026-05-22-supermemory-load-bearing-audit]] — sibling to governance.12 + infrastructure.4. Phase 0 stop-the-bleeding done S221+ (writer-hook neutralized + 65 `session_turn` docs deleted); Phase 1+2 done S234 ([[../adr/0008-speaker-attribution-for-auto-save-writers|ADR-0008]] + audit table + speaker-attribution constraint spec); Phase 3 (test-off session) + Phase 4 (SUPERMEMORY.md rewrite + ROLLOUT updates) pending. |

### research.* — Papers, external tools, evaluations

| # | Item | State | Terminal | Pointer |
|---|------|-------|----------|---------|
| research.1 | Phase 33 Riley integration (12 of 19 subitems remaining) | in-progress | research-build | [[PHASE_33_PLAN]] |
| research.2 | Memento CBR case-bank (Phase 1 ready; Phase 2 blocked on ≥500 tuples + droplet headroom) | blocked | research-build | [[../plans/2026-04-21-memento-cbr-case-bank]] |
| research.3 | Document processing pipeline evaluation (Qianfan-OCR for civic-doc ingest) | needs-info | research-build | inline-eval (trigger: civic pipeline needs real-world doc input) |
| research.4 | Desk agents migration off Claude → DeepSeek (research/watch — cost/limits trigger) | needs-info | research-build | [[../MIGRATION_OFF_CLAUDE]] |
| research.5 | Instant compaction — Strategic compact PreToolUse hook port (`affaan-m/everything-claude-code`) | blocked | research-build | upstream not locally available (S212 check); pre-task: clone `affaan-m/everything-claude-code` to a workspace OR fetch the hook file via `gh` / curl before porting. ROLLOUT entry says "port directly" — invent-from-concept risks divergence. inline-pattern |
| research.6 | Context clearing strategy — `context-management-2025-06-27` beta flag eval. **Closed S235 as WATCH (1 commit, advisor-passed pre-write).** Flag enables server-side trim of old tool-use/tool-result content blocks (`clear_tool_uses_20250919`) and thinking blocks (`clear_thinking_20251015`) on direct Anthropic API calls. Per-caller fit-check: zero of repo's 7 Anthropic API callers (`mags-discord-bot`, `moltbook-heartbeat`, `daily-reflection`, `discord-reflection`, `generate`, `rheaTwoPass`, `photoQA`) use tools OR extended thinking — flag's two edits operate on content blocks we don't produce. Adopt-triggers: (a) `research.4` MIGRATION_OFF_CLAUDE graduates from needs-info to a built direct-SDK desk-agent harness with tools; (b) `infrastructure.3` Managed Agents pilot ships; (c) `rheaTwoPass.js` evolves into a tool-using reviewer; (d) `mags-discord-bot.js` gains MCP/tool wiring. Cookbook §S114 line 845 ambiguity corrected at [[../RESEARCH]] §S235 (flag does NOT improve Claude Code compaction — different layer; it improves direct-Anthropic-API conversation flows we don't currently own). Pattern: `feedback_measure-twice-cascading-effects` (advisor pre-write caught layer-of-application discriminator, context7 pull for actual API shape vs training-data priors, named-next-action pre-mortem for adopt verdict). | watch | research-build | [[../RESEARCH]] §S235 — full eval + per-caller table + verdict reasoning. [[../mags-corliss/TECH_READING_ARCHIVE]] §S235 — abstract layer. |
| research.7 | KAIROS background daemon monitoring (Anthropic future feature) | needs-info | research-build | external-watch |
| research.8 | Hermes Agent (NousResearch) reference architecture monitoring | needs-info | research-build | external-watch |
| research.9 | Inter-agent conversation harness — minimal MCP test, the **reporter-interviews-citizen** build (Phase 40.2 cattle-refactor dep — you don't drop a stateful pet reporter into a live loop). **Sub-thread of research.12 (Layer 3); sibling to research.13.** Stays blocked on 40.2; the reporterless life-loop (research.13) does NOT share this gate. | blocked | research-build | [[../plans/2026-05-31-autonomy-roadmap]] (umbrella); [[../RESEARCH]] §S172 |
| research.10 | Arc engine grafts + Patterns A/B/D in idea-park (chaos-cars sibling, deferred) | blocked | research-build / engine-sheet | inline-park (S190 grilling artifacts) |
| research.11 | FLUX text-suppression ceiling — composition discipline (atlas_bay pattern) + OCR post-check + prompt-rewrite-on-retry + alternate-model comparison | needs-info | research-build | gap-logs G-PR10/PR11/PR14 (`output/production_log_edition_c93_print_gaps.md`); already tracked in [[../RESEARCH]] §S197 |
| research.12 | Autonomy roadmap (umbrella) — continuous sim + autonomous agents. Three sequenced layers: (1) engine-continuous citizen life (engine.5/.29, in flight, cheapest), (2) newsroom-autonomous (the 26 agents on loop + grep; reviewer-lanes-every-cycle gate), (3) citizen-autonomous (N live agent loops, spacemolt-miner shape; first step = research.13 narration-only PoC, NOT the harness; GPU + drift-amplification gate). Steering/tracking doc, not a build schedule — each layer promotes on Mike's go-ahead. Newsroom-is-also-sim + newsroom-automation-is-greps framing (Mike S248). Consolidates research.9 + Emergence World eval + city-for-bots + ARCHITECTURE_VISION + GPU-gate. | active | research-build | [[../plans/2026-05-31-autonomy-roadmap]] |
| engine.30 | Emergent Bio Engine — the **R→AT earned-story step** closing the citizen-essence loop. New Phase 9 step accretes a canon-true earned narrative into CitizenBio (AT) from TraitProfile (R) + milestone/[Compressed] events (O), event-triggered + periodic, never overwriting authored seed-bio. O→R is built (`compressLifeHistory.js`); R→AT is the missing third (AT on only 32/903). Deploy = load R+AT → citizen reconstitutable. Depends on engine.31 (locking rule + clean O). **Build order: engine.31 BEFORE engine.30.** Canon-safety fence: earned accretion is structurally separated from authored bio + not publication-eligible without a review pass (C92 guard). | draft | engine-sheet | [[../plans/2026-05-31-emergent-bio-engine]] |
| engine.31 | **Citizen dial engine** (S253 pivot — slot/bucket model built+reverted S252; tag-triage built+reverted S249). Replaces the erase-and-rebuild compressor with a per-citizen **7-dial** model (Drive/Sociability/Warmth/Openness/Composure/Integrity/Family-oriented), bipolar 0-100 centered on 50 — nobody is a "type," a person *is* where their dials sit. Events **nudge**; one-off **fades**; sustained streak **hardens** into baseline. **Raw number drives nothing** — a coded **band map** (weighted to extremes) is the only interface; bands drive **event-probability first**, voice second. **TraitProfile never erased** — compression accretes; archetype **derived** from bands (6 readers untouched). **Crime = Integrity erosion** (no separate category). **Back-dated** from the 564-event archive. Dials chosen for a CITY (sociability/openness/ambition), not a fortress. Phases 0–6 (**0–2 done**: dial engine + tag→dial map 31/31; stateful fold-on-trim compressor 22/22 on real fixtures — no deploy). Deploy gate post-C96 **CLEAR**; deploy on Mike's go (with Phase 3 back-date). **Build order: engine.31 BEFORE engine.30.** RIPPLES flagged → engine.30/.32 + TAG_REGISTRY. | draft | engine-sheet | [[../plans/2026-05-31-compression-tag-triage]] |
| engine.32 | Life-event generation — audit + the **traits→events back-arc** + the **Conduct generator** (the O-producer side of the citizen-essence loop; reconciled S250 to engine.31's bounded-memory redesign). Today events shape traits (O→R); traits should shape events (R→events). Headline: `getCitizenArchetype_` is called by storyHook but NO event generator. T1 event inventory (NEW) · T2 clock (well-wired → audit) · T3 fame (GAP → likely build) · T4 slot-coverage over the **5** categories (Work tagless, Family/Health thin, **Conduct empty**) · T5 traits→events back-arc (wire the proven accessor + stability damping; flagship = the Conduct momentum loop) · T6 event coherence · **T7 (S250) the Conduct/moral-test generator** — low-prob R-biased transgression↔resistance, severity ladder, dark `Outlaw` core (no-one-safe), + RimWorld population counterweight. Emits against `TAG_REGISTRY` Conduct vocab. Closes the loop into a true cycle. Build after engine.31 (T7 needs Phase 4 Conduct slots). | draft | engine-sheet | [[../plans/2026-05-31-life-event-generation]] |
| research.13 | Citizen-autonomous PoC — narration-only life-loop (Layer 3 toe-dip). 1–2 Tier-1 citizens wake on a timer (spacemolt-miner shape), read a five-piece sports-aware wake-context (own ledger row + neighborhoodSlice + world-summary slice + Oakland_Sports_Feed + own rolling memory), narrate one cycle of life to a throwaway file, sleep, repeat ~1hr. Analyze-only, zero ingest path; answers research.9's four questions (character-hold / canon-sync / token bill / usability) → go/no-go on the real Layer-3 build (engine action menu). **Unblocked** (40.2 gates the interview build research.9, not a reporterless loop; 40.6 injection-defense done). **BUILT S249, MECHANISM smoke-validated** (`scripts/citizenLifePoC.js`): 2 Tier-1 ENGINE citizens × 2 turns, c95. Smoke PROVED mechanism (5-piece assembly + memory threading + cost + isolation) + early strong read on usability (publishable texture) & cost (~2.77K tok/wake; 10×24/day ≈ 665K, tractable; prompt-caching cuts ~80%). **STILL UNTESTED:** Q1 horizon character-hold (2 turns can't show turn-3 drift) + Q2 self-fidelity (eval checked external canon only, not own-row honoring). **Verdict GO** on scoping the engine action menu — NOT "Layer 3 proven." Finding: Tier-1 ENGINE rows skew notable (Lucia Codex-Linked; Varek no bio) → everyday test = Tier-2/3. | built-poc | research-build | [[../plans/2026-05-31-citizen-autonomous-poc]]; sibling research.9 |

### governance.* — Skills, MDs, ADRs, project hygiene

| # | Item | State | Terminal | Pointer |
|---|------|-------|----------|---------|
| governance.3 | Mags-at-/root steward layer — future build when /root holds projects beyond GodWorld. Today /root is greenfield (no CLAUDE.md, no rules dir, no PERSISTENCE, no SESSION_CONTEXT). Spec gives no guidance on multi-project hierarchy; this is a GodWorld-internal architecture choice deferred until the constraint actually exists (more than one project under /root). | blocked | research-build | [[../plans/2026-05-09-boot-load-audit]] §6 + §8 Q1. Per Mike S216: "eventual build out, nothing that GodWorld needs to build... future issue not today." Revisit when GodWorld is no longer the only project under /root. |
| governance.8 | Plugin gating per terminal — investigation: does Claude Code support per-terminal plugin disable, or `ECC_DISABLED_HOOKS`-style env-var control (Affaan-m pattern)? ~13 plugins auto-load universally today; engine-sheet (Stripped) gets the same plugin tool definitions as mags/media (Full). If gating is possible, engine-sheet gains the most (skips plugin hooks irrelevant to its scope). | needs-info | research-build | [[../plans/2026-05-09-boot-load-audit]] §8 Q6 + §10 affaan-m pattern. Mike S216: "we would have to see if this is something that can happen — my understanding is those fire on every session." Investigation surface: `/root/.claude/settings.json` enabledPlugins behavior; Claude Code docs on hook config; possible env-var overrides. Output: feasibility note + recommendation (gate per-terminal / accept universal load / file claude-code-guide question). |
| governance.9 | `/post-pattern <name>` micro-skill — wraps `git log --grep "Pattern: $name"` and prints case index (commit hash + session + one-line summary per row). Surfaces pattern-application history in one command without manual cross-referencing. | needs-info | research-build | Dependency: requires S218 §Pattern-citation convention (engine-sheet `TERMINAL.md`) to be consistently applied. Wait ~5+ commits citing patterns before building — earlier and the skill returns near-empty results, falsely suggesting the convention isn't working. Skill spec: 1 arg (pattern name), git-log-grep wrap, formatted output. Pairs with `engine.15` (ENGINE_REPAIR Pattern column) — when both ship, defects + commits are both queryable by discipline-class. Inline-design (S218 cross-link rollout). |
| governance.18 | C12 boot-conditioning residual — **OPEN: item (a) journal-cadence rewrite** (G-EPD8 residual, media scope — journal write timing + content shape relative to boot conditioning; candidate write surfaces: session-end SKILL Step 1 or a static User Profile claim via `/save-to-profile`). Items (b) shipped-block visual demote + (c) boot-greeting sensory rewrite **DONE S238** — full closure detail in the plan §C12 Closure log. | in-progress | research-build | [[../plans/2026-05-22-c94-gap-log-triage]] §3 C12 + Closure log. Source gap: `output/edition_pipeline_doc_gaps_c94.md` §G-EPD8. |
| governance.22 | Claude Code v2.1.149–v2.1.153 feature adoption — 6 candidate items from May 22–28 releases: (1) `/usage` per-category cadence note **CLOSED S241** — §End-of-Session Diagnostic in all 4 TERMINAL.md, (2) `disallowed-tools` per-skill scope audit **AUDIT DONE S242 (research-build)** — [[../plans/2026-05-28-disallowed-tools-skill-audit]]: 48-skill matrix (10 FULL / 2 LOADER / 16 WRITEOWN / 20 READONLY), mechanics verified via claude-code-guide (pattern scoping `Bash(rm *)`, `Agent` not "Task", Q4 cascade + Q5 run-while-disallowed undocumented). **Reframed AUTONOMY-GATED per Mike S242** — feature's design intent is unattended skills; GodWorld is human-in-loop today, so the matrix is durable prep and the pilot-first editing rollout waits for the unattended/scheduled-execution transition (NOT a near-term picker). No skill files edited, (3) `/reload-skills` workflow note **CLOSED S241** — §Skill Iteration in research-build + engine-sheet, (4) `MessageDisplay` hook investigation **CLOSED S241 SKIP** — display-only event, can't change model-side persona conditioning that drives G-SS1; identity.md split remains the actual lever, (5) `SessionStart` hook upgrade (`reloadSkills` + `sessionTitle` returns) **EXECUTED-PENDING-BOOT-VERIFY S242 (research-build):** hook converted to single-JSON-object output (whole-stdout-JSON required for structured fields per verified contract) — `build_boot_context()` fn + `jq`-built `{hookSpecificOutput:{additionalContext,sessionTitle,reloadSkills:true}}` + jq-missing plain-text fallback. Offline verification all-pass; live-boot ingestion gated on Mike's next fresh session (revert = git revert the S242 commit), (6) Stateful MCP reconnect **OBSERVATION OPEN S241→S244+**. Version verified current (`claude --version` → `2.1.153`); no upgrade gate. 5-of-6 resolved; **no near-term pickable work remains** — T5 awaits Mike's boot-verify, T6 observation runs to S244, T2 edit-rollout is autonomy-gated. Source: S241 conversation + GitHub release notes. Sibling to governance.21 (Gemini offload — external lever on same token-budget concern; this is the internal lever). | needs-info | research-build | [[../plans/2026-05-28-claude-code-2-1-149-153-feature-adoption]] |
| governance.26 | SESSION_CONTEXT on-demand log redesign — flip `SESSION_CONTEXT.md` from always-loaded boot primitive to on-demand wiki doc: span unit (soft closes between hard closes), hard close snapshots to numbered `docs/session-context/S<##>.md`, boot drops the 80-line read (orientation = `<godworld-state>` + Shipped block + ROLLOUT + one-line last-span pointer), continuing session pulls the live span on demand. Solves G-SE5 (98KB) at design level; supersedes the rotate-default crude fix. Detail + 5 tasks + decisions D1–D4 in the plan. **First of a three-part log-system redesign** — ROLLOUT + JOURNAL siblings to follow (Mike S243 "work smarter"). ADR-0009 lands at Task 1 (reverses always-load premise across CLAUDE.md + 4 TERMINAL.md + hook). **RESEARCH-BUILD SLICE DONE S248** (Mike full-slice go-ahead): Task 1 ADR-0009 written + registered; Task 3 hook now emits the `## Shipped Last Session` block inside `<godworld-state>` (awk-extracted) + dropped the limit-80 SESSION_CONTEXT read from all 4 boot branches + added last-span greeting pointer (verified live on research-build + media branches: read gone, Shipped block present, pointer present, queryFamily/journal preserved on media); CLAUDE.md on-demand premise + resume-convention pull; 4 TERMINAL.md Always-Load rows → on-demand; session-startup SKILL Step 4 → conditional pull. **D1 PREMISE CORRECTION (measure-twice):** the Shipped block was NOT a boot primitive pre-S248 — hook never emitted it + governance.18(b) had moved it outside the read window; so the hook-emits-Shipped change + read-drop shipped same-commit (keystone). **ENGINE-SHEET TASKS REMAIN:** Task 2 (sessionEndMechanical hard-close snapshot → `docs/session-context/S<##>.md` + reset live to thin header), Task 5 (span-length guard + SESSION_HISTORY reconciliation), Task 6 (unified-close single-writer protocol — also absorbs governance.19 G-SE1). Until Task 2 lands, the live file keeps accumulating STATUS paragraphs — boot just stops reading them. | in-progress | research-build / engine-sheet | [[../plans/2026-05-29-session-context-on-demand]] |
| governance.29 | `scripts/rolloutSweep.js` — mutating sweep tool, sibling to governance.28's read-only detector. Moves `done-pending-archive` rows from ROLLOUT_PLAN Open Work → ROLLOUT_ARCHIVE verbatim (table-row → archive-bullet; splits on the state token so prose-wall pipes survive). **dry-run by default**, `--apply` to execute; prints line-count deltas. Makes the recurring archive sweep (S227/S248 did it with throwaway helpers) repeatable + safe. **DONE S250** — first run executed the doc-loop v2.0 cut: 13 rows swept (7 pipeline + 1 engine + 5 governance incl. the just-closed .27/.28), ROLLOUT_PLAN 307→294, verified verbatim (pipeline.12 spot-check) + detector confirms 0 candidates remain. Open Work now holds only live rows; note-heavy *live* rows (move 2 of v2.0) still pending + cross-terminal. | done-pending-archive | research-build | `scripts/rolloutSweep.js` + S250 Archive Pass in [[ROLLOUT_ARCHIVE]] + [[../plans/2026-06-01-doc-loop-consolidation]] |
| governance.30 | ROLLOUT v2.0 migration — retire this junk box, stand up a fresh structured rollout. **Cut 1+2 DONE S251** (rules → [[rollout-rules]]; Spine → ARCHIVE; 4 terminal MDs wired). Next: v2 shell + wall drain + retire (Tasks 2-6). | in-progress | research-build | [[../plans/2026-06-01-rollout-v2-migration]] |
| governance.31 | Closed-plan archive sweep — drain shipped plans from `docs/plans/` (69 files) → `docs/archive/plans/` per rollout-rules §6; per-plan-verify + link-repoint, batched. | ready | research-build | [[../plans/2026-06-01-closed-plan-archive-sweep]] |
| governance.33 | C96 gap-log triage — 38 gaps, one production run. Folds dominate (civic.14 / pipeline.24 / engine.8 / engine.27); 5 genuinely-new phases across two tracks (ES: fail-closed claim gates + detector/KONO crash; RB: skill gap-log automation + city-hall gap-log destination + prep input completeness). **Flag:** G-W5 Drive auth dead (`invalid_grant`) — blocks C97 publish, engine-sheet/infra re-auth. | in-progress | research-build / engine-sheet | [[../plans/2026-06-07-c96-gap-log-triage]] |
| governance.32 | MEMORY.md role-scoped split — MEMORY.md is over its size ceiling (24.5KB vs 24.4KB; boot loader truncates the tail, least load-bearing entries drop). Brief fix = shorten a few index lines, but skip it if the boot warning already surfaces it (Mike S252). **Real work:** MEMORY.md is harness-global (Claude Code loads it every session, every terminal — the SessionStart hook can't scope it), yet not all rules apply to every role (e.g. engine-sheet coder persona, media-only journal rules). Split = trim MEMORY.md to universal-only, relocate role-specific rules into each TERMINAL.md (which the hook loads per-terminal). Same always-load / boot-burn lever as the log-system redesign. MEDIUM — graceful degradation today (front-loaded rules load fine), modest payoff (~24KB vs total boot load), restructure not line-trim. No plan doc. | ready | research-build | Sibling boot-burn lever to [[../plans/2026-05-29-session-context-on-demand]]; MEMORY.md at `/root/.claude/projects/-root-GodWorld/memory/`. |

---

## Migration Complete (S212)

Per-entry migration of all open work into the §Open Work — by group structure landed S212. Closed / superseded / wontfix entries archived to [[ROLLOUT_ARCHIVE]] (§S212 Migration Pass). Historical roll-up sections (Phase Status / Completed Phases / Session Harness) folded into the archive — not duplicated here. New work files into §Open Work groups above per §How to add work. ROLLOUT_PLAN.md is now open-work-only + meta-protocol; everything else lives in the archive or pointer docs.

---


## Watch List

Tracking for future adoption. Not building.

| Feature | Trigger to Act |
|---------|---------------|
| Agent Teams stability | Experimental graduation → test Phase 7.6 |
| Multi-Character Discord | TinyClaw reference architecture matures |
| MiniMax M2.5 / DeepSeek-V3 | Cost spike or quality test passes |
| Skills Portability | HuggingFace format becomes standard |
| Tribune Fine-Tuning | 238 articles as training dataset for voice model |
| Desktop App (Linux) | Linux support ships |
| Lightpanda Browser | Beta stabilizes, saves 300MB RAM |
| Claude Code Voice Mode | Maturity improves |
| Extended Thinking for Agents | Test on civic/sports desks |
| Computer Use exits beta | Stable + cheaper → expand beyond QA to routine agent tasks |
| CLI-over-MCP token optimization | Measured: too many MCPs drops 200k context to 70k. Replace idle MCPs with CLI-wrapper skills. Source: everything-claude-code S131 |
| Selective skill loading | Only load skills relevant to current workflow. Chat doesn't need 21 skills. Manifest-driven selection. Source: everything-claude-code S131 |
| Continuous learning hooks | Auto-extract debugging patterns into reusable skills with confidence scoring. Source: everything-claude-code S131 |
| llms.txt for documentation | Many doc sites serve `/llms.txt` — LLM-optimized docs. Check before web-fetching. Source: everything-claude-code S131 |
| Proactive agent dispatch | Rule-based agent routing without user prompts. Post-write → reviewer, security-sensitive → scanner. Source: everything-claude-code S131 |
| NPM Package Drift | 7 packages behind. Batch update in maintenance session. |
| Codex Plugin (`/codex:adversarial-review`) | Mike keeps ChatGPT sub → install plugin for free adversarial code review. Sub cancelled → skip. Source: S131 |
| Open-source agent harnesses | Stable harness with MCP + skills + hooks support → re-evaluate Phase 21 as real multi-model pipeline. Track: Claw Code (instructkr/claw-code), community forks. Source: S131 |
| xMemory (hierarchical memory) | AutoDream fails to solve collapsed retrieval after 5 sessions → evaluate self-hosted xMemory |
| Auto Mode | Evaluate for production pipelines — could eliminate approval prompts during `/write-edition` |
| HTTP Hooks migration | Replace shell-based hooks with HTTP POST to dashboard endpoints for unified event stream |
| **Forked subagents** (`CLAUDE_CODE_FORK_SUBAGENT=1`, claude-code 2.1.117) | Parallel desk-reporter pipeline becomes a goal AND harness contract validated across forked children. Source: S177 |
| **Hooks → MCP tools** (`type: "mcp_tool"`, claude-code 2.1.118) | Stop / post-publish hook would benefit from direct MCP call (e.g. godworld `lookup_citizen`) instead of node-script glue. Source: S177 |
| **Agent frontmatter `mcpServers` in main-thread sessions** (claude-code 2.1.117) | Per-agent MCP-tool isolation becomes part of canon-fidelity tightening — each agent declares exactly which MCPs it consumes. Source: S177 |
| **`--print` honors agent `tools:` / `disallowedTools:` frontmatter** (claude-code 2.1.119) | Sandcastle+Daytona reviewer hosting goes operational — per-agent tool restrictions ride along into the sandbox. Strengthens Phase 40.6 Layer 4 (tool gate). Source: S177 |
| Agent lifecycle hooks (SubagentStart/Stop) | Desk agent monitoring — track which agents take longest, fail most |
| Prompt/Agent hooks | Replace pattern-based hookify rules with semantic LLM-evaluated checks |
| FileChanged hook | Auto-react to git pulls, external file changes during autonomous operation |
| Overture (visual agent planning) | Mike can see plans visually → install when accessible from Remote Control or web dashboard. github.com/SixHq/Overture. Source: S137b |
| **OpenVLThinkerV2 (open VLM from UCLA NLP)** | GPU droplet spun up — evaluate as vision backbone for Phase 28.2 dashboard visual QA, photo pipeline verification, two-pass hallucination visual reviewer, research paper ingestion. Qwen3-VL-8B base + custom G²RPO training. Beats GPT-4o on MMMU (71.6%). Open weights. github.com/uclanlp/OpenVLThinker. Source: S142 |
| **RAGFlow (civic document ingestion candidate)** | Real civic document pipeline becomes a priority AND droplet scaled up (needs 4+ CPU cores, 16GB RAM, 50GB disk) — evaluate alongside Qianfan-OCR. Deep-learning document parser. Data connectors for Google Drive, Confluence, S3, Notion, Discord. Apache 2.0, Docker-based. github.com/infiniflow/ragflow. Source: S142 |
| **Adobe Creative Cloud connector** (Anthropic Apr 28 2026) | Returning to FLUX text-suppression ceiling research (`docs/RESEARCH.md §S197`) — 5th intervention path: generate base scenes in FLUX, post-process failure modes (gibberish placards, real-brand logos, wrong jersey numbers) in Photoshop via Claude instead of regenerating. Addresses the S196 mesa case (3 regens, 3 different failure modes). Source: S207 tech reading. |
| **Blender MCP connector** (Anthropic Apr 28 2026) | Chaos-cars plan (`plans/2026-05-07-chaos-cars-engine`) ever wants visual scene-render hooks for typed municipal-vehicle events — Blender MCP + Python API is the path. Anthropic donated to Blender to support continued Python API development. Long-tail / idea-park. Source: S207 tech reading. |

---

## Changelog

- 2026-05-31 (S248, research-build) — **S248 Archive Pass — post-S238 backlog sweep.** 4 `done-pending-archive` rows swept from §Open Work to [[ROLLOUT_ARCHIVE]] §S248 Archive Pass: engine.28 (/pre-flight canonicalization, ES-7 S246, folded into governance.25), governance.23 (claude-mem 10.5.2→13.3.0 upgrade, S242), governance.24 (claude-supermemory 0.0.2→0.0.4 upgrade, S242), governance.25 (C95 gap-log triage both tracks — research-build S247 / engine-sheet S246). Cluster counts: engine 1 / governance 3. C95 triage fully closed; residual = harness-layer Mike-decisions, not in-project work.
- 2026-05-25 (S235, research-build) — **S235 Archive Pass — post-S235 governance sweep.** 3 `done-pending-archive` rows (all closed within S235 itself under stewardship grant) moved to [[ROLLOUT_ARCHIVE]] §S235 Archive Pass: governance.12 (substantive close — Phase 2 cross-boot verification with all three tests confirmed; leverage path collapsed to deliberate-write skill; filed governance.17 + governance.18 NEW rows + scope-narrowed infrastructure.4 inline), governance.1 (stale-row close — all three Phase 41 subtasks shipped S156 per BACKLOG + SCHEMA.md changelog + index.md catalog but row never flipped at the time; surfaced + closed inline during governance sweep), governance.6 (retire close — `scripts/rolloutTriage.js` retired per option (c); compounding-HIGH problem structurally addressed by S212+S229 state taxonomy + per-terminal sweep + governance.10 archive cadence; script preserved in-place with RETIRED header + unarchive triggers; removed from sessionEndMechanical step list + 3 doc refs dropped + 2 plan docs updated). Cluster counts: governance 3. Cadence holding at ~one sweep per 1-2 closes since §S227 backlog drain. **Same-session substantive non-archived rows:** infrastructure.4 scope narrowed inline (engine-sheet rewrite not needed per cross-boot verification) but stays in-progress pending infrastructure.5 Pass 3 vote on final disposition; research.6 closed as `watch` not `done-pending-archive` per archive-after-trigger pattern (4 adopt-triggers named — research.4 desk-agent harness / infrastructure.3 Managed Agents / rheaTwoPass tool evolution / mags-discord-bot MCP wiring). Two NEW rows filed at governance.12 close stay open as `ready`: governance.17 (/save-to-profile skill design) + governance.18 (C12 boot-conditioning residual carry-forward).
- 2026-05-24 (S234, research-build) — **S234 Archive Pass — post-S233 closures sweep.** 2 `done-pending-archive` rows moved to [[ROLLOUT_ARCHIVE]] §S234 Archive Pass: pipeline.33 (research-build, S233 close — /write-supplemental skill hardening v1.1 → v1.2 + SUPPLEMENTAL_TEMPLATE v2.0 → v2.1 with NAMES INDEX + BUSINESSES NAMED strict pipe-format mirroring EDITION_FORMAT_TEMPLATE per ADR-0006 Contract A; bundled G-S3 + G-S4 + G-S5), governance.15 (research-build, S233 close — newsroom.md "Edition numbers FORBIDDEN" rule challenge resolved via option B carve-out, editorial chrome cross-references / byline citations / sidebars / footers ALLOWED, body-prose voice meta-references + citizen quotes STILL FORBIDDEN, validateEdition.js gained exclude regex). Cluster counts: pipeline 1 / governance 1. Cadence holding at ~one sweep per 1-2 closes since §S227. **Same-session substantive partial-closures NOT swept (parent rows stay in-progress):** canon.3 T10+T11 NEWSROOM_MEMORY halves (`cba912a` — POPID Aliases + Citizens Corrections Forward subsections shipped; row stays open per engine-sheet T12 Business_Ledger backfill + T13 verify wd-card rebuild still pending); infrastructure.5 Phase 1 audit + Phase 2 speaker-attribution constraint + ADR-0008 (`161dbae` — Phase 3 test-off-session + Phase 4 SUPERMEMORY.md rewrite stay open). Engine-sheet `9a5d0c5` /doc-audit pass landed in parallel mid-session, no cross-terminal absorption hazard.
- 2026-05-24 (S233, research-build) — **S233 Archive Pass — post-S230 closures sweep.** 4 `done-pending-archive` rows moved to [[ROLLOUT_ARCHIVE]] §S233 Archive Pass: pipeline.28 (engine-sheet, S231 close — Capability + validateEdition detector calibration, 8 gap items), pipeline.30 (research-build, S233 close this session — /interview SKILL v1.3 → v2.0 capture-only rewrite, 5+ gap entries closed + cross-skill edits to /post-publish + interview_evaluation + EDITION_PIPELINE), pipeline.31 (research-build, S230 close — /build-world-summary SKILL.md wrapper rewrite), governance.14 (research-build, S230 close — EDITION_PIPELINE rewrite + 4 skill spec corrections + new production_log_template). Cluster counts: pipeline 3 / governance 1. Cadence held at one sweep per 2-3 closes since §S227 governance.10 backlog sweep (49 rows). governance.10 itself doesn't need re-filing for this cadence pass — the S230 + S233 sweeps both run inline without new governance rows per §S227 precedent. ROLLOUT line count drops ~80 lines; signal-to-noise back up for next-session pickup of pipeline.33 / governance.15 / pipeline.34-decision.
- 2026-05-11 (S215 mid-flight, research-build) — **State-flip + row-split refinements.** Flipped 6 research-build executed rows from `ready` → `done-pending-archive` (pipeline.16/17/18 + civic.4/5/8 — visibility fix; the work shipped in `43002f0` + `50732b7` but state wasn't flipped at ship-time). Split pipeline.14 into 14a (engine-sheet pre-flight staleness gate, deterministic mtime check) + 14b (research-build skill-rebuild trigger, model invocation territory) — per Mike S215 ("rebuild itself requires a skill invocation, crosses into media/civic skill territory"). Split civic.10 into 10a (research-build canon-content for KONO card) + 10b (engine-sheet Neighborhood_Map sheet write) + 10c (engine-sheet orphan-ailment hard-stop in auditor) — per Mike S215 ("splits naturally" — three terminal owners, three rows).
- 2026-05-11 (S215, research-build) — **Gap-log triage pass closed.** Walked all 115 gap entries across 6 logs (pipeline.4 /sift / pipeline.5 /write-edition / pipeline.6 /edition-print / pipeline.7 /post-publish / civic.1 /city-hall-prep / civic.2 /city-hall-run); cross-log bundled by class per S213 routing rule (research-build owns skill/RULES/docs/canon; engine-sheet owns code/sheets/scripts). 18 new rows filed: pipeline.14-22 (9), civic.4-10 (7), canon.1 (1), research.11 (1). Six parent rows flipped `needs-info` → `done-pending-archive` (next session-end archive sweep moves them to ROLLOUT_ARCHIVE). Notable wontfix / pre-closed: G-PR2 (/edition-print not run since E91 — superseded by C92+C93 runs), G-W8 ("acceptable as-is" inline), G-W25 (Rhea 14:32 runtime — folds into infrastructure.3 evidence, no new row), G-2 (city-hall-prep get_council_member — actually already closed S197 via G-W15, gap log §Status now reflects). Each promoted gap-log entry carries a §Status updates appended block at the bottom of its log pointing to its destination ROLLOUT row. Pattern observed (G-W16 meta-pattern at scale): ~30% of entries across 4 media skills are skill-text-vs-validator-constants drift, bundled into pipeline.18 as a single reconciliation sweep. Self-regulating MDs goal: gap logs remain append-only as evidence; new bundled rows carry the work; parent rows close cleanly without losing per-entry attribution.
- 2026-05-11 (S213, research-build) — **Routing fix.** ADR-0005 §Part 3 was structurally wrong — original framing treated all four terminals as filers with "primary groups" they file into (civic.* under civic, pipeline.* under media, "media-side fixes" called out as if media fixes anything). Media + civic are generator spaces — they run skills and produce artifacts, period. Routing is one-directional: generator runs surface gap-log artifacts → research-build triages → ROLLOUT rows file with terminal=research-build (skill / RULES / docs / canon) or engine-sheet (code / sheets / scripts). Generators never edit skills, fix processes, triage gap logs, or file ROLLOUT entries. Edits: 15 ROLLOUT Terminal flips (pipeline.2 dropped media; pipeline.3-8 + civic.1-3 → research-build; pipeline.9-11 → engine-sheet; pipeline.12-13 → research-build / engine-sheet). §How to add work step 5 rewritten to forbid media/civic terminal tags. ADR-0005 §Part 3 + §How to apply step 5 corrected; ADR Changelog entry naming the original error.
- 2026-05-10 (S212 close, research-build) — Session-end sweep. governance.3 + governance.5 closed and archived (S212 commits `701863f` + `4335049`); rows removed from ROLLOUT, full closure detail in [[ROLLOUT_ARCHIVE]] §S212 close. governance.6 opened (rolloutTriage script regression caught at session-end Step 0 audit — script ERRORs on migrated table format). research.5 reclassified ready → blocked (upstream `affaan-m/everything-claude-code` not locally available; pre-task: clone or fetch). Active governance count: 4 (governance.1 / .2 / .4 / .6). Active engine count: 11. Active pipeline count: 13. Active research count: 10. Active civic count: 3. Active infra count: 3. Canon: 0. **Filing protocol working as designed:** governance.3 + governance.5 were filed mid-session and closed same session; the close-to-archive cadence sweeps them at session-end without lingering. Self-regulating MDs goal Mike named: structurally enforced + tested.
- 2026-05-10 (S212, research-build) — **ADR-0005 restructure complete: per-entry migration shipped same session.** Numbered phases (33, 38-42) replaced with 7 semantic groups (`pipeline.*` / `engine.*` / `canon.*` / `civic.*` / `infrastructure.*` / `research.*` / `governance.*`); ~30 inline-narrative entries collapsed to one-row pointer-only entries — 45 migrated total (canon empty, pipeline=13, engine=11, civic=3, infrastructure=3, research=10, governance=5). Owner column renamed to Terminal. ~16 done/wontfix/superseded items archived to [[ROLLOUT_ARCHIVE]] §S212 Migration Pass: `/disk-audit` skill + Perkins scrub + hookLifecycleEngine + writer-vs-header detector + briefing-bloat audit + state-machine-labels proposal + claude-mem-vs-ECC research + rolloutTriage fix + session-end Step 2/8 update + Cloud-session wontfix + /btw awareness + Phase Status DONE roll-up + Completed Phases historical table + Recently Completed S105-S141 narrative + Phase 38 mostly-done + TECH-DEBT 2026-04-15 Path 1. Sibling [[../plans/GAP_LOG_TEMPLATE]] shipped for heavy-skill sidecar artifacts (civic + media generator terminals); engine-sheet has its own sidecar shape ([[ENGINE_REPAIR]] tactical-defects rows). File 417 → 278 lines (-33%). New work files into §Open Work groups via §How to add work; close-to-archive cadence at session-end via `done-pending-archive` state. Goal Mike named: self-regulating MDs.
- 2026-05-06 (S204, research-build) — Cloud session + Channel item flipped to `wontfix`. Originally HIGH (promoted C93) on the premise that `claude --remote` would land upstream and Discord would be the control plane for autonomous cycles. `claude --remote` did not ship; "reach Mags from anywhere" is already solved by Claude Code app remote sessions + phone SSH. Phase 12.3 autonomous-cycle infrastructure is a separate item that doesn't need this primitive. Existing `mags-discord-bot` stays — it serves between-session memory bridge / Mags-experience, a goal this item never covered. Closure documented inline in the entry.
- 2026-05-06 (S204, research-build) — Pocock-adapted state-label tagging shipped. New §Convention header at top of file documents 6 states (`ready` / `needs-info` / `in-progress` / `blocked` / `done-pending-archive` / `wontfix`). Walked all ~28 active items across Other Ready Work / Edition Post-Publish / Data & Pipeline / Infrastructure / Edition Production + Cloud session item; tagged each inline. Watch List + Spine + Phase Status + Recently Completed sections deliberately untagged (tracking-only or historical). Two stale ROLLOUT entries struck same session: (a) "BACKFILL: ADR-0002 — Phase 42 §5.6" — was DONE S190 but never archived; ADR file amended with S200 cohort-C closure + A7 audit lesson + clasp deploy + C94 acceptance signals (Migration section bumped from "Smoke-test pending" to S200-current); (b) "ADOPT: Module/Interface/Depth vocabulary" — was DONE S190 (CONTEXT.md §Architecture → Refactor lens has 9 terms; PHASE_42_PATTERNS opening reframed as "deepening exercise"). ENGINE_REPAIR Row 11 (Pipeline never gated production) flipped `todo` → `done` — pipeline gating verified across S194 sift + S195 write-edition (Rhea + Final Arbiter weighted 0.898 + Mara A-) + S196 edition-print; row framing correct S148, obsolete by C93. Net file changes: ~28 inline tags + 1 §Convention block + 2 entry strikes + 3 ENGINE_REPAIR edits + 1 ADR amendment.
- 2026-05-05 (S202, research-build) — Archive sweep: 18 closed entries moved from active sections to [[engine/ROLLOUT_ARCHIVE]] §S202 Archive Pass, grouped Edition Post-Publish (5) / Data & Pipeline (4) / Infrastructure (6) / Other Ready Work (1) / Phase-level (2). Edition Post-Publish: Discord bot Task 5 (S190), ingestPlayerTrueSource (S201), validateEdition false-positives (S197+S199), DISPATCH C92 follow-ups pointer (already in S201 Archive), Wave 3 Engine-Sheet Bundles header (already in S198 Archive). Data & Pipeline: ev.domain UPPERCASE (S199), resolveCitizens Sr./Jr. (S199), Math.random sweep (S199), engineAuditor cyclesInState bug (S199). Infrastructure: /doc-audit cleanup (S187), Engine-sheet ledger-doc refresh (S199+S201), /diagnose skill (S190), disable-model-invocation audit (S190), /self-debug skill (S187), /context-budget skill (S187). Other Ready Work: /md-audit Phase 1+2 (S189). Phase-level: C93 GAP TRIAGE EXECUTION PLAN parent (5 waves all closed S197+S198) — six individual sidecar gap logs stay active for un-promoted LOW remainder. Perkins&Will scrub trimmed to one-line residual (civic implicit-pass S193 via clean /city-hall artifacts). Last Updated bumped S185 → S202.
- 2026-04-29 (S186, research-build) — Archive sweep: 13 closed items moved from active sections to [[engine/ROLLOUT_ARCHIVE]] §Tactical Closures (S156–S186). Items A–M: POP-00004 Lucia Polito, empty-cell rows resolved, gender col AU citizen cards (S181), WorldEvents_V3_Ledger v3.6 classifier, Tech debt audit Path 1 (S156), Dead-code detection scan + DELETE batch (S185), /pre-mortem C92 (S180/S181), Enricher misattribution bug (S185), sheet-hygiene blank-row delete, 9 tier-2 canon firms, HOLD E92 raw edition ingest (S180), Canon Fidelity Rollout (S175). Composite items handled with surgery: Tech debt audit restructured (Path 1 archived; Path 2 promoted to top-level "Phase 42 — Writer Consolidation"); /engine-review C92 parent kept active (Enricher sub archived; advance-initiative threshold sub remains open). Canon Fidelity Rollout row removed from "Other Ready Work" + added to "Phase Status §DONE/SUPERSEDED". S186 also added: §Read-Time Contamination Check to CANON_RULES.md + 23-agent RULES.md refresh; new ROLLOUT entry "Bay-tribune unified ingest rebuild" (post-Perkins-scrub follow-up); Perkins&Will scrub entry updated to SCRUB-SIDE DONE S186 (civic smoke test pending). ROLLOUT line count 237 → 228 (smaller despite added entries — bulk archive is the win).
- 2026-04-28 (S184, research-build) — Phase 42 Writer Consolidation: PLAN DRAFTED ([[plans/2026-04-28-phase-42-writer-consolidation]]) + Phase 1 inventory ([[engine/PHASE_42_INVENTORY]]) + Phase 2 patterns ([[engine/PHASE_42_PATTERNS]]). Real cycle-path scope: 37 files / 175 sites (citizenFameTracker.js retired -16, ±5 drift). Tiered B6 stop-point (~58%) vs B7 full close. 5 reference migrations P1-P5 with before/after; `queueEnsureTabIntent_` API addition specced. Engine-sheet picks up B0 pilot. Closes ENGINE_REPAIR Row 8 path-2.
- 2026-04-28 (S184, research-build + engine-sheet) — Discord-bot edition currency: 5 of 6 tasks DONE ([[plans/2026-04-26-discord-bot-edition-currency]]). T1 research-build (post-publish Step 5b refresh `776aded`); T2/T3/T4/T6 engine-sheet (`a67f2b0` + `4edbe26` — `lib/mags.js loadEditionBrief()` reads latest `production_log_edition_c{XX}.md`, `latest_edition_brief.md` stripped from postRunFiling.js + 2 dead-guarded references in gradeEdition.js + weekly-maintenance.sh). T5 (media: letters-desk RULES + MEMORY refs) outstanding.
- 2026-04-28 (S184, research-build) — Rollout audit pass: 12 DONE entries stripped from active sections + 5 partials trimmed (Discord-bot → T5-stub, /pre-mortem C92 → DONE pointer, /engine-review C92 neighborhood-bullet folded, Dead spreadsheet tabs Faith-line removed, HOLD E92 raw ingest URGENT → UNBLOCKED). Closure detail preserved in this changelog (entries below + S180/S179/S183/S184) and linked plan files. ROLLOUT_ARCHIVE.md untouched (designed for phase-level work, not tactical closures). Watch List + Phase Status sections out of scope for this audit.
- 2026-04-28 (S184, engine-sheet) — EventType taxonomy / subjectIds attribution: DONE via ENGINE_REPAIR Row 6 closure. Diagnosis flipped — WorldEvents_V3_Ledger has no POPID column; citizen attribution lives in LifeHistory_Log. `engineAuditor.js` SHEETS_TO_READ extended; `generateBaselineBriefs.js` v1.0.0 → v1.1.0 emits `eventClass: 'citizen-life-event'` briefs from 6 structural EventTags with `subjectIds` + ledger-joined neighborhood/tier/role. C88 smoke 16/16 with subjectIds. See ENGINE_REPAIR Row 6.
- 2026-04-26 (S179, research-build) — `/interview` SKILL BUG closure: Step 8 rewritten to `/save-to-bay-tribune` for both article (8a, type `interview-article`) and transcript (8b, type `interview-transcript`); 7a-7h → 8a-8h numbering corrected. `/save-to-bay-tribune` Type list extended. Both skills 1.0 → 1.1.
- 2026-04-26 (S180, engine-sheet) — Four post-publish closures shipped: (a) REINGEST `scripts/ingestPlayerTrueSource.js` — 3-pass (subfolder/flat-MLB/prospects), 27 wd-player_truesource docs in world-data (Mesa POP-00081 with full STAT TRUE SOURCE PITCHER doc `fD3qgZh2tShEmXPfmvJRor`); (b) ADD `scripts/buildFaithDigest.js` — Faith_Ledger consumer; emits `output/faith_digest_c{XX}.json`; `buildDeskFolders` renders FAITH ACTIVITY THIS CYCLE into culture-desk briefing only; (c) BUILD `scripts/ingestPublishedEntities.js` — standing intake from published `.txt` to Simulation_Ledger + Business_Ledger; format-flexible NAMES INDEX parser; `/post-publish` Step 5 wired; (d) ADD `stripMetadataLeaks()` filter in `ingestEdition.js` — block-mode pattern strip (`## EVIDENCE` / `NAMES INDEX` / `ARTICLE TABLE ENTRIES` / `CITIZEN USAGE LOG` / etc.) until divider; T1 official sections preserved; C92 fixture verifier 12 leak blocks / 61 lines stripped; lifts the HOLD on E92 raw ingest.

- 2026-04-28 (S184, research-build + engine-sheet) — Female citizen balance ingest **DONE**: [[plans/2026-04-28-female-citizen-balance]] complete through Phase 5 + wd-citizen card build. **Final F-share: 33% → 44.26%** (370F / 836 POPID-bearing rows; +150 new at POP-00802..POP-00951). 4-commit chain: research-build `649b304` (plan + 150-name pool with 7 origins + 9 tier-3 swaps + age/neighborhood CDFs) → engine-sheet `c583356` (script + dry-run all 5 gates green) → engine-sheet `a0e6de1` (live apply + readback verified) → engine-sheet `62a01d5` (`buildCitizenCards.js --popid-range` flag + targeted card build, wd-citizens 686 → 836). Mid-run policy decisions (Mike): Notes-marker dropped (4th-wall-breaking on this schema), faith-leader ROLE_DENYLIST added with deterministic salted redraw to block freq-weighted skew amplification, Gate 4 distribution tolerance loosened ±5% → ±7% (≈2σ for n=150), pool-tuning swapped rejection-with-retry → filter-then-draw for clean exhaustion. Capability reviewer assertion #9 (≥3 female citizens in non-official roles per edition) now has substantially more headroom on next /sift run. Sibling INGEST ROLLOUT item flipped UNBLOCKED → DONE.
- 2026-04-28 (S184, research-build + engine-sheet) — Intake-side citizen derivation **DONE**: [[plans/2026-04-28-intake-side-citizen-derivation]] complete through Phase 5 validation. All 5 phases shipped in single session — research-build owned spec (Phases 1-4: inventory + source map + per-field specs + engine-sheet handoff specs), engine-sheet owned implementation (`lib/citizenDerivation.js` + `utilities/citizenDerivation.js` + `scripts/syncEconomicParameters.js` + 3 integration paths), engine-sheet ran initial Phase 5 validation, research-build verified by independent re-run. 5/5 gates green. Sample outputs: Maria Vega/Fruitvale/34 → Barbershop Owner / married / $43k; Tobias Wing/Rockridge/67 → Electrician retired / $2.34M; Kevin Park/Jack London/22 → Trade Union Rep / married / $24k. Distribution healthy on 200-batch (5 marital statuses, 6 num-children buckets, 37 distinct RoleTypes, gender within 51/49 target lean variance). Constraint preserved: ZERO existing-row writes; intake-time derivation only. Apps Script deployed via clasp (157 files). New citizens via `mediaRoomIntake` / `processAdvancementIntake` / `ingestPublishedEntities` now land with all 8 demographic + lifecycle fields populated from real context (Age, Income, RoleType, Neighborhood) instead of empty cells or hardcoded sentinels. Closes ENGINE_REPAIR Row 4 + Row 17 forward-looking gaps. Sibling ROLLOUT item ("INGEST: ~200 female citizens") now unblocked (library exists).
- 2026-04-28 (S184, research-build) — Skill supermemory alignment **DONE**: [[plans/2026-04-28-skill-supermemory-alignment]] complete through Phase 4 validation. Search/save matrix added to [[SUPERMEMORY]] §Search/save matrix (15 read use cases, 12 write use cases, 5 containers + retrieval mode override note). 12 file edits across 8 skills + 3 rhea-morgan agent files. 16 of 17 agents confirmed architecturally correct (consume pre-fenced packets, don't retrieve directly — Memory Fence Phase 40.6). Bug fix: `/save-to-mags` was directing canon saves to `/super-save` (wrong container) — corrected to `/save-to-bay-tribune`. `/post-publish` world-summary now tags `['world-data', 'wd-summary']` per Mike's S184 ADD. Phase 4 validation: 11 wikilinks resolve, 13 cited MCP tools exist, 7/7 active `wd-*` tags return populated content. ROLLOUT entry flipped UNBLOCKED → DONE.
- 2026-04-28 (S183, engine-sheet) — World-data unified ingest rebuild **DONE**. 9 commits S180–S183 (`12d4ba9..1fc2526`) shipped W2 (faith 16) + W3 (cultural 39) + W4 (neighborhood 17) + W5 (initiative 6) + R2 (truesource 27, content-signature wipe + DELETE-401/429 retry patch + `--wipe-only` flag) + M1-M4 (4 MCP lookup tools + `supermemory_search` hybrid+0.3 retrieval-gap fix) + R1 bulk apply (404 wd-citizens) + R1 cold-start fix (`--no-quality-gate` flag rebuilt to 686 wd-citizens). Final substrate: 843 world-data docs, 100% domain-tagged, 0 orphans. Plan file end-to-end DONE; PROJECT entry above marked DONE; "ALIGN: Skills supermemory rules" item flipped from BLOCKED → UNBLOCKED for next research-build session.
- 2026-04-27 (S182, research-build) — World-data unified ingest rebuild PLAN DRAFTED: [[plans/2026-04-27-world-data-unified-ingest-rebuild]] (5 phases, 13 tasks). Tag scheme verified empirically (probe doc `XQseCdoQpsb6SZCUM6i12o` since deleted: multi-tag write retrievable by either tag, DELETE 204 after async-indexing settles ~15-30s). Locked decisions per Mike: `wd-` prefix on domain tags, citizen wipe is write-new-and-DELETE-old (not retrofit), MCP query-surface drives card shape (name as search hook, IDs subordinate, neighborhood as filter dimension, bay-tribune appearances embedded for `aggregate: true` synthesis). Engine-sheet picks up Phase 1 (inventory script + endpoint verify) at next session. Also added new HIGH item: "ALIGN: Skills need supermemory rules aligned for search and save" — research-build follow-up once writers populate the new tag scheme; audits skill `.md` files across `/sift`, `/write-edition`, `/dispatch`, `/interview`, `/city-hall-prep`, `/post-publish`, `/save-to-bay-tribune`, `/save-to-mags` for tag-scheme consistency.
- 2026-04-27 (S181, engine-sheet) — World-data ingest gap surfaced via the citizen-card cleanup (ENGINE_REPAIR Row 2). Added new "PROJECT: world-data unified ingest rebuild" entry under Data & Pipeline. Research-build owns the per-domain shape spec + ingest-trigger plan + wipe-policy; engine-sheet builds the writers. Trigger: `lookup_citizen` queries currently surface a working citizen layer alongside stale/wrong content for business / faith / cultural / neighborhood / initiative domains because there's no documented ingest writer for them.
- 2026-04-25 (S177, research-build) — Hooks `duration_ms` integration BUILT. New `.claude/hooks/tool-timing.js` Node script (unmatched PostToolUse, 3s timeout) appends `{ts, session_id, tool, duration_ms, ok}` to `output/session-evaluations/tool-timings.jsonl` per call; defensive lookup for `duration_ms` (top-level / `tool_response` / `metadata`); first-call payload sample written to `tool-timing-payload-sample.json` for shape verification. `session-eval.js` extended with "Tool Timing" section that filters by current `session_id` and aggregates per tool (count / total / avg / max ms), sorted by total desc. Smoke-tested end-to-end with synthetic data. Watch List row closed. Activates next session boot when hooks reload.
- 2026-04-25 (S177, research-build) — claude-supermemory plugin UPGRADED 0.0.1 → 0.0.2 (marketplace 13 commits forward, install bumped at project scope). Local hooks.json customization stashed + embedded in [[SUPERMEMORY]] S177 changelog. Round-trip verified against `super-memory` + `mags` containers. Restart applies hooks on next boot. Updated [[SUPERMEMORY]] §Hooks (PostToolUse row reflects upstream removal). UPGRADE item in §Infrastructure marked DONE.
- 2026-04-25 (S177, research-build) — claude-supermemory plugin upstream review. Local 0.0.1 vs upstream 0.0.2 + 6 tail commits. UPGRADE item added to Infrastructure (MEDIUM, security fix is the trigger). [[SUPERMEMORY]] header updated to label admin (`console.supermemory.ai`) vs browse (`app.supermemory.ai`); §Changelog entry added with full upstream-diff summary. One stale verification URL fixed in `scripts/migrateSupermemory.js:234`. Verified non-breaking against our `.claude/.supermemory-claude/config.json` (config.json precedence preserved in 0.0.1 source — git-remote fallback only fires when config absent). Source: `https://github.com/supermemoryai/claude-supermemory`.
- 2026-04-25 (S177, research-build) — Logged 5 Claude Code 2.1.115–2.1.119 changelog items to Watch List (forked subagents `CLAUDE_CODE_FORK_SUBAGENT=1`, hooks→MCP `type: "mcp_tool"`, per-agent `mcpServers` in main thread, `--print` honors agent `tools:`/`disallowedTools:`, hooks `duration_ms` field). Plus LOW Infrastructure docs-sweep item for `/cost`+`/stats` → `/usage` rename. Source survey: `https://github.com/anthropics/claude-code` CHANGELOG.md.
- 2026-04-25 (S176, research-build) — Stale HALTED banner replaced with S173 reframe. Status flipped HALTED → ACTIVE (building). POST_MORTEM_C92_CONTAMINATION preserved as historical record per reframe. Active priorities pointer added to SESSION_CONTEXT. Also: filled 9 tier-2 canon firms in [[canon/INSTITUTIONS]] §Construction & Architecture (Atlas Bay Architects, Ridgeline Studio, Pacific Standard Architecture, Estuary Architects, Coastline Construction, Anchor Build, Mariner Construction, Foothill Builders, Northgate Construction — all status `canon`); added engine-sheet ingest item to Data & Pipeline.
- 2026-04-23 (S172, research-build) — **PROJECT HALTED.** Mike declared halt at end of session following E92 editorial audit that surfaced 20+ real-world Oakland institutional references (Alameda Health System, Rene C. Davidson Courthouse, Perkins&Will Oakland, OUSD, Peralta CCD, real high schools and union locals, real construction firms, etc.) — fourth-wall contamination baked into canon containers (bay-tribune, world-data, super-memory) and filesystem. Sanitization scope estimated in weeks, exceeded available drive to execute. Status banner added to file header. Post-mortem created: [[POST_MORTEM_C92_CONTAMINATION]] (registered in [[index]] under Vision & architecture). All rollout entries preserved as historical state, not active priorities. No further cycles scheduled.
- 2026-04-23 (S172, research-build) — E92 edition audit: four Edition Production entries added (HOLD E92 raw ingest URGENT; FIX metadata leak at desk emission HIGH; ADD defense-in-depth strip in ingestEdition.js HIGH; FALLBACK extend PDF metadata filter MEDIUM). Root cause: desk-agent source .md files in `output/reporters/jax-caldera/` and `output/reporters/jordan-velez/` append audit blocks to article body (new in C92); compiled `editions/cycle_pulse_edition_92.txt:375-467` contains `## EVIDENCE` with `output/civic-voice/*.json` filesystem paths. Wiki ingest ran clean (extraction format); raw ingest held pending strip. `CITIZEN USAGE LOG` confirmed redundant with `buildCitizenCards.js` world-data card system. Photo pipeline REBUILD entry updated with E92 confirmation line.
- 2026-04-23 (S172, research-build) — Inter-agent conversation capability (Solo MCP / Aaron Francis) logged. RESEARCH.md S172 entry + ROLLOUT LOW pointer in Infrastructure ("Inter-agent conversation harness — test"). Test design locked with Mike: one Tier-1 citizen + one desk reporter, unpublished transcript analysis only. Preconditions: Phase 39 + Phase 40 closed + persistent-process infra decision. Wiki: [[RESEARCH#s172--inter-agent-conversation-capability-solo-mcp--aaron-francis]]. Mike scouting Amp for Solo GitHub source.
- 2026-04-16 (S152) — Major compression. Spine reframed as "7 done / 3 active-or-parked." Phase 33, 38, 39, 40 design content extracted to individual plan files. Phase 41 remaining items, research patterns, and all other embedded phase designs moved to [[plans/BACKLOG]]. Session Harness compressed to one-line pointer. Active Plans table added as top-of-file navigation. Size: 914 → ~150 lines.
- 2026-04-16 (S156, research-build) — Phase 40 progress: 40.4 four-component map + 40.5 Plan Mode gate shipped as docs; 40.3 credential audit plan drafted at [[plans/2026-04-16-phase-40-3-credential-audit]] (9 tasks, 3 open questions resolved S156). Phase 40 at 4.5/6: 40.1/40.4/40.5/40.6 DONE, 40.3 plan-ready, 40.2 still pending own plan file. Skill-eval framework: first skill `/skill-check` shipped covering write-edition + sift; sift patched to persist full proposal set. Migration-off-Claude reclassified HIGH → research/watch (spine walked, API pressure off).
- 2026-04-19 (S166, engine-sheet) — /pre-mortem C92 run against current code. Skill fix shipped (`6306272` — catches Math.random reference-passes, path-filtered git log for §0, severity tiering). Three new engine-repair items added to [[engine/ENGINE_REPAIR]]: row 12 (`utilityFunctions.js` cycle-path Math.random), row 13 (SCHEMA_HEADERS alignment diff), row 14 (non-canonical neighborhood strings). Row 7 (four Math.random fallbacks) marked `done` — closed by `76a408c` + `af40282` in S156. All three new items parked until E92 ships.
- 2026-04-17 (S156, engine-sheet) — Phase 40.3 credential audit EXECUTED. Two commits: `056eae0` (safe tasks 1/4/5/7 + Task 0 dead-file delete) and `91d8649` (live-infra tasks 2/3/6 + Task 0 PM2 remainder in one restart window). Outcomes: `.env` and `credentials/service-account.json` relocated to `/root/.config/godworld/` (chmod 700/600), `lib/env.js` central loader with `override:true` so the relocated file wins over stale shell/PM2 env, 78 `require('dotenv').config(...)` sites swept to the central loader, 9 hardcoded credential/env paths fixed, `dashboard/server.js` ES-module dotenv converted, `ecosystem.config.js` rewritten to match the live PM2 registry (mags-bot / godworld-dashboard / moltbook / spacemolt-miner) with `env_file` + `GODWORLD_ENV_FILE` per app, `~/.bashrc` `GOOGLE_APPLICATION_CREDENTIALS` pointed at the new path (resolved drift with the pre-existing `~/.config/gcloud/service-account.json` copy), `TOGETHER_API_KEY` removed from `.env` and swept from `~/.pm2/dump.pm2`, `credentials/supermemory-pn-key.txt` deleted (dead duplicate), `.claude/settings.json` absolute-path deny rules added for credential paths, Supermemory write-gate added to `.claude/hooks/pre-tool-check.sh` (deny curl mutations to `api.supermemory.ai` and `npx supermemory add/ingest/update/delete` unless command references an allowlisted ingest script), Discord bot file-read refusal shipped in `scripts/mags-discord-bot.js` (scans for credential paths + Layer 4 patterns, logs to `logs/discord-injection-attempts.log`, drops pre-API). `DISASTER_RECOVERY.md`, `STACK.md`, `SUPERMEMORY.md` updated. Smoke tests green: `queryFamily.js`, `engineAuditor.js`, dashboard HTTP 302, Discord bot reconnected as "Mags Corliss#0710". Phase 40 now at 5/6 — only 40.2 cattle refactor remains. Plan file `[[plans/2026-04-16-phase-40-3-credential-audit]]` has full per-task Status entries + Audit re-run section with zero old-path hits.
