---
paths:
  - "phase*/**/*.js"
  - "scripts/*.js"
  - "lib/*.js"
  - ".claude/skills/run-cycle/**"
  - ".claude/skills/pre-flight/**"
---

# Engine Code Rules

Engine code skill bag for production-critical infrastructure: conservative defaults, empirical verification, caller-graph awareness, blast-radius awareness, willingness to reverse on evidence. Procedures below. (S212 — LLMs are bags of skills, not single tools. Full principle: `docs/adr/0004-skill-bag-naming-principle.md`.)

## Your standing reference docs — consult before guessing, don't wait to be told (S250)

These are your **memory** of the engine and the ledger. Reach for them reflexively at the first "how does X work / what writes column Y / what's the execution order" question — Mike should never have to point you at them. The anti-guess rule (`identity.md`) says "read the code"; **these docs are the index *into* the code — consult them first, then open the file.**

| The question | Your memory doc |
|---|---|
| How a function works / what it reads+writes / phase execution order | `docs/engine/ENGINE_STUB_MAP.md` (condensed ctx-map) → `docs/engine/ENGINE_MAP.md` (full) |
| Ledger columns, what writes a column, letters past Z, citizen-data fields | `docs/SIMULATION_LEDGER.md` + `docs/SCHEMA.md` |
| Which script reads/writes which tab | `docs/engine/SHEETS_MANIFEST.md` |
| Known defects / open repair rows | `docs/engine/ENGINE_REPAIR.md` *(already Always-Load)* |
| What connects to what (dependency trace) | `graphify query "..."` (persistent graph) |

**Load on demand, not at boot** — `ENGINE_STUB_MAP` is ~86KB; auto-loading it every session is pure boot-burn (the dominant token lever — auto-memory `feedback_token-burn-hierarchy`). The point is reflexive *use*, not always-resident.

**Keep them true in real time — that IS the maintenance mechanism, not the session-close audit (S250, Mike-direct).** A truth doc you reconcile *later* is a lying memory *now*, while you're coding against it. So: change engine structure → regenerate STUB_MAP (`/stub-engine`) **in the same commit** as the code; change rows/columns/schema → update SIMULATION_LEDGER + SCHEMA_HEADERS **in the same commit**. The doc is the working surface you and the next session code against. Proof of the failure mode: SIMULATION_LEDGER still reads "858 rows, last audited S234" vs live **903** — it drifted +45 across the S243 youth seed because the update waited for an audit instead of riding the change commit. The session-close audit table is a **backstop** for what slips through, NOT where maintenance happens. This is how memory hands off cleanly in a stripped-persona terminal: the truth docs ARE the handoff.

**Why it pays despite the in-session token cost (S250, Mike).** Truing-up the doc is a **second measure-twice pass through a different lens.** The code-side measure checks callers + blast-radius; the doc-side update checks **connections** — every consumer of the schema/function you just touched. If the doc *won't* true up cleanly, that resistance is the signal the change is **half-built** — so the discipline doesn't just record truth, it *detects* the half-built edit before it ships. That is the core failure mode this terminal exists to prevent (half-built processes, column misalignment, dead branches). Token burn *within* a session; net savings *across* sessions — the expensive failures (re-learning a drifted doc, debugging a half-built landmine cycles later) never get created. A mechanical rule gets skipped under time pressure; this why is what makes the housekeeping get done.

## Measure twice, cut once + cascading-effects review (S199 — confirmed-effective)

When Mike opens an engine-sheet session with repair/cleanup work — ROLLOUT triage, ENGINE_REPAIR rows, dead-code deletes, file renames, schema dedup, migration batches — apply this discipline at **every substantive step before any destructive operation**. Empirical caller analysis + runtime state check BEFORE the cut. Reverse when evidence contradicts the hypothesis. Document the measure-twice findings inline in commit messages so the discipline is visible.

**Why this exists:** S199 + S200 caught deep-rooted bugs that quick mechanical work would have missed: Phase A reversal (recordWorldEventsv25.js was LIVE not dead — caller graph caught it before delete); B.2→B.3 wedding-bond semantics drift (rename cascaded into wrong createBond_ binding); v1.9 off-by-one trapped pre-deploy via 5-case re-trace; Path (c) Row 18 elimination via live quant (32% blast radius, not "small minority"); **S200 cohort-C scope expansion** (S185 §5.6.6 audit had grep'd file names instead of `process*_` exposed function names → 4 of 6 "orphans" were live; advisor + caller-graph caught it before commit #1).

**How to apply (per item, in order):**

1. **READ the impl(s)** end-to-end before any destructive operation — never trust an audit's summary alone.
2. **CALLER GRAPH** — `grep -rn "fnName("` excluding defs; count + classify call sites by phase.
3. **EMPIRICAL STATE check when relevant** — `Engine_Errors` via `lib/sheets.js getRawSheetData(...)`, live ledger row counts, git log for the touched files.
4. **SHOW Mike** the triage + reasoning + cascading risks BEFORE touching code.
5. **REVERSE when evidence contradicts** the initial hypothesis (Phase A reversal is the canonical case; S200 cohort-C scope-expansion the most recent).
6. **DOCUMENT** the measure-twice findings inline in commit messages — visible discipline is part of the work product.
7. **For multi-commit batches**, re-run audits between commits to confirm the deltas (collision-audit script ran after every Phase B.x to confirm count drops).

Full case studies + rationale: `/root/.claude/projects/-root-GodWorld/memory/feedback_measure-twice-cascading-effects.md`. Read once if context allows; otherwise the 7-step checklist above is sufficient.

## Engine rules

- 100+ script system with cascade dependencies. Check what reads/writes affected ctx fields before editing.
- Never use `Math.random()` — use `ctx.rng` for deterministic simulation runs.
- Never write directly to sheets — use write-intents (`ctx.writeIntents`). Only `phase10-persistence/` files execute sheet writes.
  **Documented exceptions (direct sheet writes outside Phase 10):**
  - Phase 1 engine core: `godWorldEngine2.js` — exception scope narrowed S237 Phase 42 §B5 audit-miss 3 per-site classification: (a) `Engine_Errors` insertSheet + header appendRow (schema-setup §1.1 carve-out, ≤1× per spreadsheet lifetime); (b) `Engine_Errors` error-row appendRow (error-path transactional carve-out — cannot defer to Phase 10 since Phase 10 failures must persist their own log); (c) `Intake` clearContent (operator-source-clear carve-out, transactional with `processIntake_` stage-then-clear pattern, same class as healthCauseIntake §11). Cycle-path direct writes that previously appeared in this entry are now resolved: `World_Population` (`appendPopulationHistory_` DELETED S237 — was dead since v3.2, two removal markers L438/L1738 already in place); `Riley_Digest` (`writeDigest_` migrated to `queueAppendIntent_` in S237 cohort-close commit — appendRow → intent + dryRun guard dropped. Companion file `phase09-digest/applyCycleWeightForLatestCycle.js` DELETED in same commit — was a v2.3 two-step wrapper [(a) call applyCycleWeight_ for signal (b) write CycleWeight/Reason/Score + 6 calendar cols to Riley_Digest via getLastRow+lastIndexOf]. Empirical sheet check exposed: (1) Riley_Digest only has 28 cols — CycleWeightScore + all 6 calendar cols don't exist in schema → header-guard zeroed those writes silently; (2) cycle-order bug — Phase 8 ran the wrapper BEFORE Phase 10 wrote this cycle's row, so lastIndexOf(cycle) missed → fallback to lastRow → corrupted PREVIOUS cycle's CycleWeight + Reason. Fix: cycle-phases sites now call `applyCycleWeight_` directly (signal-only, no sheet write); writeDigest_ at Phase 10 writes the canonical row including weight/reason from ctx.summary populated in Phase 8. Net result: previous-cycle corruption stopped, dead calendar writes removed, 1 wrapper file retired); `Simulation_Ledger` (processIntake_ ledger.rows push via shared ctx.ledger per Phase 42 §5.6, no longer direct-write). Also resolved S237: stub `applyCycleWeightForLatestCycle_` (was here at L1306) DELETED — collided with canonical v2.3 in phase09-digest/, surfaced when audit script CLASP_DIRS was corrected (had stale `phase09-rollups`/`phase11-misc` entries → blind spot, fixed S237). (`advanceSimulationCalendar.js` direct writes to Simulation_Calendar migrated to `queueCellIntent_` in S236 Phase 42 §B5/2 P3 — removed from exceptions list.) `initSimulationLedger.js` (Simulation_Ledger — single-cycle read into shared `ctx.ledger`, Phase 42 §5.6 redesign S188)
  - (Phase 2 world state: `applyEditionCoverageEffects.js` direct write to Edition_Coverage_Ratings migrated to `queueCellIntent_` in S236 Phase 42 §B5 P3 — removed from exceptions list.)
  - Phase 3 population: `applyDemographicDrift.js` (World_Population), `updateNeighborhoodDemographics.js` (Neighborhood_Demographics), `updateCrimeMetrics.js` (Crime_Metrics), (`finalizeWorldPopulation.js` 17-site direct writes to World_Population migrated to `queueCellIntent_` in S236 Phase 42 §B5/3 P3 — removed from exceptions list.) `generateMonthlyDriftReport.js` (World_Drift_Report — monthly drift summary — **UNWIRED S236**: zero cycle-path callers, retained as manual Apps Script editor diagnostic tool; B5 mechanical migration skipped per Phase 42 §3.5 S236 audit-miss subsection)
  - Phase 3 helper: `updateCityTier.js` (caller-passed sheet — column-update helper, write target decided by caller)
  - Phase 4 event generators: `generateGenericCitizenMicroEvent.js`, `generateGameModeMicroEvents.js`, `generationalEventsEngine.js` — write LifeHistory_Log + Simulation_Ledger citizen event rows (`generateCitizenEvents.js` deleted S185 — was dead duplicate of phase05's `generateCitizensEvents.js`)
  - Phase 4 helper: `worldEventsLedger.js` (caller-passed sheet — event ledger append helper)
  - Phase 5 Tier-5 engines: `householdFormationEngine.js`, `generationalWealthEngine.js`, `educationCareerEngine.js` — write to their own tracking sheets
  - Phase 5 SL writers: `generateCitizensEvents.js`, `generateNamedCitizensEvents.js`, `checkForPromotions.js`, `runEducationEngine.js`, `runAsUniversePipeline.js` — write to Simulation_Ledger + logs
  - Phase 5 civic: `civicInitiativeEngine.js` (Initiative_Tracker), `updateCivicLedgerFactions.js` (Civic_Ledger), `generateMonthlyCivicSweep.js` (Civic_Sweep), `updateCivicApprovalRatings.js` (Civic_Office_Ledger + Initiative_Tracker — approval recomputation), `generateCivicModeEvents.js` (Civic_Office_Ledger + LifeHistory_Log + Simulation_Ledger — civic-mode citizen events), `runCivicRoleEngine.js`, `runCivicElectionsv1.js` (Election_Log schema-setup carve-out per Phase 42 §1.1 — `insertSheet` + headers + `setFrozenRows` fire ≤1× per spreadsheet lifetime, outside cycle-write path. Cycle-path writes migrated to `writeIntents` S229: L394 `queueAppendIntent_(Election_Log)` + L432 `queueRangeIntent_(Civic_Office_Ledger)`)
  - Phase 5 citizen life engines: `runHouseholdEngine.js`, `runRelationshipEngine.js`, `runNeighborhoodEngine.js`, `runCareerEngine.js`, `bondEngine.js` (Relationship_Bond_Ledger L1469 Phase-10-location carve-out per Phase 42 §3.5 — read-state-then-write hazard inapplicable since no later phase reads the tab via getDataRange between this write and end-of-cycle; L1417 dead-fallback REMOVED S229, replaced with fail-loud throw when `ensureSheet_` helper missing), `seedRelationBondsv1.js`, `bondPersistence.js` (Relationship_Bonds + Citizen_Directory + Neighborhood_Map — relationship/household/career lifecycle), `migrationTrackingEngine.js` (Household_Ledger + Neighborhood_Map + Simulation_Ledger), `gentrificationEngine.js` (Neighborhood_Map)
  - Phase 5 citizen generators: `generateChicagoCitizensv1.js` (Chicago_Citizens — **DISABLED S229, retained for reversibility**: Path B no-grow-legacy mirror of S205 generateGenericCitizens disable; Chicago canonically dead in sim. Both `safePhaseCall_(ctx, 'Phase8-ChicagoCitizens', …)` + `safePhaseCall_(ctx, 'Phase10-Chicago', …)` sites commented out at `phase01-config/godWorldEngine2.js` production + cycle-phases entry points. Pool frozen at ~124 rows in Chicago_Citizens sheet; cycle-path SEED/MAINTAIN/CHURN sites at L78/L102/L136 unreachable. `createChicagoCitizensSheet_` L173 lazy-create stays direct as schema-setup carve-out per Phase 42 §1.1 — only fires if tab is ever deleted, which is downstream of phase-disable anyway), `generateGenericCitizens.js` (Generic_Citizens — **DISABLED S205, retained for reversibility**: Path B no-grow-legacy; SL is single source for citizens; both `safePhaseCall_(ctx, 'Phase5-GenericCitizens', …)` call sites commented out at `phase01-config/godWorldEngine2.js:192` + `:1501`), `processAdvancementIntake.js` (Advancement_Intake tabs + Generic_Citizens + Simulation_Ledger + LifeHistory_Log — citizen promotion pipeline; GC paths inert post-S205, retire pending C94+C95 monitor), `generateMediaModeEvents.js` (LifeHistory_Log + Simulation_Ledger)
  - Phase 6 tracking: `arcLifecycleEngine.js` (DELETED S199 commit `bbdca3a`), `processArcLifeCyclev1.js` (Story_Arcs), ~~`hookLifecycleEngine.js`~~ (DELETED S201 commit `954d994` — was dead since landing), `storylineHealthEngine.js`, `updateStorylineStatusv1.2.js` (Storylines), `applyMigrationDrift.js` (Neighborhood_Map), `economicRippleEngine.js` (World_Population — S244 G-ER6 correction: writes World_Population at L691, NOT Population_Stats; Population_Stats tab does not exist)
  - Phase 7 media intake: `mediaRoomIntake.js` (cycle-path `processMediaIntake_(ctx)` via Phase 11 `safePhaseCall_` L467/L1762 + shared operator path `processMediaIntakeV2()`), `mediaRoomBriefingGenerator.js` — write Press_Drafts, Media_Ledger, Storyline_Tracker, Cultural_Ledger, MediaRoom_Paste, Media_Intake, Media_Briefing + citizen/advancement/continuity intake tabs (media pipeline from paste → structured tabs). **`parseMediaRoomMarkdown.js` — OPERATOR-FIRED carve-out (S257 B7 per-file analysis):** all entry points (`parseMediaRoomMarkdown`/`parseAndProcessMediaRoom`/`diagnosePasteSheet`) self-open the spreadsheet, use `SpreadsheetApp.getUi().alert`, take no ctx, ZERO cycle-path callers (no `safePhaseCall_`) — same class as continuityNotesParser/healthCauseIntake. writeIntents migration N/A (intents would queue with no Phase 10 commit). Direct writes appropriate. Confirmed via caller-graph + claude-mem #29167. Pattern: feedback_measure-twice-cascading-effects
  - Phase 7 tracking: `citizenFameTracker.js` (Chicago_Citizens + Citizen_Media_Usage + Cultural_Ledger + Generic_Citizens + Simulation_Ledger + Storyline_Tracker — fame score propagation), `storylineWeavingEngine.js` (Storyline_Tracker)
  - Phase 7 helpers: `updateTrendTrajectory.js`, `updateMediaSpread.js` (caller-passed sheet — column-update helpers). `culturalLedger.js` removed from exceptions S237 Phase 42 §B6 — all 9 sites (8 UPDATE setValue + 1 CREATE appendRow) migrated to `queueCellIntent_` + `queueAppendIntent_` v2.4 → v2.5; per-cycle `ctx.summary.culturalRegistry` resolves same-cycle multi-register dedup hazard exposed by snapshot-vs-pending-intent visibility gap (caller-graph caught what the audit summary missed — Pattern: feedback_measure-twice-cascading-effects).
  - Phase 8: `v3NeighborhoodWriter.js`, `v3DomainWriter.js`, `v3ChicagoWriter.js` — tracking sheets
  - Phase 9: `applyCycleWeightForLatestCycle.js`, `applyCycleWeight.js` — Cycle_Weight tab
  - Phase 11 maintenance: `utilities/archiveLifeHistory.js` (`maintainLifeHistoryLog_` cycle-path entry — **transactional read-modify-delete carve-out, engine.38 S264**: trims LifeHistory_Log by appending old rows to LifeHistory_Archive + `clearContent`/`deleteRows`/`setValues` rewrite of the active tab. Cannot route through `writeIntents` — intents are append/cell, not delete-rows/full-rewrite. Runs as the final Phase-11 `safePhaseCall_` AFTER all reads, row-count-gated [CYCLE_TRIGGER_ROWS], so no later phase reads the tab between the trim and end-of-cycle. Same class as the operator-fired intake carve-outs; the manual `archiveLifeHistory`/`archiveLifeHistoryDryRun` entry points remain. Moved from `maintenance/` [claspignored] so the wiring actually deploys.)
  - Phase 11 media intake: `healthCauseIntake.js` (Health_Cause_Intake + Health_Cause_Queue + Simulation_Ledger — health-cause pipeline — **OPERATOR-FIRED carve-out S236**: runProcessHealthCauseIntake L527 manual UI trigger with stub ctx, no Phase 10 commit path; direct writes required for operator workflow; B5 mechanical migration skipped per Phase 42 §3.5 S236 audit-miss 2), `continuityNotesParser.js` (Continuity_Intake + Raw_Continuity_Paste — continuity notes pipeline — **OPERATOR-FIRED carve-out S237 Phase 42 §B7 audit-miss**: all three entry points are manual UI triggers — `parseContinuityNotes` + `setupContinuityParser` use `SpreadsheetApp.getUi().alert`, `parseContinuityText` is programmatic-API alternative — none take ctx, none on cycle-path. Plan §B7 classified this file as mechanical (4-file batch, 65 sites total); per-file analysis on the smallest of the four (13 sites / 383 lines) reverses to full carve-out same class as healthCauseIntake. §2.2 read-state-then-write hazard at L90 (`getLastRow() + 1`) bounded to single manual invocation — no cycle-path concurrent intent overlap, so the Phase 2 §2.2 Option A wiring (`queueAppendIntent_` migration) is unnecessary at the cost-benefit floor; would require invasive ctx-into-UI-trigger wiring. Surfaced via same cohort-C pattern as B5 audit-miss 3 — plan's "mechanical batch" framing missed file-level classification work)
  All non-exception direct writes are bugs. Phase 40.3 Path 1 audit (S156, 2026-04-17) verified all 38 previously-undocumented writers against `docs/engine/tech_debt_audits/2026-04-15.md` — every file writes to an engine-owned tracking or intake tab consistent with existing patterns; zero were bugs. See `docs/engine/tech_debt_audits/` for latest scan.
- Always check `ctx.summary` and `ctx.snapshot` field dependencies before modifying any phase function. Engine files alias `ctx.summary` as `S` — search for both patterns. See `docs/engine/tech_debt_audits/` for orphan field inventory.
- Engine phases execute in order (phase01 through phase11). Each phase reads ctx fields written by earlier phases.
- Test changes against the pre-commit hook: no `Math.random()`, no direct sheet writes outside persistence, no engine language in media files.
- **No maintenance scripts for ledger work.** Use the service account (lib/sheets.js) directly to read and write data. Scripts add conditional logic that silently skips rows. Direct writes are transparent — they work or they don't.
- **Verify after every write.** Read the live sheet data back and confirm values landed. Never report work as "complete" based on script output alone.
- **Depth over speed.** The user is not asking for fast. They are asking for correct and verified.
- **Math.random() fallbacks are violations.** FIXED S156 (Phase 40.3 Path 1): every fallback site now throws instead of silently falling back to Math.random. Closed: `phase04-events/generationalEventsEngine.js` (initRng_, rand_), `phase05-citizens/generateGenericCitizens.js` (inner rand()), `utilities/v2DeprecationGuide.js` (v3Random_ — had zero callers, throws if ever invoked).
- **Tech debt audits live in `docs/engine/tech_debt_audits/`.** Run `/tech-debt-audit` every 3-5 sessions.
- **Engine health commands:** `/health` (quick 30s check), `/ctx-map` (field dependency map), `/deploy` (clasp push + verify), `/pre-mortem` (full pre-cycle scan), `/tech-debt-audit` (comprehensive periodic scan).
- **Post-write hook catches engine violations at edit time.** Math.random() in engine files, new unrecognized ctx.summary fields, and simulation language in agent files are all flagged automatically.
