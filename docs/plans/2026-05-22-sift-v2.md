---
title: /sift v2 Rebuild Plan — Reporter-Agency + Sheet-Primary + Civic-Opt-In
created: 2026-05-22
updated: 2026-05-22
type: plan
tags: [pipeline, sift, edition-pipeline, draft]
sources:
  - "docs/plans/2026-05-22-c94-gap-log-triage.md §3 Cluster C2"
  - "output/production_log_edition_c94_sift_gaps.md (G-S1, G-S2, G-S3, G-S5, G-S8, G-S11, G-S13, G-S21)"
  - "output/production_log_edition_c94_write_gaps.md (G-W30, G-W31, G-W32, G-W33, G-W39)"
  - "output/production_log_edition_c94_print_gaps.md (G-PR2)"
  - ".claude/skills/sift/SKILL.md — current v1.2"
  - "docs/media/brief_template.md — pre-reporter-agency template"
  - "docs/adr/0005-rollout-plan-structure.md — pointer-only entries"
pointers:
  - "[[../engine/ROLLOUT_PLAN]] — pipeline.24 row points here"
  - "[[2026-05-22-c94-gap-log-triage]] — parent triage master"
  - "[[../EDITION_PIPELINE]] — pipeline overview"
  - "[[../adr/0006-parser-validator-format-contracts]] — Pattern A applies to brief-format contract"
  - "[[SCHEMA]] — doc conventions"
  - "[[index]] — register in same commit"
---

# /sift v2 Rebuild Plan — Reporter-Agency + Sheet-Primary + Civic-Opt-In

**Goal:** Replace /sift v1.2 with v2 — a brief generator built for the reporter-agency model that reads sheet-primary inputs, treats civic content as one slice (not the spine), emits canonical brief shape with newspaper-section ordering + cadence caps + dispatch.json + Memory-Fence wrap, and triages by fold / covered-by-feature vocabulary rather than slate variants.

**Architecture:** /sift v1.2 was built for a "city-hall paper" load-out — civic content over-weighted, world_summary as primary input, brief_template assumes desk-curated citizen pre-rolls. S220–S222 surfaced the model mismatch: 5-variant slate loops, JSON pre-approval ambiguity, multi-article-per-reporter briefs, letters-desk candidate-pool collisions with rest-cycle rule, no dispatch.json emission, untitled proposals breaking djDirect title-matching. v2 inverts: sheet primary, world_summary orientation-only, civic as one of six sections, reporter-agency briefs (signal + voice direction), cadence cap baked into slate selection, mandatory headline + section + reporter assignment per proposal, dispatch.json + Memory-Fence wrap emitted as deterministic outputs of Step N.

**Terminal:** research-build (skill text + template rewrite, multi-session). Upstream sheet-primary + canon archive + NEWSROOM_MEMORY load-bearing — work depends on `lib/sheets.getSheetData()` already in place (verified S221).

**Pointers:**
- Prior work: `.claude/skills/sift/SKILL.md` v1.2 (current state)
- Brief template: `docs/media/brief_template.md` (pre-reporter-agency, drift surface)
- Triage master: [[2026-05-22-c94-gap-log-triage]] §3 C2
- ADR-0006: [[../adr/0006-parser-validator-format-contracts]] — every brief-format contract either ships canonical exemplar OR fails loud
- Related cluster: C3 (world_summary fidelity) — sift Step 1 sheet-primary discipline pairs with engine-side world_summary cleanup
- Related cluster: C4 (format-contract docs) — sift's dispatch.json + brief contract land together with /write-edition's compile template
- Related cluster: C7 (canon drift) — sift Step 4 enrichment embeds employer biz in citizen brief to close G-W35 fabrication surface
- S215 brief-led mode (validated G-W41) — don't roll back; v2 keeps brief-led, fixes upstream brief production

**Acceptance criteria:**
1. /sift v2 SKILL.md reads sheet primary (`Oakland_Sports_Feed`, `Riley_Digest`, `Simulation_Ledger` + initiative tracker) as Step 1, with world_summary downgraded to orientation-only.
2. Brief shape canonicalized: one brief per article (no multi-article-per-reporter files), each carries headline + newspaper-section + reporter assignment + 1-paragraph signal + voice direction + Memory-Fence wrap.
3. dispatch.json emitted by /sift Step N with reporter→article→file mapping; /write-edition reads it directly (no fallback title-matching).
4. Cadence caps baked in: per-reporter max + per-newspaper-section cap; civic content capped at 2-3 articles (one slice, not spine).
5. Letters-desk receives candidate-pool brief (canonical shape: letters-candidates.md), with explicit rest-cycle awareness — letters-desk LENS still owns final selection (S215 self-defense pattern preserved).
6. Triage vocabulary: `fold` (combine two proposals into one), `covered-by-feature` (drop because section feature absorbs it), `defer-to-supplemental` (interview/dispatch handles it) — eliminates 5-variant slate loops by giving structured reduction primitives.
7. Brief omits prose-template body; replaces with bullet-form signal + 3-5 voice-direction lines (no skeletonized article).

---

## Tasks

Tasks are sequenced; later tasks depend on earlier ones. Estimate: 2–3 research-build sessions total.

### Task 1: Audit /sift v1.2 against gap-cluster mapping

- **Files:**
  - `.claude/skills/sift/SKILL.md` — read
  - `docs/media/brief_template.md` — read
- **Steps:**
  1. Map every gap in C2 (G-S1, S2, S3, S5, S8, S11, S13, S21, W30-33, W39, PR2) to the specific section of SKILL.md or brief_template.md that produces the behavior.
  2. Write `output/sift_v2_gap_map.md` — gap-ID → file/line → defect summary.
- **Verify:** Map covers all 13 gaps; every entry has file + line ref.
- **Status:** [x] DONE S228 — gap-map at `output/sift_v2_gap_map.md` (14 gaps mapped; plan §sources listed 13, cluster actually carries 14 including G-W39 + G-PR2 + cross-cutting findings)

### Task 2: Draft v2 Step list

- **Files:**
  - `docs/plans/2026-05-22-sift-v2.md` — append §v2 Step List
- **Steps:**
  1. Enumerate Steps 0–N for v2: Step 0 staleness gate retire (per C1 + G-S11), Step 1 sheet-primary read, Step 2 canon archive + NEWSROOM_MEMORY load, Step 3 candidate generation, Step 4 enrichment (employer biz, three-layer threading), Step 5 triage with fold/covered-by/defer vocabulary, Step 6 cadence-cap + section-cap enforcement, Step 7 brief emission (canonical shape), Step 8 dispatch.json emission, Step 9 Memory-Fence wrap, Step 10 letters-candidates emission.
  2. For each step, name input artifacts + output artifacts + verification probe.
- **Verify:** Each step has explicit IO; no `TBD`; no `similar to vN`.
- **Status:** [x] DONE S228 — §v2 Step List appended below. 11 steps (0-10); Step 0 retired per pipeline.23; each step carries I/O/V triple + named defect closures. Cross-skill dependencies + Q resolutions named.

### Task 3: Brief template v2 draft

- **Files:**
  - `docs/media/brief_template_v2.md` — create
- **Steps:**
  1. Replace prose-template body with: headline (mandatory) / section (FRONT_PAGE | CIVIC | NEIGHBORHOODS | SPORTS | BUSINESS | LETTERS | QUICK_TAKES | EDITORS_DESK) / reporter (single, role=reporter at source) / signal (1 paragraph: what happened + why now + three-layer angle) / voice-direction (3-5 bullets: tone, pacing, what to lean into, what to avoid) / canon-pointers (citizen POPIDs + business BIZIDs + initiative IDs the reporter must respect) / canonical-exemplar reference link.
  2. Ship one exemplar brief file alongside (`docs/media/brief_template_v2_exemplar.md`) per ADR-0006 Contract A.
- **Verify:** Template has zero prose body; exemplar parses against any future brief validator without manual reformat.
- **Status:** [x] DONE S228 — `docs/media/brief_template_v2.md` (spec) + `docs/media/brief_template_v2_exemplar.md` (canonical exemplar per ADR-0006 Contract A) shipped. v1 `brief_template.md` carries DEPRECATED banner; stays in tree until SKILL.md v2.0 (Task 6) removes the last reference. Three variants in exemplar: main article / quick-take / letters candidate pool. Section enum locked to underscored routing form (FRONT_PAGE | EDITORS_DESK | CIVIC | CULTURE | BUSINESS | SPORTS | OPINION | LETTERS) — display label derived in /write-edition rendering. QUICK_TAKES + NEIGHBORHOODS removed from plan's allowlist (not in [[EDITION_FORMAT_TEMPLATE]] — QT routes into parent section per pipeline.26; neighborhood pieces use CULTURE). QT brief path corrected to `output/quick-takes/c{XX}_QT{N}_brief.md` (QTs may be reporter-less; per-reporter dir was wrong shape).

### Task 4: dispatch.json schema spec

- **Files:**
  - `docs/media/dispatch_schema.md` — create
- **Steps:**
  1. Define dispatch.json shape: `{cycle, generatedAt, articles: [{briefFile, reporter, desk, section, slot, headline}], letters: [{candidatesFile, deskAssigned}], quickTakes: [...], notes}`.
  2. Ship canonical exemplar `output/examples/dispatch_c{N}_example.json` per ADR-0006 Contract A.
  3. Name the downstream consumers (write-edition Step 1, djDirect Step 0) and required field set per consumer.
- **Verify:** Schema referenced by both /sift v2 emitter spec and /write-edition consumer spec.
- **Status:** [x] DONE S228 — `docs/media/dispatch_schema.md` (STRICT SCHEMA contract class per ADR-0006) shipped + canonical exemplar at `docs/media/examples/dispatch_canonical_example.json` (placeholders) + worked example at `docs/media/examples/dispatch_c94_worked_example.json` (C94 real values). Top-level shape + articles[] + quickTakes[] + letters reconciled with existing /write-edition Step 1 expectations (`brief_path` → `briefFile`, `desk_agent` → `desk`, `voice_directive` → `voiceDirective`). Three downstream consumer requirements documented (/write-edition Step 1 / djDirect Step 0 / reviewer lanes). Validation rules + Contract B fail-loud requirements named. Closes G-W30 (dispatch.json not emitted) + G-PR2 (untitled-title break) structurally. **Audit-the-audit catch:** initial draft placed exemplars in `output/examples/` per plan §Task 4 path — but `output/` is gitignored. ADR-0006 §Decision Contract A requires "stable path"; gitignored files don't satisfy. Moved both JSONs to `docs/media/examples/` (new dir) before commit; updated all pointers in schema doc.

### Task 5: Triage vocabulary spec

- **Files:**
  - `docs/media/sift_triage_vocabulary.md` — create
- **Steps:**
  1. Define `fold(proposal_a, proposal_b) → merged_proposal` — when to fold (shared anchor citizen + same week + complementary angles).
  2. Define `covered-by-feature(proposal, feature_id)` — when section feature absorbs (front-page feature + civic round-up + sports recap absorb candidates).
  3. Define `defer-to-supplemental(proposal, target=interview|dispatch|wiki)` — when out-of-scope for edition but valid for adjacent artifact.
  4. Provide 3-5 worked examples from C93/C94 proposals.
- **Verify:** Vocabulary used in Step 5 of v2; eliminates "variant A / B / C / D / E" loops by giving structured reduction primitives.
- **Status:** [x] DONE S228 — `docs/media/sift_triage_vocabulary.md` (SPEC contract class) shipped. Six decisions formalized: `promote` / `publish-as-baseline` / `suppress` / `fold(into=<pieceId>)` / `covered-by-feature(by=<pieceId>)` / `defer-to-supplemental(target=<interview|dispatch|wiki>)`. JSON shape + required-field rules per decision. Decision-tree diagnostic for triage flow. Five worked examples (3 real C94 baselineDecisions + 2 synthetic for v2-only decisions). What v2 eliminates from C94 ad-hoc usage named. Cross-spec dependencies to brief_template_v2 + dispatch_schema + non-edition publishing pipeline documented. Closes G-S13 (triage vocabulary gap).

### Task 6: SKILL.md v2 draft

- **Files:**
  - `.claude/skills/sift/SKILL.md` — modify (v1.2 → v2.0)
- **Steps:**
  1. Replace Step 0–N per Task 2.
  2. Reference brief_template_v2.md + dispatch_schema.md + sift_triage_vocabulary.md (Task 3/4/5 outputs).
  3. Add cadence-cap section: per-reporter max from `.claude/agents/REPORTER_DESK_INDEX.md`, per-section cap configurable.
  4. Letters-desk handling: emit candidate-pool brief; explicit rest-cycle awareness note; final selection deferred to letters-desk LENS.
  5. Memory-Fence wrap emission at Step 9 (deterministic, scripted).
- **Verify:** v2.0 SKILL.md cold-reads and runs cleanly; no inline TBDs; references resolve.
- **Status:** [ ] not started

### Task 7: Dry-run on C94 inputs (regression check)

- **Files:**
  - `output/sift_v2_dryrun_c94.md` — create
- **Steps:**
  1. Manually walk v2 Steps 0–N against the C94 inputs that produced the broken C94 sift.
  2. Confirm v2 would have caught: Paulson framing fabrication (Step 1 sheet primary), Civis Systems naming as anchor (Step 2 canon archive + Riley_Digest), Beverly Hayes POPID drift (Step 4 enrichment cross-check), letters rest-cycle (Step 10 letters candidate-pool not assignment).
  3. List remaining failure modes v2 does NOT close — file as follow-up.
- **Verify:** Dry-run report documents the v1.2 → v2 delta on a known-failure cycle.
- **Status:** [ ] not started

### Task 8: Live-run on next available cycle (C95)

- **Files:**
  - `output/production_log_edition_c95_sift_gaps.md` — create at run-time
- **Steps:**
  1. Run v2.0 on C95 with gap-log discipline (S215 brief-led pattern).
  2. Compare against C94 gap counts: target <5 HIGH (C94 had 14 HIGH + 4 MED + 3 LOW).
  3. File any new gaps as either v2-revision items OR genuine new findings.
- **Verify:** C95 sift gap-log HIGH count materially lower than C94.
- **Status:** [ ] not started

---

## v2 Step List

Drafted Task 2, S228. Each step names input artifacts (**I:**), output artifacts (**O:**), and verification probe (**V:**). No TBDs, no "similar to v1.x" references. Steps 1-6 build the slate; Step 6 carries the Mike approval gate; Steps 7-10 emit artifacts.

### Step 0 — RETIRED (G-S11 / pipeline.23, S225)

v2 omits Step 0 entirely. Confirmation only — staleness gate retired per pipeline.23. Canonical sequencing is world_summary → city-hall-prep → city-hall → /sift → /write-edition; no per-step staleness check needed downstream.

### Step 1 — Sheet-primary read

**Closes:** G-S1 (world_summary over-trust), G-S8 (input pipeline silo, sheet half)

- **I:** `lib/sheets.getSheetData('Oakland_Sports_Feed')` + `('Riley_Digest')` + `('Initiative_Tracker')` + `('Simulation_Ledger')` direct reads (canon content source); `output/production_log_city_hall_c{XX}.md` (city-hall voice outputs as one input, not spine); `output/engine_audit_c{XX}.json` (patterns + tribuneFraming); `output/baseline_briefs_c{XX}.json` (carries to Step 5); `output/world_summary_c{XX}.md` (orientation only — engine numbers + tables; narrative content not consumed).
- **O:** Raw extracted thread set in operator working memory.
- **V:** Sheet read returns non-zero rows for Oakland_Sports_Feed + Riley_Digest + Initiative_Tracker. Probe log: which world_summary sections were read (limited to numbers/tables; narrative quotes trigger failure). Fail-loud if sheet primary unavailable (service account at `/root/.config/godworld/.env`).

### Step 2 — Canon-archive + NEWSROOM_MEMORY load

**Closes:** G-S8 (input pipeline silo, canon half)

- **I:** `search_canon(query)` MCP (active arcs, prior coverage per Step 1 threads); `mcp__plugin_claude-mem_mcp-search__search` (past-session adjacent threads); `search-memory.cjs --user` Supermemory mags (editorial decisions, character continuity); ranged-read `docs/mags-corliss/NEWSROOM_MEMORY.md` per S215 prescription (last E{XX-1} entry + Standing Directives + Character Continuity + topic-specific lookups); `docs/media/CITIZEN_NARRATIVE_MEMORY.md`; `docs/media/CITIZENS_BY_ARTICLE.md`; `.claude/agent-memory/letters-desk/MEMORY.md §Rest Cycle Tracking` (preload for Step 10).
- **O:** Per-thread annotations (NEW / ARC: E{XX}-present / GAP: last covered E{XX}, {N} cycles ago / FOLLOW-UP / RECURRING / CONNECTS); errata corrections list; rest-cycle tracker state.
- **V:** `output/sift_canon_archive_log_c{XX}.json` emitted with query list + NEWSROOM_MEMORY sections read (line ranges). Fail-loud if zero canon-archive searches OR NEWSROOM_MEMORY not opened.

### Step 3 — Candidate generation

**Closes:** G-S2 (thread template buckets), G-S3 (atmospheric-overlay rule), G-PR2 (real-headline emission), G-S14 (role=reporter filter — cross-link from engine.21a)

- **I:** Step 1 threads; Step 2 annotations; `engine_audit_c{XX}.json` `tribuneFraming.storyHandles[desk]` + `tribuneFraming.suggestedFrontPage`; `baseline_briefs_c{XX}.json`.
- **O:** Candidate proposal set; each entry carries `{id, headline (REAL string), section (allowlist: FRONT_PAGE | CIVIC | NEIGHBORHOODS | SPORTS | BUSINESS | LETTERS | EDITORS_DESK), reporter (role=reporter only), sourceSignal, bucket (WORLD | TEXTURE-CITY-LIFE | SPORTS | CIVIC-WITH-WEIGHT | CIVIC-TRACKER-ONLY), narrativeWeightTest (for civic), crossSourceConnections, atmosphericOnly (for FamousPeople / streaming / food trend)}`.
- **V:** Every candidate has non-placeholder headline (regex `!^(untitled|TBD|placeholder)$`). Every civic candidate passes narrative-weight test (introduces-canon | closes-or-escalates-arc | engine-vs-action-puzzle | new-voice) OR routes to CIVIC-TRACKER-ONLY (not slate). Every reporter assignment is role=reporter (no Mags / DJ / Rhea / Arman). Atmospheric-overlay signals tagged `atmosphericOnly: true` (never anchored as standalone scene).

### Step 4 — Enrichment (three-layer threading + canon-pointers)

**Closes:** G-W35 (employer biz omission, cross-link from canon.3)

- **I:** Step 3 candidates; `lookup_citizen()` per named entity; `lookup_business()` for employer biz; `get_council_member()` for civic; `lookup_initiative()` for initiative-relevant.
- **O:** Each candidate annotated with `{threeLayerFraming: { engine, simulation, userActions }, canonPointers: { citizens: [{name, POPID}], businesses: [{name, BIZID, role}], initiatives: [{id, name, phase}] }, employerBiz: { citizen, BIZID, name }, councilRosterFrozen (for civic) }`.
- **V:** Every named citizen has POPID confirmed (no fabricated names). Every employer-biz reference has BIZID. Three-layer present on anchor pieces (FP1, C1, S1 — not all candidates required). Civic candidates carry canonical 9-member council roster (D1-D9 + Mayor frozen block).

### Step 5 — Triage (6-decision vocabulary)

**Closes:** G-S13 (fold / covered-by-feature vocabulary)

- **I:** Step 4 enriched candidates; `baseline_briefs_c{XX}.json`.
- **O:** Triaged decisions per 6-vocabulary:
  - `promote` (rewrite as feature, Tier A/B)
  - `publish-as-baseline` (Tier C automated copy-through)
  - `suppress` (genuine noise)
  - `fold(into=<pieceId>)` — required `foldedInto` field; baseline absorbed inside another piece
  - `covered-by-feature(by=<pieceId>)` — required `coveredBy` field; slate already has feature absorbing
  - `defer-to-supplemental(target=<interview|dispatch|wiki>)` — required `supplementalTarget` field; out-of-edition scope, file for adjacent artifact
- **V:** Every baseline brief carries exactly one of 6 decisions. Required fields populated. No ad-hoc fields outside vocabulary (no `promotedInto` / `customDisposition`).

### Step 6 — Cadence-cap + section-cap enforcement → Mike approval gate

**Closes:** G-S5 (slate-variant loop, proposalState ambiguity, civic-as-spine, no section ordering)

- **I:** Step 5 triaged slate; reporter cadence caps from `.claude/agents/REPORTER_DESK_INDEX.md`; per-section caps (civic max 3 [one slice, not spine], sports max 3, other max 2-3); Engine A priority data from `Story_Seed_Deck` cols M-R per T4.1 (priorityScore, consequenceFloor, bylineCandidate, bylineConfidence, priorityComponents, bylineRationale).
- **O:** `output/sift_proposals_c{XX}.json` with `proposalState: "draft"`; cap-enforced slate; floor tags applied (T4.1 floor semantics: HIGH severity + uncovered crisis OR HIGH severity + HEALTH/SAFETY/CIVIC + arc ≥ 2 cycles); newspaper-section ordering (FRONT PAGE → CIVIC → BUSINESS → NEIGHBORHOODS → SPORTS → LETTERS → QUICK_TAKES); T5.2 rationale suffix per proposal (`[priority N.N / floor / <reporter> <conf>-conf — <gloss>]`).
- **Mike approval gate:** Present slate to Mike. **CADENCE CAP: ONE slate variant per session.** If rejected, surface rejection-shape question (story selection / civic-weight / citizen pool / format / section ordering) BEFORE re-propose. **Hard stop on variant 2** — no automatic re-propose. After approval: `proposalState: draft → locked`.
- **V:** Per-reporter count ≤ cap (default 2). Per-section count ≤ cap (civic ≤ 3). Floor tags applied where `consequenceFloor === TRUE`. JSON `proposalState ∈ {draft, locked}` exactly (no other values). Newspaper-section ordering present in JSON. Mike approval logged with timestamp + sha of locked JSON.

### Step 7 — Brief emission (canonical shape, per-slot naming)

**Closes:** G-S21 (template-design conflict), G-W31 (multi-article naming collision), G-S5 (brief constraint-sheet)

- **I:** Step 6 locked slate; `docs/media/brief_template_v2.md` (Task 3 output).
- **O:** One brief file per article slot at `output/reporters/{reporter-slug}/c{XX}_{slot}_brief.md`. Examples: `output/reporters/maria-keen/c94_C2_brief.md`, `output/reporters/maria-keen/c94_N1_brief.md`, `output/reporters/jordan-velez/c94_FP1_brief.md`. Each brief carries: `# {Reporter} — C{XX} {SLOT}: {HEADLINE}` / `## SECTION` (one of allowlist) / `## SIGNAL` (1 paragraph: what happened + why now + three-layer angle) / `## VOICE DIRECTION` (3-5 bullets: tone, pacing, lean-into, avoid) / `## CANON POINTERS` (POPIDs/BIZIDs/init IDs reporter must respect) / `## WHAT NOT TO COVER` (overlap prevention) / `## CANONICAL EXEMPLAR` (reference link).
- **V:** Brief count = slate proposal count (one brief per slot; multi-slot reporters get multiple briefs). Reporter-slug ↔ slot naming present (no `c{XX}_brief.md` collisions). Every brief follows v2 template — no prose body, no full citizens table, no SPECIFIC DATA dump.

### Step 8 — dispatch.json emission

**Closes:** G-W30 (dispatch.json not emitted), G-PR2 (real-headline emission, cross-link)

- **I:** Step 7 brief files (paths); Step 6 locked slate; `docs/media/dispatch_schema.md` (Task 4 output).
- **O:** `output/dispatch_c{XX}.json` per schema: `{cycle, generatedAt, articles: [{briefFile, reporter, desk, section, slot, headline}], letters: {candidatesFile, deskAssigned}, quickTakes: [...], notes}`.
- **V:** Parses against schema (Task 4 exemplar validates). Every `articles[].briefFile` exists at named path. Headlines match `sift_proposals_c{XX}.json` headlines exactly (no "untitled" placeholders). `/write-edition` reads dispatch.json without REPORTER_DESK_INDEX fallback.

### Step 9 — Memory-Fence wrap on every embedded canon excerpt

**Closes:** G-W32 (Memory Fence bypass)

- **I:** Step 7 brief files; `lib/memoryFence.js`.
- **O:** Brief files re-emitted with every canon excerpt wrapped via `wrap(text, 'bay-tribune')`. Targets: `search_canon` excerpts; `lookup_citizen` / `lookup_business` / `lookup_faith_org` / `lookup_cultural` results; `get_neighborhood_state` results; NEWSROOM_MEMORY quoted blocks. Context-scan probe: `lib/contextScan.scanFile()` on every brief; blocked excerpts logged to `output/injection_blocks.log`.
- **V:** Every brief passes `scanFile(briefPath).safe === true`. Wrap markers present in every brief embedding canon (grep-detectable convention). Zero injection blocks OR blocks surfaced to Mags (skill aborts handoff if blocks > 0).

### Step 10 — Letters-candidates emission (with rest-cycle filter)

**Closes:** G-W33 (sequencing mismatch, sift half), G-W39 (rest-cycle conflict)

- **I:** Step 6 locked slate (thematic awareness); `.claude/agent-memory/letters-desk/MEMORY.md §Rest Cycle Tracking` (preloaded Step 2); citizen pool from `lookup_citizen` + canon-archive (recent-coverage candidates).
- **O:** `output/letters/c{XX}_candidates.md` — candidate pool brief (NOT assignment): cycle theme summary (3-5 lines from approved slate); filtered candidates (REST-passed only) with one-line context per candidate; explicit note "letters-desk LENS owns final selection; this is pool, not assignment"; cross-reference to /write-edition Step 3.5b second-stage handoff for final letter selection post-compile.
- **V:** Every candidate passes rest-cycle filter (no citizens with `REST through E{XX-1}` or later). Pool size ≥ letters-desk per-cycle cap (3-5 candidates standard; passes FULL pool per plan Q2 resolution). File path matches `dispatch.json letters.candidatesFile`. Cross-skill note documented: /write-edition Step 3.5b is the second-stage handoff for final selection post-compile.

---

### Cross-skill dependencies

- **/write-edition Step 3.5b** — new step required: regenerate letters-brief from compiled edition + relaunch letters-desk with named-piece references. Cross-skill commit; v2 sift Step 10 emits candidate pool, /write-edition Step 3.5b emits final letters-brief. (G-W33 closure cross-skill — file as `pipeline.<n>` row tagged `(media terminal)` for /write-edition skill text edit, OR fold into existing pipeline.26 if format-contract scope absorbs it.)
- **engine-sheet djDirect.js** — fallback to slot-order if title-match fails (already filed pipeline.29 §djDirect.js). Defense in depth alongside Step 8 real-headline emission. (G-PR2 closure cross-skill.)
- **canon.3** — Step 4 employer-biz pulls require `lookup_business()` MCP working against current world-data state; canon.3 cross-layer citizen/business drift remediation closes the upstream layer (closes G-W35 fabrication surface that Step 4 mitigates).

### Plan Q resolutions (S228)

- **Q1 — Quiet-pipeline detector for "expected but missing" beats?** Recommendation: OUT of v2 scope. Editorial coverage-gap detection is auditor concern (engine-audit lane), not sift concern. v2 stays scoped to slate proposals from observed inputs. File as engine.* / governance.* row if Mike wants it. **Awaiting Mike's call.**
- **Q2 — Letters cap pre-trim or pass full pool?** RESOLVED: Pass full pool, let LENS rank (plan lean confirmed). Step 10 designed with full-pool emission.
- **Q3 — dispatch.json `slot` field assignment?** RESOLVED: /sift assigns slot in Step 3; /write-edition Step 1 validates against allowlist (plan lean confirmed). Step 8 designed accordingly.

---

## Open questions

- [ ] **Q1 — Quiet-pipeline detector for "expected but missing" beats?** Recommendation S228 (research-build): OUT of v2 scope — auditor concern, not sift. File as engine.* / governance.* row if Mike wants it. Awaiting Mike's call.
- [x] **Q2 — Letters cap pre-trim or pass full pool?** RESOLVED S228: Pass full pool, let LENS rank (plan lean confirmed). Step 10 designed with full-pool emission.
- [x] **Q3 — dispatch.json `slot` field assignment?** RESOLVED S228: /sift assigns slot in Step 3; /write-edition Step 1 validates against allowlist (plan lean confirmed). Step 8 designed accordingly.

---

## Changelog

- 2026-05-23 — Task 1 + Task 2 shipped (S228, research-build). Gap-map at `output/sift_v2_gap_map.md` (14 gaps mapped + 4 cross-cutting findings: input-hierarchy inversion / reporter-agency rebalance at 3 layers / JSON-as-contract gap cluster / civic-as-default-spine). §v2 Step List drafted (11 steps including retired Step 0; each carries I/O/V triple + named defect closures + cross-skill dependencies). 2/3 open questions resolved (Q2 full-pool letters / Q3 sift assigns slot); Q1 quiet-pipeline detector awaiting Mike's call. Next session: Task 3 (brief_template_v2 + exemplar) → Task 4 (dispatch_schema + exemplar) → Task 5 (triage vocabulary spec) → Task 6 (SKILL.md v2.0 rewrite) → Tasks 7-8 (dry-run + live-run).
- 2026-05-22 — Initial draft (S225, research-build). Filed under governance.13 Phase 2 cluster C2 row pipeline.24. Multi-session plan; first execution session pending C95 schedule + Mike clearance to invest in v2 vs. quick-patch v1.2.
