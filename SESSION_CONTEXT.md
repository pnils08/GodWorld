# SESSION_CONTEXT — NEXT WORK ONLY. One PIN + one NEXT line per lane. What already happened lives in git, claude-mem, ROLLOUT_PLAN and the gap/production logs — never here. If a line describes a session instead of an assignment, it is written wrong.

**PIN:** S478 | Day 196 | canonical C108 | **PROD @105 = clasp v94 = `d5738907`** | Bench SANDBOX 0908 @75 = `d5738907` at C116 — bench-only, never replay | claude-mem observer on `nvidia/nemotron-3-super-120b-a12b:free` (infrastructure.11 open); a new worker env var needs `npx claude-mem stop` then `start` — `restart` re-spawns with the OLD env | `diag187` rides the web-app fire response only, an editor fire has none | engine-sheet LEADS the sim repairs | never `--amend`; wiring card before every cut; deploys from an isolated `git archive` stage | DOCTRINE §13/§15/§16/§17 | Sunday 21:00 city-hall slot runs the C108 chain | civic.38 is the house project: plan `docs/plans/2026-09-19-civic-wake-game-loop.md` §Lane assignments, one owner per file, dry-run only | Ledger 952 rows

**NEXT[engine-sheet]:** Smoke C109 on PROD @105 (log `record:` = last feed row's W-L). Build engine.250 first (both hood effect buses dead; card `output/agent_engine-wiring_2026-09-20T19-28-00.md` — card the approval carry path before cutting), then engine.249 hood migration (ruled, no rebase), then civic.38 Task 5 step 1 sponsor read, then Task 4 + revival guard once a move lands on a tracker. Own session: engine.248. Rest: `output/production_log_run_cycle_c108_gaps.md` G-EC53–68. No clasp push this session.

**NEXT[research-build]:** civic.38 sequencing and review against the plan; keep §Lane assignments current. Mike-gated: Baylight INIT-006 target metric (blocks Task 4.4), signature bands (block Task 6.3 live gating), which no-lever domain gets its engine path first — housing or safety (plan §Open questions).

**NEXT[kimi]:** civic.38 chain A — the plan was reconciled 2026-09-20, re-read your tasks before building: Task 1 + the Task 3 pack blocks together (pack builder is `scripts/buildCivicOfficeSlice.js` `buildPack`, not `domainSlice`), then Task 2 (rebuilt: move ledger + Sunday fold, not the decisions envelope), then 8, then 9 rota + work-wake packs, then the Task 6.3 Sunday-prep wiring. You alone edit `scripts/cron-civic-run.js`, `buildCivicOfficeSlice.js`, `applyTrackerUpdates.js`, `validateTrackerUpdates.js`, `cron-civic-gate.js`, `cron-work-wake.js`, `work-wake-packages.json`. Dry-run only: no `--apply`, no crontab edits. Standing: adversarial review on repair waves (reviews in `output/kimi/`; open: holidaySeeds bank pin, owed post-fire 240 bench, QoL composite-vs-QolLevel split).

**NEXT[codex]:** Your civic.38 review is accepted and reconciled into the plan (review file now in `docs/research/`, acceptance note at its foot). civic.38 Task 6 as amended — `scripts/civicPetitions.js` (deterministic counter, no LLM; rent burden annualized `MonthlyRent * 12 / HouseholdIncome`, zero/missing income its own counter, hardship band and support band separate, safety/housing counts print but gate nothing) + `Reflection_Intake` as an OPTIONAL tab in `scripts/dumpBeatTabs.js`. Do not edit `cron-civic-run.js`; tell kimi when the counter lands so the Sunday-prep wiring follows. Sim-repair review on request.

**NEXT[antigravity]:** civic.38 Task 9 tests — you own `scripts/cron-civic-game.test.js` (synthetic fixtures for Tasks 1, 2, 3, 6, 8 as each lands), plus adversarial review of every landed task in `output/antigravity/`. Re-read the file before every claim; reviews have gone stale while sitting on permission prompts.

**NEXT[grok]:** No pane; nothing queued.
