# Tech Debt Audit — 2026-03-26

Session: 120 | Cycle: 89 | Files scanned: 122 engine JS files

**Revision 2:** Corrected 3 false positives (C4, C5, W1). Engine files alias `ctx.summary` as `S` — initial grep for `ctx.summary.field =` missed `S.field =` writes. careerSignals, economicMood, and patternFlag are all properly connected.

---

## CRITICAL (3 issues — 2 FIXED)

### C1. Math.random() fallback — generateGenericCitizens.js:96 — FIXED

```js
// WAS: return rng ? rng() : Math.random();
// NOW: logs warning and caches ctx.rng fallback
```

**File:** `phase05-citizens/generateGenericCitizens.js:96`
**Status:** FIXED S120 — now logs warning instead of silent fallback.

---

### C2. Math.random() fallback — generationalEventsEngine.js:105,111 — FIXED

```js
// initRng_: WAS: return Math.random; NOW: logs warning + returns Math.random
// rand_: WAS: silent fallback; NOW: logs warning before fallback
```

**File:** `phase04-events/generationalEventsEngine.js:100-114`
**Status:** FIXED S120 — now logs warnings instead of silent fallback.

---

### C3. Math.random() fallback — v2DeprecationGuide.js:202-203

`v3Random_()` helper returns `Math.random()` when called without ctx.rng. Any code using this helper without passing ctx is non-deterministic.

**File:** `utilities/v2DeprecationGuide.js:202-203`
**Status:** Open. File is a migration guide — verify no active callers before marking deprecated.

---

### ~~C4. ctx.summary.careerSignals~~ — FALSE POSITIVE

Written via `S.careerSignals` in runCareerEngine.js (S = ctx.summary). Read via `S.careerSignals` in economicRippleEngine.js. Pipeline is connected.

---

### ~~C5. ctx.summary.economicMood~~ — FALSE POSITIVE

Written via `S.economicMood` in economicRippleEngine.js:638. Read via `S.economicMood` in applyMigrationDrift.js:189. Pipeline is connected.

---

## WARNINGS (20 issues)

### Orphaned ctx Fields (Written, Never Read)

| # | Field | Written In | Line | Verified |
|---|-------|-----------|------|----------|
| ~~W1~~ | ~~`patternFlag`~~ | ~~applyPatternDetection.js~~ | ~~35, 45~~ | FALSE POSITIVE — 9+ reader files via S alias |
| W2 | `patternCalendarContext` | applyPatternDetection.js | 36, 46 | ORPHANED — no readers found |
| W3 | `storylineUpdates` | updateStorylineStatusv1.2.js | 171 | ORPHANED — no readers found |
| W4 | `arcResolutions` | processArcLifeCyclev1.js | 200 | ORPHANED — no readers found |
| W5 | `arcPhaseChanges` | processArcLifeCyclev1.js | 201 | ORPHANED — no readers found |
| W6 | `advancementResults` | processAdvancementIntake.js | 53 | ORPHANED — no readers found |
| W7 | `chicagoPopulation` | generateChicagoCitizensv1.js | 161 | ORPHANED — chicagoCitizens array IS read, count is not |

Note: processArcLifeCyclev1.js IS the active engine file (called by godWorldEngine2.js:311). ENGINE_MAP incorrectly says it was superseded by arcLifecycleEngine.js.

### Direct Sheet Writes Outside Phase 10

**Documented exceptions:** householdFormationEngine.js, generationalWealthEngine.js, educationCareerEngine.js.

These are NOT in the exceptions list. Most write to tracking sheets (Story_Arcs, Hooks, Storylines, Neighborhood_Map, Cycle_Weight), not Simulation_Ledger. They bypass the write-intent pattern but may be intentional.

| # | Phase | File | Writes | Target Sheet |
|---|-------|------|--------|-------------|
| W8 | 6 | hookLifecycleEngine.js | 11 setValue/appendRow | Hook tracking |
| W9 | 6 | economicRippleEngine.js:698,709 | 2 setValue | Population_Stats |
| W10 | 6 | updateStorylineStatusv1.2.js:162,166 | 2 setValue | Storyline tracking |
| W11 | 6 | arcLifecycleEngine.js | 12 setValue | Story_Arcs |
| W12 | 6 | applyMigrationDrift.js:395,402 | 2 setValue | Neighborhood_Map |
| W13 | 6 | storylineHealthEngine.js | 6 setValue | Storyline tracking |
| W14 | 6 | processArcLifeCyclev1.js | 7 setValue | Story_Arcs |
| W15 | 8 | v3NeighborhoodWriter.js | 7 setValues | Neighborhood_Map |
| W16 | 8 | v3DomainWriter.js:411 | 1 setValues | Domain_Tracker |
| W17 | 8 | v3ChicagoWriter.js:54 | 1 appendRow | Chicago output |
| W18 | 9 | applyCycleWeightForLatestCycle.js | 12 setValue | Cycle_Weight |
| W19 | 9 | applyCycleWeight.js | 3 setValue | Cycle_Weight |

**Phase 5 writers beyond documented exceptions:**

| # | File | Writes | Target |
|---|------|--------|--------|
| W20 | updateCivicLedgerFactions.js:161 | setValues | Civic_Ledger |
| W21 | runAsUniversePipeline.js:588,593 | setValues | As_Roster + log |
| W22 | runEducationEngine.js:438,454 | appendRow, setValues | Education log + SL |
| W23 | generateCitizensEvents.js:1448,1452 | setValues | SL + Life_Log |
| W24 | generateNamedCitizensEvents.js:697,715 | appendRow, setValues | Log + SL |
| W25 | generateMonthlyCivicSweep.js:53,151 | appendRow | Civic_Sweep |
| W26 | checkForPromotions.js:487-514 | multiple | SL + Generic tab |
| W27 | civicInitiativeEngine.js | multiple | Initiative_Tracker |

---

## INFO (4 items)

1. `ctx.summary.healthCauseBriefing` — appears only in JSDoc comment (healthCauseIntake.js:607), not in actual code. Write was commented out or never implemented.

2. `prePublicationValidation.js:433` — `ctx.summary.validationReport` assignment is commented out. The engine sets it in `godWorldEngine2.js:276` instead.

3. `v2DeprecationGuide.js` — entire file is a migration guide with Math.random() references in documentation + fallback helpers. Should be treated as deprecated utility, not active engine code.

4. **ENGINE_MAP correction needed:** processArcLifeCyclev1.js is the ACTIVE engine file (called at godWorldEngine2.js:311), not superseded. arcLifecycleEngine.js has a different signature and is NOT called by the engine.

---

## Summary

| Severity | Count | Fixed |
|----------|-------|-------|
| Critical | 3 (was 5, 2 false positives) | 2 fixed (C1, C2) |
| Warning | 20 (was 27, 1 false positive) | 0 |
| Info | 4 | 0 |

**Recommendation:** MOSTLY CLEAN — C3 is low priority (deprecated helper), orphaned fields are dead weight not bugs, sheet writes are intentional for tracking sheets.
