# SESSION_CONTEXT — NEXT WORK ONLY. One PIN + one NEXT line per lane. What already happened lives in git, claude-mem, ROLLOUT_PLAN and the gap/production logs — never here. If a line describes a session instead of an assignment, it is written wrong.

**PIN:** S417 | Day 189 | canonical C105 (fired 2026-08-31, clean; Mike fires live) | **PROD @45 (2026-09-04 ~05:00, engine-sheet) = @44 + engine.161 relocation is a share and the burden is the cause (`relocationCap_` = World_Config `relocationMaxShare` 0.05 × movable units, floor 1 — 43 at C105, replacing the flat 2; rent burden ≥ `RENT_BURDEN_CRISIS` 0.50 → `planning-to-leave`, ≥ 0.40 → `considering`, stashed on `ctx._unitBurden`; the engine.157 posture multiplies the misfit lane only); @44 = @43 + engine.157 the maneuver phase (`Phase5-Maneuver` before Career: ambition = 0.7·drive + 0.3·openness off `DialState.base`, goal off the rung establish/home/wealth/tenure/revive, posture climb ≥60 / retreat on DebtLevel ≥6 or a line within 5 of its bar / hold — a 1.5/0.5/1 multiplier on the casino placement + stake band, solo door, home purchase, relocation lane and the willing cross-field hire; `DialState.maneuver {p,g,a,c}` memory + on-change life line, hold silent; owner's draw reads the active-line map; 6 `maneuver*` World_Config keys self-arm); @43 = @42 + engine.156 heritage loss (`HeritageScore` is STANDING — 10×log10(NetWorth/$1M) + gens + civic + fame + 4×businesses (minted ∪ Key_Personnel-owned) + 2×homes − 5×scandal, tiers 0/30/50/70 up or down; TENURE `TenureCycles`/`TierTenure`/`PeakTier`; dormancy 0-living ×4 or standing <10 ×52 for an ever-Established line, blank tier = no line; revival by any door under the same LineageId; 7 `Heritage_Ledger` columns self-arm); @42 = @41 + engine.160 one hood rent rule (`MedianRent` = World_Config `hoodRentShare` 0.30 × `MedianIncome` / 12, derived Phase 2, rendered Phase 5; the 12-hood 2026 rent table, the dead 15-hood price table, the `: 1700` guards and the trajectory rent drift retired; off-ledger hoods throw); @41 = @40 + engine.158 home-purchase gates (hood-priced house, NetWorth band ≥ hood WealthMin, mortgage ≤ 30 % HouseholdIncome, 1 % × rung roll — live 236/319 → 104 qualified, ~1 buy/cycle) + engine.159 (an owned home's burden is its mortgage, not its price — 48 live owned households leave a false crisis that was dissolving buyers at 10 %/cycle); @40 = @29 + bloodline cuts 5–7 (engine.151 Tier pays, .154 spouse quality + the romance gate unwelded, .155 Door C at $50M / owned home / no Tier) + engine.96 Business Lifecycle Generator Tasks 5/6/7/10/11 (drift, decline sheds, closure wind-down → `Business_Archive`, the owner's draw off Key_Personnel, the heritage business born in the family's field) — 28 World_Config `biz*` keys self-arm at the next live fire; pull-back byte-verified every push; repo HEAD ≠ live ONLY by the four HELD engine.131 T7 files** | live-only data writes S413: six ghost founders → `Generic_Citizens` (6/6 read back) | bench SANDBOX 0831 **re-synced from live at C105 S416 (builder cleared the properties), @39 == live @45, at C111** (bench-only: C106–C111 memory, the maneuver stamps — all die at the next re-sync) | `Casino_Ledger` armed on live | Partnership NORMAL | Ledger 968 rows live | output/ git-tracked
**NEXT[engine-sheet]:** engine.162 — ENGINE-clock gate inverted vs the builder's 2026-09-04 rule (money + education universal; profession floor for every clock but GAME; MEDIA/CIVIC/GAME cannot change jobs). ROLLOUT row + plan §263. Reading unconfirmed. (S417)
**NEXT[research-build]:** **S417 verification (not trust-the-doc): the mayor's datawake fabrication problem was ALREADY CLOSED before this session, by a prior session's own work — this line was stale.** Root cause (grok's diagnosis, commit `625ccd1e` civic.29, 2026-09-01): `buildCivicOfficeSlice.js` zeroed constituents/turf/life/chaos for `seatKind==='role'` seats (mayor, DA, police chief), so the mayor's pack carried only approval scores + one thin INIT line — nothing to cite, so the model invented numbers, and the (correctly-working) anti-fabrication gate rejected them 08-25/26/27. Fix: role seats with a hood-specific turn now re-run `pickTurn` with real people/turf/life loaded for that hood (`buildCivicOfficeSlice.js` ~L744-759); regression-tested (5 assertions) + live-validated against C105 (mayor's pack now shows 4 named West Oakland constituents + the Stabilization Fund business, plus real $28M/12/57/280 disbursement figures). The one 08-31 22:46 mayor success (`output/cron-civic/datawake/civic-office-mayor_2026-08-31.json`, "57 households... $28M") ran on the earlier interim fix (facts only, still 0 people) — BEFORE the full role-seat load landed same day at 14:59. Net: **fix is code-verified but has not yet had a live end-to-end datawake run with the complete fix** — mayor hasn't been picked by the LRU rota since (Sep 1-3 covered other seats; Fri/Sat/Sun don't run office wakes). No manual re-fire (no-manual-pipeline-demos) — confirm on her next Mon-Thu turn. `civic-project-health-center` carries the same disposition (flagged same root cause, last known result 08-31 success, not re-tested since). Minor doc correction: rota is 18 offices, not ~22. civic.29 was never filed as a ROLLOUT row (commit-only) — left as-is, substrate-routine per S218. Prior carried items (14 never-cited ROLLOUT rows, governance.30 close, ADR-0018 Tier-A conversion, canon.5 sheet renames) still untouched, not re-investigated this pass.

**NEXT[kimi]:** Casino-ledger 4b design-question record shipped — `docs/research/2026-08-29-casino-ledger.md`, watch verdict (trigger: 4a texture ≥2 cycles + Mike-only sign-off), registered + cross-linked (541f4e2a). Review copy filed in the inbox per Mike: `docs/for-claude-review/2026-08-29-kimi-casino-ledger.md` (627ff9c0) — reviewer note attached; on accept, just clear the copy. Open offer: Undocked show review/enhancement pass.

**NEXT[codex]:** Subscription ended (2026-08-29). Nothing queued; the Task 17 seats moved to NEXT[engine-sheet]. AGENTS.md authorization unchanged for when it returns.

**NEXT[antigravity]:** loreWriter.js handoff long since applied by research-build (S377-S383, superseding the CLI-scratch draft) — line stale, cleared. Nothing queued.

**NEXT[grok]:** **4b casino ledger COMMITTED by engine-sheet (S406, builder-direct) — `phase05-citizens/casinoLedgerEngine.js`, `scripts/casinoLedger.js` + 2 suites, the Step 2.45 call site, truth docs trued up.** It had no commit behind it; it does now. **Code LIVE at PROD @13 (S409, 2026-09-01). Builder sign-off to arm GIVEN S410 ("turn on the casino", 2026-09-01 22:27) — `Casino_Ledger` armed on the BENCH by engine-sheet S410 (15 headers + HOUSE 250000); live arming follows the bench C106 placement proof.** Builder's reasoning for Claude committing it: grok's ability is trusted, but only Claude knows how Claude coded the surrounding engine, so it lands in git where future sessions can review it. Design records kept in the inbox rather than cleared — this is tracked, not accepted. Business-ledger hood fill ACCEPTED (S398, 2026-08-30) — moved to `docs/research/2026-08-29-business-ledger-hood-fill.md` with a `## Review` section (46 renames, pay re-base; landed on bench 72/72). Inbox copy cleared. Idle otherwise; civic.24 Task 9 stays gated on C104+ (`--stage=chain`, no `--apply`).
