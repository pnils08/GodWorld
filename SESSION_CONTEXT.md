# SESSION CONTEXT - GodWorld

**The thin state pin + one next-opener per terminal. Boot injects only the PIN and your terminal's NEXT line — nothing else is carried.** What shipped → `git log`. What's open → `docs/engine/ROLLOUT_PLAN.md`. Why a thing was done → claude-mem search.

---

**PIN:** Session: 272→273 (S271 engine-sheet: **C100 RAN + SHIPPED [E100 published]; engine.41 — chaos/texture/Riley→seeds — BUILT + CLASP-DEPLOYED LIVE** via isolated worktree; chaos-stamp `f2c96348` rode the same push; gated riders held at baseline; **SMOKE @C101** [additive/safePhaseCall_-wrapped, feature-verify not regression-gate]) | Day: 161 | Cycle: 100 | Edition: C99/E99 CANONIZED end-to-end S265 (graded A; Arbiter 0.866; bay-tribune COMPLETE 53 wiki+906 cards; PDF Drive 1sPDVe928GBGBQuYGCYGIiH-Uf5rleZIJ) | engine v3.4 — **engine.38 Phase A SMOKE-TESTED CLEAN 5/5 at C99 S265** (coverage 65.9% single-gen / 83.6% all-gen 758/907; archiver dormant 5199<12k; B3 regime-shift guard suppressed spurious spike; patternFlag NOT volume-pinned — "High event volume (8)" normalized identical to C98 despite 149→859 jump; named citizens Vinnie/Benji/Deacon covered). **engine.11 chaos-cars CLASP-DEPLOYED LIVE S265 (Mike go-call):** 5 new + 5 modified Apps Script files, 323/323 tests, 0 collisions, .test.js excluded (C98 crash lesson held). **SMOKE PENDING C100** (first chaos cycle — Chaos_Cars tab lazy-creates; 3-15 events; 0-1 scale; asymmetric decay; NM fold consumes; Tier-1→createChaosArcs_; Watch Q3 arcLifecycle). **engine.38 C100 WATCH: sentiment-uniformity** (G-EC33 — 20/21 hoods +0.4-0.5 into tight 0.6-0.86 band C99; confirm divergence not re-compression). **S268 (engine-sheet ENGINE_REPAIR + ROLLOUT sweep — all pushed `990e7d67..a8a74a24`, NO clasp):** ES-3 SHIPPED (baylight agent emits cycle-form gate schedules — governance.42 ES track **7/7 → done-pending-archive**; IDENTITY/LENS calendar residual → research-build follow-up). **Row 23 CLOSED** (life-event milestones live-verified firing C96–C99 via LifeHistory_Log; Birth=0 residual → Row 22/25). **Row 31 CLOSED** by DELETION (61 malformed LifeHistory_Archive rows removed via `deleteDimension`, snapshot `output/lifehistory_archive_row31_deleted_S268.json`; measure-twice REVERSED the documented realign — "Engine" sentinel had no schema home + ~17 had clean twins). **Row 8** scope tightened to 2 shared-path files (mediaRoomIntake + processAdvancementIntake) + stale §3 table reconciled. **Rows 22/24/25 🚫 BLOCKED until C100 chaos smoke** (rate-changing reactivations). **2 manual editor deletes STILL owed — Mike doing in EDITOR UI** (NOT clasp-doable in isolation: clasp push is full-replace + would also deploy research.18's undeployed `utilities/tier1EssenceEvents.js`). **NEXT CLASP PUSH MUST BE DELIBERATE** — tier1EssenceEvents.js (inert/guarded/no-collisions) sits undeployed in clasp source + rides any push.

**NEXT[media]:** C99 CANONIZED end-to-end (sift→write-edition→edition-print→post-publish all DONE S265; edition A / Arbiter 0.866; bay-tribune COMPLETE 53 wiki + 906 cards + 27 civic records; coverage ratings written). **Next media run = C100** once engine runs it (watch: first chaos-cars cycle may surface vehicular/sideshow events in editions — engine.11 deployed but did NOT execute C99). **NEW LANE (Mike, S265): "direct pieces" run through Gemini — Gemini writes + corrects + ingests them; my role = REVIEW + canon-check, and the fix must land BEFORE Gemini's ingest because ingest = canon (a wrong fact propagates into next sift).** Live example: Jax "The Heat Shield" column (jax_varek_column.md, Drive 1PKyP93Qbc11L8b35jiNduWvIaLPvJj80) — WNBA→NBA correction owed before ingest (Oaks are NBA, canon-solid E91/93/94/97); byline mislabeled civic-not-opinion + Paulson-as-subject are Mike's judgment calls, not vetoes. Open handoffs to research-build (gap logs, do NOT write ROLLOUT from media): G-P-C99-1 (Step 5b base_context cycle field at .baseContext.cycle, gate text ambiguous) + G-P-C99-2 (2a wall-time ~36min understated; stale citizen_card_failures dump survives clean run); carried Davis POP-00021 drift in newsroom.md Five Goods table + checkLetterEligibility.js exclusion-line false-positive HALT (it barred REAL reporter Elliot Graye POP-00012 this cycle, G-W1). 5 canon-drift citizens owed engine-sheet backfill (canon_drift_c99.json: Elliot Abraham, Herbert Jones, Rosario Medina, Delia Vargas-Ruiz, Jerome Pittman). FLUX photoQA over-filters stadium frames (G-PR-C99-1, research-build→engine-sheet rubric retune).
**NEXT[civic]:** run `node scripts/buildInitiativePackets.js` each cycle so the tracker JSON doesn't re-stale (last auto-refresh ~C89, civic.14).
**NEXT[engine-sheet]:** **S271 CLOSE — engine.41 (engine-output→canon coverage) BUILT + CLASP-DEPLOYED LIVE; C100 RAN + SHIPPED (E100 published — the failed-run cleanup in the history below is RESOLVED).** engine.41 thesis (Mike: "half the engine makes it to canon — unacceptable"): the seed pipeline IS the canon router but ~half of per-cycle engine output never reached it — C100 scan = ~20 of the story-signal outputs reach a consumer, ~18 die in-tab; world_summary reads 6 tabs / desk_packets 21, drifted, no contract. **4 wires** (plan `docs/plans/2026-06-24-engine-output-canon-coverage.md`, registered in index): **(1)** broad chaos field → seeds — the existing path seeded ONLY `consequenceFloorFired`; at C100 all 6 chaos events had floor=FALSE → 0 chaos seeds (THAT was why chaos missed desks, not the stamp); now every `chaosCarsEvents` w/ non-empty `narrativeSeed` seeds @prio 3 (narrativeSeed = engine's own "desk-worthy" signal; empty = skip, no fabricated prose). **(2)** `Texture_Trigger_Log` (28 dead rows/cyc) → seeds @prio 2 (cap 8, FAITH sub-cap 2 — must not amplify the flood). **(3a)** `Riley_Digest` evening layer → desk packets — Node (`buildDeskPackets` → `eveningContext.riley`), **LIVE NOW**, end-to-end verified C100 (TV lineup/famousPeople/cityEvents/streaming/nightlife). **(3b)** Riley story-worthy (cityEvents/streaming/famous-consolidated) → seeds @prio 2 CULTURE. Commits `4b94bda3`/`09af10ac`/`b387ec8d`, pushed `3263c5bf..b387ec8d`. **CLASP-DEPLOYED via isolated worktree** (baseline `8822e6b5`, HEAD−gated reverted to baseline): engine.41 (`applyStorySeeds.js`) + chaos-stamp `f2c96348` (`saveChaosCars`/`chaosCarsEngine`) now LIVE; idempotent re-push "Script is already up to date" = live==verified-worktree; **gated HELD at baseline** (processAdvancementIntake[Row8] / citizenDialMap / citizenMemory / compressLifeHistory[research.14] / tier1EssenceEvents.js) — confirmed absent from manifest, `.test.js` excluded. **SMOKE @C101 (NOT engine-breaking — additive seeds.push + safePhaseCall_-wrapped; worst case malformed seed [cosmetic] or no-seeds [no-op], never a dead cycle — so feature-verify not regression-gate):** chaos+texture+Riley seeds in `Story_Seed_Deck` @Priority≥2 + appearing in desk packets; **faith share DOWN not up.** **engine.41 SEPARATE LANES (do NOT fold in):** faith generation skew (9 events/5 faith = phase04 generation, not routing — own prerequisite pass); `Event_Arc_Ledger` re-enable = Row 28 rebuild (generator fabricates specificity); coverage registry/contract = optional anti-drift layer. **ROLLOUT engine.41 (file row).** **STILL CARRIED (off engine.41):** ctx.now wall-clock ~145 sites sheet-wide (S271 fixed only the sim/citizen subset — full fix = one source-level pass, file ROLLOUT); 5 canon-drift citizens backfill owed (canon_drift_c99.json: Elliot Abraham/Herbert Jones/Rosario Medina/Delia Vargas-Ruiz/Jerome Pittman); next clasp window still carries Row 8 migration + research.14 drain (gated on RB affect re-audit) + tier1EssenceEvents.js + 2 manual editor deletes. **— S271 FIX HISTORY (RESOLVED, kept for provenance — C100 re-ran clean + shipped, persist/chaos-crash/in-world-date/chaos-stamp fixes all live):** **C100 RAN AND FAILED → diagnosed, fixed, isolated-deploy live (4 commits `cceabdc8`/`f3b24806`/`eb7ef6d6`/`f2c96348`).** **WHAT FAILED:** /run-cycle 100 — (1) **chaos-cars crashed** `Phase4-ChaosCars: data.slice is not a function` (caught by safePhaseCall_, 0 chaos events, cycle continued) — `loadBusinessRows_`/`loadNeighborhoodNames_` treated `ctx.cache.getData()` (returns wrapper `{values,header,...}`) as a 2D array; tests passed because they run w/o ctx.cache (direct-read fallback). (2) **THE SERIOUS ONE — persist total failure:** `executePersistIntents_` hit a transient Google "Service Spreadsheets timed out" → **0/25 intents committed → cycle computed but DID NOT SAVE.** Persist had NO retry + per-cell `setValue` loop (max exposure). Known recurring transient (9 daemon hits/~10d; first to land on cycle persist). Doc only 24% of cell cap, C99 persisted fine → NOT a size wall. **FIXES (all verified, deployed live except the 4th):** `cceabdc8` **persist hardening** = `persistWithRetry_` wraps every write op (ensure/replace/cell/range/append), backoff 0/2/5/12s, transient-only retry, real bugs rethrow; happy path zero-latency; unit-tested recover/rethrow/exhaust. `f3b24806` **chaos crash** = unwrap `.values` + `Array.isArray` guard both loaders; 323/323 chaos tests green. `eb7ef6d6` **in-world dates** = new `inWorldStamp_(ctx)` helper (advanceSimulationCalendar.js → `S.cycleRef` "Y{yr}C{cyc}", e.g. **Y2C48**) replacing `ctx.now` wall-clock at 21+ life-record/packet sites (LifeHistory_Log Timestamp + col-O strings + Faith/Transit/youth ledgers + buildCyclePacket). `f2c96348` **chaos stamps** = Chaos_Cars `TimestampUtc`→`CycleStamp` in-world + chaosCarsEngine iLastU → **COMMITTED NOT DEPLOYED, MUST ride next clasp push BEFORE first chaos cycle or Chaos_Cars lazy-creates w/ wall-clock col.** **DEPLOY METHOD (reuse): ISOLATED worktree push** — baseline `8822e6b5` + cherry-pick the S271 fix commits (disjoint from gated files, clean pick); gated files HELD BACK (processAdvancementIntake/citizenDialMap/citizenMemory/compressLifeHistory reverted to baseline + tier1EssenceEvents.js excluded as new-file). Worktree `/tmp/gw-deploy-s271`, `clasp push -f`, verified via `clasp pull` (fixes present, gated absent). **⛔ PRE-RUN CLEANUP OWED before C100 re-run (agent BLOCKED by classifier on live-sheet deletes — Mike-run or explicit approval):** (a) **trim stray C100 junk rows** from the failed partial-persist (contiguous-at-tail, regenerate on re-run): LifeHistory_Log 769 (rows 5201–5969), Relationship_Bond_Ledger 321 (5568–5888), Cycle_Packet 1 (row 60). Tool ready: `scripts/trimStrayC100Rows_S271.js` (untracked; recompute-live + contiguous-assert + verify). (b) **clear `PREV_EVENING_JSON` + `PREV_CYCLE_STATE_JSON`** (PropertiesService = GAS-only, Mike runs) — failed run advanced them to C100; re-run would double-advance carry-forward (load fns do NOT validate cycle#; C99 snapshot overwritten/gone, so clearing = clean null-start, analyzers skip 1-cycle cross-compare). **NOTE: a single GAS function can do (a)+(b) together** (SpreadsheetApp deleteRows + Properties.deleteProperty). **WHY ONLY DIRECT-WRITE TABS HAVE C100 ROWS:** persist failed at Phase 10 (intents) but Phase 4/5/8 direct-writers + PropertiesService fired earlier → partial state; Neighborhood_Map/demographics update-in-place (self-heal, leave). **C100 FAILED-RUN PACKET observations (Cycle_Packet survived as direct write — read for re-run watch):** sentiment STILL compressed (neighborhood 0.48–0.55 in identical lockstep groups = G-EC33 confirmed not resolved); **EveningTraffic=NaN** (math leak); crime counts all 0 despite "elevated" hotspots; **36 arcs ALL peak/t=10 w/ dups** (arc lifecycle not progressing, Q3); shock dur 9 cycles. **BROADER FINDING (file as ROLLOUT, do NOT whack-a-mole):** engine stamps `ctx.now` wall-clock across ~145 sites sheet-wide; S271 fixed the sim/citizen-facing subset; proper full fix = one source-level pass. **⚠️ PROCESS NOTE (self): this session DRIFTED** — dumped code/scripts in chat + asked Mike to hand-run mechanism (violates partnership: Mags holds mechanism, NO code in chat, three-layer artifact discipline). Mike disoriented ("who are you"). Don't repeat: handle mechanism, don't offload it. — PRIOR(S270): **S270 — ROLLOUT/ENGINE_REPAIR review + research.19 build (engine-sheet, all Node/doc, NO clasp, pushed `1cbdd0e4..e6946af0`).** SHIPPED: **research.19 T1a/T1b BUILT + LIVE** (`f17d110a`, `scripts/citizen-wake.js`, wake-side/ungated — **next cron wake uses it**): T1a canon-anchored self-state read-back ("Your life so far" milestone arc from **LifeHistory_Log**, NOT prose per AP-reframe; + **dormant** dial-trajectory hook — current==base until drain deploys, never fabricates) + T1b A's sports slice from `Oakland_Sports_Feed` (TeamsUsed-filtered A's-only, valid-record-only, narrative-not-aggregate; canon-clean). `--dry-run` now prints the perception prompt. **research.14 drain CONFIRMED BUILT** (`8d6b155c`, Phase-9 in `compressLifeHistory_` — accretes to `base` + `applied=yes` via Phase-10 intent) — engine-sheet build half COMPLETE; **deploy gated on affect re-audit N-consecutive-green (research-build) + C100 clasp window.** **engine.20c CLOSED-MOOT** (INIT-001/005 writing via trackerOwner through C99 → done-pending-archive). **engine.27 Phase B**: B2a AVOIDABLE (daemon row-hash-diffs → marker=projection-trigger not POPID payload), build HELD post-C100 (don't load a 4th change into the window). **Row 8**: processAdvancementIntake migration DEPLOY-READY (phase-position verified Phase5-Advancement < Phase10-ExecuteIntents, no silent-drop) — closes after C100-window clasp + live smoke (no-op if no unprocessed media-usage rows at cycle time). **engine.38 B3 steps 2-5**: HELD post-C100 (would confound G-EC33 attribution). **Row 28** crisis-arc rebuild: notated post-C100 — design to READ live chaos-cars output, don't start ultracode pass until C100 signal observed. **Killed dead 'bot' ROLLOUT owner-tag** (research.14/.19 + engine.38 relabeled → research-build/engine-sheet; runtime is autonomous cron, not a terminal). **engine.40 FILED** — sports-stat intake via cloud-gaming screenshot OCR: **feasibility PROVED live** (MLB The Show 25 / NBA 2K render in-browser via xCloud → browser screenshot pulls full leaderboards clean, canon roster, no MCP/Xbox-API needed; **control NOT automatable** — no XInput emulation, keyboard doesn't reach xCloud → Mike-drives/Claude-reads; two-layer model career→TrueSource [Tier-1, season+awards, structured] + in-season→live store [`Player_Season_Stats`/extend feed, feeds T1b+desk+storylines]; verify step load-bearing — zoom can distort; **task-zero = read a real athlete TrueSource card schema**; plan doc pending Mike approval). **NEXT-SESSION UNGATED PICKS (off the C100 wall):** research.19 **T2** (world-summary→lived-particulars translation, lean shared cycle artifact) + **engine.40 build**. **Everything else C100-gated:** Row 8 deploy, engine.27 Phase B, engine.38 B/C, Rows 22/24/25, the research.14 drain deploy. **C100 clasp window carries:** Row 8 migration + `tier1EssenceEvents.js` rider + 2 manual editor deletes. — PRIOR: **S269 — Row 8 B7 ANALYTICALLY RESOLVED (2 local commits `d7480691`+`3b326493`, pushed this close, NO clasp).** (1) `processAdvancementIntake.js` MIGRATED — the 2 cycle-path Citizen_Media_Usage processed-flag writes (processMediaUsage_ L264/L331) → new `markUsageProcessed_` ctx-branch: cycle→`queueCellIntent_` (commits Phase 10, ATOMIC with the same-cycle UsageCount bump — fixes a real crash-bug: flag persisted @Phase 5 but bump commits @Phase 10, so a crash-between left the row flag='Y'-but-bumpless = skipped-forever next cycle); operator path `runAdvancementIntakeManual` now sets `ctx.manualRun:true`→direct (no Phase 10). Other 8 direct-write sites confirmed carve-outs (ledger §5.6 / 3× LifeHistory_Log appends / clearContent / 'Processed' header-create KEPT direct so the col exists at commit / inert Generic_Citizens). (2) `mediaRoomIntake.js` FULL CARVE-OUT — NO migration possible: cycle entry `processMediaIntake_` runs **Phase 11, AFTER the sole intent executor `Phase10-ExecuteIntents`** (`godWorldEngine2.js` L474/L1794) which ends in `clearAllIntents_` → a queued intent there silently drops. Reverses the S268 "designed split-helper" framing (it never checked phase-vs-executor order). **General structural rule recorded in `.claude/rules/engine.md`** (any write after the executor MUST be direct) + trued up the stale mediaRoomIntake entry (L467/L1762→L483/L1801; dropped removed-S98 Press_Drafts). **Row 8 stays `wip` — closes ONLY after the processAdvancementIntake migration clasp-deploys + smoke-tests live** (cycle-path behavior change; stub proved routing only, NOT the live executor writing Citizen_Media_Usage from a cell intent — verify-after-write owed). That deploy rides the next deliberate clasp window (gated below: C100 chaos + tier1EssenceEvents rider + 2 manual deletes). B5/B6/§3.5 already closed → B7 was the last open Row 8 scope. — PRIOR: **S268 — ENGINE_REPAIR + ROLLOUT sweep, all pushed (`990e7d67..a8a74a24`), NO clasp.** governance.42 **ES track 7/7** (ES-3 shipped — baylight `RULES.md`+`SKILL.md` emit cycle-form gate schedules; IDENTITY/LENS calendar residual → research-build ES-3 follow-up; row → done-pending-archive). **ENGINE_REPAIR closes:** Row 23 (life-event milestones live-verified firing C96–C99 via LifeHistory_Log read; Birth=0 residual cross-ref'd Row 22/25), Row 31 (61 malformed LifeHistory_Archive rows DELETED via `deleteDimension`, snapshot `output/lifehistory_archive_row31_deleted_S268.json`; measure-twice REVERSED the documented realign — "Engine" sentinel had no schema home + ~17 had clean twins). **Row 8** wip, scope now precise = 2 shared-path files (mediaRoomIntake + processAdvancementIntake — each needs a designed split-helper/ctx-branch, own clasp-gated session; all other B7 files are carve-outs; §3 table reconciled). **Rows 22/24/25 🚫 BLOCKED until C100 chaos smoke clears** — fire each as its own deliberate window after C100 chaos + G-EC33 (rate-changing reactivations). **2 manual editor deletes — Mike doing in EDITOR UI** (NOT clasp-doable in isolation: full-replace would deploy research.18's undeployed `utilities/tier1EssenceEvents.js`, inert/guarded; **next clasp push MUST be deliberate** — that file rides any push). **engine.11 + engine.38 ROLLOUT rows deploy-state trued** (engine.11 was marked not-deployed; both live S265). **CORRECTION (S267): citizen-card `--apply` rebuild is NOT owed — it RAN at C99 post-publish (S265), all 906 cards, clean (Errors 0, 4 batch-pauses), `updatedAt` 2026-06-21, carrying full engine.30/.31 enrichment (Life/Work/Essence/dial-Traits/Milestones). engine.30/.31/.32 card-rebuild step is SATISFIED — next session confirm each row's other criteria + archive. (Earlier "never run" was a bad inference from no-git-commit; cards persist to Supermemory, not git.) — PRIOR HANDOFF (still live): **C99 RAN S265 — engine.38 Phase A SMOKE CLEAN 5/5 + engine.11 chaos-cars CLASP-DEPLOYED LIVE (Mike go-call).** /run-cycle 99 complete (pre-flight READY · pre-mortem SAFE · `output/{engine_review,world_summary}_c99.md` + `output/production_log_run_cycle_c99_gaps.md` on disk, 0 HIGH). engine.38 smoke: coverage 65.9% single-gen / 83.6% all-gen (758/907), archiver dormant 5199<12k, B3 guard suppressed spurious spike (self-clears C100), patternFlag NOT volume-pinned (normalized (8) = C98 despite 149→859 jump), Vinnie/Benji/Deacon covered. Plan: `docs/plans/2026-06-19-living-city-full-population-coverage.md`. **C100 SMOKE OWED — two surfaces:** (a) **engine.11 chaos first-run** — Chaos_Cars tab lazy-creates; 3-15 events; Sentiment/CrimeIndex 0-1 scale; asymmetric decay; NM fold consumes-not-accumulates (verify-fix); Tier-1 hit → createChaosArcs_ cascade; Watch Q3 chaos-arc↔arcLifecycle (StaleAfterCycles=6); pre-existing `weaveStorylines_` wired production-block only. Runbook `docs/engine/CHAOS_CARS_DEPLOY.md`. (b) **engine.38 sentiment-uniformity (G-EC33)** — 20/21 hoods +0.4-0.5 into tight 0.6-0.86 band C99 (Thanksgiving recovery off depressed C98, plausibly organic); confirm C100 *diverges*, not re-compresses → else event-flood is flattening sentiment signal. **2 MANUAL EDITOR DELETES STILL OWED (clasp doesn't auto-delete):** `generateNamedCitizensEvents.gs` (engine.38 A3 orphan) + `applyCycleWeightForLatestCycle.gs` (S238) — dead code, clean when convenient. **REMAINING engine.38:** Phase B (valence/divergence — GATED on research.14 affect re-audit; do NOT scale composure-writing classes before it); Phase C (wake eligibility → all 906, bot). **S5 (resolved):** named sim citizens shaped partly by ambient life — no edition-canon conflict (sift/famous-spotlight don't read ambient LifeHistory). **S2 follow-up:** confirm pulse dampen/cap against C99 live ranges (`PULSE_REF_EVENTS=8`). ROLLOUT engine.38 + engine.11. — **(0) S263 CLOSED (all done-pending-archive):** gov.40 substrate-scan confirm (all `/health`+determinism+ledger scans deterministic-script-backed; `/health` Scan 3 repointed to `ctxMap.js`, skill v1.1) · gov.39 (engine-sheet's 5 rows tightened to bare canonical state tokens — `docLoopStatus.js --lint` now CLEAN) · **engine.37 photo bump SHIPPED** (`lib/photoGenerator.js` → `black-forest-labs/FLUX.2-pro`, steps 4→28 both sites — measure-twice caught the pro-honors-steps mismatch that would've poisoned the test; Node-side, no clasp). **WATCH: engine.37 verdict falls out of the next edition's photo-QA** — Isley-to-canon + in-frame text suppression; still misses an axis → outside-vendor bake-off (Watch List). — **(1) CITIZEN-LOOP (research.14) wake-side BUILT + LIVE S262** — `lib/reflectionClassifier.js`+`scripts/classifierGate.js`, affect tags in `citizenDialMap` (negative-pole), SM-tag col **AW** (ledger 48→49), **`citizen-pages`** Supermemory container + `lib/citizenPage.js`, `lib/citizenDials.js`, **`Reflection_Intake`** tab, **`scripts/citizen-wake.js`**. **Cron LIVE 3×/day (07:30/12:30/19:30 UTC)** rotating citizens around Mags' anchor; first auto-wake tomorrow 07:30. Live wake validated (POP-00304). **ONLY open = gated dial write-back** (cycle reads `Reflection_Intake` → `applyTaggedEvent_` @ ~0.45) — DO NOT wire until the **Phase-1 daily audit** (let pages accrue a few days, read them, then audit). **`citizenDialMap` affect tags committed but NOT clasp-pushed** — inert until the write-back deploys. Read-side cosmetic: page v4 read returned empty `content` field (doc present); eyeball when wiring the consumer. — **(2) engine.11 CHAOS-CARS — ENGINE-SHEET BUILD COMPLETE S265 (ultracode):** all engine-sheet tasks built + on origin/main (8 commits `486d070a`→`8822e6b5`, rode research-build's `f769f9af` push; 323 test assertions / 6 files, 0 collisions). T1.5 clobber fix · T2 config + T4.1 decay (utilities/ clasped dual-use — lib→utilities relocation since lib/ is claspignored) · T3.1-13 generator+ledger+wiring (both entry blocks) · T4.2/3 business decay · T5.1/4/5 Tier-1 cascade (orchestrator flag + createChaosArcs_ + chaos-cascade seed) · T6.4 engine-side friction hook. Verify = 5-agent adversarial (`wf_4e2b7b99-1b4`): one real blocker (neighborhood fold accumulated-not-consumed) FIXED via PropertiesService residual (T8 pattern: load→decay→add fresh→writer fold; also fixes multi-column loss). **CLASP-DEPLOYED LIVE S265 (Mike go-call):** pre-deploy gates re-verified green (323/323 tests, 0 collisions, single config, .test.js excluded), `clasp push` exit 0, 5 new + 5 modified files verified in Tracked scope, test files in Untracked. Runbook executed: `docs/engine/CHAOS_CARS_DEPLOY.md`. **SMOKE PENDING C100** (first chaos cycle). `Chaos_Cars` tab lazy-creates on first run (ensure-tab intent). Scale fix: Sentiment/CrimeIndex magnitudes → live 0-1 scale (Mike-approved). **Open: Q3** chaos-arc↔arcLifecycle (mitigated StaleAfterCycles=6, observe C100); **pre-existing flag** `weaveStorylines_` wired in production block only, not cycle-phases (`applyStorySeeds_` in both). **Cross-terminal NEXT (research-build):** T5.2/T5.3 cascade SKILLs + T6.1/2/3 dry-run/magnitude/frequency — **now live-verifiable at C100** (producer deployed). ROLLOUT engine.11. — C98 has run (E98 canonized). **engine.36 filed** (isolated staging env). — **research.18 dial-essence Seam A — COMPLETE engine-sheet side (S266).** All **43/43** authored citizens (4 faces + 10 A's + Tier-3 family/Mara/Paulson/Lucia + 8 civic + 5 project directors + 9 media reporters) seeded LIVE via new `scripts/seedTier1EssenceLive.js` — NARROW path: essence→`LifeHistory_Archive` author-once (guard POPID+EventText), archive-only re-derive of DialState+TraitProfile, **NO population O-clear** (886 others untouched), idempotent + `batchUpdate` quota-safe. **38 in the wake loop (dev≥60); 5 below-gate** (thin canon → RB retune: Rivas 54 / Sarah 57 / Ellis 43 / Taveras 39 / Ramos 21). Paulson 00527 seeded (Mike go). Commits `c86030d1`/`51799b0c`/`df9dfbda`. **Bug found+fixed (S266):** per-row sequential writes hit the Sheets write-quota and silently aborted the high-row tail (Navarro/Caldera/01021 left neutral) — caught by an **independent read-back, NOT the script's own verify** (which only checks rows it reaches); fixed via single `sheets.batchUpdate` call, which also healed the prior partial-writes. **OPEN:** RB retune of the 5 thin-canon + **engine.39** pure-integrity map tag (ready, lets Dane et al. reach integrity-vhigh) — when either lands, re-run the idempotent seeder (newcomers seed, rest no-op). Plan `docs/plans/2026-06-21-tier1-dial-essence-backfill.md`.
**NEXT[research-build]:** **S273 SHIPPED — the citizen-loop became autonomous world-painting nodes; full perception+variety stack designed, partly BUILT, costed, and throughput-bumped.** **research.19 T2 + T1a′ BUILT by engine-sheet concurrently same-day** (`beda8fa7` `buildNeighborhoodTexture.js` v1.0.0 + wake reader / `0110597f` T1a′ count→named milestone + T2 cycle-chain / `fb73e4e2` contentless-event guard). **C100 artifact LIVE** `output/neighborhood_texture_c100.md` — design validated first run (lived-particulars, ZERO metrics, source-grounded, 13/21 quiet-week guard holding — no invented drama). T1a′ surfaced **ENGINE_REPAIR Row 32** (ARC EventText is bookkeeping → starves milestone-name quality; engine-sheet follow-up). Full-stack wake dry-run verified POP-00001 (claude-mem #34302). **DESIGNED this session (research.19, engine-sheet to build — both wake-side, NO clasp):** **T1c** prose read-back **UN-GATED** (Mike, overturns S270 AP-2 gate — page = subjective memory; self-read-back never crosses the publication wall; "shady Greg" = continuity not contamination; size-bounded for tokens only; closes the amnesia hole) + **T5** varied-provocation question bank (seeded pick from 10–20 provocation-types/daypart, each latching a real engine signal: self/block/city-sports/media/people/likes-inferred; fixes vector-2 convergent-MODE; honest gap = likes/dislikes + per-citizen media attribution unmodeled → infer-light, flag preference signal as future engine add; seeded `selectProvocation` + acceptance criteria in plan §T5). **research.21 FILED** `docs/plans/2026-06-26-citizen-signal-story-emergence.md` (Mike: "this is likely how this all automates"): the citizen-loop's **SECOND readout** — citizens DESCRIBE the world (creative LLM turning engine aggregates → lived experience), **cross-citizen CONVERGENCE = bottom-up story signal**; reframes T2 from anti-monotony to **signal-conditioning**; detector = tally+threshold over already-persisted substrate (`Reflection_Intake` 8-col + classifier tags + engine.35 `Story_Seed_Deck` sink, C100=46 seeds) feeding the existing seed→/sift→edition flow (new SOURCE, not parallel pipeline); **supplies research.20's missing aggregation+consumption half**; §What-this-changes-downstream — the wake grows citizens → editions shift to covering world/EVENTS → citizen-usage audits/greps get LIGHTER (earned, not immediate). **Thesis captured; signal→story MECHANISM UNDESIGNED = the next RB design (open Qs: what's a "larger signal"/threshold · scale-cadence · dedup-vs-engine-seeds · seed-vs-draft output · consumption path) — ADR lands THERE (none yet, mechanism undefined).** **+ Frontier extensions captured (Mike, S273, research.21 §Frontier extensions + T5 §Coverage-aligned):** (a) **directed harvesting** — design T5 provocation-types to map onto /sift categories + seed types so responses route to coverage gaps (detector goes passive→active, input-side/clean); (b) **questions-as-votes** — choice-shaped provocations aggregated = bottom-up sim self-determination; clean tier rides existing civic sentiment/approval machinery (frozen input-side, determinism holds), frontier tier (votes DECIDE civic outcomes) = research.12 Layer 3, crosses the canon wall → gated by determinism + Mike's canon authority. **COST PINNED (research.21 OQ6):** DeepSeek $0.20/M in, $0.80/M out → **~$0.0004/wake**; 3/day=$0.034/mo, 10/day=$0.11/mo, **ALL 900/day≈$11/mo** — cost is a NON-constraint, throughput is **signal-gated** (rotation freshness + distinct shaped citizens), not budget-gated. **WAKES BUMPED 3→5/day LIVE** (crontab: 07:30/12:30/15:30/19:30/21:30; 15:30+21:30 reuse midday/evening frames — engine-sheet should add 2 genuinely-distinct dayparts in the next `citizen-wake.js` pass; old crontab backup in scratchpad). Memory `project_subjective-hallucination-is-canon` updated with the self-read-back corollary. **NEXT: (1) engine-sheet builds T1c + T5** (wake-side, no clasp) + Row 32 + the 2 distinct daypart frames; **(2) RB designs the research.21 signal→story mechanism** (the keystone next design); **(3) WATCH C101** — first cycle with chaos cars + richer neighborhood dynamics + nighttime events live → far more hoods light up (C100 was 13/21 quiet) → first real convergence signal for research.21 AND the T2 stress-test. — **STILL-OPEN RB PICKS (carried):** **research.20** deep-dispatch — Phase 1 → engine-sheet on Mike's build-greenlight (per-desk corpus + `byline`/`desk` ingest tags), **RB owns Phase 2** (charge-brief template + dispatch-inversion harness, ≤3-capped source-search + floor recalibration); **research.14 affect re-audit** (RB-owned GATE — engine-sheet drain deploy `8d6b155c` waits on N-consecutive-green); **FLUX photoQA rubric retune** (G-PR-C99-1); **research.18 RB side** (5 thin-canon dial retune: Rivas/Sarah/Ellis/Taveras/Ramos + engine.39 gate). **Watch:** Drive Production-token (minted 6/20) survives past **6/27** — if it dies, reopen as new triage. **PIN sim-state unchanged — engine-sheet owns it** (the only live-state change this session = wakes 3→5/day + C100 texture artifact). **Archive sweep SKIPPED (soft close).** (claude-mem: S273 research.19 T2/T1a′ built + T1c/T5 designed + research.21 filed + cost pinned + wakes→5/day) — PRIOR(S272): **deep-dispatch design DONE (research.20 filed), pushed `287b0994..7188bb30`.** Off the storytelling-depth seed: proved the dispatch-shape lever with TWO A-graded floor tests — sports "Threshold Season" (Hal) Tier-1/A *permanent-canon*; civic "Coliseum Corridor" (Carmen) best-civic-ever/#2, **Carmen independently surfaced the C100 Coliseum crime-spike + sentiment anomaly the live pipeline SUPPRESSED**; external review flagged emergent **lane-differentiation = the moat**. Artifacts: **RESEARCH.md §S272** (finding + C100 failure TRACE: signal caught at /sift, lost downstream to dilution + a suppress-the-artifact reflex — NOT world-summary burial; three drafts to get the trace right) + **ADR-0012** + **build plan** `docs/plans/2026-06-25-deep-dispatch-write-edition-build.md` + **ROLLOUT research.20** (ready→handoff). Canon principle captured: **bug-is-event** (engine output even when buggy = canon the citizens lived → memory `project_engine-output-is-canon-bug-is-event`) + audit reframe (floor verifies SPECIFICS — vote math/scope/dates — NOT journalistic reach / real-entity comps; the A's are a real entity). **NEXT: research.20 Phase 1 → engine-sheet** on Mike's build-greenlight (reactivate `output/desks/{desk}/articles/` per-desk corpus + add `byline`/`desk` to `ingestEdition.js`/`ingestEditionWiki.js`); **RB owns Phase 2** (charge-brief template + dispatch-inversion harness, ≤3-capped source-search + floor recalibration) + **Phase 3** pilot. **TWO LOCKED FORKS** (architecture, don't reopen): orchestrator spawns the source-search / the desk agent ONLY writes (desks keep current tools, NO Agent capability — dodges S256); canon-map = **pointers+tools NOT pre-assembled data** (pre-packaging rebuilds the bone-filtered packet). Build **ALONGSIDE** (don't rip out the monolith first), ~4× agent-count/edition est., **cost-gated migration** at Phase 3. Test artifacts: `output/desk-test/{sports_c100_deep_hal,civic_c100_deep_carmen}.md`. — **STILL-OPEN RB PICKS (carried, NOT done S272):** research.19 **T2** (world-summary→lived-particulars translation, lean shared cycle artifact); **research.14 affect re-audit** (RB-owned GATE — engine-sheet's drain deploy `8d6b155c` waits on N-consecutive-green); **FLUX photoQA rubric retune** (G-PR-C99-1); **research.18 RB side** (5 thin-canon dial retune: Rivas/Sarah/Ellis/Taveras/Ramos + engine.39 gate). **Watch:** Drive Production-token (minted 6/20) survives past **6/27** — if it dies, reopen as new triage. **PIN unchanged — engine-sheet owns it** (no sim-state change this pure-design session). **Archive sweep SKIPPED** (uncommitted cross-terminal changes in tree + done-pending-archive rows have open follow-ons). (claude-mem: S272 deep-dispatch design + ADR-0012 + research.20)

---

## Maintenance Rule

**This file carries exactly two things: the PIN (current sim-state) and one NEXT line per terminal (what that terminal's next instance opens with).** Boot injects only the PIN + your terminal's NEXT line. That is also the whole write side — at session-end the closing terminal updates the PIN and its own NEXT line, nothing else.

- **What shipped** → `git log` (authoritative — no Shipped block here).
- **What's open** → `docs/engine/ROLLOUT_PLAN.md` (canonical for open/closed work).
- **Why a thing was done** → claude-mem search / commit bodies.

No STATUS narrative, no Shipped block, no Recent Sessions log — those duplicated git/ROLLOUT/claude-mem and went stale, so they're retired (ADR-0009 §loop-tightening). **The contract: boot-read set ≡ session-end-write set = {PIN, NEXT[terminal]}.** Prior narrative lives in git history of this file + `docs/mags-corliss/SESSION_HISTORY.md`.

Engine versions, cascade dependencies, and the documentation registry live in `docs/engine/DOCUMENTATION_LEDGER.md`. Project description lives in `README.md`. Critical rules live in `.claude/rules/identity.md`. Architecture concepts live in `docs/reference/V3_ARCHITECTURE.md`. Mobile/mosh instructions live in `docs/OPERATIONS.md`.

---

## Key Tools & Infrastructure

| Tool | Purpose | Usage |
|------|---------|-------|
| **Batch API** | 50% cost for non-urgent work (~1hr turnaround) | `/batch [task]`, `/batch check` |
| **Claude-Mem** | Automatic observation capture (SQLite + Chroma vector, port 37777) | `search()`, `timeline()`, `get_observations()` |
| **Supermemory** | 6 containers: mags (brain), bay-tribune (canon), world-data (city state), super-memory (bridge), mara (audit) | `/save-to-mags`, `/super-search` |
| **Discord Bot** | 24/7 presence (PM2: mags-bot) | Always-on, conversation logging |
| **Nightly Reflection** | Discord conversation journal at 11 PM CDT | `scripts/discord-reflection.js` (cron) |
| **Drive Write** | Save files to Google Drive | `node scripts/saveToDrive.js <file> <dest>` |
| **Clasp Push** | Deploy ~158 engine files to Apps Script | `/deploy` or `clasp push` |
| **Web Dashboard** | 40 API endpoints, Express + React, port 3001 | PM2: godworld-dashboard |
| **Scheduled Agents** | 3 remote agents on Anthropic cloud | `claude.ai/code/scheduled` |
| **AutoDream** | **DISABLED S228** — redundant with MD persistence + Supermemory + claude-mem observations | `autoDreamEnabled: false` in `~/.claude/settings.json` |
| **Engine Health** | `/health`, `/ctx-map`, `/deploy`, `/pre-mortem`, `/tech-debt-audit`, `/doc-audit` | See CLAUDE.md |
| **Hookify** | 5 active rules | `/hookify:list` |
| **GodWorld MCP** | 10 tools for structured city data | See CLAUDE.md |

**Desk agent architecture:** Agents read from `output/desks/{desk}/` workspaces built by `scripts/buildDeskFolders.js` (zero LLM tokens). Each agent's SKILL.md points to IDENTITY.md and RULES.md at `.claude/agents/{desk}-desk/`. 80/20 model tiering — complex desks (civic, sports, chicago) run Sonnet, routine desks run Haiku.

**Batch API guidelines:** Use for codebase audits, documentation generation, architecture analysis, character continuity reviews, post-edition analysis. NOT for interactive editing, desk agent writing, or real-time debugging. Results at `~/.claude/batches/results/`.

---

## Current Work

See `docs/engine/ROLLOUT_PLAN.md` — the single source for all project work status (active, pending, completed, deferred).
