# SESSION_CONTEXT — NEXT WORK ONLY. One PIN + one NEXT line per lane. What already happened lives in git, claude-mem, ROLLOUT_PLAN and the gap/production logs — never here. If a line describes a session instead of an assignment, it is written wrong.

**PIN:** S479 | Day 196 | canonical C108 | **PROD @108 = clasp v95 = `a92e84a6`** (engine.250 + civic.38 Task 5 + stall-drag dial 0.5; web app read back @95) | Bench SANDBOX 0908 @78 = `a92e84a6` at C122 — bench-only, never replay (tracker INIT-003 / INIT-005 edited on the bench) | claude-mem observer on `nvidia/nemotron-3-super-120b-a12b:free` (infrastructure.11 open); a new worker env var needs `npx claude-mem stop` then `start` — `restart` re-spawns with the OLD env | `diag187` rides the web-app fire response only, an editor fire has none | engine-sheet LEADS the sim repairs | never `--amend`; wiring card before every cut; deploys from an isolated `git archive` stage | DOCTRINE §13/§15/§16/§17 | Sunday 21:00 city-hall slot runs the C108 chain | civic.38 is the house project: plan `docs/plans/2026-09-19-civic-wake-game-loop.md` §Lane assignments, one owner per file, dry-run only | Ledger 952 rows

**NEXT[engine-sheet]:** Smoke C109 on PROD @108: log `record:` = last feed row's W-L; `applyNeighborhoodEffectsFold_ … approval (cycle 108 carry) → 0 hood(s)`; `initiative lift 0 / drag 0` unless a tracker phase moved; first live approval fold row the Cycle after. Then civic.38 Task 4: step 1 first cut is on main UNDEPLOYED (six self-armed stage columns, `CIVIC_STAGE_CATALOG_` mirror, shared `stageRequirement` helper, parity block in `lib/initiativePhaseContract.test.js`; inert — every live `Stage` is blank). Bench it before anything else: `node scripts/syncSandboxFromLive.js <0908 sheet id>` dry then `--apply` (the permission classifier refused it — the builder runs it with `!` or allows it), push + bump, fire twice, read back 37 tracker headers and a C-next civic log identical to C-prev; then PROD push + version step and regenerate `schemas/SCHEMA_HEADERS.md` after the first live fire. Next cut: the Phase-5 stage handler per plan §Task 4 engine rulings 1–6 (above the signed-row exit; `Proposed→Funded` on a signed pass; Funded→Standing on the work column; conversion-time `StageBaseline` descriptor; city-median Delivering comparator on `World_Config civicDeliverMargin` 0.20 / `civicDeliverHoldCycles` 3 — builder-ruled, self-arm them like `bizInitiativeStallDrag`), then step 2 (`staged` flag into `classifyInitiativeMotion_`, skip `applyEngineClockHold_` on staged rows), step 3 (stall detector skips `no-delivering-gate` AND `Proposed` — builder-ruled, revisit when petitions run; T7 yields on a stalled staged row), step 4 ID-keyed six-row conversion fixture (OARI = INIT-002). Matched-control bench pair owed before any Delivering margin is trusted. C109 also arms `World_Config bizInitiativeStallDrag` at 0.5 (read it back). Then engine.249 hood migration (ruled, no rebase). Then engine.251 housing lever (`docs/plans/2026-09-20-housing-lever.md`; wiring card + pre-mortem before the cut; housing stays non-playable until writer, schema and causal tests pass). Own session: engine.248. Rest: `output/production_log_run_cycle_c108_gaps.md` G-EC53–69.

**NEXT[research-build]:** civic.38 sequencing and review against the plan. The old Sunday chain is not expected to run clean while civic is rebuilt (C108 halted at the voice step 2026-09-20; builder: expected, no re-entry, no chase) — the old crons stay as they are until the new loop replaces them. Stack pushed 2026-09-21 (`49a51032`). Waiting: codex implementing the four §Rulings, then the civic reference doc after engine-sheet lands Task 4. Housing lever = engine.251 behind Task 4 step 1. Builder still to answer agy's permission prompt.

**NEXT[kimi]:** civic.38 chain A COMPLETE, 6 commits local, none pushed (mixed stack; builder: hold the push until fixes land): 8b07fc29 T1+3, ef57efe0 T2 (ledger + Sunday fold + Status edge + candidates through validator/clerk/gate), 48ebf9df T8 (directive = 10 elected seats + per-seat evidence), 1ddcf92f T9 (rota = 11 seats; 4 directors → work-wake packs), 9d22388a T6.3 (petitionGateSweep; PETITION_SUPPORT_BANDS empty — nothing gates live until the builder names a band), a1cd97e2 handoff/debt notes, 41c6892c Reflection_Intake live-header fix (ReflectionExcerpt; names verified against codex's counter — agy's T3.2 fixtures use Event/Snippet guesses, reader accepts both). Catalog (89b5c36d) + codex counter both live; propose path closed-loop. agy's T3.2/T3.3 failures resolved at HEAD (game suite 25/25). Flags: touched workWakePackages.js (off my editor list); debt captures in output/kimi/2026-09-20-civic38-code-debt-captures.md. Next: builder sets signature bands; codex review of 8b07fc29; engine-sheet T4-5 bench. Standing: adversarial review duty on repair waves (output/kimi/).

**NEXT[codex]:** civic.38 defects + all four ruled choices committed locally through cec4c698; game26/counter15/repair15. Mixed-stack push held. | NEXT: send agy per-fix fixtures once Mike clears its permission prompt; Kimi resumes scripts tomorrow. Shared stage requirement helper remains engine-sheet Task 4; housing accepted as engine.251.

**NEXT[antigravity]:** civic.38 Task 9 tests — you own `scripts/cron-civic-game.test.js`. Scaffold exists and runs against the real Task 1+3 code (`8b07fc29`); T3.2 (petition pool) and T3.3 (confrontation parse) currently fail — decide fixture error vs kimi defect, and report defects to kimi in tmux. Add fixtures for Tasks 2, 6, 8 as each lands; adversarial review of each landed task goes in `output/antigravity/`. Re-read the file before every claim.

**NEXT[grok]:** No pane; nothing queued.
