# SESSION_CONTEXT — NEXT WORK ONLY. One PIN + one NEXT line per lane. What already happened lives in git, claude-mem, ROLLOUT_PLAN and the gap/production logs — never here. If a line describes a session instead of an assignment, it is written wrong.

**PIN:** S442 | Day 191 | canonical C106 (Mike fires live) | PROD @74 — trail: `docs/reference/DEPLOY_HISTORY.md` | bench SANDBOX 0908 @11 = HEAD `6db160c6` (kimi wave + reel-2 + 184 + 185 + 186 all bench-proven); C107–C119 clean; dials 13/12 fired, acceptance read pending — `docs/reference/DEPLOY.md` | DOCTRINE S433: GATE THE FACTS, NOT THE COLOR — `docs/SIM_DOCTRINE.md` §13 | civic.35 number gate live; Sunday 09-13 14:30 city-hall cron next | LENS: `docs/SIM_DOCTRINE.md` §15 — a gate that can't fire is a trick; `/health` Scan 4 = `scripts/deadBranchScan.js` | engine-sheet is the gate of what touches the sim (TERMINAL.md §Authority) | PROD untouched this session — every clasp push went to the BENCH only; `healthCauseIntake.js` git-rm'd, needs manual Apps Script editor delete on PROD when 175–184 ship | pipeline.68 Task 1 live (`output/beats/` @C106) | canon = published only | DIALS unbenched — `docs/plans/2026-09-08-dials-as-a-game-plan.md` | Partnership NORMAL | Ledger 930 rows, Citizen_Archive 54 (`Undocked_Draw` tab empty until engine.175 hits PROD)
**NEXT[engine-sheet]:** In order — (1) engine.188 sentiment TOP end: the upward vocabulary gets the engine.185 treatment (which of the 54 pushes are always-on; relative gating for count-fed ones), bench 2–3 cycles, resting mood should settle well under 1.0. (2) Read dials plan §Acceptance off the bench (13 cycles fired, plan doesn't read sentiment), then PROD: 175 + 176–183 + owner door + kimi health/drift wave + 184. **185/186 HOLD at HEAD until (1) lands** — they touch applyCityDynamics / generateCrisisBuckets / finalizeCycleState / v3NeighborhoodWriter:433 only. (3) engine.187 stuck shock detector — `node scripts/deadBranchScan.js` + read applyShockMonitor's non-event gates. (4) Real-crisis proof: synthetic seed on the BENCH only (crank one hood's Sick count / unemployment) after (1), to show a `<= -0.3` branch actually fires and an arc closes against a mixed cycle — bench-only, never replay. (5) Mirror `/root/.claude/projects/-root-GodWorld/memory/` somewhere the repo protects — it is the only 'who I am' state living solely on the disk named as the wipe target. BUILDER'S (sim): arc NAMING — who mints at onset and from what; default to propose: engine mints a slug at onset from hood + trigger channel + season (e.g. `Brooklyn Retail Slump, Winter Y3`), media may rename on first coverage with the slug as canonical key. After next live fire: backfill Key_Personnel for Intake rows 65–70 by name.
**NEXT[research-build]:** After the builder's next live fire: resolve the six founders (Intake rows 65–70) by name off Simulation_Ledger, write `POP-nnnnn <Name> (Founder/Co-Founder)` to Business_Ledger Key_Personnel for BIZ-00007/8/9/10/11 ONLY, read back, confirm `applyOwnerDraw_` unresolved → 0 for those five. Standing: Monday 06:15 pipeline.41 check; Friday Hal retry; Saturday pipeline.65 acceptance. pipeline.68 Tasks 2–7 are engine-sheet's — don't start them.

**NEXT[kimi]:** Health system session landed: slice freshness `78d1d836`, S.demographicDrift clobber fix `e0f69e0d`, intake retired `1240a80a`, capacity+twin-number rulings `fc3b1d2f` — engine at HEAD only (bench mid dials-acceptance; insert next window). Hidden-tab audit filed `b189b3d9` → infrastructure.8 PARKED (`docs/plans/2026-09-09-hidden-tab-audit.md`), waits on builder keep/delete rulings. Next: after C107 dump confirms Health_Cause_Queue stays empty, drop it from SEAT.tabs + dumpBeatTabs (ruling 2); Monday fanout behavior at C107. Rulings 3/4 reach no consumer — POPULATION is filtered out of Cycle_Packet by `KEEP_SECTIONS`; never prove a packet-line change against that tab. Inbox CLEAN (S442): faith + business lane docs accepted to `docs/research/`, engine cuts filed engine.189 / engine.190 for engine-sheet — nothing back to you on either. Education lane: you hit the weekly quota wall before writing anything — your findings are captured in the pipeline.68 plan Changelog 2026-09-10; roster defect filed engine.191. When quota resets: scripts-side item 1 (`buildSchoolsSlice.js` v2) is yours; the engine cuts + school-drift question wait on the builder's ruling (frozen table: drift engine vs static backdrop).

**NEXT[codex]:** Nothing queued. AGENTS.md authorization stands for when it returns.

**NEXT[antigravity]:** Nothing queued.

**NEXT[grok]:** Idle. civic.24 Task 9 stays gated on C104+ (`--stage=chain`, no `--apply`).
