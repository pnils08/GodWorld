---
title: Engine Repair Tracker
created: 2026-04-16
updated: 2026-04-29
type: plan
tags: [engine, citizens, active]
sources:
  - SESSION_CONTEXT.md (S148 audit findings — Next Session Priority)
  - docs/engine/tech_debt_audits/2026-04-15.md
  - docs/engine/tech_debt_audits/2026-04-27.md
  - docs/engine/LEDGER_REPAIR.md
  - S166 /pre-mortem for C92 (items 12, 13, 14)
  - S181 /tech-debt-audit §6 closure (items 15, 16)
pointers:
  - "[[engine/ROLLOUT_PLAN]] — strategic phases; this tracker is tactical repairs"
  - "[[engine/LEDGER_REPAIR]] — read before touching ledger"
---

# Engine Repair Tracker

Working list of known engine/simulation issues. One row per issue. Pointers only — no
exposition. Update status as work moves. Source findings: S148 audit
(SESSION_CONTEXT.md), `docs/engine/tech_debt_audits/2026-04-15.md`.

**Status legend:** `todo` · `wip` · `done` · `blocked` · `deferred`

---

## P0 — Foundation (blocks everything downstream)

| # | Issue | Status | Session | Evidence | Fix pointer |
|---|-------|--------|---------|----------|-------------|
| 1 | Promotion pipeline typo — generator writes `CreatedCycle`, sheet has `EmergedCycle`. 11 promotions in 91 cycles. | done | S181 | S148 audit; S181 verification | Closed S181 — `phase05-citizens/generateGenericCitizens.js:58,495` renamed `iCreatedCycle` → `iEmergedCycle` (matched live Generic_Citizens schema col H) and value format aligned with `checkForPromotions.js:511` (`"Cycle " + cycle`). `utilities/cycleRollback.js:55` Generic_Citizens cycleCol fixed to `EmergedCycle`. Chicago_Citizens kept on `CreatedCycle` (verified col H of live schema). Note: the "11 promotions in 91 cycles" stat is about promotion frequency — separate from this column-name typo. |
| 2 | Supermemory `world-data` citizen cards cross-contaminated; MCP `lookup_citizen` reads poisoned data. | done | S183 | S148 audit; S181 partial close (filter); S183 full DELETE pass | Closed S183 — full container rebuild closes the contamination. R1 cold-start fix (`1fc2526`) wiped all 408 prior wd-citizens + wrote 686 fresh via `--no-quality-gate` + idempotency-filter drop, so old contaminated cards no longer co-exist with new ones. Substrate post-S183: wd-citizens=686, 100% domain-tagged, 0 orphans. S181 full-name post-filter (`097b792`) eliminated cross-contamination at write time; S183 wipe eliminated the historical co-residence problem. MCP `lookup_citizen` now reads only the fresh layer (verified S183 via tag-count snapshot). Plan-file: [[plans/2026-04-27-world-data-unified-ingest-rebuild]]. |
| 3 | Simulation_Ledger corruption flagged S68 — `LEDGER_REPAIR.md` claims S94 recovery complete. Confirm or reopen. | done | S181 | `docs/engine/LEDGER_AUDIT.md` §"Current State — S181 refresh" | Closed S181 — verified live state against S94 closure claims. 0 missing names, 0 historical "Citizen" roles, all tiers numeric, age sanity holds (0 OOB on 2041 anchor). 4 new "Citizen" roles are post-S94 drift (Row 17). Audit tool: `scripts/auditSimulationLedger.js`. Boot blocker text in `.claude/hooks/session-startup-hook.sh:132` updated to reflect post-recovery reality. |

## P1 — Simulation depth (the sim doesn't simulate)

| # | Issue | Status | Session | Evidence | Fix pointer |
|---|-------|--------|---------|----------|-------------|
| 4 | Lifecycle engines stamp identical defaults on 600+ citizens (YearsInCareer=12.5, DebtLevel=2, NetWorth=0, MaritalStatus=single, NumChildren=0, etc.) | done | S184 | S148 audit; live ledger 95-99% top-value share confirmed pre-backfill | Closed S184 — root cause was the migration scripts (`addEducationCareerColumns.js`, `addHouseholdFamilyColumns.js`, `addGenerationalWealthColumns.js`) seeding uniform constants across every existing row when columns were added. Lifecycle engines (`educationCareerEngine.js` increments YearsInCareer +0.1/cycle, hence 10 → 12.5 over ~25 cycles; `generationalWealthEngine.js` handles inheritance; `householdFormationEngine.js` handles marriage/divorce/birth) update on triggers, but most citizens never had a trigger so 95-99% sat at the migration default. **Fix:** `scripts/backfillLifecycleDefaults.js` (Node + service-account direct write per engine.md) gives each row a demographically-plausible starting value derived from existing context (Age via BirthYear, Income, RoleType, CareerStage). Determinism: djb2 hash on POPID — same POPID always gets same values; safety: only overwrites rows currently at the migration constant; preserves engine-applied updates and real edits. Field derivation: YearsInCareer (age-bracket bell, retiree-aware), DebtLevel 0-10 (income-inverse + age curve), NetWorth (age-conditioned + income-multiplied + retiree-bonus), MaritalStatus (age-bracket CDF), NumChildren (age + marriage-conditioned fertility CDF). **Live state post-backfill** (top-value share dropped from 95-99% to 2.6-49.6%): YearsInCareer 308 distinct values (was 5); DebtLevel 9 distinct 0-7 bell-centered at 2-3 (was 1); NetWorth 557 distinct (was 3); MaritalStatus married 49.6% / single 21.7% / divorced 14.3% / partnered 7.2% / widowed 7.1% (was 98% single); NumChildren 0-5 distribution peak 0 (was 99.3% zero). Sample verification: Vinnie Keane (POP-00001, age 38) net worth preserved at $240M canonical; Marcus Osei (POP-00036, age 68, council) → 44.9 yrs career / widowed / 2 children / $940k; Benji Dillon (POP-00018, age 36, MLB) → 5.6 yrs / married / $623k. Node-only changes; no Apps Script, no clasp deploy. **Out of scope (separate ROLLOUT items):** (a) POP-00004 Lucia Polito column-misalignment ('Aura ' in RoleType + 'Wellness Practitioner' in DebtLevel — looks like split write); (b) 137-165 empty-cell rows (no value in column at all, lacks BirthYear or row shorter than schema); (c) engine-side derivation so new emergences get realistic values at intake time (currently lifecycle engines only fire on triggers; intake leaves these fields empty). |
| 5 | Citizen generator name clusters — 62 first / 53 last names for 686 citizens. Dupe check misses first-name clustering. | done | S184 | S148 audit; live GC tab inventory shows pre-v2.6 clusters Ronald=11, Johnson=12 | Closed S184 — `phase05-citizens/generateGenericCitizens.js` v2.6 → v2.7. (a) Pool expanded 62 → 186 first names + 53 → 144 last names = 26,784 combos (was 3,286). New entries diverse for 2041 Oakland: Latino, Black, East/Southeast Asian, South Asian, Middle Eastern, African (West/East), Anglo. No Tier 3 real-public-figure names introduced. (b) `getUniqueName` now tracks per-first and per-last counts via `firstCounts`/`lastCounts` maps populated from existing GC tab at runtime; rejects new draws that would push a first name past `FIRST_NAME_CAP=3` or last name past `LAST_NAME_CAP=4`. Pre-v2.6 clusters (Ronald=11, Johnson=12, etc.) grandfathered in via the snapshot — cap blocks NEW additions only. (c) Smoke-test (500-draw simulation with grandfathered clusters seeded): 0 null returns, max-per-first 3 / max-per-last 4 as expected, 0 new Ronalds / 0 new Johnsons. Determinism preserved — `randItem` still uses ctx.rng pattern unchanged. Apps Script change in phase05; awaits clasp deploy (next batch). |
| 6 | EventType taxonomy collapsed to `misc-event` — Phase 38.8 baseline briefs can't attribute events to citizens. | done | S184 | S146 open items; S148 audit; S184 verification (C88 fixture, 16 briefs all with `subjectIds`) | Closed S184 — diagnosis was wrong target. The `WorldEvents_V3_Ledger.EventType` field IS 91% `misc-event` (212 of 233 rows), but that ledger has no `POPID` column at all — it never could carry citizen attribution regardless of taxonomy. Citizen life events live in `LifeHistory_Log` (3,589 rows, keyed by POPID, 34 distinct EventTags). The auditor wasn't reading that tab. **Fix:** added `'LifeHistory_Log'` to `engineAuditor.js` SHEETS_TO_READ; new section in `generateBaselineBriefs.js` v1.0.0 → v1.1.0 reads the log, filters by current cycle + 6 structural EventTag values (`Life Event`, `Promotion`, `Advancement`, `CivicRole`, `Retirement`, `Stabilized`), emits `eventClass: 'citizen-life-event'` briefs with `subjectIds: [popId]` + neighborhood resolved from ledger when LifeHistory carries placeholder ('Engine'/'Generational'). Promotion hints tier-aware (Tier-1/2 = feature candidate), tag-aware (Promotion / Retirement / CivicRole / Birth into population each get specific hints), neighborhood-aware (active-ailment overlap). Smoke test: C88 fixture (9 Promotions + 2 Retirements live) → 16 briefs, 16/16 with subjectIds populated, real names/neighborhoods/roles joined from ledger, tier-2 feature-candidate hint fires on Marcus Osei (advancing to council). C92 fixture → 0 briefs (correct — C92 had 0 structural events live, all 73 LifeHistory rows were texture). High-frequency texture tags (Micro-Event, Daily, Relationship, etc. — 27 of 34 tags, ~95% of rows) excluded by design — those are color, not structural events. **Out of scope:** improving `recordWorldEventsv3.deriveDomain` keyword coverage to reduce the 91% misc-event share — that's polish that doesn't affect subject attribution; logged as separate ROLLOUT followup. |
| 15 | `citizenFameTracker.js` silently no-ops because expected columns absent from live tabs — fame tracking has been dead for many cycles. | done | S184 | S181 audit `docs/engine/tech_debt_audits/2026-04-27.md` §6 Class 2; S184 Mike decision (Path B retire) | Closed S184 — Path B (retire). Decision rationale: tier system already encodes fame (T1 most-known → T4 least), `UsageCount` on Simulation_Ledger drives tier promotion (the appearance counter wd-citizen cards read), `EmergenceCount` on Generic_Citizens drives generic → SL promotion. The fame-tracker was a fourth signal trying to do the same job, blocked by missing columns. Adding 14 columns across three tabs to revive a redundant signal is sheet-bloat against the current architecture. **Retire steps:** (a) `phase07-evening-media/citizenFameTracker.js` deleted (`git rm`); (b) `phase07-evening-media/mediaRoomIntake.js` v2.6 → v2.7 — `typeof updateCitizenFameFromMedia_ === 'function'` block removed at line 149-151, header docstring updated; (c) `docs/engine/ENGINE_STUB_MAP.md` citizenFameTracker section replaced with retired-note + retired-architecture explanation; (d) migration helpers `scripts/addCitizenFameColumns.js` + `scripts/rollbackCitizenFameColumns.js` retained for archaeology with RETIRED header notes (do-not-run). **Cultural_Ledger.FameScore intact** — that's an independent system (live, read by `recordMediaLedger.js`, `compileHandoff.js:751`, `citizenContextBuilder.js:639`, written by `culturalLedger.js:280`). Touching it would break canon — out of scope for this retire. Apps Script change in phase07; needs clasp deploy. |

## P2 — Architecture integrity

| # | Issue | Status | Session | Evidence | Fix pointer |
|---|-------|--------|---------|----------|-------------|
| 7 | 4 live `Math.random()` fallbacks — determinism violation. Flagged 2026-03-28. | done | S156 | `docs/engine/tech_debt_audits/2026-04-15.md` | Closed by `76a408c` (documented 4) + `af40282` (55-site sweep → `safeRand_(ctx)`) |
| 8 | 38 undocumented direct sheet writers / 197 call sites. | wip | S184 (plan drafted) | `docs/engine/tech_debt_audits/2026-04-15.md` | Plan: [[plans/2026-04-28-phase-42-writer-consolidation]] + Inventory: [[engine/PHASE_42_INVENTORY]]. Real scope post-S184: 37 files / 175 sites (citizenFameTracker.js retired -16, ±5 drift). Tiered acceptance B6 stop-point (29 files / 110 sites = 58%) vs B7 full close. Phase 1 inventory DONE S184; Phase 2 (research-build per-category decisions) next; Phase 3-5 (engine-sheet 6-8 sessions). Frame: efficiency not correctness per Path 1 closeout S156. |
| 9 | 78 orphaned `ctx.summary` / `S.` writes. | done | S156 (reclassified) | `docs/engine/tech_debt_audits/2026-04-15.md` + `2026-04-27.md` §3 | Not a bug — `utilities/exportCycleArtifacts.js` serializes the entire `ctx.summary` every cycle (`JSON.parse(JSON.stringify(S))` → Drive `cycle-<N>-summary.json`), and `phase10-persistence/buildCyclePacket.js` consumes most of it for the cycle packet. Every orphan field IS read, just by bulk serialization not per-field reference. Audit methodology codified in `/tech-debt-audit` skill so future passes don't rediscover it. |
| 12 | `utilities/utilityFunctions.js:29,36,53` — `pickRandom_`, `pickRandomSet_`, `maybePick_` use `Math.random` on cycle path (reference-pass). Callers: `godWorldEngine2.js`, `buildCityEvents.js`, `buildNightLife.js`, `buildEveningMedia.js`, `buildEveningFood.js`, `buildEveningFamous.js`, `sportsStreaming.js`, `generateChicagoCitizensv1.js`. Shipped through every prior cycle. Missed by `af40282` sweep (invocation-only grep). | done | S180 | S166 /pre-mortem C92 | Closed S180 — three helpers take `rng` parameter and throw if missing (matches S156 phase05 generateChicagoCitizen_ pattern). 8 caller sites updated to pass `rng`. Side-fix: `getChicagoOccupation_` was un-seeded (silent bug); now threads `rng`. |
| 13 | SCHEMA_HEADERS full alignment diff never run — 2026-04-15 export happened two days *before* the Phase 38/39/40 write-intent changes in later S147/S148/S156 commits. Unknown whether any new write-intent columns drifted from sheet headers. | done | S181 | S166 /pre-mortem C92 §4 | Closed S181 — schema regenerated via new `scripts/regenSchemaHeaders.js` (Apps Script API-executable path unavailable, fell back to local Node + service account per engine.md rule). Live-sheet diff harmless (2 new tabs, 1 column-shrink, all without engine references). Code-side diff surfaced 3 classes of real drift now tracked as items 15, 16 + Class 3 doc cleanup. See `docs/engine/tech_debt_audits/2026-04-27.md` §6. |

## P3 — Specific bugs

| # | Issue | Status | Session | Evidence | Fix pointer |
|---|-------|--------|---------|----------|-------------|
| 10 | Temescal initiative stuck 88 cycles — crude date-string parse in Phase 38.1. | done | S181 | S148 audit; S181 verification via live `Initiative_Tracker` inspect | Closed S181 — `scripts/engine-auditor/detectStuckInitiatives.js:49-51` cold-start fallback was passing `row.LastUpdated` (a date-string `"4/21/2026"`) to `parseCycleHint(/C?(\d+)/i)`, which matched the month digit `4` and computed `cyclesInState = 92 − 4 = 88` for every initiative. Temescal got named because it was narratively load-bearing; bug affected all 6 active initiatives uniformly. Replaced with `parseCycleHint(row.VoteCycle)` — VoteCycle is an actual cycle number column (78/80/82/etc.), not a timestamp. Smoke-tested with C92 fixture: INIT-001=14, INIT-005 Temescal=12, INIT-007 (empty VoteCycle, announced phase) correctly filtered out. NextActionCycle dropped from the fallback — it's forward-looking and the `<= cycle` guard rejected it anyway. |
| 11 | Pipeline never gated production — /sift, Rhea, desk verification, Phase 39 reviewer lanes never ran on real editions. E89/E90/E91 hand-assembled. | todo | — | S148 audit | — |
| 14 | Non-canonical neighborhood strings used as first-class (not mapped): `Eastlake`, `San Antonio`, `Glenview`, `Ivy Hill`, `Coliseum`, `Elmhurst`. Hits in `phase03-population/updateNeighborhoodDemographics.js:234`, `phase05-citizens/generationalWealthEngine.js:90`, `phase05-citizens/bondEngine.js:112-113`, `phase05-citizens/updateCivicApprovalRatings.js:110-115`, `phase08-v3-chicago/v3NeighborhoodWriter.js:38,188-192,333`. Either the skill's 17-canonical list is stale, or these paths write to ghost neighborhoods. `phase05-citizens/checkForPromotions.js:191-199` has a mapping table that normalizes some → canonical; other sites skip it. | done | S180 | S166 /pre-mortem C92 §5 | Closed S180 — root cause was the `/pre-mortem` skill's hardcoded 17-list (Downtown / Piedmont / Montclair / etc.) that didn't match either Simulation_Ledger (canon-12 where citizens live) OR Neighborhood_Map (17 fine-grained where world-state lives). Engine code itself is correct under parent-child ontology (canon-12 parents ← fine-grained children, mapping at checkForPromotions.js:190-209). Fix: SKILL.md §5 rewritten to recognize both layers as canonical; child names like Eastlake/San Antonio no longer flag as non-canonical. Folds in the engine-review C92 baseline-brief finding (same root cause). |
| 16 | `Advancement_Intake` / `Advancement_Intake1` tabs both missing from live spreadsheet — `phase05-citizens/processAdvancementIntake.js:260-262` falls back from `Advancement_Intake1` → `Advancement_Intake` → silent return `if (!intakeSheet) return results;`. Also referenced by `phase07-evening-media/mediaRoomIntake.js:537-538` (same fallback chain). Pipeline non-functional until at least one tab exists. Different from `Continuity_Intake` / `Election_Log` which lazy-create themselves; advancement intake silently drops. | done | S181 | S181 audit `docs/engine/tech_debt_audits/2026-04-27.md` §6 Class 1 | Closed S181 — lazy-create added on the writer side at `phase07-evening-media/mediaRoomIntake.js:539-544` (matches `continuityNotesParser.js:73` pattern). Inserts `Advancement_Intake1` with 10-col header (`First, Middle, Last, RoleType, Tier, ClockMode, CIV, MED, UNI, Notes`) when neither tab exists. Reader path (`processAdvancementIntake.js:262`) keeps its silent-return — correct no-op when nothing has been routed yet. |
| 17 | Post-S94 RoleType="Citizen" drift — literal "Citizen" sentinel landed on the live ledger from the intake writer's fallback path. Same anti-pattern S94 fixed (399 cases). Root cause: `phase05-citizens/processAdvancementIntake.js:296` defaulted to `'Citizen'` when intake row's RoleType cell was empty. | done | S184 | S181 audit `docs/engine/LEDGER_AUDIT.md` §"Current State"; S184 backfill verified 0 sentinels live | Closed S184 — Path B (demographic-voice fallback) chosen. Writer patched: `phase05-citizens/processAdvancementIntake.js` now declares a 65-role `DEMOGRAPHIC_VOICE_ROLES` pool drawn from the S94 cohort + LEDGER_AUDIT §7 15-category distribution; new citizens with empty intake RoleType receive a deterministic per-(first,last,popId) draw via djb2 string hash (Apps Script-safe, no Math.random). Existing-citizen branch preserved — empty `rawRole` no longer overwrites a real ledger value (truthy guard at line ~393 unchanged). Smoke test: 1000-draw distribution across full 65-role pool (max 30, min 5 per role; no dead entries). Backfill: of the 4 originally flagged (POP-00794 / 00795 / 00798 / 00801), only POP-00798 (Grace Yamamoto, Adams Point) and POP-00801 (Maurice Franklin, Rockridge) still carried the sentinel by S184; both patched via `lib/sheets.js` direct write (Independent Bookstore Owner / Drone Fleet Coordinator respectively, picker-derived). Live verification: 0 rows with `RoleType='Citizen'` sentinel. **Followup (research-build):** ROLLOUT entry — `RoleType is preferred upon intake`. Upstream intake writers (mediaRoomIntake.js + future intake paths) should compute RoleType BEFORE routing; the demographic-voice fallback is a safety net, not a replacement for real role assignment. EducationLevel + Gender empty on the same 4 rows is a separate gap (intake path doesn't backfill demographics) — not addressed here. |

---

## Log

(Brief one-line entries when a row changes status. Date + session + item # + what moved.)

- 2026-04-16 S152 — tracker created. All 11 items `todo`.
- 2026-04-19 S166 — item 7 marked `done` (closed by `76a408c` + `af40282`); items 12, 13, 14 added from /pre-mortem C92.
- 2026-04-26 S180 — item 12 marked `done`. Three helpers (`pickRandom_`, `pickRandomSet_`, `maybePick_`) now take `rng` parameter and throw if missing; 8 caller sites updated; `getChicagoOccupation_` un-seeded silent bug fixed.
- 2026-04-26 S180 — item 14 marked `done`. Two-layer ontology surfaced: canon-12 parents (Simulation_Ledger, where citizens live) ← fine-grained children (Neighborhood_Map, world-state buckets). Engine code already correct; root cause was `/pre-mortem` SKILL §5's hardcoded 17-list that didn't match either layer. SKILL.md §5 rewritten + version bumped 1.1 → 1.2. Folds engine-review C92 baseline-brief finding (same root cause).
- 2026-04-26 S180 end — item 13 tagged for **NEXT ENGINE-SHEET SESSION (S181 entry-task)** per Mike at session close. Run `/tech-debt-audit` §4 + regenerate `schemas/SCHEMA_HEADERS.md` via `exportAllHeaders()` + diff write-intent column sets.
- 2026-04-27 S181 — item 13 marked `done`. Schema regenerated via new `scripts/regenSchemaHeaders.js` (Apps Script API-executable path unavailable). Live-sheet diff harmless. Code-side diff surfaced 2 production silent-failure paths now tracked as items 15 + 16, plus 5 stale engine.md docstring entries (Class 3, doc-only). Audit doc: `docs/engine/tech_debt_audits/2026-04-27.md`.
- 2026-04-27 S181 — item 15 added (P1). `citizenFameTracker.js` silently no-ops on Simulation_Ledger / Generic_Citizens / Chicago_Citizens because expected fame columns are absent. Decision needed: re-add columns or retire the tracker.
- 2026-04-27 S181 — item 16 added (P3). `Advancement_Intake` / `Advancement_Intake1` tabs missing; `processAdvancementIntake.js` silent-returns. Pipeline non-functional until pre-created or converted to lazy-create.
- 2026-04-27 S181 — quick-win sweep: items 1, 9, 16 marked `done`. Row 1 — `generateGenericCitizens.js` + `cycleRollback.js` aligned with live Generic_Citizens schema (`EmergedCycle`, not `CreatedCycle`); value format matches `checkForPromotions.js`. Row 9 — reclassified (bulk-serializer consumes ctx.summary; not a bug). Row 16 — lazy-create added in `mediaRoomIntake.js` writer path (10-col header, matches `continuityNotesParser` pattern).
- 2026-04-27 S181 — item 3 marked `done`. S94 LEDGER_REPAIR.md recovery claim verified against live state via new `scripts/auditSimulationLedger.js`. Live: 686 extant citizens, 0 missing names, all tiers numeric, age sanity holds, S94 recovery cohort clean. Snapshot persisted in `docs/engine/LEDGER_AUDIT.md` §"Current State — S181 refresh". Boot blocker text updated in `.claude/hooks/session-startup-hook.sh` to reflect post-recovery reality.
- 2026-04-27 S181 — item 17 added (P1). 4 post-S94 citizens (POP-00794/795/798/801) carry RoleType="Citizen" — same anti-pattern S94 fixed. Root cause: `processAdvancementIntake.js:296` defaults to literal "Citizen" when intake row's RoleType cell is empty. Same intake path leaves EducationLevel + Gender empty for new citizens. Decision needed: reject empty RoleType upstream OR replace default with demographic-voice role draw.
- 2026-04-27 S181 — item 10 marked `done`. `detectStuckInitiatives.js` cold-start fallback was misparsing the `LastUpdated` date-string as a cycle (regex matched the month digit). Switched fallback to `VoteCycle` — which is a real cycle number. Smoke-tested with C92 fixture: realistic cyclesInState values (Temescal=12, Stabilization=14, etc.) instead of uniform 88.
- 2026-04-28 S184 — item 2 marked `done`. World-data unified ingest rebuild S183 (R1 cold-start fix `1fc2526`) wiped all 408 prior wd-citizens and wrote 686 fresh via `--no-quality-gate`. Substrate clean: wd-citizens=686, 100% domain-tagged, 0 orphans. S181 full-name post-filter (`097b792`) eliminated cross-contamination at write time; S183 DELETE pass eliminated the historical co-residence. MCP `lookup_citizen` reads only the fresh layer.
- 2026-04-28 S184 — item 17 marked `done`. Path B (demographic-voice fallback) shipped. `processAdvancementIntake.js` declares 65-role pool spanning S94's 15 demographic categories; new citizens with empty intake RoleType receive deterministic per-(first,last,popId) draw via djb2 hash. Existing-row branch preserved (truthy guard). Backfill: POP-00798 → Independent Bookstore Owner, POP-00801 → Drone Fleet Coordinator (picker-derived). POP-00794 / 00795 had been remediated between S181 and S184 (now `Magician` / `Drug Dealer`). Live sentinel count: 0. Followup queued in ROLLOUT (research-build): "RoleType preferred upon intake" — upstream writers should compute RoleType, fallback is safety-net only.
- 2026-04-28 S184 — item 5 marked `done`. `generateGenericCitizens.js` v2.6 → v2.7. Pool expanded 62 → 186 first / 53 → 144 last = 26,784 combos (was 3,286). `getUniqueName` adds `firstCounts`/`lastCounts` maps + caps (FIRST=3, LAST=4); pre-v2.6 clusters grandfathered (Ronald=11, Johnson=12 stay; no new additions). Smoke-test: 500 draws with grandfathered seed, 0 null returns, max-per-first 3 / max-per-last 4 as expected, 0 new Ronalds / 0 new Johnsons. Determinism preserved (ctx.rng unchanged). Awaits clasp deploy with the Row 17 patch.
- 2026-04-28 S184 — item 6 marked `done`. Diagnosis flipped: WorldEvents_V3_Ledger has no POPID column — could never carry citizen attribution regardless of EventType taxonomy. Citizen life events live in LifeHistory_Log (3,589 rows, keyed by POPID). Added LifeHistory_Log to engineAuditor SHEETS_TO_READ. generateBaselineBriefs v1.0.0 → v1.1.0 reads the log, filters by cycle + 6 structural tags (Life Event, Promotion, Advancement, CivicRole, Retirement, Stabilized), emits eventClass=citizen-life-event briefs with subjectIds=[popId] + ledger-joined neighborhood/tier/role + tag-aware promotion hints. C88 smoke: 16 briefs / 16 with subjectIds (Marcus Osei tier-2 feature-candidate hint fires correctly). C92 smoke: 0 briefs (correct — no structural events that cycle). Node-only changes (auditor + brief generator); no Apps Script, no clasp deploy needed.
- 2026-04-28 S184 — item 15 marked `done`. Path B (retire) per Mike: tier system + UsageCount + EmergenceCount cover the fame signal already; adding 14 columns to revive a redundant tracker is sheet-bloat. `phase07-evening-media/citizenFameTracker.js` deleted. `mediaRoomIntake.js` v2.6 → v2.7 (call site removed, docstring updated). ENGINE_STUB_MAP.md section replaced with retired-note. Migration helpers (`addCitizenFameColumns.js` / `rollbackCitizenFameColumns.js`) retained for archaeology with RETIRED headers. Cultural_Ledger.FameScore intact — independent system, live readers (`recordMediaLedger`, `compileHandoff`, `citizenContextBuilder`).
- 2026-04-28 S184 — item 4 marked `done`. Diagnosis: migration scripts seeded uniform constants when columns were added; lifecycle engines update only on triggers (most citizens never triggered). `scripts/backfillLifecycleDefaults.js` (Node + service-account direct write) derives demographically-plausible starting values per row from Age + Income + RoleType + CareerStage; deterministic via djb2 hash on POPID; safety matcher overwrites only rows currently at migration constants. Live state post-apply: top-value share dropped from 95-99% to 2.6-49.6% across 5 fields (YearsInCareer 308 distinct, DebtLevel bell 0-7 centered at 2-3, NetWorth 557 distinct, MaritalStatus 5 categories realistic, NumChildren 0-5 fertility curve). Three separate-cleanup items flagged: POP-00004 Lucia Polito column-misalignment, 137-165 empty-cell rows, engine-side intake derivation for new emergences.
