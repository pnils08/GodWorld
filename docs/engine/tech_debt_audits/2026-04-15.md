---
title: Tech Debt Audit — 2026-04-15
created: 2026-04-15
updated: 2026-04-15
type: archive
tags: [engine, infrastructure, audit]
sources:
  - ".claude/rules/engine.md — rule set this audit enforces"
  - "docs/engine/tech_debt_audits/2026-03-26.md — prior audit (18 days stale)"
  - "docs/engine/PHASE_38_PLAN.md — latest engine build context (S146)"
pointers:
  - "[[engine/ROLLOUT_PLAN]] — writers reconciliation is a fill-in item"
  - "scripts/engineAuditor.js — Phase 38 auditor (separate layer, not in scope)"
---

# TECH DEBT AUDIT — 2026-04-15

**Session:** 146 `[engine/sheet]` | **Prior audit:** 2026-03-26 (18 days stale)
**Scope:** `phase*/**/*.js` + `utilities/**/*.js`. Read-only. No source files modified.
**New since prior audit:** Phase 38 auditor in `scripts/` (out of scope — not in `phase*/`).

---

## CRITICAL — active bugs / determinism violations

1. **`phase04-events/generationalEventsEngine.js:113`** — `Math.random()` fallback inside `rand_(ctx)`. Warning-guarded with `Logger.log`, but fires if `ctx.rng` not wired. Same pattern flagged in 2026-03-26 audit as C2 — still present, not fixed.
2. **`phase04-events/generationalEventsEngine.js:106`** — `initRng_` returns `Math.random` as last-resort bare function reference. Warning-guarded, same root issue.
3. **`phase05-citizens/generateGenericCitizens.js:98`** — `rng = ctx.rng || Math.random;` silent fallback, no warning log. Worse than (1) — undetectable at runtime if `ctx.rng` is undefined.
4. **`utilities/v2DeprecationGuide.js:202-203`** — `v3Random_` helper with warning log + `Math.random()` fallback. Flagged as C3 in 2026-03-26; engine.md documents this as a deprecated helper. If unused, delete; if used, route callers to ctx.rng.

## CRITICAL — direct sheet writes outside `phase10-persistence/` **not** in the engine.md exceptions list

Engine rule: "All non-exception direct writes are bugs." 67 files contain `setValue|setValues|appendRow` calls outside `phase10-persistence/`. After subtracting the documented exceptions in `.claude/rules/engine.md`, the following files are **undocumented writers** — either bugs requiring refactor to write-intents, or the exceptions list is incomplete:

| Phase | Undocumented writer | Write count |
|---|---|---|
| 01 | `godWorldEngine2.js` | 7 |
| 01 | `advanceSimulationCalendar.js` | 6 |
| 02 | `applyEditionCoverageEffects.js` | 1 |
| 03 | `finalizeWorldPopulation.js` | **27** |
| 03 | `generateMonthlyDriftReport.js` | 2 |
| 03 | `updateCityTier.js` | 1 |
| 04 | `generateGenericCitizenMicroEvent.js` | 2 |
| 04 | `generateGameModeMicroEvents.js` | 2 |
| 04 | `generateCitizenEvents.js` | 2 |
| 04 | `worldEventsLedger.js` | 2 |
| 04 | `generationalEventsEngine.js` | 2 |
| 05 | `updateCivicApprovalRatings.js` | 1 |
| 05 | `bondEngine.js` | 2 |
| 05 | `generateCivicModeEvents.js` | 2 |
| 05 | `migrationTrackingEngine.js` | 2 |
| 05 | `generateChicagoCitizensv1.js` | 4 |
| 05 | `processAdvancementIntake.js` | 14 |
| 05 | `runCivicRoleEngine.js` | 2 |
| 05 | `runHouseholdEngine.js` | 2 |
| 05 | `generateGenericCitizens.js` | 1 |
| 05 | `runRelationshipEngine.js` | 2 |
| 05 | `runNeighborhoodEngine.js` | 2 |
| 05 | `generateMediaModeEvents.js` | 2 |
| 05 | `seedRelationBondsv1.js` | 1 |
| 05 | `bondPersistence.js` | 3 |
| 05 | `gentrificationEngine.js` | 1 |
| 05 | `runCareerEngine.js` | 2 |
| 05 | `runCivicElectionsv1.js` | 4 |
| 07 | `parseMediaRoomMarkdown.js` | 9 |
| 07 | `citizenFameTracker.js` | 16 |
| 07 | `storylineWeavingEngine.js` | 3 |
| 07 | `updateTrendTrajectory.js` | 1 |
| 07 | `mediaRoomIntake.js` | 31 |
| 07 | `mediaRoomBriefingGenerator.js` | 2 |
| 07 | `updateMediaSpread.js` | 2 |
| 07 | `culturalLedger.js` | 9 |
| 11 | `healthCauseIntake.js` | 5 |
| 11 | `continuityNotesParser.js` | 10 |

**38 undocumented-writer files. 197 direct-write call sites.** Prior audit (2026-03-26) documented 20 writers across phases 5/6/8/9. This pass finds **38** across phases 1/2/3/4/5/7/11 — a ~2× undercount in the prior audit. Phase 03 `finalizeWorldPopulation.js` (27 writes) and Phase 07 `mediaRoomIntake.js` (31 writes) are the largest single concentrations.

**Recommendation:** Either append these to the `.claude/rules/engine.md` exceptions list with a one-line justification per file, or open refactor tickets to route through write-intents. Matters for Phase 38.2 (mitigator check) because the ailment detector reads institutional state that these writers produce.

---

## WARNINGS — code health

### Orphaned `ctx.summary`/`S` writes (written, never read)

78 fields where the write-count equals the total reference count (i.e. read-count = 0), searching both `ctx.summary.X` and `S.X` in files aliasing `S = ctx.summary`.

Representative sample (full list: re-run scan — 78 total):

| Field | Written at | Writes |
|---|---|---|
| `cyclePacket` | `phase10-persistence/buildCyclePacket.js:764` | 1 |
| `domainCalendarContext` | `phase07-evening-media/domainTracker.js:295` | 1 |
| `healthCauseBriefing` | `phase11-media-intake/healthCauseIntake.js:607` | 1 |
| `canonSportsPhase` | `phase05-citizens/runAsUniversePipeline.js:598` | 1 (labeled "Expose for debugging") |
| `activeRippleCount` | `phase05-citizens/civicInitiativeEngine.js:1666` | 1 |
| `approvalChanges` | `phase05-citizens/updateCivicApprovalRatings.js:28` | 2 |
| `chaosCategoryMap` | `phase02-world-state/calendarChaosWeights.js:472` | 1 ("Export mapping for reference") |
| `cityDynamicsCapacity` | `phase02-world-state/applyCityDynamics.js:1287` | 1 |
| `civicDemographicContext` | `phase05-citizens/civicInitiativeEngine.js:429` | 1 |
| `civicRoleEvents` | `phase05-citizens/runCivicRoleEngine.js:440` | 1 |
| `hookSuppression` | `phase08-v3-chicago/applyCycleRecovery.js:320` | 4 |
| `householdFormation` | `phase05-citizens/householdFormationEngine.js:196` | 1 |
| `initiativeImplementationEffects` | `phase02-world-state/applyInitiativeImplementationEffects.js:86` | 2 |
| `lifeHistoryCompression` | `utilities/compressLifeHistory.js:284` | 1 |
| `creationDayActive` | `phase02-world-state/applySeasonWeights.js:336` | 1 |
| `editionCoverageEffects` | `phase02-world-state/applyEditionCoverageEffects.js:29` | 2 |
| `editionSentimentBoost` | `phase02-world-state/applyEditionCoverageEffects.js:30` | 2 |
| `eventPrioritization` | `phase06-analysis/prioritizeEvents.js:407` | 1 |
| `famousSightingsContext` | `phase07-evening-media/buildEveningFamous.js:473` | 1 |
| `gameModeMicroEventDetails` | `phase04-events/generateGameModeMicroEvents.js:578` | 1 |

Caveat on methodology: some of these may be consumed via `JSON.stringify(ctx.summary)` or passed whole to downstream Apps Script functions. Spot-check before deleting. A value write labeled "Expose for debugging" (`canonSportsPhase`) is a signal that at least some orphans are intentional diagnostic exports.

**Up from 6 in the prior audit.** Same caveat: the prior audit didn't search the `S.` alias, so comparison isn't clean.

**2026-04-17 UPDATE (S156, engine-sheet):** The methodology caveat above is the actual case. `utilities/exportCycleArtifacts.js:112` serializes the ENTIRE `ctx.summary` via `JSON.parse(JSON.stringify(S))` every cycle, writing the result to `cycle-<N>-summary.json` in Drive (the cycle artifact archive). Every field in `ctx.summary`, orphaned or not, is consumed via that bulk serializer. The 78 "orphans" are not dead writes — they're part of the cycle artifact payload. Deleting them would strip fields from the cycle export. **Finding reclassified: not a bug.** Future audits should either (a) exclude `ctx.summary` fields entirely from orphan detection, or (b) walk the bulk-serialization graph (`exportCycleArtifacts_`, `buildCyclePacket_`, `JSON.stringify` of the whole object) and only flag fields that are ALSO excluded from those serializers.

### Dead-code detection

Not performed this pass — full unreferenced-function scan across 162 engine files is expensive and produces many false positives for Apps Script functions called via trigger names or `eval`-style dispatch. Recommend a dedicated pass with an allowlist for entry points referenced from `godWorldEngine2.js` and Apps Script triggers.

### Sheet header misalignment

Schema file `schemas/SCHEMA_HEADERS.md` last updated 2026-01-21 — **84 days stale**. Cannot reliably detect column drift against it. Recommend running `exportAllHeaders()` (the generator named in the file header) to refresh before the next audit attempts this check.

**2026-04-17 CLOSED (retroactive — actually closed S146 coda 2026-04-15).** `SCHEMA_HEADERS.md` refreshed and deployed via clasp push after the S146 engine-sheet coda: the generator (`utilities/exportSchemaHeaders.js`) had three bugs fixed first (stale branch reference → `main`, wrong file path → `schemas/SCHEMA_HEADERS.md`, UI-context crash in `exportAllHeaders` wrapped in try/catch), then run. File grew 1,099 → 1,349 lines. Clasp push deployed. See SESSION_CONTEXT.md S146 coda for full detail.

---

## INFO — refactoring opportunities

- `Math.random()` fallback pattern (warn+call) appears in 3 files with near-identical shape (`generationalEventsEngine.js`, `generateGenericCitizens.js`, `v2DeprecationGuide.js`). Candidate for a shared `safeRand_(ctx)` helper in `utilities/` that enforces ctx.rng presence or throws.
- `ctx.summary = S;` assignment pattern at function bottoms (20+ occurrences) is ceremonial — `S` is already a reference to `ctx.summary`, so reassigning to itself is a no-op. Not a bug, but reads as defensive code whose purpose has faded.

---

## SUMMARY

- **Critical issues:** 4 Math.random fallbacks + 38 undocumented direct-writer files (197 call sites) = **42 critical findings**.
- **Warnings:** 78 orphaned ctx.summary fields, schema file 84 days stale, dead-code scan deferred.
- **Info:** 2 refactor opportunities.
- **Files scanned:** 162 engine files (`phase*/` + `utilities/`).
- **Files skipped by design:** `scripts/` including the new Phase 38 auditor (not engine code).

**RECOMMENDATION: NEEDS ATTENTION** — the direct-writer gap is the largest single finding. Before Phase 38.2 (mitigator check) executes, decide per-file: exception + justification or refactor. The Math.random fallbacks have been present ≥18 days and have escaped two audits; tightening them should be a one-PR job.

---

## Changelog

- 2026-04-15 — Initial audit (S146, engine/sheet terminal). Methodology fixes vs 2026-03-26: searched both `ctx.summary.X` and `S.X` for write/read counts; verified exceptions list against every phase not just 5/6/8/9.
- 2026-04-17 — Path 1 CLOSEOUT (S156, engine-sheet terminal). All 38 undocumented writers triaged: every file writes to an engine-owned tracking or intake tab consistent with the existing exceptions pattern (Engine_Errors, Simulation_Calendar, Riley_Digest, World_Population history, LifeHistory_Log, Simulation_Ledger citizen events, civic ledgers, media intake pipelines, health/continuity intake). Zero were bugs. All 38 added to `.claude/rules/engine.md` exceptions list with one-line justifications, grouped by phase. Path 2 (refactor-to-write-intents) remains a future phase for efficiency; Path 1 closes the correctness question. All 4 audit-flagged Math.random fallbacks closed in the same pass: `generationalEventsEngine.initRng_` / `rand_`, `generateGenericCitizens.rand()`, and `v2DeprecationGuide.v3Random_` (zero callers — kept as a stub that throws if invoked).
- 2026-04-17 — Math.random residue sweep (S156, engine-sheet terminal, same session). Expanded the Math.random audit beyond the 4 flagged sites: grep across `phase*/ + utilities/` found ~70 additional sites using the silent-fallback pattern. Classified into three categories: (1) **43 cycle-path sites** `(typeof ctx.rng === 'function') ? ctx.rng : Math.random` → replaced with new centralized `utilities/safeRand.js` helper `safeRand_(ctx)` which throws if neither `ctx.rng` nor `ctx.config.rngSeed` is available; (2) **12 multi-line ternaries** that XOR with per-subsystem salt constants — preserved seed-mixing logic and converted only the terminal `: Math.random;` to an IIFE that throws a file-tagged error; (3) **19 parameter-rng fallback sites** (`rng = rng || Math.random` in helper functions taking rng as parameter) — initial sweep deferred, then closed later same session (see next entry). 56 files modified in cycle-path sweep; all syntax-check clean.
- 2026-04-17 — Parameter-rng sweep (S156, engine-sheet terminal, same session). Closed the 19 deferred parameter-rng sites + 1 additional site found during residue grep (`generationalWealthEngine.calculateIncomeFromBand_` — same silent `if (!rng) rng = Math.random` pattern, missed in the initial audit). 22 helper-function throw-on-missing-rng entries across 10 files: `eventArcEngine.js` (pickNeighborhoodForDomain_, generateSafeUuid_), `civicInitiativeEngine.js` (resolveCouncilVote_, resolveExternalDecision_), `seedRelationBondsv1.js` (generateBondId_), `householdFormationEngine.js` (dissolveStressedHouseholds_), `generateChicagoCitizensv1.js` (5: generateChicagoCitizen_, generateAge_, generateTier_, pickWeightedRandom_, generateId_), `generationalWealthEngine.js` (calculateIncomeFromBand_), `economicRippleEngine.js` (createRipple_ — throws on `S._rng` missing, not `rng` param), `processArcLifeCyclev1.js` (calculateTensionDrift_, getResolutionText_), `chicagoSatellite.js` (generateChicagoWeather_, calculateChicagoSentiment_, generateChicagoEvents_), `v3ChicagoWriter.js` (4: deriveChicagoFeedV24_, deriveChicagoWeatherTypeV24_, deriveChicagoTempV24_, deriveChicagoSentimentV24_). Caller audit verified: every callsite either flows from `safeRand_(ctx)` or explicitly passes `Math.random` as a truthy function value (1 manual-votes path, 1 test path). Pattern applied uniformly: `if (typeof rng !== 'function') throw new Error('<file>.<fn>: rng parameter required (Phase 40.3 Path 1)');`. All 10 files syntax-check clean. Post-sweep, the ONLY remaining `Math.random` references in engine code are documented intentional paths: 1 manual-votes bare (civicInitiativeEngine.js:2009), 1 explicit test call (generateChicagoCitizensv1.js:433), 4 "centralization prep — no ctx in scope" stubs in utilityFunctions.js + citizenContextBuilder.js, and the dead-code stub in v2DeprecationGuide.js that throws if ever invoked. Silent-fallback class eliminated.
- 2026-04-17 — Warnings-section review (S156, engine-sheet terminal, same session). (1) **78 orphaned ctx.summary fields: RECLASSIFIED, not a bug.** `utilities/exportCycleArtifacts.js` serializes the entire `ctx.summary` every cycle via `JSON.parse(JSON.stringify(S))` and writes it to the Drive cycle artifact archive (`cycle-<N>-summary.json`). Every field is consumed via bulk serialization; none are dead. Audit methodology was incomplete — next audit should walk the bulk-serializer graph before flagging ctx.summary orphans. Warning content updated inline. (2) **Schema header staleness: CLOSED retroactive.** `SCHEMA_HEADERS.md` was refreshed + clasp-pushed in the S146 engine-sheet coda (1,099 → 1,349 lines, three generator bugs fixed first). Warning content updated inline. (3) **Dead-code detection: DEFERRED.** Audit explicitly said "not performed this pass" and recommended a dedicated run with an allowlist for Apps Script trigger entry points. No action taken; this belongs in a future dedicated scan, not Path 1 cleanup. (4) **`ctx.summary = S;` ceremonial no-ops (INFO section, 20+ occurrences): SKIPPED.** Not a bug, just dead ceremony — low value vs. diff noise. Audit is now fully triaged; only deferred item is dead-code detection (new scan).
