# SESSION_CONTEXT — NEXT WORK ONLY. One PIN + one NEXT line per lane. What already happened lives in git, claude-mem, ROLLOUT_PLAN and the gap/production logs — never here. If a line describes a session instead of an assignment, it is written wrong.

**PIN:** S478 | Day 196 | canonical C108 | **PROD @108 HEAD = `a92e84a6` (engine.250 + civic.38 Task 5 + stall-drag dial 0.5); web app still pinned v94 = @105 — version step owed: `clasp version` then `clasp deploy -i … -V 95`, commands in DEPLOY_HISTORY §PROD @108 (the auto-mode classifier refuses them from this seat)** | Bench SANDBOX 0908 @78 = `a92e84a6` at C122 — bench-only, never replay (tracker INIT-003 / INIT-005 edited on the bench) | claude-mem observer on `nvidia/nemotron-3-super-120b-a12b:free` (infrastructure.11 open); a new worker env var needs `npx claude-mem stop` then `start` — `restart` re-spawns with the OLD env | `diag187` rides the web-app fire response only, an editor fire has none | engine-sheet LEADS the sim repairs | never `--amend`; wiring card before every cut; deploys from an isolated `git archive` stage | DOCTRINE §13/§15/§16/§17 | Sunday 21:00 city-hall slot runs the C108 chain | civic.38 is the house project: plan `docs/plans/2026-09-19-civic-wake-game-loop.md` §Lane assignments, one owner per file, dry-run only | Ledger 952 rows

**NEXT[engine-sheet]:** Smoke C109 on PROD @108 — an editor fire runs HEAD, a web-app fire runs @105 until the version step lands: log `record:` = last feed row's W-L; `applyNeighborhoodEffectsFold_ … approval (cycle 108 carry) → 0 hood(s)`; `initiative lift 0 / drag 0` unless a tracker phase moved; first live approval fold row the Cycle after. Then civic.38 Task 4 step 1 on (stage columns + engine mirror of `INTERVENTION_CATALOG` with a parity test — sports metric is two columns; six new tracker columns ride SHEETS_MANIFEST + schema docs in the same cut; re-sync the bench from live first, it carries Task 5 tracker edits). C109 also arms `World_Config bizInitiativeStallDrag` at 0.5 (read it back). Then engine.249 hood migration (ruled, no rebase). Housing-lever engine row arrives from research-build. Own session: engine.248. Rest: `output/production_log_run_cycle_c108_gaps.md` G-EC53–69.

**NEXT[research-build]:** civic.38 sequencing and review against the plan; keep §Lane assignments current. Watch: agy's T3.2/T3.3 failures (petition pool, confrontation parse) — real kimi defects go to kimi, fixture errors back to agy; codex's review of `8b07fc29` and housing-lever plan `docs/plans/2026-09-20-housing-lever.md` land → register, hand the lever row to engine-sheet. Builder rulings 2026-09-20 in plan: Baylight metric = district retail/nightlife vs baseline; housing band 0.30 is a counter threshold only, housing/safety gate nothing until their levers exist, housing lever first. Push waits — mixed unpushed stack, check `git log origin/main..HEAD` first.

**NEXT[kimi]:** civic.38 chain A COMPLETE, 6 commits local, none pushed (mixed stack; builder: hold the push until fixes land): 8b07fc29 T1+3, ef57efe0 T2 (ledger + Sunday fold + Status edge + candidates through validator/clerk/gate), 48ebf9df T8 (directive = 10 elected seats + per-seat evidence), 1ddcf92f T9 (rota = 11 seats; 4 directors → work-wake packs), 9d22388a T6.3 (petitionGateSweep; PETITION_SUPPORT_BANDS empty — nothing gates live until the builder names a band), a1cd97e2 handoff/debt notes, 41c6892c Reflection_Intake live-header fix (ReflectionExcerpt; names verified against codex's counter — agy's T3.2 fixtures use Event/Snippet guesses, reader accepts both). Catalog (89b5c36d) + codex counter both live; propose path closed-loop. agy's T3.2/T3.3 failures resolved at HEAD (game suite 25/25). Flags: touched workWakePackages.js (off my editor list); debt captures in output/kimi/2026-09-20-civic38-code-debt-captures.md. Next: builder sets signature bands; codex review of 8b07fc29; engine-sheet T4-5 bench. Standing: adversarial review duty on repair waves (output/kimi/).

**NEXT[codex]:** (1) Adversarial review of kimi's civic.38 Task 1+3 commit `8b07fc29` against plan Tasks 1 and 3 — findings to `docs/research/` and a short list to kimi in tmux. (2) Housing-lever DESIGN plan `docs/plans/2026-09-20-housing-lever.md` (design only, engine-sheet executes): what a housing initiative moves, engine path with file:line, INTERVENTION_CATALOG entry, effect bus, acceptance tests, baseline from your counter; register in `docs/index.md`. Task 6 is done (`7291d25e`); no engine or `cron-civic-run.js` edits. Sim-repair review on request.

**NEXT[antigravity]:** civic.38 Task 9 tests — you own `scripts/cron-civic-game.test.js`. Scaffold exists and runs against the real Task 1+3 code (`8b07fc29`); T3.2 (petition pool) and T3.3 (confrontation parse) currently fail — decide fixture error vs kimi defect, and report defects to kimi in tmux. Add fixtures for Tasks 2, 6, 8 as each lands; adversarial review of each landed task goes in `output/antigravity/`. Re-read the file before every claim.

**NEXT[grok]:** No pane; nothing queued.
