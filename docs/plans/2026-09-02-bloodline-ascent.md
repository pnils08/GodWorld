---
title: Bloodline Ascent — a generic citizen's path to heritage (engine.147)
created: 2026-09-02
updated: 2026-09-02
type: plan
tags: [engine, citizens, heritage, household, tier, dials, active]
sources:
  - builder direction 2026-09-02 (S411, engine-sheet chat) — recorded verbatim in §Direction
  - docs/engine/TIER_MOBILITY.md — the three mobility mechanisms as they exist
  - docs/plans/2026-09-01-education-career-connection.md — the four loops + engine.145/146 this arc builds on
pointers:
  - "[[engine/ROLLOUT_PLAN]] — engine.147"
  - "[[engine/TIER_MOBILITY]] — CLIMB / fame door / demotion as built"
  - "[[plans/2026-05-30-citizen-lifecycle-fame-system]] — engine.29, parked; the fame half of this arc"
  - "[[plans/2026-07-13-family-household-loop-build]] — the household half, as built"
  - "[[plans/2026-07-31-citizen-memory-perception]] — engine.94, dials + memory + approval ceiling"
  - "[[index]] — registered same commit"
---

# Bloodline Ascent — a generic citizen's path to heritage (engine.147)

**Status: MEASURED (S412) + BUILDER DIRECTION ON THE CHAIN CAPTURED; build order revised to the chain; NOTHING BUILT.** §The chain as directed vs as built is the agreement; §Build order, revised is what ships next, one row per cut.

## Direction (builder, 2026-09-02, recorded as said)

> The idea here is the citizen's path to success in this world. Take a generic citizen (a Tier-5 in waiting) with very little impact on its world, and how they can go from that to Tier 1, with a full household, and ascend to a heritage figure in the sim. Where the Keane family is carried in off professional sports fame and riches, the same path (albeit weighed against them) can ascend to that same level with chance events, cron wakes, media coverage and lottery-type events like UNDOCKED and casino gains. Like real life, a citizen is impacted by where they live, the choices the civic engine makes, the choices its cron makes in a wake, and how those hitting their dial system start to alter their life path — the same way someone on heritage can lose it all with the inverse. The idea is to have the cycles throwing all this data at a cron and it knowing it needs to maneuver up in Tier and dial state to advance their bloodline.

Earlier the same day: *"we will be checking heritage and households in the next project to ensure all the systems landed and the infrastructure is set on the path to success or failure of bloodlines."*

**Read of the direction (engine-sheet):** two halves. (1) **Audit** — do the systems that exist actually connect into one path from Tier-5 to heritage, and the inverse? (2) **The maneuvering cron** — a citizen-loop wake that reads what the cycle threw at a citizen (hood state, civic choices, media coverage, casino/UNDOCKED outcomes, dial deltas) and acts to move Tier and dial state toward advancing the bloodline. The test for every piece: does a row drive a fate (universal protagonism).


**Builder, 2026-09-02 15:41 (S412, after reading the measure) — recorded as said:**

> Ok lots of disconnected processes. I think generics have a slight chance to be married into Tier 4 from there as well, but yes Tier is the citizen hierarchy and raising your Tier should increase a citizen's life but exposure can become negative as well which would have the inverse effect. Once a citizen hits 25 media usage they assume fame and only their fame fluctuates once at that stage. An individual on their own can build a heritage line without a family but most will achieve it combined. So a citizen may start single as Tier 4, generate their pay, and the newer engines that could increase their pay and/or Tier; as that citizen builds their wealth they may purchase a house; once you have a house your odds of marriage increase; where you live, what your net worth is, determines the quality of spouse; now with a house and a spouse the odds of children increase; same neighborhood affects that kid's opportunities. Most citizens in the ledger were pre-populated before this system existed so they are the gifted era of not earning necessarily what they have, but going forward these elements are the ladder to your name and family being the pillars of the sim. So as a professional athlete has a better chance, a generic citizen could arguably become that as well — albeit a lot of engine determinism going their way.

**Read (engine-sheet):** the chain is the spec. Tier is the hierarchy and must *pay* in lived odds; coverage runs both ways; fame is a UsageCount bar (25) that mints permanence; a line can be founded solo; the seeded counts are the gifted era and stay as they are; the maneuvering is engine determinism ("going their way"), so the maneuver step is an engine phase and the wake stays the voice.

**Builder, 2026-09-02 17:32 (S412, after cut 1 shipped) — recorded as said:**

> The point of the intake is to hit their usage and/or add media-covered changes. Great news on Tier-5 citizens, and the reward should be: no one can fall back to Tier 5. Once you become Tier 4 that's the furthest you can descend. Once you reach fame you can only become "less" famous. So Vinnie Keane is still famous when he is 80 but not as famous as he is at 37 — I mean he could still be, but you get it. The media I agree, as the Tier makes them more seen, but more importantly it's telling us their cron writes the best articles, and this would be the routing system to get more "work". My theory is as this moves, these cron LLMs will become more advanced. As they reach the point of understanding the "game" they are trying to solve within the sim, this will get interesting. If citizens know they need to Tier up and make a family, and media knows they need to produce more, civic knows their approval rating keeps them in office — I believe it'll change their behavior. So this all is the system that makes the math a real issue for them to master. You can't even do it without reading the code. So it's a layered "game" these crons are playing: the better you are, the more you get to wake up; the more you wake up, the more the sim knows you.

**Read (engine-sheet):** four rules and one doctrine. (1) **Tier 4 is the floor** — nothing demotes below 4 and no row leaves the ledger for the pool (true by construction today: decay stops at 4, no writer sets 5). (2) **Fame is permanent, only its grade moves** — the engine.118 cut. (3) **Media Tier is a routing signal** — the desk pipeline should hand more assignments to the higher-Tier byline; that is a cron-desk change (research-build lane), filed below, not an engine phase. (4) **Intake's job** is to land usage and coverage-driven changes — never to set a Tier (the guard). Doctrine: **the wake economy** — Tier and fame decide who wakes and how often; the more a citizen wakes, the more the sim knows them; the crons are playing a layered game whose math is the engine, and it should change their behavior as they learn it.

**Builder, 2026-09-02 21:06 (S412, on the fame measure) — recorded as said:**

> No, usage count is correct. You wouldn't be famous if the media didn't cover you, and since the media writes every day and one step is interview a citizen, the usage count is literally what drives their Tiers to increase — the 3-6-9 system is that. 25× being covered by the media pushes you to fame: a citizen whose presence alone becomes news. Media is the sim's voice; if the citizen's voice is making it to me they are doing something right. That's who I want to hear from and about. They earn that with consistency — perfect example, all I've heard about from the sim this week is Berkin Runner; the entire sim is talking about him right now. The cultural ledger is another thing — some citizens were planted as famous, so only really a couple of A's players I added manually are there. And Mark Aitken showed up in C104 and was covered a ton for it.

**Read (engine-sheet):** the fame door stays on UsageCount (cut 3 as shipped). The Cultural_Ledger is a planted layer, not the fame truth; its FameScore is the A/D-list grade and the antenna feed. The one wire still open: the recognition antenna (`generateCitizensEvents.js:2370`, `buildEveningFamous.js:230`) reads only the cultural ledger, so a ledger-Famous citizen with no planted row (Paulson, Kelley today) is not yet "presence alone becomes news" in events — a read-side merge, filed as cut 3b.

**Builder, 2026-09-02 21:24 (S412, on the feed) — recorded as said:**

> Merkin Runner [Merkin Jumper, POP-00688] is on UNDOCKED right now and is getting a lot of coverage. The articles are likely being written now, but that's kind of my point. He won the "lottery" of being chosen to be in the show; citizens are having events naming him — so he should be seeing citations for that. I already manually made him a Tier 2 but that's me cheating. The A's players is earned and it's more of what you see. This project started with no cycles and articles about tiny A's players; that turned into running long media newspapers with agents in the CLI; now it's offloading to crons where 90% of articles never make it to me or anyone — by design a little, as we are still trying to make them canon accurate. So the articles are happening at a rate like never before; it's the publishing of them and getting the proper usage out of that. Benji Dillon has something like 90 articles mentioning him and Vinnie in the 60s — it's in deep canon; articles go back to C1 and a lot before that. This is 12 months of a project that started as a sports media outlet only.

**Read (engine-sheet):** the "gifted era" cells are deep canon the usage tab is too young to hold — held as they are, now for the right reason. The ladder's feed, not its bars, is the constraint: since C90 the tab caught four citizen citations in fifteen cycles because the Saturday step skipped quoted citizens on a handoff the interview wake never fulfilled. Fixed S412 (quoted credited at the canon door). Merkin: 4 feed rows, 13 life events, a staged article quoting him, 0 usage rows — the exact shape of the gap.

**Builder, 2026-09-03 00:20 (S413, on Door C at $500K and 30 home purchases) — recorded as said:**

> It feels a bit light — if you are on the heritage ledger you are officially branded old money, becoming a Rockefeller type. I think once on here you can open businesses. So heritage isn't meant to be easy to obtain, nor is having a house for that matter. It's important to understand these systems are not in place to give them to the citizens; they are meant to be earned. I get a bit concerned when we say 30 people bought a house — why did they? Why was it so easy for them to do that?

**Builder, 2026-09-03 00:48 (S413, the household economy) — recorded as said:**

> I'm assuming some real-world Oakland logic is still at play here since this is 2042 Oakland after a two-decade tech boom — some of this is 2026 Oakland pay scales. On home buying: the point of the "game" — you start single and Tier 4, the goal is to be on heritage. Gaining a house is a huge step, as now your chance of gaining a wife is higher than not having one. Creating a bond and marrying is the other opportunity; that same citizen looking for love can also be chosen as a spouse — this combines income and determines where you live and how you live there. "Gifted" citizens have Tier-5 spouses and kids and households with generic salary for untracked Tier-5 citizens; this won't be the process moving forward. Generic citizens and existing citizens become the marriage pool; we will build a generic-citizen generation to add 1–2 citizens a cycle, skewed 80/20 to female (a low population in the sim). For households: net worth determines where you can live; your income or incomes determine how you live in that hood — so each hood should have a minimum income to buy a house where your net worth says you live. Wives keep maiden names on the Simulation Ledger, as they can receive inheritance from a deceased parent after marriage. How these numbers fluctuate is an open concept — do purchases drop your income, or does a hood tax its citizens quarterly? Once someone has a household the possibility of kids greatly increases, still on a lottery, with a cap on how many kids can be born in a cycle — even if you hit the lottery you can still not have the baby if too many others also hit it that cycle. Now you have two working parents and the climb to heritage begins; once on there the stakes change and you can become an owner of businesses, which is a system of how net worth increases.

**Builder, 2026-09-03 00:59 (S413, heritage is money) — recorded as said:**

> Wouldn't gate heritage on Tier. If you've got $100M, whether you're famous or not doesn't make your money worth more. Heritage's main driver is money — that's why even $5M may be low; that makes every A's player a Rockefeller type. Elias Varek at $10 billion but Tier 3 doesn't change the fact he's the richest citizen in the sim. Where Tier plays a factor is the casino — someone could get rich, ascend to heritage as a single person, and now the family part fast-tracks. I realize 0 citizens stand a chance to make heritage like that, so there are missing pieces to this puzzle.

**Builder, 2026-09-03 01:09 (S413, the scale and the business system) — recorded as said:**

> This is getting closer but the scale is off a bit. Elias Varek's net worth is $10 billion; John Ellis is an A's player, but that's not really the point. The A's are making a salary based on 2042. The citizens are not. Anything using real-world Oakland for its basis will break the sim. Rich get richer is real life. Heritage can own a business and multiple (likely not built); however there are plenty of business owners not on heritage — so citizens can open a business as their job, but the business system determines its success. An off-heritage citizen is dependent on the business for a salary; heritage business owners collect gains on top of their income. Think of it as the Matrix movie — it's a coded environment. Who becomes the Neo? Who's the Oracle? Who's the protagonist?

**Builder, 2026-09-03 09:40 (S413, on Dillon Studio) — recorded as said:**

> Some businesses aren't filled completely on the business ledger, which causes breaks in the code. Heritage names are active on business creation but not sure what's minting their staff numbers or profits. We have business owners that have no POPID but own a business. These are too many broken arms to a system being built. It may be easy to say "that's a different session's issue," but all these systems were built halfway and not wired properly or logically. Is Dillon Studios a logical business? Would Benji Dillon open this business type? Does a studio employ two people? Does a new business have $340M in profits as a start-up?

**Builder, 2026-09-03 10:21 (S413) — recorded as said:**

> I'm sure I'm asking for something very complex here, as the engine doesn't carry much that'll help determine the business type these heritage types may own. I don't know the answer to this; the fact it works at all should be my focus. I'm just hoping the work is meeting my doctrines and isn't falling into real-world Oakland stuff. I'm not really able to follow a lot of this, so I'll trust the mechanics and that the wiring is true with all of this.

**Read (engine-sheet):** the engine carries a field for every row (RoleType, SkillTags → the hiring categories, hood) and a line has members with fields — enough for "the line opens a business in the field a member works in, in the hood the staker lives in"; character (what Benji would *want*) has no column and is not invented. Trust is the signal it always was; the guardrails owed for it: wiring card first, the doctrine test in the plan before code, the prediction before the fire, the record naming what the bench did not prove. Filed as engine.96 Task 11.

### Direction, second sitting (builder, 2026-09-03 22:58, recorded as said)

Context: after the money-loop walk-through and the heritage-loss explainer, the builder opened design latitude on heritage entry and exit — *"I think I may have limited you creativity on the inclusion and based on the depth of the money system I'd imagine you could design better than I can tell you."*

- **The Heritage_Ledger is the "Rockefeller" ledger.** *"This is where the most wealthy bloodlines populate. If life starts as a generic citizen, the dream is to one day be on the heritage_ledger."*
- **Wealth is the gate in, and the defining factor.** *"Right now wealth is the gate in. And is the most defining factor as your bloodline's success and or failure depends on how long that bloodline has that status."*
- **Varek as the archetype.** *"Take Elias Varek, 10 billion, owns Civis Systems, majority owner of The Oaks, and set off the West Oakland tech boom, single young male. The most influential in the sim and someone like reporter Jax Caldera raises concerns on Varek power. He single-handedly created a bloodline. So his kids, wife carry that legacy. How their lives get determined tells the story as it goes."*
- **Dillon as the other archetype.** *"Take someone like Benji Dillon, wife kid and almost retired. So his income stops but opening businesses can keep him getting paid, unless he decides to work in another field after."*
- **Influence, not headcount.** *"The idea isn't that this list should have 50 families on it, it should have the most influential. Money to back what they do, whether fame or tier adds to this is likely, should determine what they do so all heritage families act differently."*
- **The top level of the game.** *"This is the top level of the game within the sim. If you make it here your LLM waking as a cron understands the game, and the engine has brought them luck."*
- **What is built vs what is being built.** *"The names there now are all manual adds. Varek and A's players are core implants. What we are building out now is the math that makes everyone fair game and the math plus cron influence tell me who is special in the sim."*

Read for the cut (engine-sheet): heritage is an INFLUENCE ledger, capped by influence not by a count; entry is wealth, tenure is the score; the engine (math) decides who is fair game, the cron (the citizen waking) decides what a heritage family does with it; the inverse (engine.156) must be real so tenure means something. Design latitude granted on entry/exit rules — numbers proposed in the cut, approved on trust.

## The chain as directed vs as built (S412)

| Link (builder's words) | As built | Pointer | State |
|---|---|---|---|
| Generic marries into Tier 4 | engine.66c family-slot lottery: 20 %/slot event, one GC reel × one open-slot reel, match = same hood / sex / age | `processAdvancementIntake.js:1266-1282` | WIRED, never matched on live |
| Raising Tier increases a citizen's life | Tier gates almost nothing: solo household (≤3), election eligibility (2–4), one micro-event chance (T1), newsroom context (≤2) | `householdFormationEngine.js:270`; `runCivicElectionsv1.js:211`; `generateGenericCitizenMicroEvent.js:456`; `citizenContextBuilder.js:804` | GAP — Tier has no lived payoff in pay, hiring, housing, courtship |
| Exposure can be negative → inverse | Every citation climbs; `Citizen_Media_Usage` carries no sentiment; no path lowers Tier on coverage | `mediaRoomIntake.js`; `processAdvancementIntake.js:428` | GAP |
| 25 media usage → fame; only fame fluctuates after | engine.118 designed on FameScore ≥ 25; builder now sets the bar on **UsageCount 25**; permanence + decay exemption; FameScore keeps grading A/D-list | `TIER_MOBILITY.md` §3 | GAP — re-specified |
| One person can found a line alone | Solo founding only at $350M (Door B); Door A needs three | `generationalWealthEngine.js:1449-1453` | GAP — an earned solo door |
| Pay, and engines that raise pay / Tier | E2 promotion, E3 hiring, hood reference pay, employer success (engine.135), field-first settlement (engine.144) | `runCareerEngine.js`; `educationCareerEngine.js` | WIRED |
| Wealth → buys a house | Home purchase is household physics — the citizen needs a `Household_Ledger` row; a single needs the solo door, which needs **Tier ≤ 3** | `householdFormationEngine.js:268-280`; `generationalWealthEngine.js:1529` (`trackHomeOwnership_`) | GAP — a single Tier-4 earner cannot own a home |
| A house raises marriage odds | Household presence ×1.5 on the wedding roll; owned vs rented makes no difference | `generationalEventsEngine.js` `checkWedding_` (engine.57 P4) | PARTIAL |
| Where you live + net worth → spouse quality | Compatibility: same hood +3, same job family +2, age gap, warmth. Net worth absent; "quality" is not a concept | `bondEngine.js:885-911` `bondCompatibility_` | PARTIAL — no wealth term, no quality |
| House + spouse → children | Birth needs a household; family type ×1.25; heritage multiplier; dial familyFreq | `checkBirth_` | WIRED |
| Same neighborhood → the kid's opportunities | `SchoolQualityIndex` by hood → settlement band; the hood's businesses pull the field | engine.60/66/143/144 | WIRED |
| The gifted era | 58 ENGINE rows carry seeded UsageCount above their citations; pre-populated wealth | measure hop 8 | HELD — by the builder's framing these stay; earned-only from here |
| A generic could become an athlete-level figure | Needs every link above plus the fame door | — | the whole build |

## Build order, revised to the chain (S412 — proposed; numbers to be set in each cut)

1. **engine.150 — the ladder as a state, earned counts only.** Tier follows the citation-backed bar every cycle; the 58 seeded cells are the gifted era and hold.
2. **engine.152 — coverage runs both ways.** The media-room intake stamps each usage row with a sentiment; a negative citation counts −1 on UsageCount (the decay path already demotes below the bar). Fame-permanent citizens lose FameScore instead of Tier.
3. **engine.118 (re-spec) — fame at UsageCount 25.** Permanent famous marker + Tier-1 floor + decay off; FameScore grades from there.
4. **engine.153 — a single can own a home.** Solo household on income alone (drop the Tier gate), then the wedding roll reads owned > rented > none.
5. **engine.151 — Tier pays.** Tier enters the same places dials do: hire-slot order, promotion order, spouse pool, home-purchase odds — a multiplier per rung. This is the "raising Tier increases a citizen's life" link.
6. **engine.154 — spouse quality.** Compatibility adds a net-worth-band term and the hood's prosperity; the drawn spouse's band tracks the citizen's own hood + net worth.
7. **engine.155 — Door C, earned, solo or family.** An owned home + a net-worth floor + Tier ≤ 2 (solo), or the same across a household (family), founds a line. Doors A and B stay.
8. **engine.156 — heritage loss.** Score can fall; tier steps down; an empty line goes dormant.
9. **engine.157 — the maneuver phase.** Engine determinism, Phase 5 after the money loop: reads Tier + dials + hood, sets the cycle's risk posture (casino stake, field change, relocation, the solo door), writes the LifeHistory line that moves the dials. Needs the ambition dial (engine.94 Track B) in the same cut.

Each is its own row and plan cut; 1–4 need no further design words from the builder; 5–9 get their numbers proposed in the cut and approved on trust.

### engine.151 — Tier pays (cut 5, S413) — numbers proposed, approved on trust

One table, one helper, four doors. `TIER_PAY = { 1: 1.50, 2: 1.30, 3: 1.15, 4: 1.00 }` and `tierPayFactor_(tier)` sit beside `TIER_BAR` in `processAdvancementIntake.js` (the ladder's bars and the ladder's payoff in one place; blank / 0 / ≥ 5 read as Tier 4 — engine.150 already ate a blank-Tier bug once). Tier enters exactly where the dials and the housing boost already enter; nothing new fires, nothing is gated — a rung buys pace and queue position, never cash.

| Door | Today | With Tier pays | Lived cause |
|---|---|---|---|
| Hire slot (`hireSlotOrder_`, E3) | poorest $10k band first → credential → income → POPID | the band is taken on income ÷ pay (a T3 at 50k queues as 43k; a T1 at 50k as 33k) — need still leads, a rung buys up to ~2 bands | `Hired at X (Tier N counted)` when the rung jumped a full raw band (band-level causes only, the engine.144 precedent) |
| Promotion (`promotionOrder_`, E2) | longest since last promotion → credential → lower earner → POPID | waited cycles × pay (never-promoted still first); T3 vs T4 only — Tier 1–2 stay outside by the engine.135 rule (§Direction: T1 never auto-moved, T2 only by a story event) | `Promoted at X (Tier N counted)` when the rung outran a longer raw wait |
| Spouse pool (courtship step, `updateExistingBonds_`) | step 0.20 / 0.10 × housing (owned 2.0 / rented 1.5), cap 0.5 | × pay of the pair's best rung, same cap. The GC draw stays an unweighted lottery (doctrine 10: no probability dials on the grab); the engine.59 romance-flip orbit (T1×T1 0.25×, Mike-ruled S320) stays as ruled — a super couple is rare to form and, once courting, quick to marry | none — pace only |
| Home purchase (`trackHomeOwnership_`) | eligible household rolls 6 %/cycle | × pay of the best member rung: 6.0 / 6.9 / 7.8 / 9.0 %. `HOME_ELIGIBLE_NW` untouched — Tier does not buy cash | none — the existing `[Home]` line |

Live reach (snapshot 2026-09-02): tagged unemployed pool 22 (T2 3 / T3 17 / T4 2); tracked ENGINE staff T3 47 vs T4 320; active households by best member rung T1 13 / T2 20 / T3 64 / T4 320. Effects are pace multipliers — a bench cycle proves the wiring and the suffixes, not a rate.

**Recorded, not built — the standing inverse:** an ENGINE citizen who climbs to Tier 2 on coverage (engine.150) leaves `applyEmployerSuccess_` and the career loop (`runCareerEngine.js:197`, `:791`) by the engine.135 rule above. Live today that class is 28 rows, 25 employed, almost all authored (the pastors, Jade Orion) — the rule protects them correctly. When an *earned* rung reaches 2 (the engine.150 marker distinguishes it), the career engine should keep that citizen; that is a one-line scope change filed here for when it bites, not this cut.

### engine.154 — spouse quality (cut 6, S413) — numbers proposed, approved on trust

Builder: *"where you live, what your net worth is, determines the quality of spouse."* Measure (live 2026-09-02): 597 active bonds, **0 romantic, 0 GC courtships**; 220 singles, WealthLevel 0 → 9 spread (58 unpriced at 0, 38 at 5, 33 at 6, 27 at 7, 21 at 8). Bench C107: 329 friendships, 12 single×single opposite-sex, **5 past the romance line (7.6 / 7.0 / 6.2 / 6 / 6)** — the gate is reachable and compatibility (≥ 6) is what decides who clears it. Every GC spouse minted today reads `hs-diploma` because `deriveEducationLevel_` is handed `null` frequencies; net worth comes from age + income alone — the citizen's own standing never enters. Two halves:

| Half | Today | With spouse quality |
|---|---|---|
| **Compatibility at the romance gate** (`processRomanceAndMarriage_`, ≥ 6) | same hood +3, same job family +2, age gap ≤ 10 +2 / ≤ 20 +1, same life-stage +1, warmth 0–2 — the same score that forms friendships (like-at-all floor 3) | **+ `bondSpouseQuality_` at the gate only** (6b — the first cut put it in `bondCompatibility_`, which also gates friendship formation): net-worth band term on `WealthLevel` gap ≤ 1 → +2, ≤ 3 → +1, ≥ 6 → −1, else 0 (blank → 0); hood prosperity term, both `IncomeTier` known and within 1 → +1. Like clears the gate sooner; a gulf costs one point and never bars. **6b, the welded door:** the gate's recompute indexed the name-keyed lookup by POPID, so every existing friendship scored as two strangers (~2 < 6) — the romance door had not opened since engine.67 step 9 (S325). Resolved POPID → name; the bench's adult pairs at the gate (Bodhi Davis × Lila Bennington, Fruitvale; Jax Caldera × Nandini Muhammad, Temescal) now score 6–11 and roll the 0.10 × orbit × fitness × family flip each cycle |
| **The drawn GC spouse** (`processGCCourtship_` mint) | Income by the hood's reference pay; education `hs-diploma` always; NetWorth by age + income | Income unchanged. **Education drawn from the ledger's own adult distribution** (`hoodEducationFreq_`, one pass per ctx — the hood bucket when that hood + age bracket carries ≥ 5 distinct levels, else citywide; `deriveEducationLevel_`'s rule, unchanged). **NetWorth band = the citizen's own band ± 1 (seeded), clamped into the hood's `WealthMin..WealthMax`**, then a seeded point inside that band (`netWorthForBand_`, the one band table beside `deriveWealthLevel_`). The spouse is priced by who they married and where |

Nothing minted, nothing forced: the GC draw stays a lottery, marriage stays years of a maintained bond (doctrine 10/11). Live reach: 5 pairs at the gate on the bench; the term moves the gate for matched pairs by up to +3. Expect 0 romances per cycle most cycles still — the flip is 0.10 × orbit × fitness × family per pair per cycle.

### engine.155 — Door C, the earned heritage door (cut 7, S413) — numbers proposed, approved on trust

Doors today (`updateHeritage_`): **A** = three living on-camera members in one `Family_Relationships` unit + combined NetWorth ≥ $1M; **B** = one citizen at ≥ $350M. Live: 5 lines (Corliss A, Dillon A, Keane B, Kelley B, Varek B — all C103), 11 citizens on a line. Nothing between "three on camera" and "apex cash" — the chain's own objects (a rung, a home, a household) open no door.

**Door C — the household door (re-set 2026-09-03, builder ruling: heritage is MONEY, no Tier gate; $500K and $5M are light).** An active `Household_Ledger` household whose `HousingType` is `owned`, with no member on a line, founds when its NetWorth — one member or combined — is ≥ **$50M** (band 10, the top of the wealth ladder). ~~solo $500K + Tier ≤ 2 / family $1M + a Tier ≤ 2 member~~ (shipped @33, overruled the same night: the bench founded 26 lines by C116 on it). Live at C105: nine citizens hold $50M outside a line, all the city's richest; with an owned home today, Paulson and Horn. Tier is the column as it stands (authored or earned — Tier is the hierarchy however it was reached; the gifted era holds by the builder's word). Runs after A and B (apex cash still founds by B). Every living member of the household joins the new line; FamilyName = the unit's most common surname (A's rule); `FoundedDoor` = `C`; the `[Heritage]` line reads *"a home, a name, and the standing to keep both"*. No clock filter (B has none — Keane and Kelley are GAME).

Live reach at C105: 117 owned households → **4 found on the first fire** — Keisha Ramos (T2 CIVIC, $613K, Lake Merritt), Mike Paulson (T1 GAME, $76M, Jack London), Danny Horn (T1 GAME, $90M, Temescal), the Necklar household (Johhny Necklar T2 ENGINE + Camila Cook, $6.4M, Rockridge). Bench at C109 (three cycles of purchases in): 151 owned → 7 (those four + Rivas/Yamamoto, Richards/Foster, Skenes). Earned-rung Tier 1–2 citizens today: 0 — the door is open before the first climber reaches it, which is the point.

**Filed for the desk pipeline (not engine, research-build lane):** *media Tier as routing* — `cron-desk-run` hands the higher-Tier byline more assignments; and *the wake economy* — the citizen-loop rota weights wake frequency by Tier / fame (builder direction 17:32). Neither gates a cut above.

### engine.158 — the home-purchase gates (S414, 2026-09-03) — numbers proposed, approved on trust

Builder (2026-09-03 00:20 / 00:48, above): *"nor is having a house [meant to be easy] … why was it so easy for them to do that? … net worth determines where you can live; your income determines how you live in that hood — each hood should have a minimum income to buy a house where your net worth says you live."* Measure (live C105, `Household_Ledger` + `Neighborhood_Map` + the ledger snapshot): 319 renting households, **236 clear the old rule (74 %)** — the price rode the household's own lease, and every lease comes from the 2026 `estimateRent_` table ($1,450–$2,400) or a seeded value ($991–$2,946) while the hood rows say $1,912–$3,166. Everyone's house was cheap. Three causes now sit in front of the dice, all read from the hood row the sheet already carries (ADR-0016); no cap, no quota:

| Cause | Rule | Live C105 (of 319) | Bench C117 (of 364) |
|---|---|---|---|
| the hood prices the house | price = max(hood `MedianRent`, own lease) × 12 × 22; cash floor 35 % of price unchanged | 222 | 212 |
| net worth says where you live | combined-NetWorth band ≥ hood `WealthMin` (floor only) | 204 | 184 |
| income says how you live there | mortgage × 12 ≤ **30 %** of `HouseholdIncome` (the column `processMoneyLoop_` reads for burden; blank / 0 carries no loan — 0 such rows on either sheet) | 110 | 117 |
| **all three** | | **104** (T1 11 / T2 13 / T3 19 / T4 61) | **100** (T1 6 / T2 9 / T3 12 / T4 73) |
| the dice | `HOME_BUY_P` 6 % → **1 %** × the rung (1.0 / 1.15 / 1.3 / 1.5 %) | 15.2 → **1.16** buys/cycle | 14.97 → **1.07** |

The third cause IS the builder's per-hood minimum income, derived from the hood's own rent instead of authored: Rockridge ≈ $156K, Jack London ≈ $149K, Downtown / Lake Merritt ≈ $145K, Chinatown ≈ $127K, Temescal ≈ $126K, Uptown ≈ $94K. Two levers, recorded separately so the fire reads as two: the gates alone at the old 6 % would give ~7 buys/cycle; the 1 % roll is the "a house is a huge step" claim — years of saving to qualify, then about a year of looking (median wait ~70 cycles at 1 %), not a quarter.

**Departures from the S413 NEXT line and the literal words — named so "approved on trust" is informed:**
- **Tenure floor (≥ 1 year rented) dropped.** 191 of 319 renters were seeded at C84; 0 of 236 eligible reach 52 cycles today — it would freeze every purchase until ~C136 and it is not a cause the builder named.
- **Hood `MedianIncome` as the floor, folded into the carry test.** The literal reading (`HouseholdIncome` ≥ hood `MedianIncome`, on top of the other two) leaves **71** on live instead of 104; the carry-derived floor sits under `MedianIncome` in the priciest hoods (Piedmont Ave $108K vs $150K; Lake Merritt $145K vs $185K). One word from the builder flips it to the literal reading.
- **Above-band households may buy where they already rent** (73 of the 236 sit above their hood's `WealthMax`). `hoodAdmits_` rejects above-band on a *move*; buying is not moving, and the migration physics still act on them.
- **`KONO` `MedianRent` 994** is a hood-row defect (every other hood 1,912–3,166); the `max()` shields this cut. Row left alone — the sheet is the truth until the builder trues it.

Bench prediction (C118 on SANDBOX 0831, pool of 100 listed in the S414 census): **0–3 purchases** (mean 1.07: P(0) ≈ 34 %, P(≤ 3) ≈ 98 %) against the 11–14 per fire the bench has been logging; every new owner comes from that pool; `HousingCost` = max(hood `MedianRent`, lease) × 264; new `MonthlyRent` × 12 / `HouseholdIncome` ≤ 0.30, so none trips the money loop's 50 % crisis the cycle it buys; 0 `Engine_Errors`. Tests: `householdReconcile.test.js` engine.158 block (12) + the engine.151 roll re-based.

### engine.160 — one hood rent rule (S414, 2026-09-03) — builder: "make sure whatever you build aligns with the sim"

Builder (2026-09-03 15:31): *"last session built an entire system on misaligned hood logic, making the same mistake of having different hood math everywhere, institutions.md sets the hood canon and how many there are."* Then (16:01–16:03): the money layer was real-2026 Oakland logic everywhere; *"I don't know what a house costs today how would I know what it would cost in my sim? … make sure whatever you build aligns with the sim."*

**Measure.** The hood SET is aligned: INSTITUTIONS §Neighborhoods = 22, `Neighborhood_Map` = 22 rows, `loadCanonNeighborhoods_` seeds them every cycle, `auditHoodDrift.js` passes on the set. The hood MONEY was four sources, none agreeing: (1) `estimateRent_`'s 12-hood 2026 table ($1,450–$2,400, else $1,700) set every lease — 221 of 319 live leases under their hood's `MedianRent`; (2) `HOME_PRICES_BY_NEIGHBORHOOD`, 15 hoods, no reader; (3) `Neighborhood_Map.MedianRent`, 22 rows, never authored — multiplied by the trajectory engine each cycle (KONO 994 vs 1,912–3,166; Piedmont Ave 2,184 under Laurel 2,736 against canon) — and engine.158 priced the house off it; (4) `IncomeTier` / `MedianIncome` / `WealthMin` / `WealthMax`, authored from INSTITUTIONS by engine.135 B1 — the only canon-derived money on the sheet. Ledger check: the sheet's authored `MedianIncome` runs 0.8–1.6× the median `Income` of the tracked citizens living in each hood (Rockridge 160K vs 119K; West Oakland 145K vs 89K; Temescal 68K vs 84K); `MedianIncome` also drifts (±0.2 %/cycle).

**The rule ("aligns with the sim" = one rule, read from the canon column, calibrated to what the sim's households carry).** `MedianRent = hoodRentShare × MedianIncome / 12`, `hoodRentShare` a World_Config key (self-armed, engine.94 contract) at **0.30 — the same share as `HOME_CARRY_MAX`**: housing is 30 % of a household's income whether it rents or carries a mortgage, one number with one meaning. Calibration on the 319 live renting households: current lease burden median 0.235 (the 2026 table); at 0.25 the rule gives 0.211 / 7 households over the 50 % crisis line; at **0.30 → 0.254 / 24**; at 0.35 → 0.296 / 40. Ordering check against canon: Lake Merritt 4,634 > Rockridge 4,008 > Piedmont Ave 3,750 > West Oakland 3,625 > Baylight 3,500 > … > Laurel 2,150 > … > East Oakland 1,800 > San Antonio 1,748 > Temescal 1,703. The house follows: price = rent × 264, so carrying a house in a hood needs ≈ 1.23 × that hood's median income — the builder's per-hood minimum income to buy, proportional to the hood instead of typed.

**Cut (one commit).** `hoodRentFromIncome_` beside the Phase-2 loader; `S.neighborhoodState[hood].medianRent` is the rule, never the stored cell; `estimateRent_(hood, ctx)` returns it and **throws** for a hood with no row / no income (no fallback, ADR-0016); the three `typeof estimateRent_ … : 1700` guards removed; the 12-hood table and `HOME_PRICES_BY_NEIGHBORHOOD` deleted; `neighborhoodTrajectoryEngine` renders the `MedianRent` cell from the rule (`'canon rent render'` intent, written only on disagreement) instead of drifting it — `RENT_GROWTH` / `RENT_DECAY` / `RENT_PRESSURE_KICKER` gone, income drift kept; `homeMarketRent_` unchanged in code (it already reads `medianRent`), comment trued. **Reversible half:** rent motion of its own is gone — if the builder wants rent to move apart from income, it comes back bounded, one word.

**Untouched, by rule:** the 319 existing leases (a many-row sheet write; the builder's word first — burden is computed from the household's own lease, so they are not harmed by the new medians). `Neighborhood_Map.MedianRent` cells: no hand write — the first fire renders all 22.

### engine.156 — heritage loss, designed as standing + tenure (S414, 2026-09-03 23:15) — LIVE PROD @43 (S415, 2026-09-04; bench @37 C126/C127)

**Direction it answers:** §Direction, second sitting — the Rockefeller ledger; wealth the gate and the defining factor; success = how long the line holds its status; influence not headcount; every heritage family acts differently; the math makes everyone fair game.

**Measure (what the ledger does today, `updateHeritage_`).** `HeritageScore` is a lifetime counter: every cycle a line ADDS `min(4, TotalNetWorth/500K) + (generations−1) + min(3, civic) + min(3, fame) + 2×businesses + homes`, and the tier is read off the running total (Established 50, Prominent 150, Dynasty 350). Nothing subtracts, nothing goes dormant, and the wealth term tops out at $2M — so **Elias Varek at $10B scores 12, last of the five lines on his own ledger**, under Corliss at $2.5M (16). `BusinessesOwned` counts only heritage-minted storefronts: Civis Systems and the Oaks (Key_Personnel `Elias Varek (founder)` / `POP-00789 … Owner`) count for nothing, though `parseKeyPersonnelOwners_` (engine.96 T10) already resolves them for the owner's draw. The birth multiplier and the business roll read the tier, so they already follow a tier down once one can move. Node side: **no wake, voice or packet builder reads `Heritage_Ledger`** (only `queryLedger.js`) — "the LLM waking as a cron understands the game" is not wired; a pipeline seam, filed below, not this cut.

**The reframe.** Two numbers, where there was one counter:
- **Standing** — recomputed every cycle from what the family holds NOW. It can fall. It sets the tier.
- **Tenure** — cycles the line has held its current tier, and cycles on the ledger at all. It only counts. It is the outcome the builder named ("how long that bloodline has that status"), and it is what the cron reads to know whether it is old money or new.

**Standing (proposed):**
`wealth + 3×(generations−1) + 3×min(3, civic) + 3×min(3, fame) + 4×businesses + 2×homes − 5×scandal`
- `wealth = 10 × log10(TotalNetWorth / $1M)`, floored at 0 — $1M → 0, $10M → 10, $100M → 20, $1B → 30, $10B → 40. Money is the defining term and it keeps mattering at the top; it no longer saturates at $2M.
- `businesses` = heritage-minted storefronts **plus** every active `Business_Ledger` row where a living member resolves as owner through `parseKeyPersonnelOwners_` (the same resolver the owner's draw uses). A closure (engine.96 T7 → `Business_Archive`) drops out on its own.
- `scandal` = living members holding a `Civic_Office_Ledger` seat whose status is `scandal` this cycle (the only scandal state the engine has).
- Tiers on standing: **Founding 0 · Established 30 · Prominent 50 · Dynasty 70.** Up or down, every cycle, no hysteresis — a family that loses its house or its business feels it the same week. Tenure at the new tier restarts at 0; `PeakTier` remembers the best.
- Live C105 check (the doctrine test — the richest man in the sim must lead his own ledger): Varek 40 + 8 (Civis, Oaks) = **48 Prominent-adjacent**; Kelley 25.7 + 3 fame + 2 home + 4 biz ≈ 35 Established; Dillon 26 + 3 gen + 3 fame + 4 biz ≈ 36 Established; Keane 26.5 + 3 + 4 ≈ 34 Established; Corliss 4 + 3 + 3 + 4 ≈ 14 Founding. Varek first, Corliss last — the order the canon would write.

**Dormancy (proposed):** a line with **0 living members for 4 consecutive cycles** goes `dormant` (`Status`, `DormantSince`): it stays on the ledger as history, leaves the ranked set, and its birth multiplier and business roll stop. A dormant line **revives under its own LineageId** when a descendant (a `ParentIds` chain back to a member) clears any door again — the name comes back, the tenure does not. A line whose standing sits under 10 (≈ $10M, nothing else) for 52 consecutive cycles goes dormant the same way: money gone for a year is a name, not a line.

**Entry is unchanged** (Doors A/B/C as ruled; $50M + an owned home is the front door). The ledger is not capped by count: at $50M the door already admits nine citizens at C105, and dormancy prunes. "Not 50 families" is enforced by the door and the fall, not a quota.

**Schema:** `Heritage_Ledger` gains `TenureCycles`, `TierTenure`, `PeakTier`, `Status`, `DormantSince` (append-only via `ensureHeritageSchema_`, both sheets, read back). `HeritageScore` becomes standing (the lifetime counter is retired — its only reader was the tier). `HERITAGE_TIERS` re-based to the standing bars.

**Lines (LifeHistory, members):** `[Heritage] the <name> line stepped down — <tier> now`, `[Heritage] the <name> line went dormant — no one left to carry it`, `[Heritage] the <name> line revived — <first> <last> brought the name back`. Story hooks `HERITAGE_FALL` (severity 5) / `HERITAGE_DORMANT` (6) / `HERITAGE_REVIVED` (5), domain COMMUNITY, beside the existing rise hooks.

**Blast radius (to open before cutting):** `heritageTierByPop_` (phase-4 birth boost reads the tier — dormant must read as no tier); `HERITAGE_BIZ_ROLL` (reads the tier); `lineByPop` same-cycle consumers; `scripts/householdReconcile.test.js` heritage blocks (the 67-test suite pins Door C and the counter); `queryLedger.js`.

**Bench proof:** standing computed for all 5 lines with Varek first; a seeded line with a dead member set goes dormant on the 4th fire; a seeded NetWorth cut steps a tier down with the line and the hook; 0 errors; births and business rolls stop on the dormant line.

**As coded (S415, 2026-09-04) — where the code settled the design's open edges:**
- **Schema is seven columns, not five:** `TenureCycles`, `TierTenure`, `PeakTier`, `Status`, `DormantSince` + the two dormancy clocks `EmptyCycles`, `LowCycles` (a streak needs a cell; nothing else on the row could carry it). `ensureHeritageSchema_` appends missing headers to the existing tab and `updateHeritage_` reads them the same cycle; pre-existing 16-wide rows are padded before the rewrite (Apps Script `setValues` needs the exact width).
- **Door A vs the low-standing clock:** Door A admits at $1M + 3 members — standing ≈ 0–6, under the 10 floor — so as designed every Door-A family would start its 52-cycle countdown the cycle it was founded (Corliss today: 10.0 on the nose, one nickel of rounding). Ruling in code: `LowCycles` ticks only for a line whose `PeakTier` is Established or better — "the money that made it is gone" presupposes having had it. Corliss starts no clock at C106.
- **Dormant = blank tier.** Every consumer already skips a blank `HeritageTier` (`heritageTierByPop_` for the birth boost, estate rate and lottery; `buildCityEvents_` hosting; the ECE rank check off `lineByPop`) — so "leaves the ranked set" needed no consumer edits. `heritageTierByPop_` also skips `Status` dormant as belt. `lineByPop` omits dormant members; the business roll is gated on active.
- **Revival is its own predicate** — the door loops skip anyone carrying a `LineageId`, so a dormant line's own living members are checked directly against the three bars (B: one member ≥ $350M; A: ≥ 3 living + $1M combined; C: a member in an active owned household + $50M combined). Membership growth keeps attaching kids/spouses to a dormant line, so an heir finds the name waiting. On revival `TenureCycles`/`TierTenure`/both clocks reset to 0, `PeakTier` holds, the tier is read off standing.
- **Businesses:** the heritage-minted list is pruned to BIZ_IDs still on `Business_Ledger` (a closure drops out and re-opens the roll slot); `BusinessesOwned` for standing = that list ∪ every active row a living member resolves as owner through `parseKeyPersonnelOwners_`/`resolveOwnerRow_` (living rows only), deduped by id. One `Business_Ledger` read per cycle replaces the roll's lazy read. Live: Civis + the Oaks resolve to Varek, Firehouse 29 Academy to Keane, the Bay Tribune's `(Editor-in-Chief)` tag is staff, not ownership.
- **Tenure seed:** a pre-engine.156 row seeds `TenureCycles` = cycle − FoundedCycle on its first stamp (C103 lines read 3 at C106); a line founded this cycle reads 0; `TierTenure` restarts at 0 on any tier change and counts 1 where the tier held.
- **First live fire (C106) is a re-base, name it:** all five lines sit at Founding today, so four rise to Established the same cycle — Varek 48, Keane 33.5, Dillon 32, Kelley 30.7 — and the "roots deep enough to notice — established" line lands on Vinnie, Amara, Benji, Maya, Rick, Isley and Varek. Corliss 10.0 stays Founding. No falls, no Prominent hooks, no dormancy. That is the math landing, not a surprise write on Tier-1 rows. And Established has consequences the same cycle: each of the four rolls `HERITAGE_BIZ_ROLL` at 15 %/cycle for a storefront (≈0.6 mints/cycle across them, all slots open), reads `HERITAGE_BIRTH_MULT` 1.3 from C107, hosts at 0.7 in `buildCityEvents_`.
- **Known seam — the owner's draw:** `applyOwnerDraw_` decides heritage treatment off the SL `LineageId` alone, so a dormant line's living members still take the no-cut, positive-gains-only path. The design named the birth multiplier and the business roll as what stops; it never named the draw, so this cut left it. Dormancy is not total until that reads `Status` — engine.157 or a follow-up.
- **The C125 bench fire:** ran the old tree though version 37 was deployed and the script was byte-identical — consistent with a deployment propagation window (inference, not a read cause); C126 ran the new tree. Live carries no risk from it: the builder fires hours after a deploy.
- **Stale pointer:** the blast-radius list named `queryLedger.js`; it has no heritage reference (grep, S415). Not chased.
- **Tests:** `scripts/householdReconcile.test.js` +17 (105/105): the formula; the five-line C105 fixture on a 16-column tab (columns arm, Varek first, four rise, tenure seeds, no low clock on Corliss); the fall with its line and hook; the low clock + scandal; the 52nd cycle; empty ×4 → dormant + `heritageTierByPop_` blank; a dormant line under the door stays dormant and rolls nothing on a 0.01 draw; revival by C and by B; closure prune + owner dedupe. The engine.96 T11 fixture re-seeded to $1B (its $400M Dillon is 28 on the standing bars — Founding — and would not roll).

**Pipeline seam, filed for research-build (not this cut):** hand the waking citizen its line's row — tier, tenure, peak, what the family holds — in the wake pack, so a Varek heir and a Corliss cousin wake as different people. Until that lands, "the cron understands the game" is one-way: the engine knows, the voice does not.

### engine.157 — the maneuver phase (cut 9, S416, 2026-09-04) — LIVE PROD @44 (bench @38 C106/C107/C108)

**Direction it answers:** §Direction — *"the cycles throwing all this data at a cron and it knowing it needs to maneuver up in Tier and dial state to advance their bloodline"*; *"engine determinism going their way"*; the second sitting — *"the math plus cron influence tell me who is special"*. Builder steer for this cut (2026-09-04 00:51): **"take TraitProfile dials into consideration."**

**Measure (what the cards say, S416).** The dial system already carries the ambition axis: `drive` is glossed *ambitious, focused, determined* (`utilities/compressLifeHistory.js:275` `TRAIT_MODIFIERS`), Striver is the high-drive archetype, and `openness` is the willingness to change field or hood. No `ambition` or `risk` dial exists anywhere in `phase*/ lib/ utilities/` (wiring card, grep). Live C105 drive distribution over 810 active adult non-GAME rows: 488 sit at the untouched midpoint 50, 91 in 50–60, 78 in 60–70, 143 at 70+; no adult dial reads under 40 on drive/openness/family (the fold has almost no negative tags on them). Where the cycle's risk sits today: casino placement `p = 0.012 × driveF(1.4 if drive ≥ 60) × show × sports`, stake `weekly × (0.08 + 0.12·rng)` capped (`casinoLedgerEngine.js:268-303, :685-697`, zero World_Config keys); the solo door `10 %/cycle at income ≥ $85K` (`householdFormationEngine.js:283-284, :386`); home purchase `1 % × rung` (`generationalWealthEngine.js:1481-1487`); relocation on two lanes only — pressure 35 % (DisplacementRisk ≥ 8) and misfit 15 % (income ≥ 2.5× hood median), hard-capped at 2 units/cycle (`migrationTrackingEngine.js:119-127`); a cross-field hire is **gone** since engine.135 E3 — *"a field change is a story in a life, never a filler for an open slot"* (`runCareerEngine.js:1186-1190`). Every one of those is a roll with no reader of the citizen's own disposition except the casino's drive read — which parses `DialState.current.drive`, a key the serializer never writes (`compressLifeHistory.js:1184`), and survives only on the TraitProfile `drive:NN` regex.

**The reframe — ambition is a read, typed ambition is the rung, posture is a multiplier.**
- **Ambition** `A = round(0.7 × drive + 0.3 × openness)` off `DialState.base` (fallback: the TraitProfile face's `drive:NN|openness:NN`; no dials → 50). Not a ninth dial: FIX inside the dial system Mike built, not ADD beside it. This is the engine.94 Track B *ambition* ruling — recorded on that row.
- **Goal (typed ambition)** is read off the row's rung on the chain, never stored: no `HouseholdId` → **establish** (a place of their own); household rented → **home**; owned, no line → **wealth**; on an active line → **tenure** (the name to keep); on a dormant line → **revive**.
- **Posture** — one of `climb` / `hold` / `retreat`, set every cycle from causes, then the dice stay where they are:
  - `retreat` when `DebtLevel ≥ maneuverRetreatDebt` (6), or the line's standing sits within `maneuverStandingMargin` (5) of the bar below its tier (Established 30 / Prominent 50 / Dynasty 70) — pull in, protect what is held.
  - `climb` when `A ≥ maneuverClimbBar` (60) and no retreat cause.
  - `hold` otherwise — the quiet middle, the default state that says nothing.
- **What posture multiplies** (`maneuverClimbOdds` 1.5 / `maneuverRetreatOdds` 0.5 / hold 1.0), each at the roll that already exists — no new outcome, no cap, no quota (doctrine 1–2):
  - casino placement `p` (replaces the drive≥60 read — drive is inside A now); the stake band — climb draws `weekly × (0.12 + 0.18·rng)` under the 25 % cap regardless of WealthLevel, retreat halves the draw; `casinoDrive_` fixed to read `base.drive` where it looked for `current.drive`.
  - the solo door — `SOLO_ESTABLISH_CHANCE × factor` when goal = establish.
  - home purchase — `homeBuyChance_(bestTier) × factor` of the head of household when goal = home.
  - relocation — the lane chance × the unit head's factor (the 2-unit cap is untouched — a doctrine-1 defect **seen**, filed below, not lifted silently in this cut).
  - a field change — the citizen's side of the E3 ruling: an **unemployed** citizen in `climb` with `openness ≥ maneuverClimbBar` is *willing* cross-field; a hiring window with openings and zero same-field candidates may take a willing one, logged `Career-FieldChange` as today. The business never fills a slot with an unwilling stranger; the citizen chooses the field change as their own move.
- **Where it runs:** `Phase5-Maneuver`, after `Phase5-BusinessDynamics` and before `Phase5-Career`, in both entry points. The plan said *after the money loop*; the cards put the five consumers at Career :352, HouseholdFormation :376, GenerationalWealth :377 (owner's draw 1.7 / casino 2.45 / home 6 / heritage 7) and MigrationTracking :380 — a step after the money loop is seen the same cycle by MigrationTracking alone and every other consumer would read it a cycle late through a persistence bridge. Before Career, every consumer reads this cycle's posture, and the step reads the cycle's opening state (last cycle's committed money, standing and dials — the only state the row holds at that point anyway). Deviation on mechanism, this terminal's call.
- **What persists:** `S.maneuver.byPop[pop] = {posture, goal, ambition, drive, openness, reason}` for the same-cycle consumers; the decision itself rides on `DialState` as an additive field `maneuver: {p, g, a, c}` (the chaosExposure pattern — `deserialize_` / `serializeDialState_` gain a passthrough so the Phase 9 fold and the Phase 4 chaos rewrite carry it). The wake side (`lib/citizenDials.js`) can read it from there — the pack wiring is the research-build seam already filed under engine.156.
- **The LifeHistory line — on change only, and hold is silence.** `[Maneuver-Climb] playing to climb — the house first`, `[Maneuver-Retreat] pulling in — the debt says so` / `— the line's standing is near the bar`, `[Maneuver-Hold] settling — <goal>` only when leaving climb/retreat. Tags in `citizenDialMap.js`: Climb `{drive +2, openness +1}`, Retreat `{composure +2, family +1}`, Hold `{composure +1}` — small, and bounded by firing on change only (no drive→climb→drive runaway). A `LifeHistory_Log` row per line.
- **The owner's draw seam (engine.156 *As coded*, closed here):** `applyOwnerDraw_` decides heritage treatment off `LineageId` alone; it now reads the active-line member map (`heritageTierByPop_`, which already skips dormant lines) — a dormant line's members take the off-heritage path. When no `Heritage_Ledger` sheet is present (Node harness) the map is null and the old rule holds.
- **World_Config (ADR-0015), self-armed at open beside engine.160's:** `maneuverClimbBar` 60, `maneuverRetreatDebt` 6, `maneuverStandingMargin` 5, `maneuverClimbOdds` 1.5, `maneuverRetreatOdds` 0.5, `maneuverDriveWeight` 0.7. Missing key throws, engine.160 pattern.
- **Exclusions:** deceased, GAME clock (Paulson's), under 18. MEDIA rows are citizens and play.

**Doctrine test:** does a row drive a fate the builder didn't choose? A Striver at drive 74 with a rented flat now buys her house at 1.5× the odds of the Anchor next door, bets bigger, and moves up sooner; a family at DebtLevel 6 pulls every lever in; a Corliss cousin whose line sits at 31 stops gambling until the standing clears the bar. The dice are the same dice. Nothing here sets an outcome.

**Live C106 prediction (from the C105 snapshot, 810 adults):** **195 climb / 581 hold / 34 retreat** — climb by goal: establish 81, home/wealth 113, tenure 1; retreat: establish 24, home/wealth 10; every retreat is DebtLevel ≥ 6 (no line sits within 5 of a bar at C105: Corliss 10.0 Founding has no bar below). Tier-1/2 climbers include Richmond (A77), Delaine, Santana, Okoro, Ramos, Dane, Wright, Ashford. First stamp writes **~229 `[Maneuver-*]` lines** (climb + retreat; hold is silent) — the re-base, named like engine.156's. World_Config 85 → 91. Casino slips ≈ 12 → ≈ 14 (195 rows at 1.5×, 34 at 0.5×). Solo formations ≈ 17 → ≈ 19. Purchases unchanged in expectation (≈ 1). 0 errors.

**Bench proof:** `World_Config` +6; `S.maneuver` counts in the phase log matching the prediction ± the dice; a seeded citizen at drive 80 / DebtLevel 0 reads climb with the line; the same row at DebtLevel 7 reads retreat and the line flips; a seeded line at standing 31 Established puts every member in retreat; `DialState.maneuver` survives a Phase 9 fold; a dormant line's owner takes the off-heritage draw; an unemployed climber with openness 65 is hired cross-field into a window with zero same-field candidates.

**As proven (S416, SANDBOX 0831 re-synced from live at C105 with the script properties cleared by the builder — a true replay of live's C106):**
- **C106 (@38): ok / 192s / 132 phases (+1, `Phase5-Maneuver` ≈ 1s) / 0 `Engine_Errors`.** `World_Config` 85 → 91 (six `maneuver*` keys self-armed). **195 climb / 39 retreat**, 234 `[Maneuver-*]` life lines = 234 `DialState.maneuver` memories (the Phase 9 fold carried every one), hold silent as designed. By goal: home|climb 79, establish|climb 79, wealth|climb 34, tenure|climb 3; establish|retreat 24, home|retreat 11, wealth|retreat 4. Casino slips 14 (≈14 predicted; two at the $2,000 ceiling — the climb band), households formed 24 / 21 solo (≈19 predicted), relocations 2 (the cap), `Heritage_Ledger` 16 → 23 in the same fire. Hal Richmond (T1): *playing to climb — the house first*; Alisha Peña (T4, debt 6): *pulling in — the debt says so*.
- **The prediction missed by 5 on retreat (34 → 39) and the miss is the prediction's, not the engine's:** all 39 carry DebtLevel ≥ 6 in the C105 snapshot; the five are 17 by the memory's `2041 − BirthYear` anchor and 18 by the engine's own year (`simYearOf_`, engine.148) — the anchor convention in memory is stale against the calendar the engine runs on. Climb landed exact (195).
- **C107: ok / 199s / 0 errors.** 15 lines: 11 climb (≈10 of them goal changes — climbers who formed a solo household at C106 now read *the house first*), **4 retreat — two of them `tenure|retreat`: Vinnie and Amara Keane, *the line's standing is near the bar*** — C106's engine.156 re-base put the Keane line at 33.5 Established, within 5 of the 30 bar, and the maneuver phase read it the next cycle. The designed physics, landing on a Tier-1 family unprompted. Casino 17 slips, 28 solo households.
- **C108: ok / 144s / 0 errors.** Before the fire exactly one retreat memory sat under the debt bar (Guadalupe Lupe, d5 — the C107 money loop had cleared it after the maneuver read); the fire wrote exactly one `[Maneuver-Hold] settling — a place of their own first` and flipped her memory to hold. 6 new climbs, no churn.
- **What the bench did not exercise:** no `Career-FieldChange` row fired across C106–C108 (the 3 + 1 hires were same-field; no window with zero same-field candidates met a willing climber) — the willing cross-field path is unit-proven only. The home-purchase factor was never isolated: owned households 119 → 121 across C107 with no purchase attributed to a climb head — unit-proven at the call, not seen on the bench. Both are physics the dice will show on their own cadence; nothing to seed.
- **Known hazard, unscheduled:** the offline DialState writers `scripts/backdateCitizenDials.js` and `scripts/seedTier1EssenceLive.js` build the citizen from scratch (`serializeDialState_` without `deserialize_`), so a hand run on a row drops its `maneuver` memory and the next cycle re-stamps that row's line once. Neither is on the crontab or in any `cron-*.js` (grep S416). `drainReflectionBacklog.js` goes through `deserialize_` and keeps it.
- **LIVE PROD @44** (staged from a `clasp pull` of live + the eleven-file overlay, diff vs the bench stage empty, pull-back 174 files / 0 tests, HELD four at base). **Live C106 prediction: `World_Config` +6, 195 climb / 39 retreat, 234 lines, casino ≈ 14, solo ≈ 21, Keane line retreats at C107.**

**Superseded:** the flat `RELOCATION.MAX_UNITS_PER_CYCLE: 2` named here was ruled on by the builder and rebuilt as a share in engine.161 below.

### engine.161 — relocation is a share, and the burden is the cause (S416, 2026-09-04) — LIVE PROD @45 (bench @39 C109/C110/C111)

**Direction it answers (builder, 2026-09-04 02:27, recorded as said):** *"The relocations was to percent a max move where citizens all move at once but 2 is too slow and defeats everything we just built, you can plan the accurate number on this but given living in the wrong neighborhood can put a citizen in crisis."*

**Measure (live C105, S416).** 849 movable units (households + solo earners in a canonical hood). The misfit lane — income ≥ 2.5× the hood median — holds **61 units**. The pressure lane holds **0**: `MigrationIntent` reads `staying` 662 / blank 304 / `considering` 1 / `planning-to-leave` 1, because planning needs `DisplacementRisk ≥ 8` and the risk score tops out below that for almost everyone. `MigratedCycle` across the whole ledger: C99 ×1, C102 ×2, C103 ×3, C104 ×3 — the cap of 2 units was binding on a city that wanted ~9 moves a cycle from the misfit lane alone. **The cap was the whole traffic light.**

**Why the pressure lane is empty.** Rent burden only scores for a household that is *unbuffered*: `SAVINGS_BUFFER_MONTHS = 12` skips any household holding a year of rent in reserve. Live, 279 of 319 active rented households clear that bar, so 40 renters can carry burden risk and 9 of those sit over 50 % of income. The city is largely solvent; that is the world state, not a defect.

**The change, three parts:**
1. **The ceiling is a share, not a number.** `relocationCap_` = `World_Config relocationMaxShare` (0.05) × the movable units this cycle, floor 1 — **43 at C105**. It exists only to stop a whole-city stampede in one week; at ~12 expected attempts it never binds. Physics decides who moves, the guard decides only that the city cannot empty at once (doctrine 1: a stampede guard is not an output quota).
2. **The burden is a cause of intent, not only a risk input.** `updateMigrationIntent_` now reads the household's own rent burden beside the risk score, on the household engine's existing lines: ≥ `RENT_BURDEN_CRISIS` (0.50) → `planning-to-leave`; ≥ `RENT_BURDEN_WARNING` (0.40) → `considering`. A household paying half its income in rent plans to leave whether or not it is senior, uneducated, or in a high-pressure hood. This is the builder's *"living in the wrong neighborhood can put a citizen in crisis"* — the relocation is the escape the crisis dissolution otherwise beats them to. The burden is stashed on `ctx._unitBurden` by `assessDisplacementRisk_` in the same pass (cycle-scoped, no new column).
3. **The maneuver posture multiplies the MISFIT lane only.** Moving up is ambition; the pressure lane is the crisis escape, and a household pulling in under debt is not made slower to leave a rent it cannot carry. (engine.157 applied the factor to both — corrected here.)

**Live C106 prediction:** `World_Config` +1 (`relocationMaxShare`). Pressure lane 0 → **9 units** (the 9 unbuffered renters over 50 %), misfit lane 61, expected attempts **≈ 12.3/cycle** (9 × 0.35 + 61 × 0.15, before the destination-fit and admission filters that reject most candidates), cap 43 and not binding. Moves land as `cost` (priced out) and `opportunity` (moving up) reasons. Ledger-wide this reads as ~12 of 849 units per cycle ≈ 1.4 %, which at the 1:443 sample is a city-loud but not implausible churn.

**As proven (S416, bench @39, C109–C111, 0 `Engine_Errors` ×3):** `World_Config` 91 → 92 (`relocationMaxShare` 0.05 self-armed). The intent wire fired immediately — `planning-to-leave` 1 → 7, `considering` 1 → 70 (209 by C111 as burdens spread). **All three move reasons now appear, where every move for four cycles before this had been the same trickle:** C109 two `displaced`, C110 two `cost` (Ingrid Bautista → KONO, Renée Cabrera → East Oakland — the pressure lane's first moves in the sim's history), C111 three rows / two units `cost` + `opportunity` (Andre and Bruce Lee moved to Rockridge together — a household moving up as one unit).

**The honest read — the cap was not the main brake, and the margin should stay.** Running the real `processRelocations_` against bench state with every roll passing: **65 eligible units (7 pressure, 58 misfit), of which 49 have no destination that beats their current hood by `MIN_SCORE_GAIN` 1.5, leaving 16 reachable.** Cap 45, never binding. Expected throughput at the existing lane odds is **≈3.2 units/cycle**, and observed C109–C111 was 2 / 2 / 2 units — the physics, not a ceiling. Measured against the margin:

| `MIN_SCORE_GAIN` | reachable units | expected moves/cycle | ledger rows/yr |
|---|---|---|---|
| **1.5 (current)** | 16 | 3.2 | ≈191 (20 % of the ledger) |
| 1.25 | 40 | 6.8 | ≈407 (42 %) |
| 1.0 | 47 | 7.8 | ≈469 (48 %) |
| 0.5 | 59 | 9.7 | ≈577 (60 %) |

A real city turns over ~10 % of its households a year. At 1.5 the sim already runs at twice that; one notch looser quadruples it. **`MIN_SCORE_GAIN` stays at 1.5** — the old flat cap of 2 was clipping an expected 3.2, so lifting it recovers the missing third and unlocks the *variety* of cause (all three reasons now fire) more than the volume. A citizen who wants a better hood and has one waiting now goes, and the engine.157 posture lifts a climber's misfit odds 0.15 → 0.225.

**LIVE PROD @45** (live pull + the three-file overlay, diff vs the bench stage empty, pull-back 174 files / 0 tests, HELD four at base). **Live C106 prediction:** `World_Config` +1; `planning-to-leave` 1 → ~9 and `considering` climbing past 60; 2–4 units relocating with `cost` and `opportunity` reasons present, not `displaced` alone.

**Bench proof:** `World_Config` +1; the pressure lane non-empty for the first time; moves per cycle in the high single digits with both reasons present; 0 errors; the cap does not bind.

## What exists today (verified S411 or pointed)

- **Tiers:** four tracked tiers + Generic_Citizens as the Tier-5 pool. Mobility as built is in `docs/engine/TIER_MOBILITY.md`: CLIMB via the UsageCount ladder (media appearances), the fame door (engine.118, designed, **unwired** — zero fame→Tier writes exist), demotion. Media promotion is structurally dead (engine.108: a Tier-5 can never be covered because the packet path stopped reading Generic_Citizens). **That is the first rung and it is missing.**
- **Heritage:** `Heritage_Ledger` 5 lines, all `Founding` (Corliss, Dillon, Keane, Kelley, Varek), 1–4 living members, 1–2 generations, scores 12–17. Columns: LineageId, FamilyName, FounderPopId, FoundedCycle, FoundedDoor, Generations, LivingMembers, MembersList, HeritageScore, HeritageTier, TotalNetWorth, HomesOwned, BusinessesOwned, CivicMembers, FameMembers. No sector/trade on a line. `heritageTierByPop_` feeds the 18th-birthday settlement (+1/+2 on the draw, engine.66). Founding doors: pointed in the heritage build (grep `FoundedDoor`).
- **Household:** `Household_Ledger` 682 rows (HouseholdId, HeadOfHousehold, Type, Members, Neighborhood, HousingType, rent/cost, HouseholdIncome, Formed/Dissolved, Savings). 49 of 50 minors have a household; 47 a resolvable parent. Household income + parent education + heritage tier + school quality set a kid's start (engine.60/66/143/144).
- **Dials:** `DialState` per citizen, compressed from LifeHistory lines by `compressLifeHistory_` through `citizenDialMap.js` (every event tag → dial deltas; `youth-*` tags included since S411). Dial bands already scale participation (drive → youth-event frequency; engine.32 T5) and the E2/E3 events do not read dials.
- **Education → career (S411):** field-first settlement, credential tie-break, youth-mode texture; SkillTags = current then trained field; a parent's field pulls the kid's.
- **Chance/lottery inputs:** `Casino_Ledger` armed live (4b, grok); UNDOCKED show feed (`Undocked_Feed`, Reputation lines); chaos cars; health milestones; employer success/layoff (engine.135); civic scoring and initiatives as hood-level conditions (engine.139).
- **Crons/wakes:** the 24/7 citizen-loop wakes (citizen-voice agents), the civic Sunday chain, the media daily pipeline. None of them today reads a citizen's Tier/dial position and *acts* to move it.
- **Memory:** engine.94 Track A (grief/approval self-arm) live; Track B (typed grudge, ambition, folk memory) gated on a design session — **ambition is the dial this arc needs**.

## The measure (engine-sheet, S412, 2026-09-02) — hop by hop, with pointers

Every pointer opened this session; counts are the live ledger snapshot (968 rows, refreshed S411), the live `Generic_Citizens`, `Family_Relationships` and `Heritage_Ledger` tabs read 2026-09-02.

### Up: one generic citizen → a heritage line

| # | Hop | Mechanism as built | Pointer | State |
|---|---|---|---|---|
| 1 | The Tier-5 pool | `Generic_Citizens` — 299 rows, 269 active | `generateGenericCitizens.js` | WIRED |
| 2 | A Tier-5 gets named | Desk packets read GC as a bounded fallback (engine.108, landed `e371d815` 2026-08-16) → article → Saturday print → `mediaRoomIntake` writes `Citizen_Media_Usage`; an unmatched name routes to GC `EmergenceCount` | `scripts/buildDeskPackets.js:2144`; `processAdvancementIntake.js:361` | WIRED, never converted — 41 GCs carry `EmergenceCount` > 0, max 2; the bar is 3 (`GC_EMERGENCE_PROMOTION_THRESHOLD`, `:982`, builder-ruled S320) |
| 3 | Tier-5 → a Tier-4 row | Three doors, all keyed on that count: (a) engine.58 emergence promotion at ≥3 → `Advancement_Intake`, enters Tier 4 (`:1110`); (b) `checkForPromotions_` — world-modified 20 % base chance for a GC at the bar (`checkForPromotions.js:362-367`); (c) engine.66c family-slot lottery — 20 %/slot event, two reels must match (`:1266-1282`) | as cited | WIRED, NEVER FIRED under the current mechanisms — `Advancement_Intake1` (12 rows) carries no mint note; the GC tab holds 17 `Promoted` + 13 `Emerged` rows, all at `EmergenceCount` 0, and **none of the 17 promoted names is on the ledger** — a pre-S320 era's statuses, stale |
| 4 | First job, income | Field-first settlement at 18; E3 hires by SkillTags into the field; E2 promotes; pay off the hood's businesses | `educationCareerEngine.js:1089`; `runCareerEngine.js:1071` | WIRED (S411) |
| 5 | Household | Criteria formation: married / adult with on-ledger kids / parentless minor. Solo establishment needs **Tier ≤ 3** AND income ≥ 85k, 10 %/cycle | `householdFormationEngine.js:268-300` | WIRED — a Tier-4 earner cannot establish a household alone; the Tier gate sits on this hop |
| 6 | Dial movement | LifeHistory tags → `DIAL_MAP` deltas → `DialState`; dial bands scale odds in 11 engines (events, youth, career, conduct, relationships, household, neighborhood, education, micro-events) | `utilities/citizenDialMap.js:22`; readers by grep | WIRED as a modifier: dials reach **money through odds** — drive ≥ 60 → 1.4× casino participation (`casinoLedgerEngine.js:685-686`), `careerFreq` scales career-event chance (`runCareerEngine.js:912-913`). **No dial reads Tier and nothing reads a dial to move Tier. No ambition dial** (engine.94 Track B, gated) |
| 7 | Money | Per-cycle accrual (`processMoneyLoop_` `:294`, write `:404`); inheritance (`:877`, `:1065`); casino ± (`casinoLedgerEngine.js:588`); employer success / layoff (engine.135) | `generationalWealthEngine.js` | WIRED. T4/ENGINE median NetWorth $408k, top $3.2M |
| 8 | Tier 4 → 3 → 2 → 1 | UsageCount ladder ≥3 / ≥6 / ≥9 — fires **only inside a citation-processing pass**, on `newUsage` | `processAdvancementIntake.js:428-437` | WIRED, EVENT-ONLY. **The 12 ENGINE Tier-4 rows at UsageCount 3–15 never climbed because their counts are seeded, not earned**: 0 `Citizen_Media_Usage` rows behind any of them (POP-00529/00531 at 9, 00530 at 14, 00532 at 15, 00590 at 12 — the Feb–Mar 2026 mint era); 58 ENGINE rows in all carry a cell above their citation count. engine.69's convention (no marker = hand-authored = held) is what holds them. A state rule would lift six of them straight to Tier 1 |
| 9 | Decay | 10 quiet cycles → −1/cycle; an earned rung gives way below its bar; Tier 1 exempt | `:869-942` | WIRED (engine.69) |
| 10 | Fame tenure | FameScore ≥ 25 → permanent Tier-1 floor | engine.118 | GAP — designed, zero fame→Tier writes |
| 11 | A line is founded | Door A: a `Family_Relationships` unit with ≥3 living tracked members, none already lined, combined NetWorth ≥ $1M. Door B: one citizen ≥ $350M | `generationalWealthEngine.js:1449-1453`, `:1689-1730` | WIRED, UNREACHABLE from below — **0 candidate units** (two unlined units have 3 living members, at $78k and $0); Door B is the four existing Tier-1 apex rows. `Family_Relationships` holds 101 rows; households (682) are not the registry |
| 12 | The line grows | Kids inherit `LineageId` from a lined parent; a spouse joins by taking the surname. Score/cycle = min(4, NW/500k) + (generations − 1) + min(3, civic members) + min(3, members at UsageCount ≥ 5) + 2×businesses + homes. Tiers: Founding 0 / Established 50 / Prominent 150 / Dynasty 350 | `:1615-1640`, `:1806-1812`, `:1441-1446` | WIRED, slow — five lines at 12–17 pts; 11 ledger rows carry a `LineageId` |
| 13 | Heritage lifts a member | `heritageTierByPop_` → +1/+2 on the 18th-birthday settlement draw; birth-odds multiplier | `educationCareerEngine.js:1128`; `HERITAGE_BIRTH_MULT` | PARTIAL — heritage never moves a member's Tier or money |
| 14 | The maneuvering cron | The citizen-loop wake writes `Reflection_Intake` only → `citizen-signal-detector` → `Story_Seed_Deck` (media seeds), bond extraction, the UNDOCKED gate | `scripts/citizen-wake.js:404`; `scripts/citizen-signal-detector.js:36,238` | GAP — the wake can *cause coverage* (feeds hops 2 and 8) and nothing else; no cron or phase reads Tier + dial position and acts |
| 15 | Civic choices reach a citizen | Hood state pool → citizen event texture + condition tags; approval / initiatives stay hood-level | `generateCitizensEvents.js:1236-1246`, `:2605` | WIRED as texture; no Tier / money consequence |

### Down: a founding-line member → nothing

| # | Hop | Mechanism as built | Pointer | State |
|---|---|---|---|---|
| D1 | Lose Tier | Tier 1 is decay-exempt (engine.69 rule 3); hand-authored tiers never demote | `processAdvancementIntake.js:869-942` | GAP by design — a founder cannot fall a rung |
| D2 | Lose money | Casino stake (`:588`), heritage business stake (`:1874`), inheritance down-share (`:1368`), layoff income (engine.135), debt drag (`processMoneyLoop_`) | as cited | WIRED |
| D3 | Lose the line | Score points are all ≥ 0 → the score only rises; `heritageTierFor_` never steps down; `LivingMembers` recomputes but 0 living leaves the row in place | `:1806-1830` | GAP — no loss, no dissolve, no scandal |
| D4 | Death | Status deceased → excluded from NW / civic / fame counts; `LineageId` stays for the chain | `living()` `:1762-1772` | WIRED |

### What the table says

The ascent exists on paper and is closed at both ends. The bottom door (hop 3) has not opened under the current engine: 269 Tier-5s, nobody at the bar, and the 17 old `Promoted` rows never reached the ledger. The top door (hop 11) needs $1M across three tracked family members registered in a 101-row family tab, which no non-founding family has. In between, the one wired climb (hop 8) is an event, not a state, and the 12 citizens sitting above the Tier-3 bar are there on seeded counts, not coverage. Dials move odds (including casino and career money) but never Tier; heritage never touches Tier; the wake never touches anything but coverage. The inverse has money loss only.

### Build order (proposed for the builder's approval — nothing started)

1. **engine.150 — the ladder as a state, for EARNED counts.** Every cycle a row's Tier follows the bar its *citation-backed* count clears (decay as today). The 58 seeded cells are the builder's call, not code: **hold** them (today's convention) or **re-base** `UsageCount` to citations (a 58-cell diff-restore). A plain state rule without that choice promotes six citizens to the protected class in one cycle (Aguilar ×2, Okafor, Nair, Whitfield, Soto, Creighton).
2. **engine.151 — a reachable heritage door (Door C).** Founding by *earned* shape instead of cash: e.g. two generations on the ledger + a member at Tier ≤ 2 + a household with a home or a business. Design call on the shape is the builder's; the mechanism slots into `:1645` beside A and B.
3. **engine.152 — heritage loss.** Score can fall (no living members, NW below a floor, a scandal event tag), `heritageTierFor_` steps down, a line at 0 living for N cycles goes dormant. Gives the inverse a path.
4. **engine.118 — fame tenure** (already designed): FameScore ≥ 25 → Tier-1 floor. Second climb, independent of print.
5. **engine.153 — the maneuver step.** An **engine phase** (Phase 5, after the money loop) that reads Tier + dial bands + hood state and picks the citizen's risk posture for the cycle — stake size at the casino, taking the solo household, a field change, a relocation — and writes the LifeHistory line that moves the dials back. The wake stays the *voice* (interview, Discord) and keeps feeding coverage; the engine is the substrate that maneuvers, per doctrine. Needs the ambition dial (engine.94 Track B) first or in the same cut.
6. **Loosen hop 5's Tier gate** (solo household on income alone) — a one-line call inside 5 or on its own.

Open questions for the builder are unchanged (§below); 2 and 5 cannot be cut without answers to the first two.

## First task (as set S411): the measure — DONE above

Trace ONE generic citizen forward through the mechanisms above, on paper, hop by hop: Generic_Citizens → first coverage → Tier 4 row → household → first job → dial movement → Tier 3 → … → heritage line founded → Tier 1. At each hop name the file:line that performs it or write **GAP**. Then the inverse for a Founding-line member. Expected gaps from what S411 saw: the Tier-5 → Tier-4 door (engine.108), the fame door (engine.118 unwired), no ambition dial (engine.94 Track B), no cron that reads Tier/dial and acts, no heritage trade/field on a line, no heritage *loss* path.

Deliverable of the measure: a hop table with pointers and gaps, and a build order the builder can approve. No code before that table exists.

## Open questions (for the builder, after the measure)

- Is the maneuvering cron a **citizen-voice wake** (the citizen chooses) or an **engine phase** (the world moves them)? The direction says cron; the doctrine says the engine is the substrate and crons capture. Both may be right at different hops.
- What is "advance the bloodline" as a number — HeritageScore, HeritageTier, generations, members in Tier ≤2? The line needs one target the cron can steer toward.
- Loss: what un-founds a line (last member dies, score below a floor, a scandal)? Nothing today.

## Changelog

- 2026-09-04 (engine-sheet, S416, ~05:00) — **engine.161 LIVE PROD @45** (bench @39 C109–C111, 0 errors ×3: `cost` and `opportunity` moves fire for the first time; the measured funnel says the 1.5 destination margin is correctly calibrated and stays — §engine.161 *As proven*).
- 2026-09-04 (engine-sheet, S416, ~04:20) — **engine.161 coded** (§engine.161): the relocation cap becomes `relocationMaxShare` × movable units (43 at C105, was a flat 2 — builder: *"2 is too slow and defeats everything we just built"*); rent burden ≥ 50 % / 40 % now sets `planning-to-leave` / `considering` directly, so the pressure lane goes 0 → 9 units; the maneuver posture multiplies the misfit lane only. migrationRelocation 43/43.
- 2026-09-04 (engine-sheet, S416, ~03:30) — **engine.157 LIVE PROD @44** (bench @38: C106 195/39 exact-to-prediction on climb, C107 the Keane line retreats off its own standing, C108 one settle line — §engine.157 *As proven*). Builder steer "take TraitProfile dials into consideration" → ambition = the existing `drive` + `openness` dials, no ninth dial; engine.94 Track B ambition ruled. Owner's-draw dormancy seam closed. Bench re-synced from live at C105 this session (builder cleared the properties; `Carry_Forward_Store` carried C105 — the groundhog cold-start did not trip). Seen for the builder: the relocation cap. Chain cuts 1–9 are all live.
- 2026-09-04 (engine-sheet, S415, ~01:30) — **engine.156 LIVE PROD @43.** Bench @37: the first GET fired inside the deployment's propagation window and ran the old tree (C125 — scores accrued, 16 columns; noted, re-fired). C126 clean: tab 16 → 23 in-cycle, Varek 52 Prominent and first, 22 of the bench's 27 old-door lines stepped down (Corliss Prominent → Founding on the lifetime counter's inflation), 29 life lines, tenure seeded, low clocks only where a line was ever Established. C127 with a bench-only `LowCycles` 51 seed: Mezran dormant — blank tier, tenure frozen, the *money that made it is gone* line on Lila. Live: pull + one-file overlay, diff vs bench stage empty, pull-back byte-identical. Live C106 is a re-base up (four to Established, Corliss holds), not down. Cut 9 (engine.157, the maneuver phase) is next; the wake-pack seam is still research-build's.
- 2026-09-04 (engine-sheet, S415, ~00:45) — **engine.156 CODED, unit-proven 105/105, not yet on the bench.** `generationalWealthEngine.js` only: `heritageStanding_` (the one formula), `HERITAGE_TIERS` 0/30/50/70, seven columns self-arm via `ensureHeritageSchema_`, standing/tenure/dormancy/revival inside `updateHeritage_`, the three hooks, `S.heritage` gains `demoted/dormant/revived` + the line lists. Rulings in code recorded in §engine.156 *As coded*. `SHEETS_MANIFEST` §9 gains the Heritage_Ledger row it never had. Next: bench pull + one-file overlay (HELD four untouched), fire C125, read back the 23-wide tab with Varek first and four Established; then a bench-only `EmptyCycles` 3 seed on one line for the dormancy proof; then live.
- 2026-09-03 (engine-sheet, S414, 23:15) — **engine.156 designed, pre-code** (§engine.156 above): the lifetime counter becomes standing (recomputed, can fall; wealth on a log scale so $10B outranks $2M) + tenure (cycles held); tiers re-based on standing; dormancy at 0 living × 4 cycles or under-$10M × 52, revival by descendant; real business ownership counted through the owner-draw resolver. Measure finding on record: **Varek at $10B is last on his own ledger today (score 12 under Corliss 16)** because the wealth term saturates at $2M. Pipeline seam filed: no wake/packet reads Heritage_Ledger.
- 2026-09-03 (engine-sheet, S414, 22:24) — Builder direction, recorded as said, after the money-loop walk-through (rent is a threshold, never a cash flow; net worth = a week of salary × savings rate each cycle; debt drag, shocks and payoff are the only losses): *"What I like is if people are going broke it's a sim crisis the civic need to address."* Existing seam for that: `processMoneyLoop_` already emits `DEBT_CRISIS` / `MONEY_SHOCK` story hooks (domain COMMUNITY) — the civic chain sees them if it reads that domain. No build asked; noted for the next civic-repair pass (initiatives are the repair mechanism).
- 2026-09-03 (engine-sheet, S414, 18:40) — **The 319 live leases trued to the rule (builder: "True them up").** Live-only write through `lib/sheets.js`: every active rented `MonthlyRent` = its hood's rule rent; 221 up, 98 down, read back 319/319. Consequence, stated before the write: households over the 50 % burden line go 2 → 24, so the crisis roll (10 %/cycle) reaches ~24 households from live C106 — the rule pricing the world's leases the way the canon incomes say, not the 2026 table. §engine.160's open item is closed.
- 2026-09-03 (engine-sheet, S414, 17:20) — **engine.160 LIVE PROD @42.** Bench @36 C123 / C124 on SANDBOX 0831: ok / 0 errors ×2; `hoodRentShare` self-armed; 22/22 `MedianRent` cells rendered to the rule both fires; 6 new leases at their hood's rent (one at the Phase-2 income — the designed lag); 5 then 0 purchases, every price = rule rent × 264, mortgages 23–28 % of income; 0 owned dissolutions. Live: live pull + the eight-file overlay, diff vs bench stage empty, pull-back byte-identical 8/8, HELD four at base. Row engine.160 → done-pending-archive. Open for the builder's word only: the 319 existing leases (true them to the rule, or leave them as the world's history).
- 2026-09-03 (engine-sheet, S414, 16:40) — **engine.160 coded + unit-proven (householdReconcile 87/87, migrationRelocation 38/38), pre-bench.** The builder's ruling (15:31 / 16:03) and the measure are in §engine.160 above. One hood rent rule: `hoodRentShare` 0.30 × `MedianIncome` / 12, derived Phase 2, rendered to the column Phase 5; the 2026 table, the dead price table, the 1700 guards and the rent drift retired; off-ledger hoods throw. Existing leases untouched pending the builder's word.
- 2026-09-03 (engine-sheet, S414, 15:05) — **engine.158 + engine.159 LIVE PROD @41.** Bench C121 / C122 (@35): ok / 0 errors; owned dissolutions 0 / 0 (engine.159 holds across three fires); **C122: Emiko Zhang (POP-00935, Tier 4, $628K, income $175K) bought in Jack London — HousingCost $847,176 = the hood's C121 median × 264, mortgage $3,953 = 27 % of income, from the predicted pool** — the positive path end to end. Live: pull of PROD + the two-file overlay, diff vs bench stage empty, pull-back byte-identical 2/2, 173 / 0 tests, HELD four at base; `clasp deployments` confirms @41. No sheet writes either side. Live C106 prediction: pool 104, ~1 purchase (0 is the likeliest single outcome), owned dissolutions 0 (48 households out of the false crisis). Rows engine.158 / engine.159 → done-pending-archive. Next: cut 8 engine.156 heritage loss.
- 2026-09-03 (engine-sheet, S414, bench) — **C119 @34 / C120 @35 (engine.158 + engine.159) on SANDBOX 0831: ok / 0 errors ×2.** Purchases 0 / 0 (three zero fires at mean ≈ 1.1: P ≈ 3–4 %); the real `trackHomeOwnership_` run offline against the bench's own C120 sheets qualifies **113** households (the solo door keeps feeding the pool) and, with the dice forced, buys all 113 at hood prices ($539K–$781K) — the gates and the write path are proven; the fires' zeros are dice. **engine.159 proven:** owned dissolutions 13 (C119, old detector) → **0** (C120), genuine owner crisis on the bench 0. Live prediction for C106 (both cuts): pool 104, ~1 purchase; owned dissolutions 0 (48 households leave the false crisis — the old code would have rolled ~5 of them away).
- 2026-09-03 (engine-sheet, S414, later) — **engine.158 bench C118 (SANDBOX 0831 @34): ok / 148s / 131 phases / 0 errors; 0 purchases** (prediction 0–3, mean 1.07 — P(0) 34 %; the old rule logged 11–14 per fire). The read-back surfaced **engine.159**: 12 owned households dissolved at C118, 9–14 every bench fire since C106, all buyers — `detectHouseholdStress_` (`householdFormationEngine.js`) read `HousingCost` (the PRICE trackHomeOwnership_ writes) as an owned household's monthly cost, burden ≈ 50×, permanent crisis, 10 % dissolution roll per cycle. Live C105: **48 of 117 owned households in that false crisis** (0 after the fix), 66 of the 246 dissolved rows are owned. Every house bought since engine.65 T2 (S323) has been a slow death sentence. Fix: the burden reads `MonthlyRent` for both tenures (the mortgage on a purchase, by the engine.65 design). Tests +4 (83/83). Bench C119 (engine.158 second fire) + C120 (both cuts) + live: next entry.
- 2026-09-03 (engine-sheet, S414) — **engine.158 home-purchase gates coded + unit-proven, pre-bench.** `generationalWealthEngine.js` `trackHomeOwnership_`: the house is priced by the hood (`homeMarketRent_`), admitted by net worth (`homeHoodFloorAdmits_`, floor only), carried by income (`homeCarries_`, 30 % of `HouseholdIncome`), then a 1 % × rung roll. Live C105: 236/319 → 104 qualified, 15.2 → 1.16 buys/cycle; bench C117: 237/364 → 100, 14.97 → 1.07. Section above holds the departures. householdReconcile 79/79; collisions 0; STUB_MAP regenerated; §9 manifest row trued. Bench + live: next entry.
- 2026-09-03 (engine-sheet, S413, 12:15) — **Door C re-set LIVE PROD @40** (a quota error on the first bump; @39 and @40 are the same tree). Bench @33 C117: 0 errors, one line founded at the new floor (Bautista, $64M), none under it. Live C106: Paulson and Horn found. The six ghost founders entered `Generic_Citizens` on live as themselves (builder: "should be minted as generic citizens"). Cuts 1–7 of the chain are live in their ruled form. Next: cut 8 heritage loss, or the home-purchase gates — builder's order.
- 2026-09-03 (engine-sheet, S413, 12:00) — **Door C re-set: $50M, home required, no Tier** (builder rulings 00:59 / 01:09 / 11:48: heritage is money; the Tier column never gates it; $500K–$5M light). `HERITAGE_DOOR_C_TIER` removed, solo and family floors both $50M. Tests C1–C6 rewritten (a $90M Tier 4 founds; $613K, a $5M Tier 1, a renting $90M, a $49M family do not; exactly $50M founds). Live C106 prediction: Paulson and Horn found (owned homes, $76M / $90M), plus any ≥$50M household that buys its home that cycle. Bench + live: next entry.
- 2026-09-03 (engine-sheet, S413, 01:20) — **Course correction, builder-direct ("these systems are being built in isolation… not using the tools"):** the wiring cards and the doctrine were run on the business cut before any commit. They showed the business system already has a builder-approved plan — engine.96 (Aug 1, Growth_Rate/Revenue drift, decline → layoffs, closure to `Business_Archive`, World_Config tunables signed off) — and engine.85 Task 8 (businesses born alive), both unbuilt. The owner's-draw code written here was parked as a patch and filed as engine.96 Task 10. Withdrawn from the earlier list: a per-cycle birth cap (doctrine rule 1, no caps on outcomes). Build order now follows the written plans: engine.96 Tasks 5–9 → engine.85 Task 8 → Task 10 → Door C money-only + the home gates, re-measured against a moving economy. engine.96 Task 5 built + unit-proven the same night (see that plan).
- 2026-09-02 (engine-sheet, S413, later) — **engine.155 LIVE PROD @33.** Bench @26 C110: ok / 174s / 0 errors; **9 Door-C lines** (7 predicted — Ramos, Paulson, Rivas+Yamamoto, Richards+Foster, Necklar+Cook, Horn, Skenes — plus Steve Conrad and Ophelia Brenner, whose `[Home]` and `[Heritage]` lines carry the same Y3C6 stamp: bought the house and founded the line in one cycle, the chain link end to end), 12 citizens joined, lines 5 → 14. Read-back caught HomesOwned 0 on every new line (purchases before the line existed never reached the aggregate) → 7b counts the founding home; bench @27 C111: 0 errors, 2 more lines (Santana, Dybantsa) at HomesOwned 1. Live: one-file overlay, diff vs bench stage empty, pull-back byte-identical 1/1, HELD four at base. Live C106 prediction: 4 Door-C lines (Ramos, Paulson, Horn, Necklar) + any household that buys its home that cycle. Row engine.155 → done-pending-archive. Cuts 1–7 live. Next: cut 8 engine.156 heritage loss.
- 2026-09-02 (engine-sheet, S413) — **engine.155 cut 7 coded + unit-proven, pre-bench.** `generationalWealthEngine.js` `updateHeritage_`: Door C after Door B — one `Household_Ledger` read; an active `owned` household with no lined member, best rung ≤ `HERITAGE_DOOR_C_TIER` (2), NetWorth ≥ `HERITAGE_DOOR_C_SOLO_NETWORTH` ($500K) alone or `HERITAGE_DOOR_C_FAMILY_NETWORTH` ($1M) combined, founds through the shared `foundLine_` (door `C`, its own `[Heritage]` line and hook text). Tests C1–C6 in householdReconcile (61/61; the mock sheet now grows on a write past its last row, as Apps Script does). All suites green; collisions 0; STUB_MAP regenerated. Bench prediction at C110: 7 lines LIN-00006…00012, 10 citizens joined, 0 errors. Bench + live: next entry.
- 2026-09-02 (engine-sheet, S413, 6b later) — **engine.154 6b LIVE PROD @32.** Bench @25 C109: ok / 134s / 0 errors; bonds 607→610 (the usual 2–4, so friendship formation is back to the base score), 0 romances (0–1 predicted — two adult pairs at the gate at ~10 % each). Live: one-file overlay, diff vs bench stage empty, pull-back byte-identical 1/1, HELD four at base. The romance door is open on live for the first time since S325; the first `more than friends now` line will be the proof. Next: cut 7 engine.155 Door C.
- 2026-09-02 (engine-sheet, S413, 6b) — **engine.154 6b coded + unit-proven, pre-bench.** Two corrections after the @31 deploy: (1) the terms left `bondCompatibility_` (which also sets the like-at-all floor for friendship formation) and now live in `bondSpouseQuality_`, added at the romance gate only — friendship formation is back to the pre-cut score; (2) the gate's recompute now resolves POPID → name (`resolveCitizenName_`) before reading the lookup — indexed raw it scored every existing pair as strangers, which is why live has 0 romances since S325; a pre-existing defect surfaced by this cut, fixed inline. Education clause trued (hood bucket needs ≥ 5 distinct levels, else citywide). householdReconcile 53/53. Bench C109 prediction: bonds +2–4 as usual, romances 0–1 (two adult pairs at the gate, ~10 % each), 0 errors.
- 2026-09-02 (engine-sheet, S413, later) — **engine.154 LIVE PROD @31.** Bench @24 C108: ok / 155s / 0 errors; 607 active bonds, 0 romantic (0–1 predicted — the flip is 0.10 × orbit × fitness × family per pair per cycle), 0 GC courtships, so the spouse mint stayed unit-proven; the compatibility terms ran on every scored pair without incident. Live: two-file overlay, diff vs bench stage empty, pull-back byte-identical 2/2, HELD four at base. Row engine.154 → done-pending-archive. Cuts 1–6 of the chain are live. Next: cut 7 engine.155 Door C (an earned heritage door, solo or family).
- 2026-09-02 (engine-sheet, S413) — **engine.154 cut 6 coded + unit-proven, pre-bench.** `generationalWealthEngine.js`: the v15/D1 thresholds become the one band table `WEALTH_BAND_FLOORS` (deriveWealthLevel_ walks it, outputs unchanged across 18 probes) + `netWorthForBand_(band, unit)` (log-uniform inside the band, rounding never crosses the next floor). `bondEngine.js`: `bondWealthTerm_` (gap ≤1 +2 / ≤3 +1 / ≥6 −1, blank 0) + `bondProsperityTerm_` (IncomeTier within 1 → +1, via `S.neighborhoodState`) added to `bondCompatibility_`; the name lookup's enrichment pass now carries `WealthLevel`; the GC spouse mint draws education from `hoodEducationFreq_(ctx)` (adults, by hood + age bracket, citywide fallback — the `null` that made every spouse `hs-diploma` is gone) and prices NetWorth by `spouseNetWorthFor_` (citizen's band ± 1 seeded, clamped to the hood's WealthMin..Max; unpriced citizen → the old age+income path). householdReconcile 51/51; all six suites green; collisions 0; STUB_MAP regenerated. Bench + live: next entry.
- 2026-09-02 (engine-sheet, S413, later) — **engine.151 LIVE PROD @30.** Bench @23 C107: ok / 140s / 0 errors; pre-fire pool T2 3 / T3 15 / T4 1, E2 staff T3 48 / T4 320, eligible rented households by best rung 9 / 15 / 37 / 163 (~14 buys predicted) → 1 hire (David Okonkwo, the lone Tier 4 in the pool — no rung to jump, no suffix), 11 homes bought (owned 124→135), 0 promotions (≈1 per ten cycles city-wide by doctrine 21), Phase5-Bonds ok across 600 bonds. Pace multipliers are unit-proven; the fire proves the four doors run clean on live's state. Live: four-file overlay, diff vs bench stage empty, pull-back byte-identical 4/4, HELD four at base. Row engine.151 → done-pending-archive. Cuts 1–5 of the chain are live. Next: cut 6 engine.154 spouse quality.
- 2026-09-02 (engine-sheet, S413) — **engine.151 cut 5 coded + unit-proven, pre-bench.** One table `TIER_PAY` 1.50/1.30/1.15/1.00 + `tierPayFactor_` beside `TIER_BAR` (`processAdvancementIntake.js`). Four doors: `hireIncomeBand_(income, tier)` takes the E3 band on income ÷ pay (pool entries carry `tier`; hire line `(Tier N counted)` when the rung outran a poorer raw band); `promotionOrder_` ranks on `promotionWait_` = cycles since last promotion × pay (never-promoted still first; `(Tier N counted)` when a lower rung had waited as long or longer; Tier 1–2 stay outside by the engine.135 rule); `courtshipTierBoost_` multiplies the housing boost on the romance step by the pair's best rung under the same 0.5 cap (GC draw untouched — doctrine 10; engine.59 flip orbit untouched — Mike-ruled); `homeBuyChance_(bestTier)` = `HOME_BUY_P` × pay of the household's best member (6.0/6.9/7.8/9.0 %), eligibility untouched. Blank/0/≥5 Tier reads ×1. Suites: employerSuccess 58/58, tierLadderState 49/49, householdReconcile 39/39, careerStage 74, educationLoop 119, hoodIncome 49; collisions 0; STUB_MAP regenerated. Bench + live: next entry.
- 2026-09-02 (engine-sheet, S412 close, 21:56) — **Builder's manual C106 proving fire on the re-synced bench (live's exact C105 state, code @22 == live @29), execution log read:** 129 phases ok, 0 engine errors, 118s. `applyTierLadderState_`: 0 promoted, 77 seeded cells held, 0 ambiguous, **5 assumed fame, 4 cultural rows registered/brightened**; `decayMediaAttention_`: 38 decays, 0 demotions; `processAdvancementIntake_`: 13 usage rows processed, 11 advancements processed (the intake guard held — 0 Tier moves); `formCriteriaHouseholds_`: formed 3, **solo 17**; `trackHomeOwnership_`: **14 purchases** (the new solo households widen the eligible pool — purchase odds untouched, 6 %/cycle per eligible household); casino: 12 placed, 0 settled, float 250000; youth: 22 texture lines on 36 minors, gate stepped aside for 42; UNDOCKED airing 3 episodes incl. pilot POP-00688 (Merkin). Every prediction from the cut records landed. Side proof: the three deleted script-property blobs were **RECOVERED from `Carry_Forward_Store`** — engine.122's Layer-2 mirror worked on a real cold start for the first time on record.
- 2026-09-02 (engine-sheet, S412, later) — **engine.153 LIVE PROD @29.** Bench @22 C120: ok / 0 errors; 26 households formed — 19 solo, 16 of them Tier-4 heads (the predicted first-cycle drain), 4 `single` rows reclassified `solo`, 3 homes bought. Live: two-file overlay, pull-back byte-identical. Cuts 1–4 of the chain are live: ladder as a state, coverage both ways, fame permanent + the famous sub-roster, a single can own a home. Row engine.153 → done-pending-archive. Next: cut 5 engine.151 Tier pays.
- 2026-09-02 (engine-sheet, S412) — **engine.153 cut 4 coded + unit-proven, pre-bench.** Measure: the dice wedding (`checkWedding_`) is retired since engine.57 P5 — marriage fires in the bond engine when a romance's intensity crosses 8, paced by a per-cycle step chance that engine.74 already boosts ×1.5 when either partner holds a household. Cut: (a) `householdFormationEngine.js` — the solo door drops the Tier ≤ 3 gate (`SOLO_TIER_MAX` removed at both sites); income ≥ 85k and the 10 % roll stay. Live reach: 313 unhoused active adult singles, 160 at ≥ 85k, **128 of them Tier 4** — the backlog drains geometrically (~13 the first cycle, ~6 by the tenth). (b) `bondEngine.js` — `householdHousingById_` (one read) + `courtshipHousingBoost_`: an owned home on either side ×2.0, a rented household ×1.5, none ×1 (cap 0.5 unchanged). Home purchase itself is untouched: a rented household with combined NetWorth ≥ 35 % of price rolls 6 %/cycle. Tests in `householdReconcile.test.js`.
- 2026-09-02 (engine-sheet, S412, later) — **engine.118 cut 3b LIVE PROD @28.** Bench @21 C119: ok / 0 errors; Paulson's cultural row registered by engine.118 (fame 11, brightens to the bar the cycle after it lands), Dillon brightened above 25, Keane at 30 untouched. Live: one-file overlay, pull-back byte-identical. engine.118 complete → done-pending-archive. Cut 4 next: a single can own a home (engine.153).
- 2026-09-02 (engine-sheet, S412, 21:30) — **Feed fix (Node, live at the next Saturday `--apply`):** `cron-saturday-run.js` credits `quoted-source` names as `quoted` at publish; the old comment said wake-2 owned that class and wake-2 never wrote it. **Cut 3b coded + unit-proven (46/46):** `culturalFameOf_` / `ensureFamousOnCulturalLedger_` — a famous citizen with no Cultural_Ledger row linked by POPID (or exact name) is registered through `registerCulturalEntity_` (journalist `engine.118`, role + hood from the row; once per ctx); a linked row under 25 is brightened to the bar; a linked row at/above is left alone. Suite 194/195. Bench + live: see the later entry.
- 2026-09-02 (engine-sheet, S412, later) — **engine.118 LIVE PROD @27.** Bench @20 C118: ok / 0 errors; three assumed fame on the bench (its own cycles had decayed two under the bar). Live: one-file overlay, diff vs bench empty, pull-back byte-identical. At live C106 the five at ≥25 assume fame (Keane, Dillon, Paulson, Aitken, Kelley). Row engine.118 → done-pending-archive. Cut 4 next: a single can own a home (engine.153).
- 2026-09-02 (engine-sheet, S412) — **engine.118 cut 3 coded + unit-proven (42/42), pre-bench.** Measure: the ledger had no fame column (the S3xx fame columns were rolled back); `Cultural_Ledger.FameScore` exists per entity linked by `UniverseLinks`. Cut: `Simulation_Ledger.Famous` (BC, appended on live + bench — the grid was exactly 54 wide, so a column was appended before the header; read back both). `applyTierLadderState_` runs fame first: cell ≥ 25 (`FAME_USAGE_BAR`, the canon on record — gifted era included, per the builder: Vinnie is famous) → `Famous = C<cycle>`, Tier 1, `[Fame] Assumed fame` line + `Media|fame` log row + ripple; any famous row below Tier 1 is floored back. Decay skips famous rows (count and Tier frozen). A hard light on a famous name leaves the count and dims the highest-fame `Cultural_Ledger` row by 1 (`dimCulturalFame_`, cell intent, floor 0) with its own life line. Live dry-run: five assume fame at the first fire (POP-00001/00003/00018/00019/00527, cells 60/28/58/28/38), 0 floors. Ledger doc: BA/BB/BC rows added (BA/BB had never been tabled).
- 2026-09-02 (engine-sheet, S412, later) — **engine.152 LIVE PROD @26.** Bench @19 C117: ok / 0 errors; the seeded hard-light row took Delvon Pratt (POP-00621) UsageCount 1→0 with the life line and the `Media|hard-light` log row, seed row marked Processed. Live: one-file overlay, diff vs bench empty, pull-back byte-identical. First live sentiment lands with the next Saturday `--apply` run (2026-09-05); until then every existing row reads neutral. Row engine.152 → done-pending-archive. Cut 3 next: engine.118 fame at UsageCount 25.
- 2026-09-02 (engine-sheet, S412) — **engine.152 cut 2 coded + unit-proven, pre-bench.** Measure: usage rows are rendered by CODE from the packet's exposure lists (`cron-desk-writer.js renderPacketIntake`), not by the LLM, so no tone judgment existed anywhere in the loop. Cut: at Saturday publish, `stepSheets` extracts the sentences naming each cited citizen (`citationSnippet`, pure) and asks the cheap classifier for one of three words (`classifyCitationLight` in `lib/reflectionClassifier.js`, same transport as the reflection tagger); the word lands in a new `Sentiment` column (blank on failure = neutral, never guessed). Engine: `processMediaUsage_` reads it — negative → UsageCount −1 (floor 0), `[Media] Named in a hard light` life line + log row, a Generic citizen gets no EmergenceCount; `earnedCitationsByKey_` nets hard citations off the earned count so the state pass sees the inverse; bylines carry no sentiment (an author's own work is not coverage of them). Header self-arms in the engine; added on live + bench by hand the same session for the Saturday writer. Tests: engine 34/34, Saturday 45/45.
- 2026-09-02 (engine-sheet, S412, later) — **engine.150 follow-up LIVE PROD @25** (bench @18 C116 clean, no tier lines). engine.150 is complete: the ladder as a state; intake never lowers; earned = climbed on usage; MEDIA and CIVIC held from decay; Tier 4 the floor by construction.
- 2026-09-02 (engine-sheet, S412, 17:32) — Builder's post-cut-1 direction captured verbatim (Tier 4 floor; fame permanent, grade moves; media Tier = routing to more work; intake lands usage, never sets Tier; the wake economy doctrine). **engine.150 follow-up coded:** an EARNED rung is one climbed on usage — the old citation climb's `Promotion` line or the state pass's `Media|tier-climb` line; advancement-intake stamps (`Updated to Tier N` on an `Advancement` row, written on every intake whether or not the Tier moved) are authored and no longer count. They had been the ONLY marker decay read since S325 — bench C115's two demotions (Ariana Lee, Sandra Velazquez) were intake stamps from C106, not usage. Test 27/27; live dry-run unchanged (0 / 0 / 42).
- 2026-09-02 (engine-sheet, S412, later) — **engine.150 LIVE PROD @24.** Final shape: ladder covers ENGINE + GAME + CIVIC on citations and MEDIA on `byline-published` (×2); decay covers ENGINE + GAME and reads the old `Advanced from Tier` line as earned; **MEDIA held from decay** while the Saturday selection is unproven (bench C114 would have dropped Leary / Ortega / Navarro 3→4 — desk voices with no published work behind their Tier); CIVIC held. **Intake guard added the same cut:** an intake row never lowers an existing citizen's Tier (`intakeTierForExisting_`; a blank intake Tier defaulted to 3 and was stamped over the row — bench C106 took Vinnie Keane 1→3 that way, and live's 11 pending intake rows would have done the same at C106). Bench C113–C115 clean; live dry-run at C106: 0 promotions, 0 demotions, 42 usage decays (today's number). Row engine.150 → done-pending-archive. Cut 2 next: coverage runs both ways.
- 2026-09-02 (engine-sheet, S412, 16:15) — **engine.150 revised on builder review (still NOT deployed):** *"GAME should be eligible for tier exposure; media isn't gained by coverage but by article usage in the Saturday runs; civic maybe approval rating — maybe too complex; the old earned usage should apply, the canon is on record."* Cut: the ladder covers ENGINE + GAME + CIVIC on emergence citations and MEDIA on `byline-published` rows only (worth 2 each, the engine.88 rule); decay widened to ENGINE + GAME + MEDIA (CIVIC held); decay now reads the old climb's `Advanced from Tier X to Tier Y` log line as an earned rung, so rungs earned before today can fall. A CIVIC ladder on approval rating is left for its own cut. Test 18/18. Live dry-run: still **0 promotions**, 74 seeded cells held across the four clocks.
- 2026-09-02 (engine-sheet, S412) — **engine.150 cut 1 coded + unit-proven (`scripts/tierLadderState.test.js` 15/15), NOT deployed (builder review first).** `applyTierLadderState_` runs each cycle between the citation pass and decay: earned = min(UsageCount cell, emergence citations on record for the normalized name); Tier follows the bar it clears, upward only; ambiguous names skipped; ENGINE-clock active rows only. The citation event no longer decides Tier (count only). Promotions write `Updated to Tier N` — the marker engine.69 decay reads — where the old climb wrote `Advanced from Tier`, so a media-earned rung was never eligible to give way (latent since S325); fixed going forward. **Live dry-run against the C105 ledger + 595 usage rows: 0 promotions, 27 seeded cells held, 0 ambiguous** — nobody on the ledger today has an earned count above their Tier's bar; the change is to the future (a seeded cell can no longer carry a citizen up on one new citation, and earned rungs can now fall).
- 2026-09-02 (engine-sheet, S412, 15:41) — Builder's chain direction captured verbatim; every link mapped against the code (13 links: 5 wired, 2 partial, 5 gaps, the gifted era held); build order rewritten in chain order, nine cuts. Decisions resolved by the builder's words: seeded counts hold (gifted era); fame bar = UsageCount 25; solo founding allowed; the maneuver is an engine phase.
- 2026-09-02 (engine-sheet, S412) — **The measure.** Fifteen hops up, four down, every pointer opened. Findings: the Tier-5 door has never fired (max EmergenceCount 2 of 3; 0 lottery mints ever); Door A has 0 candidate units; the UsageCount climb is event-only (13 ENGINE Tier-4 rows stranded at 3–15); dials, heritage and the wake never touch Tier; the inverse has money loss only. engine.108 found landed (`e371d815`, 2026-08-16) with its ROLLOUT row still `ready` — row trued. Build order of six proposed; nothing cut. Advisor-driven checks the same session: dials DO reach money through odds (casino participation, career frequency) — hop 6 corrected; the 12 stranded Tier-4 rows carry seeded counts with 0 citations behind them (58 ENGINE rows seeded in all) — hop 8 and build item 1 corrected; the GC tab's 17 `Promoted` / 13 `Emerged` rows are pre-S320 statuses whose citizens are not on the ledger — hop 3 corrected.
- 2026-09-02 (engine-sheet, S411) — Direction captured as said, the existing-mechanism map pointed, the measure defined as the first task. No design, no code.
