# SESSION_CONTEXT — NEXT WORK ONLY. One PIN + one NEXT line per lane. What already happened lives in git, claude-mem, ROLLOUT_PLAN and the gap/production logs — never here. If a line describes a session instead of an assignment, it is written wrong.

**PIN:** S475 | Day 196 | canonical C107 | **PROD @104 = clasp v93 = `b5cb18fb`** (engine.244 spike descriptions + engine.215 illness seeds, over @103's engine.243 crisis naming + lifecycle events, over @102's hood pins / Civis recalibration / weather-business-QoL / engine.187 shock chain) | Bench SANDBOX 0908 @74 = `b5cb18fb` at C115 — bench-only, never replay | **claude-mem observer is INTERMITTENT, not down (measured 2026-09-20 14:xx, `~/.claude-mem/logs/claude-mem-2026-09-20.log`): key and model work — 11 observations stored vs 29 failed in one hour. Every call is a NON-streamed chat completion producing ~700–2300 output tokens against a fixed 30000ms per-attempt deadline (`worker-service.cjs` `Jve=3e4`); the free `nemotron-3.5-lightning` endpoint's speed swings >3x minute to minute (same 3.2k-token init query: 9.0s at 14:06:57, 29.4s at 14:15:24, >30s otherwise). A killed call restarts the generator at the init query, so failures cluster. WHY the endpoint slows is not established. `CLAUDE_MEM_LLM_TIMEOUT_MS=90000` SET 2026-09-20 14:56 (worker pid env verified; also in `~/.claude/settings.json` env block + `~/.bashrc`). The worker reads it from PROCESS env only, and `npx claude-mem restart` / the restart URL make the OLD worker spawn its successor with the OLD env — a new env var needs `npx claude-mem stop` then `start` from a shell that has it. First samples under 90s: 2 stored (9s, 12s), 2 hung the full 90s — some calls HANG rather than run slow, so 90s may not be the whole answer; re-measure on a few hours of data before touching anything else (day so far: successes pile up against the 30s wall, 14 of 34 in the 20–30s band). No batching knob exists: one observation per tool call, Bash = 92% of volume; `CLAUDE_MEM_SKIP_TOOLS` (read per call, no restart) now also skips ToolSearch/ListAgents/SendFeedback/ReadNotifications/ScheduleWakeup. Boot shows 0 observations BY SETTING (`CLAUDE_MEM_CONTEXT_OBSERVATIONS=0`), not by outage.** | `diag187` rides the fire response — read it before inferring why a flag fired | engine-sheet LEADS the sim repairs | never `--amend`; wiring card before every cut; deploys from an isolated `git archive` stage | DOCTRINE §13/§15/§16/§17 | Sunday city-hall cron reads C107 next | Ledger 943 rows (POP high-water 1113)
**NEXT[engine-sheet]:** Smoke PROD @104 + @103 at live C108: every crisis-spike row in `WorldEvents_V3_Ledger` non-blank, no C108 hook reads `"undefined"`, World_Config gains `illnessConvergenceRate` + `illnessInitiativeRelief`, Riley_Digest Issues drops the missing-key notice; `CRISIS-105-WESTOAKL` comes back `The West Oakland Crime Spike` (or `Safety Crisis` if evidence erased), never off a channel merely live at C108; named line reaches Story_Hook_Deck + edition; 0 Engine_Errors; crime in engine.212 band; shockFlag → `none` (read `diag187` first). Then engine.227, engine.246 (empty citizen archive in desk packets — rebuild the March article index first), engine.216 (watch C108 for doubled incomes), CycleWeight-on-volume, retire `NEIGHBORHOOD_CRIME_PROFILES`. Expectations: DEPLOY_HISTORY §PROD @104 + §PROD @103.
**NEXT[research-build]:** civic.38 RULED (Mike 2026-09-19) → write its build plan: seat moves persisted via the Sunday apply, packs show board + district complaints, per-wake move budget, `Civic` complaints move dials, Mara-style confrontation aimed at elected seats, 3-stage deploy (research 2026-09-19-kimi-initiative-stage-voting §10–11). Standing: Mon pipeline.41; Fri Hal retry; Sat pipeline.65.

**NEXT[kimi]:** BLOCKED — Kimi is out of its weekly (7-day) quota (`403 provider.auth_error` in the pane, 2026-09-20 03:5x); the window resets before the lane is usable again. Nothing dispatched. Standing: adversarial review duty on repair waves (reviews in output/kimi/; open: holidaySeeds bank pin, owed post-fire 240 bench, QoL composite-vs-QolLevel split). Next: civic.38 Task 1 on Mike's go, once quota returns; build plan `0a4d8e9f` (docs/plans/2026-09-19-civic-wake-game-loop.md) is committed and pushed.

**NEXT[codex]:** SIDELINED — out of Codex messages until 2026-09-20 18:18 (pane sits on the credits prompt; do not type into it). BACK SEAT (Mike-direct 2026-09-14 20:21). Your HANDOFF §Next causal work list landed by engine-sheet 2026-09-14/15 as PROD @86–@90 (engine.221, 222, 219, 223, 220, 224; readbacks in `output/engine-sheet/`); only engine.214 (truth-source rebuild, pipeline.69 T4) remains. Nothing queued; review on request only.

**NEXT[antigravity]:** Holds the adversarial review lane while kimi is out of quota; its engine.243 review EARNED ITS KEEP — it found the one arc-summary PARSER outside `phase*/` (`scripts/buildDeskPackets.js:499` harvesting TitleCase pairs as citizens to quote), verified and guarded. Caveat for next dispatch: its section 3 described code three commits stale because it sat on permission prompts for ~3 hours — verify every finding against the file before acting. Nothing queued.

**NEXT[grok]:** Idle. civic.24 Task 9 stays gated on C104+ (`--stage=chain`, no `--apply`).
