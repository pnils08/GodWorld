# Bench C116 — engine.247 predictions (written BEFORE the fire, 2026-09-20 ~16:35 Chicago)

Bench SANDBOX 0908 @75 = `d5738907`. Fixture: four bench-only A's feed rows at C116 mirroring live C108 rows 223–226, entered USER_ENTERED so Sheets coerces them exactly as typing does — stored `"3-0"` text, `46113` date, `46113` date, `46205` date (shown 3-0 / 4-1 / 4-1 / 7-2). WeekRecord left blank. Notes `2026-09-20 BENCH-ONLY engine.247 date-record proof - NEVER REPLAY`. NOTE: the API append landed them at rows 76–79 (INSERT_ROWS at the first table break), not at the foot; the reader filters by Cycle so position is inert.

**Control = live C108 on PROD @104, same row shape:** 12 sports slips priced win 1.44 / loss 2.52 (the 3-0 price); execution log `record: 3-0`.

1. `ok:true`, `World_Config.cycleCount` 115 → 116, `Engine_Errors` 0 rows.
2. Every new C116 `sports:as` slip is priced **win 1.41 / loss 2.59** (the 7-2 price). 1.44 / 2.52 = fix not reached; 1.83 / 1.83 = record unreadable.
3. `Neighborhood_Map.SportsSeason` = `playoffs` 22/22.
4. Open C115 A's slips settle off the first C116 game-result (streak W5 → win side pays).
