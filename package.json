{
  "name": "godworld",
  "version": "2.10.0",
  "description": "GodWorld city simulation engine",
  "scripts": {
    "lint": "eslint . --ext .js --max-warnings 50",
    "lint:fix": "eslint . --ext .js --fix"
  },
  "dependencies": {
    "@google/clasp": "^3.1.3"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  }
}
