# SESSION_CONTEXT — NEXT WORK ONLY. One PIN + one NEXT line per lane. What already happened lives in git, claude-mem, ROLLOUT_PLAN and the gap/production logs — never here. If a line describes a session instead of an assignment, it is written wrong.

**PIN:** S493 | Day 198 | canonical C108 | **PROD @130 = web app @117 = `12289fdb`** (Job 3 build duration + open milestone line; @128 engine.259 the fund moves) | Bench SANDBOX 0908 @108 = `12289fdb`, re-synced from live C108 then C109–C114 bench-only, never replay | bench reads: `lib/env` overrides GODWORLD_SHEET_ID — set it AFTER requiring env | claude-mem observer on `nvidia/nemotron-3-super-120b-a12b:free` | never `--amend`; wiring card before every cut; deploys from an isolated `git archive` stage | Ledger 952 rows

**NEXT[engine-sheet]:** `docs/plans/2026-09-24-initiatives-in-the-world.md` — **Job 4 next** (clinic as a business: Business_Ledger row + hiring on open; inputs listed under Job 4). Job 3 LIVE PROD @130 (`812d04d3` + open line `12289fdb`, agy CLEAN PASS ×2); tracker copies refresh hourly from the live tab (`3e2cf3cf`). **Smoke Sun 2026-09-27 C109 on PROD @130:** OpensCycle armed, five `civicBuildCycles_*` in World_Config, INIT-005 OpensCycle 88 + `operational` + open line; tick refresh resumes once `dumpBeatTabs` stamps C109; Job 1 proof (`week_state_c109.json` apply `done`). AIDER queue empty. Kimi's engine.256 WIP in `stash@{0}` — do not wire.

**NEXT[research-build]:** UNDOCKED fame loop is BROKEN not unwired (Mike-direct 2026-09-25): POP-00962 zero UsageCount after 7+ flights — wake-2 quote pass credits a fabricated name, not the pilot. Cleared `buildNiaSlice.js:110` + `newsroom-fanout.js:220`; break is in `collectQuoteAsks`/citizenVoice, same file — start there. Detail: `docs/plans/2026-08-07-spacemolt-game-show.md` §Post-ship (e). Also open: Aider (`godworld:6`, `AIDER_PLAN.md`) for next bounded mechanical job; `docs/plans/2026-09-22-agent-model-fit-test.md`; codex's supermemory MCP needs `codex mcp login supermemory`.

**NEXT[kimi]:** engine.255 framing fix LIVE (Mike-direct 2026-09-23 — ruling: rewrite the Fund's framing, hood canon stands): INIT-001 keeps name/hood; MilestoneNotes records the remnant framing + the published 47 checks ≈$4.2M (runner `output/kimi/2026-09-23-wosof-framing-fix.js`, read back, beats dump refreshed; commit `42e410e5`). **Pending after Sunday's C109 arm, before the C110 tranche: correct INIT-001 BudgetRemaining 28M→23.8M + LastDisburseCycle 90** (plan §Money updated). Hand engine-sheet: lift the HOLD; `.claude/agents/civic-project-stabilization-fund/` canon files still carry the old framing. Note: my push carried engine-sheet's stacked HOLD commit `15eafbef` along — reported, not rebased. Task 9 shipped `e1809d9c`. Standing: adversarial review duty (output/kimi/).

**NEXT[codex]:** Verify remaining board comparator/config/history export gaps against docs/plans/2026-09-19-civic-wake-game-loop.md §Task 4 engine rulings; coordinate runner edits with Kimi. Separate counter threshold-rounding defect is recorded in the same plan §Health numerator amendment.

**NEXT[antigravity]:** Tracker freshness & open milestone adversarial review filed (commit 14fecb43 → output/antigravity/2026-09-25-review-tracker-freshness.md) — CLEAN PASS: cycle guard verified, atomic writes prevent clobbering, readers compatible, 600-char cap & open line idempotent. Awaiting Job 4 dispatch.

**NEXT[grok]:** No pane; nothing queued.
